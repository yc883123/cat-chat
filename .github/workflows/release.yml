name: Build and publish Windows update

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

env:
  RELEASE_VERSION: 2.7.0-beta
  RELEASE_TAG: v2.7.0-beta
  PACKAGE_NAME: naiba-chat-2.7.0-beta-windows-x64

concurrency:
  group: naiba-chat-release
  cancel-in-progress: true

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install build dependencies
        run: |
          python -m pip install --disable-pip-version-check --upgrade pyinstaller pywebview pystray "mcp==2.1.1" pymupdf "av==18.1.0" python-multipart
          python -m pip install --disable-pip-version-check --upgrade --force-reinstall --no-cache-dir pillow
          python -c "from PIL import Image; print('Pillow', Image.__version__)"

      - name: Run tests
        shell: pwsh
        run: |
          if (-not (Test-Path "tests")) {
            Write-Host "tests directory not found; skipping unit tests."
            exit 0
          }
          $attempts = 2
          for ($attempt = 1; $attempt -le $attempts; $attempt++) {
            python -m unittest discover -s tests -v
            if ($LASTEXITCODE -eq 0) {
              exit 0
            }
            if ($attempt -lt $attempts) {
              Write-Host "Tests failed on attempt $attempt; retrying once."
            }
          }
          exit 1

      - name: Build executable
        shell: pwsh
        run: |
          $env:NAIBA_BUILD_COMMIT = "${{ github.sha }}"
          $env:NAIBA_BUILD_VERSION = "${{ env.RELEASE_VERSION }}"
          python -m PyInstaller --noconfirm --clean naiba-chat.spec

      - name: Create update manifest
        shell: pwsh
        run: |
          $hash = (Get-FileHash -Algorithm SHA256 dist\naiba-chat.exe).Hash.ToLowerInvariant()
          $releaseNotes = @(Get-Content -Raw -Encoding UTF8 release_notes.json | ConvertFrom-Json)
          $manifest = [ordered]@{
            repository = "yc883123/naiba-chat"
            version = "${{ env.RELEASE_VERSION }}"
            commit = "${{ github.sha }}"
            asset = "naiba-chat.exe"
            sha256 = $hash
            published_at = (Get-Date).ToUniversalTime().ToString("o")
            release_notes = $releaseNotes
          }
          $manifest | ConvertTo-Json | Set-Content -Encoding UTF8 naiba-chat-update.json

      - name: Create Windows package
        shell: pwsh
        run: |
          $packageDir = Join-Path "dist" "${{ env.PACKAGE_NAME }}"
          New-Item -ItemType Directory -Path $packageDir -Force | Out-Null
          Copy-Item -LiteralPath "dist\naiba-chat.exe" -Destination $packageDir
          Copy-Item -LiteralPath "naiba-chat-update.json" -Destination $packageDir
          Copy-Item -LiteralPath "README.md" -Destination $packageDir
          Compress-Archive -Path "$packageDir\*" -DestinationPath "dist\${{ env.PACKAGE_NAME }}.zip" -Force

      - name: Publish 2.7.0 Beta release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Naiba Chat 2.7.0 Beta
          prerelease: false
          make_latest: true
          files: |
            dist/naiba-chat.exe
            dist/${{ env.PACKAGE_NAME }}.zip
            naiba-chat-update.json
          body: |
            Naiba Chat 2.7.0 Beta Windows build from `${{ github.sha }}`。

            本次更新（完整清单见 `release_notes.json` / 应用内「更新说明」）：
            - 给失控的长思考装上三道闸门（本次重点）：思考型模型在复杂长任务里偶发「想个没完」——推理刷屏数万字符、正文一个字没有，输出额度被烧光后上游直接截断；更糟的是上一轮的失控思考会被原样全量回灌进下一轮请求，模型被自己的循环锚定，于是每一轮都复读同一结论（本次用真实数据库量化确认：单条落库思考 431,726 字符、整个会话合计 153 万字符，而正文只有二十几个字）。现在流式期有预算熔断（推理超 4.8 万字符且正文仍为空就主动中断、走已有的降档重试，正文一出现即解除）、回放期有双闸门（单条 4000 / 整轮 16000 字符，由新到旧分配额度并附省略标记，只截回放不动落库，截断确定性 ⇒ 前缀缓存不破）、计费期有默认上限兜底（不再「不填就等于上游默认近乎无限」，未显式设置时按 min(32768, 端点上限) 兜底，你自己设的值永远优先）。
            - 单次请求总时长兜底：180 秒是「每次读操作的空闲超时」，只要还有推理增量到达就不断被重置——一段思考死循环可以这样无限流下去。现在在线请求另加 15 分钟墙钟兜底。
            - 思考强度识别改为数据驱动（本次重构）：此前靠模型名子串嗅探 + 六条分支硬编码，改个中继名就静默失效；现在是一张声明式预设表（10 个预设，覆盖 OpenAI / DeepSeek / Kimi / Claude / Gemini / Ollama / LM Studio），网关用自有词汇也能配，未声明的档位在发请求前就报错。顺带修掉一个老 bug：Kimi K2 系列把思考强度从默认调到任意一档，首个请求必然白打一次 400。
            - 本地模型补齐：LM Studio「有推理、零正文」以前直接报错，现在对齐在线路径走降档重试；本地模型报错也不再自称「在线模型」。
            - 重复答复会提示了：终态答复与上一轮逐字相同（且不短于 20 字符）时给一条提示，只提示不拦截。
            - 新增两个可调参数：「设置 → 运行设置」里的思考回放单条 / 整轮上限（默认 4000 / 16000，0 = 关闭）。
            - 本地验证：全量单测 1429 例通过；三条真实路径复测——真实库会话回放从 153 万字符降到 4024 字符且两次构建逐字节一致、gpt-4o 的 auto 档确认不发 max_tokens 且全程零 400 往返、12000 字正常思考确认不触发熔断。

            The application verifies `naiba-chat.exe` against the SHA-256 value
            in `naiba-chat-update.json` before installing an update.
