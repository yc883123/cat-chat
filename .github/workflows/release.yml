name: Build and publish Windows update

on:
  push:
    branches: [master]
  workflow_dispatch:

permissions:
  contents: write

env:
  RELEASE_VERSION: 2.8.8-beta
  RELEASE_TAG: v2.8.8-beta
  # 新名包（给人直接下载）：包内是 `cat-chat.exe`。
  PACKAGE_NAME: cat-chat-2.8.8-beta-windows-x64
  # 旧名包：只为兼容既存文档 / 教程 / 口耳相传的旧下载链接，内容与 `PACKAGE_NAME` 等价，
  # **仅包内 exe 名不同**。更新器从不消费 zip，所以它不参与自动更新链路。
  LEGACY_PACKAGE_NAME: naiba-chat-2.8.8-beta-windows-x64

concurrency:
  group: naiba-chat-release
  cancel-in-progress: true

jobs:
  release:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install build dependencies
        run: |
          python -m pip install --disable-pip-version-check --upgrade pyinstaller pywebview pystray "mcp==2.1.1" pymupdf "av==18.1.0" python-multipart
          python -m pip install --disable-pip-version-check --upgrade --force-reinstall --no-cache-dir pillow
          python -c "from PIL import Image; print('Pillow', Image.__version__)"

      - name: Run tests
        shell: pwsh
        run: |
          if (-not (Test-Path "tests")) {
            Write-Host "tests directory not found; skipping unit tests."
            exit 0
          }
          $attempts = 2
          for ($attempt = 1; $attempt -le $attempts; $attempt++) {
            python -m unittest discover -s tests -v
            if ($LASTEXITCODE -eq 0) {
              exit 0
            }
            if ($attempt -lt $attempts) {
              Write-Host "Tests failed on attempt $attempt; retrying once."
            }
          }
          exit 1

      - name: Build executable
        shell: pwsh
        run: |
          $env:NAIBA_BUILD_COMMIT = "${{ github.sha }}"
          $env:NAIBA_BUILD_VERSION = "${{ env.RELEASE_VERSION }}"
          python -m PyInstaller --noconfirm --clean naiba-chat.spec

      - name: Create update manifest
        shell: pwsh
        run: |
          # !!! 更新协议常量，逐字不可改 !!!
          # 仓库已更名为 cat-chat（旧地址由 GitHub 301 长期重定向），但清单这三个字段是**已发布
          # 客户端逐字校验**的协议：`repository` 必须仍是 yc883123/naiba-chat、`asset` 必须仍是
          # naiba-chat.exe、sha256 仍对 dist\naiba-chat.exe 计算。改任何一个，全部历史版本用户
          # 会永久失去自动更新，且无法用版本号界定受影响范围。详见 naiba/updater.py 模块头注释。
          $hash = (Get-FileHash -Algorithm SHA256 dist\naiba-chat.exe).Hash.ToLowerInvariant()
          $releaseNotes = @(Get-Content -Raw -Encoding UTF8 release_notes.json | ConvertFrom-Json)
          $manifest = [ordered]@{
            repository = "yc883123/naiba-chat"
            version = "${{ env.RELEASE_VERSION }}"
            commit = "${{ github.sha }}"
            asset = "naiba-chat.exe"
            sha256 = $hash
            published_at = (Get-Date).ToUniversalTime().ToString("o")
            release_notes = $releaseNotes
          }
          $manifest | ConvertTo-Json | Set-Content -Encoding UTF8 naiba-chat-update.json

      - name: Create Windows package
        shell: pwsh
        run: |
          # 两个 zip 装的是同一份字节（cat-chat.exe 是 naiba-chat.exe 的副本，SHA-256 相同），
          # 区别只在包内 exe 的文件名：新名包给人下载，旧名包留给既存的旧链接。
          Copy-Item -LiteralPath "dist\naiba-chat.exe" -Destination "dist\cat-chat.exe" -Force
          foreach ($pair in @(
              @{ Name = "${{ env.PACKAGE_NAME }}";        Exe = "cat-chat.exe" },
              @{ Name = "${{ env.LEGACY_PACKAGE_NAME }}"; Exe = "naiba-chat.exe" }
          )) {
            $packageDir = Join-Path "dist" $pair.Name
            New-Item -ItemType Directory -Path $packageDir -Force | Out-Null
            Copy-Item -LiteralPath "dist\$($pair.Exe)" -Destination $packageDir
            Copy-Item -LiteralPath "naiba-chat-update.json" -Destination $packageDir
            Copy-Item -LiteralPath "README.md" -Destination $packageDir
            Compress-Archive -Path "$packageDir\*" -DestinationPath "dist\$($pair.Name).zip" -Force
          }

      - name: Publish 2.8.8 Beta release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: Cat Chat 2.8.8 Beta
          prerelease: false
          make_latest: true
          files: |
            dist/naiba-chat.exe
            dist/cat-chat.exe
            dist/${{ env.PACKAGE_NAME }}.zip
            dist/${{ env.LEGACY_PACKAGE_NAME }}.zip
            naiba-chat-update.json
          body: |
            Cat Chat 2.8.8 Beta Windows build from `${{ github.sha }}`。

            > **关于文件名的说明**：仓库已更名为 `cat-chat`（旧链接由 GitHub 自动跳转）。本次 Release 同时提供 `cat-chat.exe` 与 `naiba-chat.exe`——**这是同一个文件，SHA-256 完全相同**；两个 ZIP 内容也等价，只有包内 exe 的文件名不同。应用内自动更新仍使用 `naiba-chat.exe` 资产与原有清单协议，**你不需要做任何操作**，已安装的旧版本会照常收到更新。

            **本次变化：手机端体验一轮修完——手机敲回车不再误发消息（回车 = 换行，Ctrl/⌘+Enter 才发送）；运行中的输入区不再被「插话」键挤成一团（插图标的毛病也修了）；图片与文件预览都有「下载」了，桌面壳点下载会弹系统「另存为」（以前点了毫无反应）；消息里的文件卡片有了「打开 / 打开文件夹 / 另存为」；附件分成「照片/视频」与「文件」两条入口，点「文件」不会再跳进相册；手机上改文件不再丢光标；插话编辑态补上「取消」。另外工具集配置修了三处：添加卡真的以当前勾选为底稿、摘要行有常驻的「编辑当前工具集」、删除工具集前会列出在用的 Agent。** 直接覆盖安装即可，**不需要迁移数据，也不需要重新配置**；历史会话、API 卡片、Agent、Skill 与 MCP 配置原样保留。

            本次更新（完整清单见 `release_notes.json` / 应用内「更新说明」）：
            - **手机裸回车 = 换行（本次重点）**：手机软键盘上敲回车以前会直接把消息发出去，想换行只能去找那个小箭头——误发之后只能删掉重打。现在「回车」就是换行，要发送请按 Ctrl/⌘+Enter（接外置键盘照旧好使）；三处把「Shift+Enter 换行、Esc 取消」写进提示语的输入框（切换编辑态、插话编辑、编辑消息）也改成手机看得懂的说明。判据不数界面，数的是聊天请求：手机上敲回车，请求数必须是 0。
            - **运行中的手机输入区不再挤成一团 + 手机终于能下载了（本次重点）**：运行中多出来的「插话」键把 360px 宽的输入区压到只剩 88px，现在手机上把输入区拆成独立一行、「插话」键带文字并长到 44px，并顺手修掉「插话键图标从来没画出来过」（描边属性写在另一个类上，它一直回落成实心黑三角）。图片与文件预览都有了「下载」；桌面壳用的是 WebView2，`<a download>` 在这里不引发保存对话框，所以桌面点「下载」原本毫无反应——现在走宿主接口弹系统「另存为」，你自己取消不算失败。
            - **文件卡片、附件、编辑、插话取消（本次修复）**：消息里的文件卡片加了动作条（鼠标悬停 / 手机常驻「⋯」）：打开、打开文件夹、另存为，其中「打开文件夹」只在桌面壳里出现；附件键改成先问「照片/视频」还是「文件」，此前「文件」只写扩展名，多数 Android 选择器会回落成「什么都收」并把相册摆在第一位（你点「文件」却进了相册），现在以 MIME 类型为主且绝不含图片/视频；文件面板编辑中不再整体重绘（以前会把光标、选区和打到一半的内容一起换掉），「保存 / 取消」移到编辑区脚底；插话编辑态补上「取消」，走与 Esc 完全同一条路径——纯界面还原，一个请求都不发。
            - **工具集配置三件事**：①「添加自定义工具集」卡以前写着「从当前勾选开始」实际却固定以「标准模式」为底稿，现在底稿真的跟着当前勾选走；②工具组合匹配不上任何卡片时以前没有入口能改它，摘要行现在有常驻的「编辑当前工具集」（进去只换视图、一个工具都不动）；③删「我的工具集」会列出用到它的 Agent 名字，并注明删除只移除卡片、不改动这些 Agent 已保存的配置；点卡片会覆盖当前勾选时也会先问一声。
            边界与安全（如实说清）：手机端的改动要更新到本版本才能看到——手机访问的是安装包里内置的界面资源，只改源码不生效。工具集删除是拷贝语义，删卡片不影响任何 Agent 已保存的配置；没限制过工具的 Agent 点卡片不会被拦；「添加」卡不覆盖勾选、也不经过确认。

            本地验证：全量单测 **1872 例通过**（本版新增 61 例守门：插话编辑态必须同时有保存与取消、取消分支一个请求都不许发；下载头拼装与「只有 `download=1` 才加头」且不得影响 Range 与缓存头；桌面桥三个动作的返回收敛与「用户取消不算失败」；附件「文件」入口必须 MIME 优先且不得含图片/视频；编辑中跳过重绘、监听只挂一次；样式表花括号配平；工具集三项——底稿跟着勾选走、编辑入口不得覆盖勾选、删除前必须在用名单。五组变异核对全部真变红）；另有六个手机端端到端冒烟共 112 项与既有工具集冒烟 95 项、输入框高度冒烟 13 项全部通过；短路径闸门（CI 环境差异自检）全量复跑通过。

            The application verifies `naiba-chat.exe` against the SHA-256 value
            in `naiba-chat-update.json` before installing an update.
