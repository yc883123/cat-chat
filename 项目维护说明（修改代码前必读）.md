# 项目维护说明（修改代码前必读）

> **本文档性质：这是一份"当下的快照"，不是修改记录。**
> 每次改动代码后，必须同步更新本文档中对应的章节（结构 / 模块 / 依赖 / 契约 / 测试 / 目录），
> **直接在原有描述基础上改写为最新情况**——禁止追加"变更日志 / 历史记录 / 旧版本曾有"类段落。
> 正确维护的标志：文中找不到任何"已过时"表述，所有描述与当前代码严格一致。
> 若发现本文档出现过期内容（如"旧方案曾…"、"待办已完成"），说明维护方式跑偏了，立即改写清除。

---

## 一、文档维护方式（后来者必读）

1. **改代码与改文档是同一件事的两次落笔**：结构、模块、契约、测试、目录变化时，本文档同步改写；
2. **直接改写，不追加**：例如新增一个模块 → 在「代码架构」章节的相应目录树/模块表中插入该模块并删除已不存在的条目；
3. **禁止出现**：变更日志、历史时间线、"之前是…现在是…"的对比叙述、"见旧方案文档"的引用（旧方案文档已随重构完成而删除）；
4. **一致性自检**（每次维护后花 1 分钟）：
   - `naiba/` 模块清单 ↔ 本文「后端模块」章节逐项对上；
   - `tests/` 文件数与测试用例数 ↔ 「验证资产」章节数字对上；
   - `public/js/` 文件清单 ↔ 「前端模块」章节对上；
   - 契约条目（事件 type / metadata 键 / run_context 键）↔ `naiba/core/contracts.py` 与 `naiba/core/messages.py` 对上。
5. **路径纪律**：文档中的路径一律用**语义写法**——`%LOCALAPPDATA%\NaibaChat`（冻结版数据目录，可展开讲述其语义）、`项目根\...`（相对仓库根的写法，如 `项目根\data\chat.db`）。**禁止代入当前运行环境的盘符绝对路径**（如真实盘符路径、`C:\Users\<用户名>\...`）——其它维护者（及其 AI）的机器上根本没有这些路径，看到只会困惑；真实路径一旦出现在示例里也必须同时说明其语义归属（哪个模式/哪类数据）。**代码、脚本与测试夹具同样适用**（见 §8.8）。

---

## 二、项目是什么

**Cat Chat**（原名 Naiba Chat）：Windows 本机 AI 自动化工作台（当前版本 2.7.7-beta）。单进程内完成：

- **HTTP 服务**（默认 `http://127.0.0.1:8765`，源码入口 `python server.py`）；
- **桌面壳**：pywebview 内嵌窗口 + pystray 托盘（入口 `launcher.py`；与 `server.py` 是同一 APP 的两种外壳，经同一 `data/server.lock` 互斥；**WebView2 用持久 profile**——`private_mode=False` + `storage_path=app_dir/webview`，否则 pywebview 默认的私有模式会让 localStorage 每次退出被清空，见 §九.54）；
- **对话主链路**：在线/本地大模型（OpenAI 兼容 / Anthropic / Gemini / Ollama / LM Studio / llama.cpp / Unsloth）+ 工具调用（内置工具 / Skill / MCP）+ 后台 Job + Plan 计划 + 视觉工具（模型按需调用）+ 视频抽帧（probe_video / extract_frames，含关键帧与联系表）+ 搜索 + 文件产物管理 + SQLite 持久化 + 自动更新；
- **前端**：原生 ES modules（无框架、无构建工具链），静态资源由后端直接服务。

### 2.1 命名契约（显示名 vs 内部标识）

**产品显示名是 `Cat Chat`（大小写与空格固定）；机器依赖的内部身份继续用 `naiba` 系标识。改文案时只动左列，右列一律不动。**

| 用途 | 取值 |
|---|---|
| 显示名（浏览器标签、桌面窗口标题、托盘悬停标题、原生对话框标题、启动横幅与 CLI 帮助） | `Cat Chat` |
| 仓库 / 包 / EXE / 更新资产（`yc883123/naiba-chat`、`naiba/`、`naiba-chat.exe`、清单协议） | 保持 `naiba` 系 |
| Windows 身份与位置（`naiba.chat` AppUserModelID、托盘内部 name、`%LOCALAPPDATA%\NaibaChat`、`app_dir/webview`） | 保持原值 |
| 配置 / 数据 / 浏览器存储（`config.json`、`chat.db`、`naibaChat*`、`naibaOnboardingDismissed`、`lanSkill*` 兼容读取） | 保持原值 |
| 协议与工具兼容（`server_version`、`application/x-naiba-file-path`、`naiba_chat_` 上传前缀、MCP 历史别名、`naiba.*` logger、`NAIBA_*` 环境变量） | 保持原值 |
| 用户内容（历史消息、工作区名、用户 Agent 名称与提示词、已上传文件名） | 不做任何自动替换 |
| **图标**（`icon.ico`、托盘/窗口/EXE 内嵌等一切引用） | **保持原动漫图标，改名、换品牌、发版均不得更换**（2026-09-20 本人明确要求） |

**只改显示名，不搬迁数据**：仓库名、EXE 名与既有数据位置不变，因此没有新增数据库迁移；`app_dir/config.json` 与 `app_dir/webview` 也不动（换应用根会让配置与浏览器偏好看起来消失）。守门测试 `tests/test_display_branding.py` 同时断言左列文案与右列原值，防止后续把两者混改。

---

## 三、代码架构总览

### 3.1 目录与入口

```
项目根\
├── .venv/                 # 本机虚拟环境（Python 3.12，依赖齐全）：**本仓库一切 python 命令都用 `.venv\Scripts\python.exe`**（见 §六 解释器纪律）
├── launcher.py            # 桌面壳入口：pywebview + 托盘；仅一处注入 AppHTTPServer；**隐藏入口 `--run-skill-script`（冻结版执行 Skill 脚本，先把 stdio 强制 UTF-8）**；**窗口就绪即 `show()` + `restore()` 一次**（`window.events.loaded` → `_on_window_loaded`：外部启动方式（更新脚本、计划任务）若带 SW_HIDE，主窗口会一起被藏起来，这条兜底把界面带出来）；**不在启动时自动查更新**（检查只由用户点「检查更新」发起，见 §3.3 侧翼——此前 `threading.Timer` 会在启动 4 秒后自动查一次，用户会觉得「没点就看到新版本」）；**显示标题统一为 `Cat Chat`（托盘悬停标题 / 窗口标题 / 启动失败消息框），托盘内部 name 与 `naiba.chat` AppUserModelID 仍保留原值**（见 §2.1）
├── server.py              # 门面（约 141 行）：PC 路径常量 / APP 注解 / naiba 侧 re-export /
│                          #   write_status / acquire_instance_lock / main 委托 / __main__
├── naiba/                 # 后端全部逻辑（领域分层、单向依赖、组合根唯一）
├── public/                # 前端：index.html + styles.css + js/（17 个 ES modules）
├── tests/                 # 单元/契约/golden 测试（102 个 test_*.py + golden_replay.py + golden/ 3 基线 + tool_testkit.py）
├── scripts/               # Playwright 浏览器回归与截图脚本（需本机 npm 安装 playwright）
├── verify/              # 验证脚本：**§六 清单里的可复用脚本已入库**（检查器/冒烟/播种器/传输校验/数据工具），其余临时产物与备份包被 gitignore（见 §六 末尾「目录纪律」）
├── docs/                  # manual/ 用户手册（生成物）；阶段交接文档（dev/）使命完成即删，已清空
├── 版本更新说明.md        # 发布检查清单（本地维护，.gitignore 排除，不随仓库发布）
├── 项目维护说明.md        # 本文档
└── data/  build/  dist/   # 运行时数据 / 编译中间 / 产出（全部被 gitignore）
```

### 3.2 后端分层与依赖红线

依赖方向：**低层不依赖高层；`launcher → server(门面) → naiba`，无任何包外中转**（根目录的旧入口 shim 已于重构期全部删除）。

```
┌─ launcher.py ─ server.py(门面) ─┐
│                                 ▼
│  组装/HTTP     naiba/app.py（NaibaChatApp 组装根 + 全部 api_* 业务方法）
│                naiba/http.py（RequestHandler 薄包装 + AppHTTPServer + 服务生命周期）
│                naiba/config.py（ConfigStore + 默认配置/内置 Agent/工具目录/provider 能力推断）
│                naiba/paths.py（PathContext + static_asset_version lazy-once）
│  编排          naiba/run/*（session/stream/manager/chat） jobs.py plans.py subagent.py
│                events.py（事件总线：写事件+唤醒单点）
│  服务          naiba/tools/*（registry 单一定义声明/分发 + executor 权限引擎 + providers/* 按域工具定义）
│                skills/*（agent/catalog/install/policy/context）
│                storage/*（store 持久化 / media 缩略图与上传管理 / media_collect 媒体采集 / job_media 产物写回） llm/runtime vision/runtime mcp.py search.py
│                pdf.py video.py capability.py updater.py
│  纯函数        naiba/core/*（contracts/history/choices/attachments/file_changes/conv_files/
│                diagnostics/messages/exceptions/cards/migration/network/paths/media_types/http_range）
│                llm/protocols.py llm/stream.py vision/images.py
│  零依赖        naiba/net.py（net_io 别名：统一出站网络与代理策略）
└────────────────────────────────┘
```

**五条红线（违反即架构事故）**：

1. **运行时模块永不 import `server.py`**（它只服务 launcher 与测试）；`naiba/app.py` 是唯一允许 import 全部子模块的组装根；
2. **低层不依赖高层**：core 不知道 run 的存在；llm/vision 的纯函数层不触碰网络锁状态；
3. **跨模块共享必须显式化**：状态/回调/协议走构造参数、注入（`AppContext`/`ConfigView` Protocol）或契约常量；禁止经全局 `APP` 中转、禁止裸字符串键；
4. **契约先行**：新增事件 type、事件负载键、metadata 键、run_context 键，必须先登记进 `core/contracts.py` / `core/messages.py` 再使用（有守门测试反查）；
5. **前端 ESM 的 import 绑定只读**：跨文件可变状态的写入必须经声明模块导出的 setter（本项目三个全局已按此改造：`sidebarScrollToActive`/`sidebarScrollRaf`/`stickToBottom`）。

### 3.3 后端模块用途（按目录）

| 模块 | 职责 | 关键符号/说明 |
|---|---|---|
| `naiba/app.py` | 组装根 | `NaibaChatApp`：构造注入全部组件（config/storage/**media_collector**/**job_media_writer**/models/catalog/mcp/executor/tools/jobs/run/plans/vision/…）；HTTP 业务方法统一 `_reply(payload, status) -> (payload, status)`，http.py 只做薄包装；`bootstrap()` 暴露前端所需名单（含 `media_exts` = `core/media_types.media_exts_payload()`：扩展名与分桶上限的唯一来源）；`browse_workspace(raw, conversation_id)`——带会话 id 时以会话工作区为根并隐藏点号条目（@ 引用弹层），不带时为全局默认工作区（设置页目录树，行为不变） |；**启动序列**：`jobs.resume_interrupted()`（按 checkpoint 复活可续作业）之后接 `runs.recover_interrupted_runs()`——把重启中断的对话轮次已经流式输出过、只存在于 `run_events` 里的内容重建为一条 partial 消息（失败只记日志，绝不阻断启动）
| `naiba/http.py` | HTTP 薄层 | `RequestHandler`（授权/路由/编解码/流式 NDJSON/静态文件 `_serve_static`）、`AppHTTPServer`（注入 app）、`write_status`/`acquire_instance_lock`/`main_entry`；**`GET /api/file` 支持单区间 Range**（206 + `Content-Range` + `Accept-Ranges: bytes`，越界 416 + `bytes */size`，无 Range 保持 200；**分块 256KB 发送**，几百 MB 视频不再整包进内存）、`_MEDIA_MIME_FALLBACK` = `core/media_types.MIME_BY_EXT`（系统 mimetypes 未注册时的兜底，含 .m4v）；上传端点 `POST /api/uploads`（multipart 流式）与 `POST /api/uploads/delete`、**Agent 头像 `POST /api/agents/avatar`（multipart 小体积进内存 → 中心裁切 WebP 落盘）与 `GET /api/agents/avatar/<文件名>`（内容哈希命名 → 可长期强缓存；文件名非法/缺失 404）**、`GET /api/imaging/stats`（缓存文件实时统计）、`GET /api/workspace/browse`（可选 `conversation_id` 切换到会话工作区根）；自定义指令端点 `GET/POST /api/starter-prompts`（响应含 `prompts` 与 `missing_presets`）、`POST/DELETE /api/starter-prompts/{index}`、**`POST /api/starter-prompts/restore`（恢复默认预设）**；快捷消息端点 `GET /api/quick-messages?sort=usage`、`POST /api/quick-messages[/{index}]`（请求体只有 `text`）、`DELETE /api/quick-messages/{index}`、`POST /api/quick-messages/{index}/use`（两套端点各自独立，勿混用）；**`POST /api/jobs/{id}/resume` 在父任务已取消时回 400 + 取消原因**（`JobRegistry.resume` 抛的 `ValueError` 映射为 `BAD_REQUEST`，不再静默 404 让人以为记录丢了） |
| `naiba/config.py` | 配置 | `ConfigStore(path, paths=PathContext)`：加载/迁移（run_command→pwsh、MCP 去重、vision 超时迁移、自动路由退役清洗、退役工具名映射、**内置 Agent 退役后摘除遗留 `built_in` 标记**等）/保存；`upsert_agent` 在 **id 留空时自动分配 `agent_<12 位 hex>` 持久化唯一 id**（用户不再手填）并**保留调用方未提交的 `avatar` 字段**（表单保存不带头像，不能顺手清掉）；`default_config`（含 `appearance: {theme: system|light|dark, skin: violet|ocean|rose|forest, chat_font_size: 13-18, chat_font_family: 结构三档(system|serif|rounded) + 预置字体键(11 个) + custom, chat_font_family_custom: ≤100 字符}`——**会话区字体**：`normalize_appearance()` 是加载路径与更新路径**共用**的归一化出口（键集固定，非法枚举回默认、字号 `clamp_chat_font_size` 夹回 13-18、自定义串 `clean_chat_font_family_custom` 截断；family 切回非 custom 时**保留**用户打过的串）。字体键清单是三个常量：`APPEARANCE_CHAT_FONT_STRUCTURAL`（3 档结构）/ `APPEARANCE_CHAT_FONT_PRESETS`（11 个点选预置：yahei/pingfang/simsun/kaiti/simhei/fangsong/noto_sans/noto_serif/lxgw/smiley/harmonyos）/ `APPEARANCE_CHAT_FONT_FAMILIES`（前两者 + custom 的 frozenset 白名单）——**前端点选列表的 `key` 必须与 `PRESETS` 逐字一致**，否则表现为"点得动、保存失败"，护栏在 `tests/test_appearance.py`；更新路径的 `_validated_chat_font_size` 对越界/非数值**显式报错**、字体族的报错文案由白名单动态拼出（列全所有可选值，别写死枚举）——滑块送来的值永远在区间内，越界只可能是客户端算错，"静默夹回"会变成"设了没生效却查不出原因"）、`built_in_agents`（**清单为空、机制保留**）、工具目录/预设（`tool_catalog_entries` 端到端顺序（与分组顺序一致）+ `TOOL_GROUP_INFO` **7 组单一维度分类**（读取与检索 / 文件写入与编辑 / 命令与脚本执行 / 联网与外部服务 / 视觉与图片 / 任务与扩展 / 长会话，每项 `name/desc/badge/tone`，风险徽标 只读·会改文件·高风险·联网·会写产物·会改动）；`tool_group_entries` 每组输出 `tools`（整组全选/计数）/`direct_tools`（平铺区）/**`subgroups`**——动态 `mcp__<server>__<tool>` 归入「联网与外部服务」并按服务器二级分组；`TOOL_PRESETS` **5 档**（只读模式 6 = 读取三件套 + 纯读取的 `read_pdf`/`probe_video` + `vision_analyze` / 标准模式 13 = 日常 8 件 + PDF 三件套 + 视频抽帧两件套 / **长会话模式 17 = 标准 + 长会话组四件套** / ComfyUI 联动 18 / 全能模式 group:* = 33）且**除 `group:*` 外一律显式列工具名**（分类变粗后组引用会把 ComfyUI/MCP/Skill 管理一起带进本不该有的预设）；预设的工具名**必须已含依赖闭包**（ComfyUI 预设显式带 `job_output/job_status/job_wait`，否则下拉显示的个数 ≠ 套用后的实际个数）；`_DEFAULT_SELECTED_TOOLS` = 标准模式（守门钉死一致，否则新建 Agent 打开显示「自定义」）；`resolve_tool_preset` 对不存在的组名/工具名**告警而非静默丢弃**）；**「我的工具集」= `tool_sets`**（`get_tool_sets` / `upsert_tool_set(name, tools, set_id)` / `delete_tool_set`；上限 `TOOL_SET_MAX=30`、新增排最前、加载时经 `_migrate_tool_sets` 规整，`public()` 与 `/api/settings` **不含**它——走 bootstrap 与 `/api/tool_sets` 专用接口）；**它必须存在 config.json，不能用 localStorage**：冻结版 pywebview 默认 `private_mode=True`，WebView2 的 localStorage 每次退出都被清空（用户保存的工具集曾因此重启即消失）；**两份互不干扰的提示词列表**：开始页「自定义指令」`starter_prompts`（`get/add/update/remove_starter_prompt`、插入顺序；**内置预设经 `BUILTIN_STARTER_PRESETS` 并入，与用户条目同权——都可编辑/删除**；每个预设带**稳定 id**、条目记 `preset_id`：改标题不影响识别、删除只记入 `starter_presets_dismissed`（永不自动复活）、**版本新增的预设会在下次启动自动补齐**；条目可带可选 `desc`/`icon`，`update_starter_prompt` 保留附加字段；`count_missing_starter_presets`/`restore_starter_presets` 支撑「恢复默认预设」——后者清空删除清单）与会话内「快捷消息」`quick_messages`（**只有正文**：`get_quick_messages(sort)` 返回带 `index/text/count/added_at/used_at`（内置预设条目另带 `preset_id`）的条目，`sort="usage"` 按 `quick_message_score` 排序；`add_quick_message(text)/update_quick_message(index,text)/remove_quick_message/record_quick_message_use`——编辑保留统计、使用计数累加；旧数据里的 `title` 字段读取时丢弃；**内置预设 `BUILTIN_QUICK_MESSAGES`（当前 1 条：`handoff-report`「写交接报告并 reset_context」）与开始页预设同款语义**：`_sync_builtin_quick_messages()` 每次加载补齐缺失项、条目记 `preset_id`、**编辑保留 `preset_id` 所以不会被原版覆盖**、删除记入 `quick_message_presets_dismissed`（永不自动复活））；**两道「别永久卡住」的运行设置**：`local_first_byte_timeout_seconds`（默认 120，0=关闭，5-1800）与 `agent_step_limit`（默认 200，0=不限制，1-1000）——留空一律回落默认（0 是有效值，不能用 `||` 回填）；两者经 `runtime_guard_options()` 注入 `generation_options()`，于是**对话 / 子代理 / 计划执行 / 计划整理四处调用方一处接入全区生效**，且随 run 快照持久化（重放口径一致）；**注意键名故意不叫 `max_agent_steps`**：那是老版本遗留、启动时被 `self.data.pop("max_agent_steps", None)` 显式丢弃的键，沿用同名会让老配置残值突然生效把用户步数卡死；**本地窗口探测回写 `remember_local_context_window(model_key, window)`**：把 `llm.local_probe` 探到的真值写进 provider 的**独立字段 `context_window_probed`**（绝不覆盖用户显式 `context_window`/`context_size`，值没变不落盘——每轮对话都会走到这里）；`_infer_context_window` 取值优先级 = 显式配置 → `context_window_probed` → 0，`_context_window_source` 相应返回 `local_config`/`provider_config`/`local_probe`/`unknown`——不回写的话探测值只活在会话 profile 副本里，设置页与其它读取点仍拿 0 → 退回在线 256k 兜底；**聊天背景图路径校验（`_validated_chat_background_image`）三口径**：① 绝对路径 + 必须落在 `data/uploads` 或 `data/backgrounds` 内（安全边界，不放宽——路径会被前端拼进 `/api/file?path=…`）、按**内容**过格式白名单；② 文件不在磁盘上（缓存清理 / 数据目录迁移 / 换机器）时**不再抛错、更不清空**，先按文件名在当前受管目录（含 `uploads/YYYY-MM-DD/` 分日目录）兜底找回并迁移路径（`_same_name_background_image`），越界路径只有真找回同名文件才迁移、否则照旧拒绝；③ 确实找不到就**原样保留路径**，由前端标记「文件暂不可用」引导重新选择——丢文件可以，丢设置不行（见 §九.77） |
| `naiba/paths.py` | 路径 | `PathContext`（app/exe/public/resource/data dir + config/status/lock 路径；`rebind_data_dir`）、`static_asset_version`（lazy-once：哈希 `public/js/*.js` 集 + `styles.css`） |
| `naiba/core/contracts.py` | 契约 | `RunContext`（TypedDict 26 键 + `RUN_CONTEXT_KEYS`/`default_run_context`/`validate_run_context`；含 `workspace_dir`——产物类工具落盘默认位置、`media_intent`——用户本轮是否明确要看图，供枚举类工具的 `intent_gated` 门禁、`pull_interjections`/`mark_interjections_consumed`——插话队列的读/消费回调）、`EventType`（30 种枚举：对话流 wire 25 种 + Job 域 4 种 + usage 1 种；无消费内部事件已随阶段 4 清理；插话两枚 `user_guidance` / `interjection_consumed` 属 wire 侧）、`EventPayload`（负载键并集，含 `media`/`media_truncated`；golden 反查守门）、`EVENT_PAYLOAD_KEYS`（事件 type→负载键权威图，与枚举一一对应；None=宽松条目仅校验 type）、`MESSAGE_ORDER_KEYS`（历史消息排序契约：后端唯一决定顺序 `(created_at, rowid)`，前端按 API 数组序渲染不自行排序——守门测试钉死）、`AppContext`/`ConfigView`（`@runtime_checkable` Protocol）；`MetadataKeys` 从 `core/messages.py` re-export |
| `naiba/core/messages.py` | 消息契约 | `MetadataKeys` + `MESSAGE_METADATA_KEYS` 权威清单（metadata 键唯一契约来源；含 `attachments_truncated`——消息级媒体分桶截断的自述信息，前端据此渲染提示块；**`session_start`——「新会话」分割线标记，标在某条消息上表示"新会话从这条消息之后开始"**） |
| `naiba/core/history.py` | 历史构建 | `build_model_history`（trace 权威重放、reasoning 透传、附件事先言；**遇到带 `session_start` 的消息即清空此前历史（该消息本身也不进新上下文）→ 同一会话窗口内可多次裁剪前缀，多条标记取最后一条**；早期版本的 role=session 标记行同样被裁掉，属向后兼容路径）、`encode_image_for_model`、`_content_read_tool_outputs`（不可信结果脱敏）；**思考回放限长双闸门（§九.103 / §B）**：单条 `MODEL_REASONING_REPLAY_MAX_CHARS`(4000) + 整轮 `MODEL_REASONING_REPLAY_TURN_CHARS`(16000)，由 `_ReasoningReplayBudget` **由新到旧**分配额度、`_clip_reasoning_text()` 截断并拼 `MODEL_REASONING_REPLAY_OMITTED` 省略标记（**唯一实现**，`message` 与 `trace` 两条出口共用，守门断言标记只拼装一次）；两个参数经 `build_model_history(..., reasoning_replay_max_chars=, reasoning_replay_turn_chars=)` 传入，由 `config.reasoning_replay_options()` 注入。**只截回放、不动落库**（库里永远原文）⇒ 截断确定性、前缀缓存不受影响（这是 §九.103「回灌毒化」的治理点） |
| `naiba/core/choices.py` | 选项识别 | `_detect_choice_groups`（意图收紧/编号剥离/紧凑拆分） |
| `naiba/core/tool_results.py` | 工具结果可见性 | 单一事实源：`model_visible_run`（模型上下文：tool/success/脱敏+截断标记 result，无 arguments/reason，**不含 media**）/`display_tool_run`（展示：模型可见 result + arguments/reason + **宿主采集的 `media`/`media_truncated`**）；按工具剥离机器字段（vision 装载 path/thumb/尺寸、artifact_report sha/绝对路径；**vision_image_ops 的 crop/heatmap 产物路径例外保留**——模型引用产物的凭据）并统一截断标记 |
| `naiba/core/http_range.py` | Range 解析 | `parse_byte_range(raw, size)`（**单区间** `bytes=a-b`/`bytes=a-`/`bytes=-n` → 闭区间；无头/非 bytes 单位/语法非法/多区间 → None 走整包 200；语法合法但越界/空文件 → ValueError 走 416）、`content_range_header`；纯函数，`/api/file` 大文件可 seek 的依据 |
| `naiba/core/media_types.py` | 媒体唯一定义 | `MEDIA_EXTS_BY_KIND`/`MEDIA_EXTS`/`MEDIA_KINDS`/`IMAGE_PROCESS_EXTS`/`MEDIA_BUCKET_LIMITS`（图 20 / 视频 8 / 音频 8）/`MIME_BY_EXT` 的**唯一来源**：`attachments.MEDIA_PRODUCT_EXTS`、`storage/media.IMAGE_SUFFIXES`、`http._MEDIA_MIME_FALLBACK`、`/api/bootstrap.media_exts` 全部由它派生；`media_kind_of/is_media_path`（路径/URL/ComfyUI `?filename=` 三类来源）、`truncate_by_kind`（分桶截断，落盘前与消息级共用）、`media_exts_payload`（前端名单）、**媒体采集声明契约**（`MEDIA_POLICIES`=inline/intent_gated/never、`MEDIA_EXTRACTORS`=none/scan/structured、`normalize_media_declaration`、**`JOB_MEDIA_DECLARATIONS`/`job_media_declaration`**——异步 Job 按 kind 声明）；纯常量+纯函数，不做 IO |
| `naiba/core/attachments.py` | 附件（纯函数） | `union_run_media(runs)`（**消息级媒体汇总**：按出现顺序合并各 run 的 `media`，同来源/同名去重（同名优先留带缩略图的），再按分桶上限截断并返回可自述的 truncated 信息）、`_image_intent`（图像意图判定，结果经 `run_context["media_intent"]` 交给工具声明 `intent_gated` 消费）、**`upload_reference_lines`/`compose_user_content`（用户轮次模型可见文本的唯一拼接口径：非空文字保持 `文字+换行+引用行` 逐字节不变，纯附件轮次以 `ATTACHMENT_ONLY_NOTICE` 固定提示行替代空文字——_run_chat 与历史重放共用）**；扩展名名单引用 `core/media_types.py`；**不再扫结果文本、不做 IO**（采集归 `storage/media_collect.py`），`ENUMERATION_TOOLS` 硬编码集合已随声明驱动退役 |
| `naiba/core/file_changes.py` / `conv_files.py` | 文件 | 本轮改动文件汇总；会话文件访问边界（改动过/工作区双门槛、越界拒绝）；**`browse_workspace_tree(root, raw)`（会话工作区浅层只读浏览：目录优先排序、恒定隐藏 `.git`/`.naiba_write_test`、`hide_dotfiles` 可选、500 条上限+truncated、越界/不存在报错、返回 root/path/rel/parent_rel/entries）**、**`resolve_file_references(text, root)`（输入框 @ 引用 → 模型可见文本：只替换工作区内真实存在的 `@相对路径`，文件→绝对路径、目录→绝对路径+尾分隔符；邮箱/越界/不存在的 token 原样保留）** |
| `naiba/core/diagnostics.py` | 诊断 | `_cache_debug_enabled`（CACHE_DEBUG_ON 或 NAIBA_DEBUG_CACHE=1，默认关）、debug 系列；**`_permission_debug_enabled`（PERMISSION_DEBUG_ON 或 NAIBA_DEBUG_PERMISSION=1，默认关）——权限判定诊断：输出 executor 标签 / 当前工作区 / 请求路径（原始+解析后）/ 允许根 / 命中结果 / 理由，只写日志不改判定** |
| `naiba/core/paths.py` / `exceptions.py` / `cards.py` / `migration.py` / `network.py` | 杂项纯函数 | `path_within`（**一律按调用方 resolve 后的实际目标判定**：Windows 大小写由 pathlib 的 Windows 语义天然不区分（实测 3.13）；工作区内链接指向外部**仍判越界**——不得用「词法路径也算命中」放宽，需要信任外部链接目标时把真实目标显式加入允许根）、**`path_within_any`（多允许根统一入口）与 `within_detail`（诊断明细：原值/解析值/命中下标，只读不改判定）**；异常类（TaskCancelled/ActiveRunError 等）；角色卡解析；旧数据迁移；LAN 探测 |
| `naiba/storage/store.py` | 持久化 | `ChatStorage(path)`：conversations/messages/background_tasks(run+job 共用)/run_events/plans/cleaned_jobs 六表，迁移 v1-v17（**v14 存量压缩**：reasoning_delta 窗口合流、done/cancelled 载荷瘦身、终态 snapshot 收缩 + VACUUM，前置自动整库备份到 data/backups；**v16 会话级 `first_turn` 列**：分支对话继承首轮上下文、清空已结束任务后仍可读；**v17 `model_name` 列**：`model_key` 指向 API profile，模型名可按会话覆盖；全部幂等）、**重启恢复的状态词表收归模块级常量 `ACTIVE_TASK_STATUSES` / `TERMINAL_TASK_STATUSES`**（启动清理、活动列表查询与取消判定共用这一份定义，`_status_in_clause` 负责渲染 SQL 字面量；**三处「会话是否被占用」的判定 `active_run` / `create_chat_run` / `create_run` 也已改为引用它**——此前各自手写 `('queued','running','waiting','cancelling')` 而漏掉 `stopping`；**`PRIMARY_RUN_KINDS`（顶层对话 Run 的 kind）也从这里导出**，任务面板据此把「回答记录」与「后台作业」分开；`list_background_tasks(..., exclude_kinds=…)` 支持只回后台作业）——`jobs.cancel()` 会把子任务置为 `stopping`，而启动清理曾漏掉它，于是「已取消但线程还没退出去」的子任务成为**重启也清不掉的僵尸**，让整条会话永久显示「回复进行中」、三个救援入口全部失效（2026-09-14 客户机实测）、工作区绑定修复；finish 收口收缩终态 snapshot（`_slim_run_snapshot`，interrupted 保留）；`compress_run_events`（**终态单 run 合流**：流式 delta → 整段 reasoning，与迁移同窗口口径）、`storage_usage`/`compact_database`（设置页历史数据管理：统计与 VACUUM）；唯一 SQLite 写入口，事件写入经 `EventBus` 单点发射；**轻量模式已退役**（2026-09）：`lightweight_mode`/`lightweight_disabled_features` 两列保留在表里（历史迁移不动、新会话走列默认值），但读写两侧都已摘除、`_conversation_dict` 不再对外暴露；首轮用户消息的会话标题回退 `_attachment_title`（**纯附件轮次无文字时取首个附件名**，不再一律「新对话」；`create_chat_run` 与 `add_message` 两处同口径）；`create_chat_run(title_text=…)` 让标题取用户原文（@ 引用轮次不会把解析后的绝对路径当标题）；**`update_message_metadata`（就地更新一条消息的 metadata + 推进 conversations.updated_at，供异步 Job 产物写回；content/role/created_at 不动）**；**`set_session_start(conversation_id, message_id, source/handoff_path/note)`（「新会话」分割线：把标记写进**被点击那条消息**的 metadata，**不新增行**——`messages` 排序键是 `(created_at, rowid)`，中间插行要构造/回移时间戳（同毫秒相邻会插错位），写 metadata 位置天然精确；允许同一会话多条标记，最新那条决定上下文起点）、`clear_session_start(message_id)`（只摘掉该键，其余 metadata 与消息本身保留）、`delete_session_start(message_id)`（遗留 role=session 标记行的删除路径，只允许删该角色）**；**长会话三件套的查询全在这里**：`find_conversations(query, limit)`（按标题子串找会话，带消息条数与末条预览；默认 20 / 上限 50）、`conversation_context_boundary(conversation_id)`（最新分割线的 `(created_at, rowid)`，与 `build_model_history` 同口径：**边界那条自己也不在上下文里**）、`search_history(query, conversation_id=…, current_conversation_id=…)`（全库模式用**窗口函数按会话分区取每会话最新 3 条**——旧版的全局 400 行候选窗会被噪音会话吃满、把老会话整个挤掉，实测复现过；返回 `scope` 明示检索范围、`total_hits`/`truncated` 让截断显式、当前会话命中带 `in_context`）、`read_conversation_messages(conversation_id, start, count)`（按 `ROW_NUMBER() OVER (ORDER BY created_at, rowid)` 的 1 起始序号切片读原文，带附件名）** |；**启动清理把「本次被判为中断的任务」一并交出**（`interrupted_tasks`：id/conversation_id/kind）——正文重建要用 `run_events`，属运行层职责，存储层只负责如实记账；中断原因文案收归模块级常量 `INTERRUPTED_TASK_REASON`（任务行与事件流共用一份）
| `naiba/storage/job_media.py` | Job 产物写回 | `JobMediaWriter.write_back(job)`（**异步 Job 终态把产物挂回发起它的助手消息**：`parent_job_id` → Run(kind∈chat/plan_execute) → 该会话 `metadata.run_id` 匹配的助手消息（优先 Run `detail.message_id`）→ 用 `MediaCollector` + `JOB_MEDIA_DECLARATIONS` 采集 → 挂到"调用该 Job 的那次工具调用"（按 result 里的 job_id 匹配 tool_runs/activity），匹配不到则并入消息级 `attachments` → `storage.update_message_metadata` 就地更新并推进 `conversations.updated_at`；幂等按来源去重，消息已删/写回失败安全放弃） |
| `naiba/storage/media_collect.py` | 媒体采集器 | `MediaCollector.collect(run, declaration, intent=False)`（**工具产出点唯一采集入口**：原始 result 只在那里可见；只对成功且非 `NEED_CONFIRM` 的结果提取——失败信息里的路径不是产物；声明驱动 `policy`（never/intent_gated/inline）与 `extract`（none/structured/scan）；候选**先按分桶上限预截断再落盘**，避免一次列目录拷几百张图卡住；结构化记录优先、文本路径兜底——**URL 只去尾部标点并以首个 CJK 字符为界（查询串 `?&=` 必须保留），Windows 路径在散文终止符处截断**；同来源/同名/同内容三级去重；**托管缓存按内容哈希落盘**（`<内容哈希16>_<name>`：ComfyUI 复用文件名时来源不变而内容已变，按来源缓存会显示旧图）并生成缩略图，失败记日志并保留原来源）→ `{media:[{kind,name,source,thumb_path}], truncated}` |
| `naiba/storage/media.py` | 媒体与上传 | 缩略图/上传缓存/清理族（显式传 data_dir/workspace_dir；`IMAGE_SUFFIXES` = `core/media_types.IMAGE_PROCESS_EXTS`（**压缩**格式）、`THUMB_SOURCE_SUFFIXES` = 压缩格式 ∪ `.gif`（**可出缩略图**格式，GIF 取首帧、原图动画不动））；**上传管理族**（2026-09）：`store_uploaded_file`（图片压缩/缩略图 + 内容级去重（同大小候选+sha256 命中复用）+ 分日目录 `uploads/YYYY-MM-DD/` 原子落盘；**缩略图命名 = 主图 stem + `_thumb.webp`**——前端兜底、去重复用 `_thumb_path_for`、删除成组 `remove_uploaded_file` 三处都按此约定推导）、`upload_target_dir`、`remove_uploaded_file`（树内校验+主图缩略图成组删除）、`auto_clean_uploads`（**B1 自动清理**：上传后超 `imaging.auto_clean_limit_mb`（默认 256，设置页可调，0=关闭）触发，只删**未被消息/快照引用**的最旧缓存组——引用中的图片永不自动删除；**2026-09 修复（用户报障：附件区破图 / 点击无法放大 / 模型 vision_analyze 报未找到图片文件）**：被引用缓存本身已超阈值时「降到上限」永不可达，原实现会退化成「删光所有未引用组」并当场删掉刚上传、还在输入框待发（尚未落库，`upload_path_referenced` 对它无效）的附件——现加两道保护：`UPLOAD_CLEAN_GRACE_SECONDS`（15 分钟）保护窗口（窗口内落盘/改动的组一律不删，同时覆盖"工具刚产出、metadata 还没写回"）+ `protect_paths`（`app._upload_spooled` 传本次刚落盘的主图/缩略图），返回 `skipped_recent`/`skipped_protected` 如实报告跳过数；手动清理 `_clean_uploads_cache`（`/api/imaging/clean`）**与自动清理共用同一阈值口径**（读取 auto_clean_limit_mb，0 时回退默认 256）保持按时间保留语义、不感知引用（**用户确认保留 + UI 明确告知**"按时间从旧到新、不区分引用、可能影响历史消息中的文件"）、`is_uploads_path`；另加两个只读判定（同一口径，供发送前校验复用）：`resolve_attachment_file`（附件本地路径解析：原路径 → 失效时按文件名在 uploads 树内兜底，与 `/api/file` 一致）与 `missing_cache_attachment`（缓存树 uploads/generated 内的附件是否已不在磁盘上；URL 与工作区等外部路径一律不判缺失）；**「在用」引用判定的集合由 `app._upload_path_in_use` 提供**（消息/快照引用之外，**聊天背景图 `chat_background.image` 也算在用**——它只写在 config.json 里、不是消息附件，不补这条判定就会被自动清理当"无人引用的旧图"删掉，背景随后"自己没了"；手动清理仍按"不区分引用"的既有语义） |
| `naiba/storage/avatars.py` | Agent 头像 | `store_agent_avatar(data_dir, agent_id, raw, previous)`（**中心裁切正方形**（`ImageOps.fit` centering 0.5/0.5）→ 256px → WebP → 落盘 `data/avatars/<agent_id>_<内容哈希12>.webp`，**换图即换名并删除上一份**；非图片/超 8MB/空文件抛 ValueError）、`read_agent_avatar`（只接受本模块命名形态，挡目录穿越）、`is_avatar_filename`/`avatar_dir`/`AVATAR_SIZE`/`AVATAR_MAX_BYTES`；**不进 uploads**——uploads 有"未被引用即清理"语义，头像会被误删 |
| `naiba/pdf.py` | PDF 服务 | PyMuPDF（1.28.2）三件套：`extract_pdf_text`（页标记 `== 第 N 页 ==`/30000 字符/50 页/截断续读起点/无文本层提示）、`render_pdf_pages`（单次≤20 页/长边 1600/单 PDF 缓存≤100 张/sha256 键控幂等/WebP 缩略图复用）、`render_pdf_region`（百分比矩形+半区关键词/scale 1-6/长边≤3072/区域过小与越界报错）；页图/局部图缓存 `data/uploads/pdf_pages/<sha16>/`（托管缓存，不污染工作区） |
| `naiba/video.py` | 视频服务 | **工具 schema/策略绑定在 `naiba/tools/providers/video.py`（与 `providers/documents.py` 逐段对照：`probe_video` 走 `_make_read_policy`、`extract_frames` 走 `_no_confirm_policy`）**；PyAV（`av`）两件套：`probe_video`（时长/帧率/分辨率/总帧数/编码/音轨；总帧数用 `duration × fps` 估算并**如实标注 `frame_count_estimated`**——很多容器没有 `nb_frames`，静默给估算值 = 误导源）、`extract_frames`（`mode=times` 按时间点 seek / `interval` 按间隔均匀 / `keyframes` 逐帧解码 + 与上一保留帧降采样比平均绝对差挑切点，超上限按差异取前 N 再按时间排序；可选 PIL 拼**联系表**（每格 320px、标注 `t=..s`、长边 ≤1600）省 token）；**缓存键 = sha256(绝对路径 + size + mtime_ns) + sha256(抽取参数)**——视频动辄几 GB，抄 `pdf.py` 的「全文件 sha256」会让每次调用多花几十秒（本方案最容易被抄错的一处），落盘 `data/uploads/video_frames/<video_sha16>/<params_key>/`，同键命中 `frames.json` 清单即复用不重解码；**两级上限缺一不可**：单参数目录 ≤ `VIDEO_CACHE_MAX_IMAGES=200`、同一视频跨参数目录合计 ≤ `VIDEO_CACHE_MAX_PER_VIDEO=600`（参数一变就是新目录，只按目录计数拦不住膨胀）；帧图与联系表都出 `<stem>_thumb.webp`（联系表在结果里是裸路径，前端按此推导缩略图，不出就是 404 破图）；**`av` 惰性导入**：顶层 `import av` 会重演 `pdf.py` 的「缺依赖 → 整组测试 error」，缺失时抛明确业务文案（见 §九.53） |
| `naiba/events.py` | 事件总线 | `EventBus`（run/job 共用单点「写事件+唤醒」：`ensure` 条件池替代历史双 condition 池、`emit` 写事件+notify、`wait_for_events`、`drop`；持 app 取最新 storage）、`validate_event_payload`（契约前置校验：type∈EventType、键⊆EVENT_PAYLOAD_KEYS∪公共键；`NAIBA_STRICT_EVENTS=1` 或 run 通道默认抛错、job 通道告警忽略）、`status_sync_for`（run 通道事件→background_tasks(status/detail) 策略表，自 manager.emit if-elif 收敛）、`strict_events_enabled` |
| `naiba/run/session.py` | 会话固化 | 工具集决议（模式×Agent 范围×固化集×图像能力×Job 依赖闭包；Harness 别名不注入 allowed_tools，仅查规范名；固化集内退役名（如 glob_files）按 RETIRED_TOOL_MAP 归一为新名）、**工具互斥组**（`MUTUALLY_EXCLUSIVE_TOOL_GROUPS` = 子代理两种上下文模式 `subagent`/`subagent_spawn`，`normalize_tool_mutex` 是唯一权威、三个入口全覆盖：`resolve_allowed_tools` 返回前 / `bake_session_tool_ids` 固化前 / `enable_conversation_tools` 追加后；前端镜像 `AGENT_TOOL_MUTEX_GROUPS`，见 §九.116）、bake/enable、生成选项、路由消息（无状态函数）；**视觉工具集 = `VISION_READONLY_TOOLS`（vision_analyze）+ `VISION_WRITING_TOOLS`（vision_image_ops，会写产物文件）**，Craft 两者皆含、Plan 仅只读集 |
| `naiba/run/stream.py` | 事件流 | `_RunEventSink`（正文 delta ≥4096 字符/≥0.1s 缓冲、**推理流 reasoning_delta 到达即落库**（保住逐词实时推送节奏，最终形态由终态合流整理）、工具请求去重、取消即抛）、活动时间线 `_safe_activity`（**排序规则：思考/工具严格物理序；最终答复（最后 prose 段）固定时间线末尾**；条目带 `ts`（事件毫秒时间戳）与 `request_index`（usage 事件为请求边界，前端以横线分割请求轮次）） |
| `naiba/run/manager.py` | Run 骨架 | `ConversationRunManager`：emit（委托 `EventBus` + `status_sync_for` 状态同步）、cancelling 冻结、取消级联、3 秒看门狗、**插话四方法（`interject` / `guide_interjection` / `edit_interjection` / `delete_interjection`：`guide` 会逐个 `reject_execute` 该 run 的待确认工具调用让模型下一步看到新指令；`delete` **不要求 run 还活着**——终态后的残留清理才是常态，§九.100 ④）**、工具确认路由、executor 隔离（`executor_for_run`：按快照 `permission_mode` clone + **快照 `workspace_dir` 必用，缺失回退"该会话自己的工作区"（绝不落回启动期默认）** + `debug_label = run:<id>`），`ACTIVE` / `TERMINAL` 直接引用 `storage` 的状态常量（因此含 `stopping` 与 `interrupted`）；**`cancel` 只在父 Run 仍活动时改写它的状态**——已终态的父只是子任务的归属锚点（后台任务的常态就是"回答早已发出、只剩子 Job 在跑"），改写它会把这一轮回答从"已完成"篡成"已取消"，现在这种情形只级联停子任务并原样返回父行；`list` 透传 `limit` / `exclude_kinds` 给存储层；**`list_primary(conversation_id, active_only)` 只回顶层对话 Run**（`kind ∈ PRIMARY_RUN_KINDS` 且 `parent_job_id` 为空）——`/api/runs` 用它回答「这条会话在不在回答」，后台子 Job 曾让整条会话被判成运行中，把「分支 / 重新生成 / 新会话」三个救援入口全部挡住 |
| `naiba/run/chat.py` | Run 主循环 | `ConversationRunMixin`：`submit_chat`/`submit_plan`（**submit_chat 放行边界：文字与可用附件至少有一个**——空文字+附件是合法的纯附件轮次，两者都空才 400；落库前经 `resolve_file_references` 解析 **@ 工作区引用**，模型可见文本为绝对路径，气泡展示与首轮标题仍取用户原文）、`_run_chat`（取消双检/4 条收尾路径；`run_context["media_intent"]` = `_image_intent(用户原文)`，供枚举类工具门禁；**系统提示的编排段按会话固化工具集条件拼接**——`web_search` 引导、`vision_prompt_sections`（vision_analyze / vision_image_ops 各自到齐才提，只开 ops 给单独一句，**且 vision_analyze 段按模型视觉能力分「分析/装载」两种文案——与 `session_tool_defs` 的 schema 分流同口径**）、PDF 策略（含 `read_pdf` 才提）、**上下文重置指引（含 `reset_context` 才提：先写交接文档、把绝对路径传 handoff_path、成功后本轮立即结束）**；工具集会话内恒定，故不破坏前缀缓存）、`_run_plan`、`_persist_aborted_message`（幂等重建）、`_persist_failed_message`；**三条收尾路径的媒体统一经 `union_run_media(runs)` 汇总**（取消/失败路径的 runs 由事件重建，媒体随事件带回），超限写 `metadata.attachments_truncated`；**done 路径把 `run_context["context_reset"]` 合并进 assistant 消息 metadata 的 `session_start`**（模型重置分割线的落库点；标记后 `build_model_history` 只取分割线之后的内容）；**finally 终态合流**（`compress_run_events`：流式期 delta 块 → 整段 reasoning，终态事件落库之后执行，失败不阻断收尾）；**首轮上下文 `first_turn` 落盘**（`_persist_first_turn_context`：完整 system/工具 JSON/生成参数/完整请求消息图片摘要化，重写于各终态事件 emit 之前——消除前端 done 后拉取竞态；技能取冻结集∪本轮引用）；本轮模型可见文本经 `compose_user_content(message, uploads)` 生成；**`_profile_with_model_override` 的本地窗口探测已补回写**：判定「是否已确定」改用 `context_window_source`（`local_config`/`provider_config` 才跳过探测，`local_probe`/`unknown` 仍重探——本地后端可能这次才起来或换了 n_ctx），探测成功后调 `config.remember_local_context_window(model_key, probed)` 落库；**计划整理走 `status=None`，必须自己声明 `lock_label="计划整理"`**（否则占住本地锁时不可归因） |；**`recover_interrupted_runs()`（重启中断恢复）**：启动清理只会把 in-flight 的 Run 标成 `interrupted` 并补一条 error 事件，模型此前吐出的正文只活在事件流里，界面上无从看到——用户看到的是「那条回复凭空消失，重启/刷新都不回来」。这里复用失败路径的重建逻辑（`_rebuild_partial_run` + `_persist_failed_message`）把它落库：正文、思考、工具活动时间线、已发出的 Skill 列表全部保留 + `partial`「未完成」标记 + 原因；一个字都没产出的轮次（无 delta/reasoning/tool 事件）跳过，只认 `PRIMARY_RUN_KINDS` 顶层 Run（子 Job 不生成空壳消息），同 run 幂等；`_persist_failed_message` 另接可覆盖的 `placeholder`（中断路径写「（本次回答未完成：服务重启，运行已中断）」，让用户不必猜这条为什么停在这里）
| `naiba/tools/registry.py` | 工具声明表 | `ToolSpec`（单一定义：名称/参数/side_effect/retryable/timeout/permission/execute/aliases/policy/system/metadata）+ `ToolRegistry`（注册/Provider 接入/别名查询层归一/单插槽分发/MCP 动态注册/退役名引导）；**`MEDIA_DECLARATIONS`（每个内置工具的媒体采集声明 policy/extract，装配期经 `declare_media` 写入 `metadata["media"]`，缺失即报错；MCP/第三方未声明走默认 inline/scan）+ `media_declaration(name)`（查询层别名归一，采集器消费）**；注册 37 个内置工具 def（core 10 + Harness 别名 4 + job 7 + comfyui 2 + capability 3 + vision 2 + search 1 + history 3 + documents 3 + video 2；**新增 `reset_context`（模型主动重置上下文，描述只留一行钩子、规则在系统提示常驻区）与长会话三件套 `find_conversations`/`recall_history`/`read_conversation`（同属「长会话」组）**；**视频抽帧两件套 `probe_video`/`extract_frames`（归「读取与检索」，已并入 `_DEFAULT_SELECTED_TOOLS`（= 标准模式 13）与预设：只读模式只收纯读取的 `probe_video`，`extract_frames` 与 PDF 三件套进标准 / 长会话 / ComfyUI）**；call_mcp、artifact_report、旧视觉 8 名与 glob_files/glob（并入 list_directory）已退役）；`schemas()` 为模型/Web 可见集（33 个）——Harness 别名仅查询层归一（执行兼容），不向模型披露，metadata（含 media 声明）不进 schema |
| `naiba/tools/executor.py` | 工具引擎 | `ToolExecutor`：权限确认状态机（confirm/auto/full/deny + NEED_CONFIRM 协议）、def 级 policy 评估（注入解析器）、`execute_unchecked` 受控直调、`resolve_tool_path`/`workspace_for_run`/`path_within` 公开助手（包装器与引擎共用同一工作区来源，判定不再分叉）；**NEED_CONFIRM 暂存完整 `run_context`（批准执行原样传回：工作区、取消信号、权限模式与依赖 run_context 的行为都恢复；不宣称"可中断已启动的阻塞工具"）**、`debug_label`（`run:<id>`/`app`，仅权限诊断日志用）；**不持有任何工具实现**（tool_* 方法/别名表/危险表已于单轨化废除） |
| `naiba/tools/providers/*` | 工具域定义 | 每域一 provider：`core.py`（ToolContext + 10 实现函数 + Harness 别名绑定 + def 级权限策略；**`reset_context`**：校验 handoff_path 非空/绝对/文件存在/大小 > 0（任一不满足即拒绝且不置位），通过后把 `{at,source:"tool",handoff_path,note,tasks}` 写进 `run_context["context_reset"]`，其中 `tasks` 是经 `ToolContext.extra["active_background_tasks"]` 注入的当前后台任务快照（快照失败只记日志、不阻断）；同轮重复调用被拒；执行器按结果里的 `ok` 判成败，权限走 `_CORE_CONFIRM_REASONS`（confirm 模式需确认、auto 免确认）；**判定与执行同源（工作区判定修复）**：`policy_workspace`（Run 快照 `workspace_dir` → 引擎传入 workspace → 装配期 ctx）与 `ctx_for_run`（执行侧 per-call 派生 `ToolContext`）保证 read_file/list_directory/search_files/write_file/edit_file/read_pdf 的**判定与执行使用同一工作区**（切换会话工作区后相对路径不再按启动期工作区解析）；越界确认理由附「当前工作区：…」便于定位；read_file **行数不限**（内容 ≤30000 字符全量返回；实测 >50 行小文件曾被行数上限截断、模型被迫多次续读）、字符预算 30000 截断（**max_chars 对模型隐藏**、执行层硬上限 30000 防浪费；显式 max_lines 保留兼容；截断标记含行区间与续读起点 end_line/with_line_numbers）、**二进制防护**（8KB 嗅探 NUL 强判定 + 非 \t\n\r 控制字符比例 >5% 辅助——PDF/图片/二进制明确报错并按类型给工具引导，GBK/UTF-8 中文不误伤；杜绝 errors=replace 直读把二进制乱码灌进模型上下文）、执行成败判定（pwsh/脚本非零退出码、http_request≥400 → success=False；**run_skill_script 子进程强制 UTF-8 运行时** PYTHONIOENCODING/PYTHONUTF8——Windows 管道 locale 编码导致中文路径/报错乱码）、**list_directory 单入口**（glob_files 已并入：pattern/files_only/recursive/start_after 续枚举，按名称排序+绝对路径）、search_files ignore_case 统一生效+超大文件跳过计数+**path 支持单文件（传文件即搜该文件；路径不存在显式报错）**）、`jobs.py`（7 任务工具，含 subagent/todo_write）、`capability.py`、`vision.py`（vision_analyze 唯一识图入口 + vision_image_ops，各 1 个 def）、**`search.py`（web_search + 长会话三件套 `find_conversations`/`recall_history`/`read_conversation`：只做参数校验与文本渲染，SQL 全在 storage 层——**provider 不许直接 `_connect()`**；渲染口径：全库模式带会话 id、命中带 `#序号`、当前会话命中按上下文边界标「在上下文中 / 已划出上下文」、截断显式、id 缺失报错不静默退回全库、片段 200 字（限定模式 600 字）、read 单条 800 字/单次 8000 字预算）**、`comfyui.py`、`documents.py`（**PDF 三件套**：read_pdf 同 read_file 越界确认；pdf_render_pages/pdf_zoom_region 免确认——只写宿主管控缓存；缓存目录经 data_dir_getter 动态注入防 rebind 漂移）；`tools() -> list[ToolSpec]` 返回单一定义并绑定 execute/policy；**def.policy 使用引擎传入的当前运行（会话级）工作区**（不闭包捕获装配期配置）；**读取策略可信根 = 会话工作区 + active Skill 根 + 宿主管控缓存**（`data/uploads`/`data/generated`，经 `ToolContext.data_dir_getter` 动态获取——用户上传附件与宿主产物读取免越界确认，真正的工作区外路径仍必确认） |
| `naiba/skills/agent.py` | Agent 编排 | `SkillAgent`（构造注入 catalog/executor/model_complete/**media_collector**）：Skill 注入（冻结集稳定渲染/`/ref` 引用）、系统提示组装（**完成后写入 `run_context["trace_system"]`**——trace 增量设计不含 system，供 first_turn 落盘取完整原文）、**按工具集条件注入的编排指引**（`comfyui_script_guide_enabled(allowed)`：确有 ComfyUI 能力——内置 `comfyui_*` 或名字带 comfy 的 MCP 工具如 `mcp__comfy-mcp__*`——且 `write_file`/`pwsh`/`run_in_background` 至少一个可用时，才提「ComfyUI/短剧自动化优先小型脚本路径」）、工具循环/协议解析（JSON/XML + **Kimi K3/Harmony `<|open|>tools` 文本协议**：支持单个/并行 call、argument JSON 类型还原，缺闭合标记按 parse_error 有界纠正而不执行半截调用）/**上下文预算（`_context_budget` 窗口未知按 kind 兜底并预留系统提示 + 原生工具 schema；`_context_fits` 计数覆盖 `content`/`reasoning_content`/`tool_calls`/`role=tool` 结果，见 `_replay_footprint_tokens`）**/**插话消费点（§九.100 ①）**：`consume_interjections()` 挂在循环顶（cancel 检查之后、`step += 1` 之前），把 guided 队列**追加**到 `messages` 末尾（前缀「用户插话（优先处理，并根据新指令继续当前任务）：」）、`mark_run_interjections_consumed`、emit `interjection_consumed`；**只 append、不改写不重排**，且「最终答复与插话同一步到达」时先落答复再把插话排回末尾——相对上次请求仍是 append-only，前缀缓存因此不受影响**/****循环内上下文复查（Q1）**：轮首闸门只看一眼「历史 + 本轮提问」，而工具结果、追加指令、图片批会在循环里继续把请求体撑过窗口——现在每次模型调用前以 `messages` 自身为基准做**增量**累计（`context_base_tokens` 只算一次，之后只累加新追加的消息，不每步重算全量历史），越预算即发 `context_full` + `status`、**停止继续调用工具并保住已完成的工具结果**（文案 `_CONTEXT_LOOP_NOTICE`，与轮首闸门的 `_CONTEXT_FULL_NOTICE` 共用同一份 `_NEW_SESSION_HINT` 指引，两处措辞不得漂移）；语义差异：轮首 = 整轮拒绝（还没干活），循环内 = 保住半成品/**截断成因区分（Q1）**：`_truncation_info(finish_reason, content, usage=, limit=, local=)` 在 `length` 时额外给出 `cause`——**本地**模型且 prompt+completion ≥ 窗口 95%（`_context_saturated`）判 `context`（窗口耗尽，续写只会让请求更大），否则 `output`（撞输出上限）；落进 `run_context["truncation"]` 与消息 metadata.truncated，前端据此分文案/并行工具/熔断；**`_collect_media`：工具产出点按声明采集媒体写回 `run["media"]`**（原始 result 只在这里可见；采集异常记录日志但不丢弃工具结果）；**每次完成模型请求即发射 `usage` 事件**（最后一次请求口径命中率 + 累计请求数 + request_ms + `requests_detail` 逐次明细，前端流式尾端用量框实时更新/展开）；**反幻觉由事实回环（job_status 等"不存在"提示）+ 系统法规承担**（逐轮防幻觉守卫已整体移除）；**Agent 循环有步数上限**：`_resolve_step_limit(max_steps, options)`（优先级 显式参数 → `options["max_steps"]` → `DEFAULT_MAX_STEPS`=200，0=不限制，脏值回落默认）——`while True` 此前只有「模型给出最终答复 / 两条熔断 / 取消」三种出口，模型「一直调工具但拿不到结论」时可以无限烧下去并一直占着本地锁；触顶时发 `run_failed` + 返回可读原因（与两条熔断同口径），预算将尽（`max(3 步, 上限 10%)`）时先往尾部追加一条收尾提醒（不动 system 前缀，缓存不受影响）；**上下文重置收尾**：工具循环末尾检测 `run_context["context_reset"]`（reset_context 成功后置位）→ 追加一句「已交接，本轮到此结束」、定稿 trace、发 `status` 事件后**立即 return**（不再带旧上下文继续干活；收尾路径把标记写进本条 AI 回复 metadata，见 `run/chat`））；**重复答复检测（§九.105 / §E）**：终态正文经 `_normalize_answer_text`（`strip` + 折叠空白）后与 `_last_answer_before()` 取到的上一轮 assistant 正文**逐字相同且 ≥ `_REPEAT_ANSWER_MIN_CHARS`(20)** 时，只发一条 `status` 事件（`_REPEAT_ANSWER_NOTICE`，`_repeat_answer_notice()` 返回文案）——**不拦截、不落 metadata**（用户可能真的要求复述；改动消息契约会波及 `build_model_history` 的字节稳定性，见 §九.100 的同类教训） |
| `naiba/skills/catalog.py` | 目录 | `SkillCatalog` 扫描/签名缓存/`read_skill_content`/`by_id`；source 取显式 `sources`（缺省仍按位置：0=builtin/1=managed/其余=external）；`package_dir` 指向打包内置目录——**同一 id 也由内置目录提供时来源强制为 `builtin`**，并给每条目附 `duplicate_dirs`（同 id 的多目录副本清单），供「删除只能隐藏、不能搬走单一副本」的判定与前端提示；`bundled_entries`/`bundled_skill_ids` 供 app 判定内置/多副本 Skill |
| `naiba/skills/install.py` | 安装 | 安装/校验（ZIP 炸弹防护）/解压/删除/引用清理；`validate_and_install_skill` 等被 capability 与 HTTP 复用；`_unique_dir` 供 app 的散装上传收口复用；**删除前校验 Skill 定义严格归属托管目录**——`root == managed`（历史遗留的裸 `SKILL.md`，即定义直接落在扫描目录根）时退化为只回收该定义文件，绝不整目录 `shutil.move` |
| `naiba/skills/policy.py` / `context.py` | 策略 | `SKILL_POLICY_MODES`/`normalize_skill_policy`；上下文预算/选择/token 估算；**`DEFAULT_CONTEXT_WINDOW`（在线兜底 256k）/ `LOCAL_DEFAULT_CONTEXT_WINDOW`（本地兜底 32k）与 `fallback_context_window(profile)`——窗口未知时按 `profile.kind` 选口径。让本地模型套用在线 256k 是 2026-09-14 客户机「学 16 小时后永久等待本地模型资源」的根因（闸门几乎永不触发）** |
| `naiba/llm/runtime.py` | 模型客户端 | `ModelRuntime`：`complete`（重试矩阵含 stream_options/tools/reasoning 三级回退 + DeepSeek 思考模式"无工具兜底"与 **429 + 全部 5xx** 退避——5xx 是供应商内部错误（响应体常只有 `Internal Server Error`），按瞬时故障重发模型请求；**codex_responses 的流式空正文、以及「有推理但正文为空」的空流**（`EmptyModelStreamError`）按同一退避 + 可取消等待 + 带次数提示重试（后者实测于 mimo-v2.5 长思考后正文为空、用户侧等于「气泡空白」，退避重试大概率恢复；既无正文也无推理的空流仍保留原 `RuntimeError`、不重试）；**失败请求体自动落盘** `naiba-model-error-payload.json`（思考回传类 400/422 与全部 5xx；超长字段经 `_sanitize_payload` 压成 `<str:N>…` 占位、总量 512KB 上限、**不含 API Key**；cwd 或 `NAIBA_ERROR_DUMP_DIR`，本机取证用））、可取消 IO、线程隔离 last_*（含 `last_reasoning_id`）、供应商管理；**等本地锁走 `_acquire_local_lock`：0.5s 粒度可中断等待（cancel_event 置位即抛「任务已取消」）+ 每 `LOCAL_LOCK_WAIT_NOTICE_SECONDS`(5s) 回报一次排队时长。原 `lock.acquire()` 无超时不看 cancel——本地锁是全进程唯一类属性，被另一处调用（尤其 `status=None` 因而界面隐形的视觉调用）占住时，主对话线程会无限静默等下去、「停止」对它无效**；**流式读取统一走 `_iter_stream_lines`：看门狗线程在 cancel 置位时 `close()` 连接，打断阻塞中的 `readline`，并把随之而来的 I/O 异常翻译成「任务已取消」**——原先三条流式路径（ollama / lm_studio / sse）都是 `for raw_line in response:`，阻塞在 socket 读、循环体不看 `cancel_event`，本地模型 prefill 期间「停止」形同虚设（只能等满 30 分钟超时）；非取消路径的异常原样上抛，不影响既有重试矩阵**；**本地流式请求另有「首字节超时」`LOCAL_FIRST_BYTE_TIMEOUT_SECONDS`(120s) + `LocalModelFirstByteTimeout`**——同一个看门狗线程多管一件事：从进入生成器起算一个字节都没读到就超过该秒数时 `close()` 连接并抛专用错误（文案给可行动原因：请求体超真实窗口 / 模型仍在加载），**在线请求不加**（云端排队合法，且已有 180s 总超时）；可用 `options["first_byte_timeout_seconds"]` 覆盖（0=关闭），由运行设置注入。原行为是「prefill 做不完」与「正在 prefill」在界面上无法区分，用户要干等 1800 秒**；**本地锁占用者具名 `lock_label`**——类级 `_local_holder_guard`/`_local_holder_label` + `_set/_clear_local_holder`，`complete` 在本地锁占用期间登记 `options["lock_label"]`（默认 `DEFAULT_LOCK_LABEL`="对话回复"，视觉识别/子代理/计划执行/计划整理/连接测试各自声明），等锁方经 `_busy_notice` **指名道姓**（首次发现忙碌就立刻回报一次，之后每 5 秒一次）——`status=None` 的隐形占锁者不再不可归因；**上下文溢出的用户可见报错（Q1）**：HTTPError 分支**最先**判定 `is_context_overflow(detail)`（或错误体的 `error` 对象/`message` 字段），命中即抛 `ContextOverflowError`——它是 4xx、重发必然再失败，绝不能被下面「剥离字段后重试」的兼容链当成协议不兼容（那样只会白打一次注定失败的请求，再把错误冲淡成原始 JSON）。文案在 `_context_overflow_error` / `context_overflow_message` 一处拼装：**后端名**（优先用户配置的供应商名，其次 `llama.cpp`/`Unsloth`/`Ollama`/`LM Studio` 标签）+ **当前窗口**（优先后端自报的 `n_ctx`/`maximum context length`，其次本机探测/配置值）+ 「新会话 / 设置 → 模型」两条可行动指引，本地与在线共用一份口径；流式路径的溢出由统一出口 `except ContextOverflowError` 补文案（本地 attempts=1 本就不重试；在线也不重试）。**注意：在 except 处理块里抛出的异常不会被同一个 try 的其它 except 子句接住**，故 HTTPError 分支必须自己拼好文案（§九.83））；**思考三段治理的「流式期 + 计费期」两道闸门落在这里（§九.103）**：`REASONING_STREAM_BREAK_CHARS`(48000) 传进三个流解析器做推理预算熔断（只在正文仍为空时抛 `EmptyModelStreamError`，带上 `reasoning_chars`），`ONLINE_STREAM_TOTAL_TIMEOUT_SECONDS`(900) 经 `_iter_stream_lines(total_timeout=…)` 做**墙钟**兜底（超时抛 `StreamTotalTimeout`；注意 `ONLINE_MODEL_TIMEOUT_SECONDS`=180 是 urllib **每次读的空闲**超时，delta 持续到达就不断重置，永远救不了持续输出的死循环），可用 `options["stream_total_timeout_seconds"]` 覆盖（0=关闭）；降档白名单扩入 `lm_studio`（「有推理零正文」改抛 `EmptyModelStreamError`，此前直接 `RuntimeError`）；**A0 默认输出上限（§九.104）**：`_complete_online` 在模型确实支持思考时注入 `min(32768, ceiling)`（`DEFAULT_THINKING_MAX_OUTPUT_TOKENS` / `max_output_ceiling`），负载打 `injected_max_tokens` 标记 ⇒ `attempts` +1 且有自愈分支——上游拒收 `max_tokens` 时**只撤自己注入的那个值**重试（`_drop_injected_max_tokens`），用户显式配置的 max_tokens 不动；判据必须模型级，`auto` 档只命中协议族预设**不发字段**（否则 gpt-4o 每请求白打一次 400 往返）；Claude 的 4096 硬兜底抬到 `min(32768, ceiling)`；思考字段与降档一律改走 `naiba/llm/thinking.py` 的预设表（`_thinking_patch` / `_thinking_preset` / `lower_effort`），旧 `_LOWER_REASONING_EFFORT` 字典与 6 条画像 if 分支已删；**空流报错自报来源**：`_empty_stream_label` / `_empty_stream_phrase` 按画像给「LM Studio / Ollama / 本地模型 / 在线模型」（此前写死「在线模型」，本地 LM Studio 报错说自己是云端，见 §九.103） |
| `naiba/llm/local_probe.py` | 本地窗口探测 | `probe_local_context_window(profile)`：llama.cpp/unsloth 读 `GET /props`（`default_generation_settings.n_ctx`）、Ollama 读 `POST /api/show` 的 `num_ctx`、LM Studio 读 `GET /api/v0/models` 的 `max_context_length`；**只取运行期真实上下文，不取训练长度**（`n_ctx_train`/`*.context_length` 常十几万，等于把「实质不设上限」换个数字引回来）；进程内按 `(base_url, model)` 记忆化、2.5s 短超时、任何失败返回 0 且**不缓存失败结果**（本地服务晚启动时仍能探到真值）；由 `run/chat._profile_with_model_override` 接入，显式配置的 `context_window` 永远优先；**探测成功后会经 `config.remember_local_context_window` 回写 provider 的 `context_window_probed`**——否则真值只在会话路径生效、设置页仍显示「未知」并退回在线 256k 兜底 |
| `naiba/llm/protocols.py` | 协议层 | `ProtocolMixins`：29 个 wire 序列化/解析 staticmethod（openai/responses/gemini/claude/ollama/lm_studio、usage/内容/流 delta 归一、端点拼接、推理动作校验、**`_codex_responses_aggregated`** 从 Responses 流式聚合事件（`response.completed`/`response.output_item.done`，排除 `incomplete`、按 item id 去重）提取正文/思考/reasoning id/tool action）；`_responses_input` 对思考模式以 **reasoning item（content 为 `[{type:"reasoning_text",text:…}]` 块数组 + 唯一 `id`）** 回传——官方 schema「reasoning_text 内容块列表」；id 优先用服务端真实 id（流式 `output_item.added`/delta `item_id` 捕获、非流式 output 提取），旧历史无 id 时合成 `rs_h_*` 确定性 id；**带 tool_calls 的 assistant 消息无条件产出 reasoning item（无 CoT 时空文本占位）**——服务端可能返回无 CoT 工具轮，缺项即 400（§九.12 最终根因）；**思考强度识别已整体上移到 `naiba/llm/thinking.py`（§九.104）**——本模块只留转发：`_is_deepseek_profile` / `_is_kimi_k3_profile` 指向 `thinking_module`，`_reasoning_params(profile, effort, strict=False)` 调 `_thinking_patch` 出字段，旧的 6 条画像 `if` 分支与 `_LOWER_REASONING_EFFORT` 字典已删除（子串嗅探会在中继改名后静默失配）；降档改走 `_lower_reasoning_effort(effort, profile)`（按预设词表**声明顺序**下探） |
| `naiba/llm/thinking.py` | 思考强度数据层 | **声明式预设表 `THINKING_PRESETS`**（10 个预设：`deepseek_codex` / `deepseek` / `kimi_k3` / `kimi_k2` / `ollama` / `lm_studio` / `openai_codex` / `openai` / `claude` / `gemini`），取代原先散在 `protocols._reasoning_params` 的 6 条 `if` 分支 + 子串嗅探（§九.104）。命中度量 **卡片 > 模型名（`preset_model_patterns`）> `request_format` 默认**；`resolve_thinking(profile)` 解析出预设 + 档位，`thinking_payload(profile, effort, strict=)` 出 wire 字段，`lower_effort(effort, preset)` 按**词表声明顺序**下探一档（不是「应用四档减一」），`max_output_ceiling(profile)` 给 A0 上限，`thinking_active(profile)` 判「这个模型到底会不会思考」（A0 注入的判据，`auto` 档协议族预设不算），`strip_thinking_fields(payload)` 供降档重建，`is_deepseek_profile` / `model_matches` / `find_preset` 供 protocols 转发。**方言映射全部集中在这一张表**（OpenAI 原样 low/medium/high；DeepSeek codex=none/low/high/max 且 off→none 真关；openai_chat+DeepSeek **不发字段**；Kimi K3=off→low(关不掉)/low→low/medium→high/high→max，K2.x 绝不命中；ollama/lm_studio 各用自家的 `reasoning` / `think` 字段）；**未声明的档位在发请求前 `ThinkingConfigError`**（不再静默丢字段）；**资格约束**：`kimi-k3` + `codex_responses` 不得命中 K3 预设（中继按 OpenAI 方言自己翻译）。`protocols._reasoning_params` 保留旧签名（`strict=False` 容错），老 config 零迁移 |
| `naiba/llm/provider_presets.py` | 供应商预设数据层 | **接入名单的唯一来源**：`PROVIDER_PRESETS` 14 条**纯数据**（顺序即界面顺序：7 家大厂 deepseek/kimi/zhipu/qwen/openai/claude/gemini → 2 家中转 te/bailan → custom → 4 个本地后端 ollama/lm_studio/llama_cpp/unsloth），每条 `id/name/abbr/kind/base_url/request_format/model/key_required/key_url/hint`（`abbr` 是 1~3 字符首字母色块，**不引品牌 logo 图片**；在线预设的 `hint` 必须是「①这是什么站 ②怎么注册/充值 ③Key 在哪创建」三段式且出现的远端域名与 `key_url` 一致）；`provider_presets_payload()`（前端 `GET /api/provider-presets` 的响应，只读副本）、`provider_preset(id)`、**`provider_preset_key_url(id)`**（「打开注册页」唯一可用的地址来源——前端不许传 URL 进来）、**`apply_preset_values(values)`**（把 `preset_id` 展开成完整连接字段，**用户显式提交的非空值优先**，未知 `preset_id` 抛 `ValueError` 供 HTTP 层转 400）。**纯数据 + 纯函数、零项目依赖（尤其不 import config）**——所以 kind/请求格式的词表一致性只能靠 `tests/test_provider_presets.py` 对表守门。**改名单只改这一个文件**（前端不许复制第二份名单） |
| `naiba/llm/stream.py` | 流解析 | `StreamMixins`：SSE/Ollama/LM Studio 流解析、`_InlineReasoningParser`/`_ReasoningStreamer`（`<think>` 推理流）、工具协议守卫（`_classify_agent_output`/前缀缓冲，含 **Kimi K3/Harmony 保留 token `<|open|>tools` / `<|open|>call` 的跨 chunk 识别**，协议不进入可见正文）；`_read_sse_response` 在增量正文为空且非工具协议时，从 codex_responses 聚合事件回填正文/思考/reasoning_id/tool action；**流内错误必须出声（Q1）**：`ContextOverflowError` / `is_context_overflow` / `_CONTEXT_OVERFLOW_MARKERS` / `_stream_error` 定义在本模块（runtime.py 再导出，避免循环导入）——Ollama 的 `chunk["error"]`（此前**整块不读**，用户只看到「流式响应中没有文本内容」）、SSE 顶层 `error`（部分本地后端不返 4xx、把错误嵌在流里）、LM Studio 的 `error` 事件三条路径统一走 `_stream_error`：命中溢出特征串 → `ContextOverflowError`（带上完整原始错误体，供解析 `n_ctx`），否则 `RuntimeError("<来源> 流式错误：<原文>"`。判定用真值（`error not in (None, "", False, {}, [])`）以免把 `"error": null` 的正常事件当错误；**思考三段治理的「流式期」闸门也在这里（§九.103）**：三个解析器（`_read_ollama_stream` / `_read_sse_response` / `_read_lm_studio_stream`）都接 `reasoning_break_chars` 与 `progress`——`_break_on_runaway_reasoning` 累计推理增量，**只在正文仍为空**且越过 `REASONING_STREAM_BREAK_CHARS` 时抛 `_reasoning_break_error()`（`EmptyModelStreamError` 带 `reasoning_chars`）；`_mark_progress()` 每次真收到 delta 就回写时间戳，供 `_iter_stream_lines` 的**墙钟**总时长兜底区分「在输出」与「真卡住」；`StreamTotalTimeout` 异常类定义在这里（runtime.py 再导出） |
| `naiba/vision/runtime.py` | 视觉 | `VisionRouter`：图片安全占位（纯文本模型把图片改写为路径引用占位，由模型按需主动调用 vision_analyze）、**本地（`kind=local`）多模态大脑另加「每请求图片总量上限」`_cap_local_history_images`（`LOCAL_REQUEST_IMAGE_LIMIT`=12 张 / `LOCAL_REQUEST_IMAGE_BYTES_LIMIT`=8MB，只保留最近 N 张、更早的转路径占位）——`MODEL_IMAGE_HISTORY_LIMIT`=3 是**每条 user 消息**的封顶（计数在 per-message 循环里重置），全对话没有总量约束，16 小时会话上百张 ~1MB 图每轮全量重发会撑爆本地视觉塔 prefill；在线模型分支不动（保住 1.6.0 的前缀缓存契约）**、2 个视觉入口（vision_analyze 同名会话化分流：文本=后端分析/多模态=图片装载；vision_image_ops=PIL 本地计算）、**单批图片限制默认 4 张**（装载/分析两路径统一；超限不静默——工具结果与注入消息标注"共 N 张/已展示前 4/其余可继续调用"，供多模态分批注入带批次序号）、能力探测、probe；自建独立 `ModelRuntime` 防污染；**crop/pixel_diff 产物默认保存到当前会话工作区根目录**（`run_context.workspace_dir`，模型可直接引用、用户直接可见）；**两处视觉调用的 options 都声明 `lock_label`**（"视觉识别" / "视觉能力探测"）——它们传 `status=None`，占住全进程唯一的本地锁时界面完全看不到，靠这个标签让等锁的主对话能指名道姓 |
| `naiba/vision/images.py` | 图像原语 | 编码/缩略/尺寸/像素读取（纯 PIL 函数） |
| `naiba/net.py` | 出站网络 | `net_io`（`from naiba import net as net_io`）：本地直连判定、代理策略状态机、`open/configure/proxy_state`；仅接受 HTTP/HTTPS 手动代理，设置变更会使手动/系统/直连 opener 全部失效并重建；幂等 GET/HEAD/OPTIONS 请求在连接异常时按当前策略重建 opener 后重试一次，错误状态不切换代理模式 |
| `naiba/mcp.py` | MCP | `MCPRegistry`/`MCPServerConnection`：stdio 生命周期/重连退避/工具发现回调/公开 `connection()` 访问器 |
| `naiba/jobs.py` | 后台任务 | `JobRegistry`：JobSpec/状态机（含 stopping/interrupted）/worker（shell/check/http_poll/comfyui/subagent）/checkpoint 恢复/级联取消；**`cancel` 同时落 `cancel_requested`**（否则 `_set_status` 的"停止请求不被 worker 覆盖"是空转的，worker 下一次状态更新会把 `stopping` 打回 `running`）、**90 秒停止超时兜底 `STOP_WATCHDOG_SECONDS`**（只对登记过取消事件的作业生效——被误传到 Job 接口的顶层 Run 不得被强行标成已取消；到点仍是 `stopping` 就强制 `cancelled` + `finished` 并摘掉线程句柄，避免它永久占住活动名额）；`resume_interrupted()` 按 checkpoint 指纹只接续最新副本，并给源记录持久写入 `resumed_into` 或 `resume_skipped`，重启不重复复活；ComfyUI 批量轮询同时核对 `/history` 与 `/queue`，丢失 prompt 按持久化 `max_resubmit` 预算重提，用尽后以可解释错误收尾；事件与唤醒经 `EventBus`（`_emit` 容错语义）；**`_finish` 终态后触发 `_write_back_media`**（异步产物挂回发起消息，失败只记录）；shell worker 与 pwsh 工具一致强制 PowerShell UTF-8 输出前缀（中文路径/输出不乱码）；**父任务取消闸门**——`start()` 时父 Job 已 `cancel_requested` 或处于 `stopping`/`cancelling`/`cancelled` 就抛 `TaskCancelled`（拦住「取消后仍派生新 Job」，但**不拦终态父**，`resume()`/`retry()` 依赖终态父重建 Job），`resume()`/`retry()` 把 `TaskCancelled` 翻成 `ValueError`（HTTP 层转 400），子 Agent worker 的 `finally` 兜底 `_cancel_children`（worker 晚于取消创建的子 Job 在这里收网），启动扫尾遇已取消父只打 `resume_skipped` 不反复重试 |
| `naiba/plans.py` | 计划 | 交互模式（craft/plan）+ `PlanManager`：prepare→ready→building→finished 状态机、步骤分批执行、归档；`CraftToolExecutor` 的写判定与引擎同源（`workspace_for_run(run_context)` + `resolve_tool_path(..., workspace=…)`，不再自持装配期工作区）；计划执行的 run_context 带 `workspace_dir`/`cancel_event`；**计划执行的 options 必须声明 `lock_label="计划执行"`**（本地锁占用者归因，见 §九.65） |
| `naiba/subagent.py` | 子 Agent | 同进程委派（depth≤2/每父≤4）：经 `JobRegistry(kind=subagent)` + `agent_runner` 执行，Job 工具语义暴露给模型；**默认 label 从 instruction 前 24 字派生**（「子任务：前 24 字…」，空白归一；AI 显式传 `label` 优先）——`label` 经 `create_run` 落进 `background_tasks.message`，任务面板行标题（前端 `taskDisplayTitle`）直接显示它，不再笼统「AI 子任务」；**继承该会话的工作区与审批档位**（独立 clone executor + `workspace_dir`/`executor`/`cancel_event` 进 run_context）——不得回退装配期默认工作区（否则会话工作区内路径被判越界、`unpack_skill_archive` 取不到工作区）；**options 必须声明 `lock_label="子代理"`**：子代理跑在自己的后台 Job 里、占住全进程唯一的本地锁时，主对话只显示「等待本地模型资源」而无从归因（§九.65） |
| `naiba/search.py` / `capability.py` / `updater.py` | 侧翼 | 搜索供应商；Skill 安装/解包/检查工具；更新检查/校验/安装（仅依赖 net + 回调解耦；冻结版更新清单按版本排序去重，自动更新过滤 2.0.0 及更早版本，历史版本提供 GitHub Release 手动下载提示） |；**更新后的自动重启必须以普通窗口启动**：`apply-update.ps1` 里两条 `Start-Process` 都不得带 `-WindowStyle Hidden`——本程序是窗口化打包（`console=False`，本来就没有控制台要藏），Hidden 会让新进程的主窗口一起处于隐藏状态，于是进程活着、托盘图标在、界面却不出来，用户只能到托盘双击「打开窗口」才能唤回；**检查更新只在用户点「检查更新」时发起**（`POST /api/update/check` → `start_check(force=True)`，绕过 6 小时缓存；后端不再有启动时的自动检查，前端只把结果渲染出来——`index.html`/`07-models-agents.js` 的默认文案写「点「检查更新」才会去查新版本」）

### 3.4 前端模块（public/js，原生 ES modules，无构建）

入口 `main.js`（`import "./15-bind-events.js"`，其模块体末尾为启动调用 `bindEvents(); initialize(); …`）；`index.html` 单条 `<script type="module" src="/js/main.js?v=__ASSET_VERSION__">`；所有 API 调用经 `01-core.js` 的 `api()` 封装（鉴权头）；流式用 `fetch` 消费 `/api/runs/{id}/events?after=N` 的 NDJSON（无 EventSource）。

**前端显示名**：`index.html` 的 `<title>` 与欢迎向导标题、各设置说明里的产品称呼统一写 `Cat Chat`；`localStorage` 键名（`naibaChat*`、`naiba.agentToolTemplates`、`naibaOnboardingDismissed` 等）是用户偏好原值，**不得随显示名一起改**（改了等于老用户设置全丢），完整边界见 §2.1。

**视频抽帧（2026-09）在前端是零改动**：抽帧产物就是普通 PNG（帧图 + 联系表），走既有 `mediaMarkup`/灯箱/末尾去重；视频附件本身也早就能就地播放（`mediaKind` 读 `/api/bootstrap.media_exts`，`<video>` 渲染与 Range 请求均已就绪）。本功能只新增后端工具与服务层，前端**不需要**任何新代码——若将来要给帧图加"跳到该时间点"之类的专属交互，再回到本表登记。

| 模块 | 职责 |
|---|---|
| `01-core` | `state`（全局状态对象）、外观主题引擎 `initializeAppearance/applyAppearance/syncAppearanceFromBootstrap/saveAppearance`（日间/夜间/跟随系统与四套皮肤，服务端持久化）、`api`/`$`/`$$`、toast、复制、文本右键菜单（**「粘贴」在两条程序化通道都不可用时的唯一提示 `PASTE_UNAVAILABLE_HINT`**——`execCommand('paste')` 已被浏览器禁用、`navigator.clipboard` 在非安全上下文里不存在，所以提示必须给可行动作而非「未授权」，§九.85）、转义与模板基础；**懒加载相关状态**（`messages` 当前会话完整消息数组、`renderStart` 渲染窗口起点（恒为轮起点）、`messagesConversationId` 这份数据属于哪个会话——切换会话重置窗口、同会话刷新保留窗口与滚动）；**`topLayerContainer()`（返回最上层模态 `<dialog>`，无则 `body`；只认 `:modal`，排除非模态的 toast）——模态弹层处于浏览器 top layer，body 上的 `position: fixed` 浮层永远盖不过它，`showTextContextMenu` 因此把 `#textContextMenu` 挂进该容器**；**`toast()` 同样先挂进 `topLayerContainer()`**（`#toast` 虽是 `<dialog>` 但用 `show()` 非模态、不在 top layer，不挂进去就会在设置/Agent 弹层里被整块盖住——空提示词点「存为快捷提示词」实测看不见）；**`setServerStatus(ok)`（侧栏底部唯一状态点的唯一写入点：`api()` 网络失败→红、成功→绿，HTTP 4xx/5xx 仍算在线）**；**`notifyComposerChanged(input)`（程序化改输入框后派发合成 `input` 事件：快捷消息 / `/` 技能引用 / `@` 引用 / 选择按钮回填 / 编辑重填都必须调用，否则发送按钮不刷新——教训 §九.25⑤）**；**聊天背景图的状态与唯一写入通道**：`state.chatBackground`（当前生效值）、`state.chatBackgroundImageMissing`（探针判定的「文件暂不可用」，**运行时标记：不落库、不进设置契约**）、`applyChatBackground()`（写 `:root` 上的 CSS 变量——真实背景层/编辑白板/设置卡缩略图三视图共用，取景不可能漂移）、`syncChatBackgroundFromBootstrap()`（启动探针：**失败绝不 `clearChatBackgroundSetting`**，只置 missing 标记 + toast；旧实现一次探针失败就永久丢设置，见 §九.77）、`probeChatBackgroundImageAspect()`（探的正好是当前背景图时顺带刷新 missing 标记）、`refreshChatBackgroundImageStatus()`（换图 / 打开设置页时重探；从"不可用"恢复成可用时先摘 `--chat-bg-image` 再挂回，逼浏览器重新加载该图）、`onChatBackgroundMissingChange()`（UI 回调注册点——01-core 是低层模块，不反向 import 设置面板）、`saveChatBackground()`（**只提交增量、且按键白名单 `CHAT_BACKGROUND_WRITABLE_KEYS`**：整份提交会让"改一下透明度"顺带把失效的 image 送审、整份被 400 拒掉）、`clearChatBackgroundSetting()`（**唯一**把 image 写回空串的入口，且只由用户点「清除背景」触发） |
| `02-markdown` | 代码高亮、Markdown/表格渲染；安全白名单 HTML 还原恒开（原「富文本」开关已移除，`markdown(text, allowRichText=false)` 仅供右侧文件面板的纯文本预览） |
| `03-media` | 图片灯箱（**会话内左右切换 + 缩放/平移**：`collectConversationImages` 取 `#messages img[data-large-url]` 按 DOM 顺序去重、`stepImageLightbox` 首尾循环、`handleImageLightboxKey` 处理 ←/→/Esc；`initImageLightboxInteractions` 统一绑定——未缩放时点左/右半屏翻页（单张图时点图片以外区域关闭，保持原行为）、滚轮以光标为锚点缩放（1–6 倍）、缩放后拖动平移（仅当图片大于视口才允许）、双击复位、触屏单指平移/双指捏合/双击切换；换图与关闭复位缩放；输入区附件与文件面板图片不参与切换）、附件/媒体（**`uploadedFileMarkup` 用户侧附件渲染也在此**：图片→缩略图+灯箱、视频/音频→就地播放器、其它→可点开的文件名 chip，媒体均带 `figcaption` 文件名）、**`mediaKind(source, name)`（媒体类型判定唯一入口：读 `/api/bootstrap.media_exts`，前端不再各写扩展名正则）**、**工具媒体就地内嵌**（`toolMediaMarkup(run)` 渲染 `.tool-media` 块并复用 `mediaMarkup`；`mediaMarkup` 每种媒体都带 `figcaption` 文件名；`mediaTruncatedNotice` 渲染"共 N 个媒体，仅显示前 M 个"；`toolRunMarkup` = 工具块 + 媒体块；`inlineMediaSources`/`remainingAttachments` 供末尾网格去重）、活动时间线（思考块一律折叠；请求轮次「·」断点记号；总耗时+平均速率+请求数+完成日期一行、「请求明细」逐次展开且每行含 `速率 %4d token/s`（= 输出 tokens ÷ 请求耗时；`requestTokenSpeed`/`averageTokenSpeed` 为唯一计算口径，平均按输出总量÷请求总耗时加权）；`performance.total_ms` 为权威本轮总耗时）；`updateContextComposerLock`（输入框在**上下文已满或正在编辑消息**时禁用；编辑态的占位提示指向会话内编辑框的确认/取消方式）+ **`setContextUsage(usage, conversationId)`（上下文圆环/弹层的唯一写入点：`state.contextUsage` 只此一处赋值；流式期间每次 `usage` 事件即刷新，历史渲染与终态经 `updateContextUsage` 复用同一入口；**取「有效用量」必须先扫到最新一条「新会话」分割线就停**——线以上（含被标记的那条消息本身）的 usage 一律不算、线以下没有 usage 即归零解锁（`isContextStartDivider` 认新形态的 `metadata.session_start` 与遗留的 `role=session` 标记行，见 §九.94）＋**运行中历史重渲染（会话/任务轮询）不得覆盖实时值**——`state.chatBusy` + `contextUsageConversationId` 守卫（**唯一例外：`contextReset` 已确证时越权归零**，见 §九.52/§九.94）** + **输入框下方 meta 行「思考 自动」右侧的实时百分比**（`#contextPercentLabel`：`renderContextPercentLabel()` 与圆环同源、同一份 `percent` 与 `CONTEXT_TONE_WARNING=70`/`CONTEXT_TONE_DANGER=90` 配色阈值，文本形如「上下文 8.2%」、title 带完整 `x / y（z%）`；无数据时显示「上下文 --」）+ **上下文提醒（`contextWarningPercent` 读「设置 → 运行设置 → 上下文提醒阈值」；**运行中**才由 `maybeWarnContextUsage`（圆环渲染后）弹窗，**空闲会话改为 `pendingContextWarning` 在点击发送时判定**——弹窗可选「继续发送」（`continueAfterContextWarning`；任何关闭路径都清掉待续动作），被拦下时不清空草稿；**距上次提醒再涨 `CONTEXT_WARNING_STEP=5` 个百分点就再提醒一次**（换会话或用量回落到阈值以下重新武装），阈值 0 = 关闭；弹窗为专用紧凑样式 `.context-warning-dialog`（`height: fit-content` + `inset:0`/`margin:auto` 才真正居中））** + **`updateSendButtonState`（发送按钮可用性唯一写入点：文字或附件至少有一个才可发送；上传中/上下文已满 → disabled；**编辑消息态下变成「重新发送」且可用性只看那个编辑框**（`state.editingHasText` / `state.editingHasAttachments`，附件随确认自动带回）；运行中变身停止键，取消中禁用；title 同步）** |
| `04-messages` | 消息气泡、消息操作区（用户消息「编辑 · 分支」；AI 回复「复制 · 重新生成 · 新会话」；运行中隐藏编辑/重新生成/分支/新会话四处入口）、滚动跟随（图片判定统一走 `mediaKind`：`preloadDraggedFile`；用户侧附件渲染 `uploadedFileMarkup` 已归 `03-media`）；**对话刻度轨**（`initTurnRail()`：右侧横条，**每个用户轮次一条**，最多同时显示视口附近 `TURN_RAIL_MAX=30` 条滑动窗口；**每根线一个固定尺寸透明块（26×9px）＝判定区，悬停/高亮只改内层线尺寸 → 块大小恒定、所有条位置不抖**；高亮 = **视口中心所在轮次**且唯一；悬停弹 `#turnTip` 概要 = `第 N 轮` 小字 + **用户消息 13.5px/600（1 行）** + **AI 回复 11.5px/muted（2 行）**，不写「用户/AI」标签、靠字号字重区分；点击把该轮滚到视口中心；行区间不变时只挪高亮、不重写 DOM）；**自定义头像**（`currentAgentAvatarUrl()`：会话绑定的 Agent 带 `avatar` 时把助手气泡的「AI」圆标换成 `<img class="message-avatar message-avatar-img">`，无头像时保留默认圆标；Agent 失效回退默认 Agent）；**媒体就地内嵌 + 末尾去重**（`remainingAttachments(metadata)` 只保留"没有就地归属"的附件：新消息的媒体都在 `run.media` 里就地渲染，旧会话无该字段则末尾网格照旧）；**首次请求上下文卡**（会话顶部「首轮上下文」折叠卡：完整系统提示词原文 + 工具 JSON 定义 + 生成参数，数据取自 first_turn API；`refreshFirstTurnCard` 在终态事件后即时拉取并就地插入，`renderMessages` 复用同一 `upsertFirstTurnCard`，两路径同序：empty → 折叠卡 → 消息）；活动时间线渲染（思考块一律折叠/横线分割请求轮次/最终答复末尾）；**流式正文自适应节流**（`scheduleStreamingMarkdown(element, raw)` 每次都是「整段 raw 重跑 markdown + 整体 innerHTML」，单次成本随长度线性增长而事件频率恒定 ⇒ 总成本 O(n²)，长回复后半段掉帧；故按累积长度拉长间隔——`streamingRenderDelay(raw)` = `40ms + floor(len/4000)×50ms`，`240ms` 封顶：短回复保持 40ms 跟手，超长回复不再每 40ms 重排一次））；**消息列表懒加载**（`renderMessages` 只渲染 `[state.renderStart, 末尾)` 这一段：**窗口恒以「轮」为边界**——`turnStartIndexes()` 取每条 user 消息下标，`initialRenderStart()` 给最近 `LAZY_TURNS_INITIAL=10` 轮，`clampTurnStart()` 把旧起点夹到合法轮起点；**一条 AI 回复上的「新会话」分割线随其锚点消息同进同出**（`messageRangeFragment()` 逐条 `append(messageElement) + sessionDividerAfter`，绝不拆散）；切换会话重置窗口、同会话刷新（轮询/保存后）保留窗口与 `scrollTop`；滚到距顶 `LAZY_TOP_TRIGGER=240px` 且**用户确实离开了底部**（`!stickToBottom`）、且不在渲染后 400ms 抑制窗内时，`extendRenderedWindow()` 往前再渲染 `LAZY_TURNS_STEP=10` 轮并把补进来的高度加回 `scrollTop`（视口不跳）；**容器默认瞬时滚动（`.messages` 不再带 `scroll-behavior: smooth`），程序化定位/补偿仍一律经 `withInstantScroll()` 临时改 `auto`**（流式期间每次 delta/工具事件都会贴底，平滑动画会让容器长期"追赶最新内容"→ 肉眼可见的晃动；经 withInstantScroll 写入则读回值准确、也不在动画期间连发 scroll 事件干扰懒加载判定；**只有用户主动跳转才平滑**：刻度轨点击在自己的调用点显式传 `behavior:'smooth'`）；刻度轨改为**数据驱动**（`collectTurns()` 读 `state.messages`，未渲染轮次没有锚点时按 120px 均摊估算偏移，刻度带 `data-turn-message-id`），点未渲染的轮次先 `ensureMessageRendered()` 再跳）；**「新会话」分割线**（入口在**每条完整 AI 回复的 `message-actions` 里、「重新生成」右侧**（`data-session-start-after=<消息 id>`，流式临时气泡与运行中不显示）；`startNewSession(afterMessageId)` POST `/api/conversations/<id>/session_start`（二次确认、运行中拒绝），`cancelSessionStart(id)` DELETE `/api/session_start/<id>`，两者经 `syncCurrentConversation()` 立即重渲染；`sessionDividerElement(message)` 渲染成横条「新会话 · 手动 · 时间」+「撤销」+ 说明行「此线以上不再进入模型上下文」，`sessionDividerAfter(message)` 决定该消息下方是否要补线（`renderMessages` 与 `replaceWithMessage()` 两条渲染路径都补，保证终态事件插入的消息也带线）；**允许多条分割线并存**，靠时间与位置区分，最新那条才是当前上下文起点）；**模型重置的种子消息**（`contextResetSeedText(info)` 按 `state.bootstrap.settings.context_reset_seed_template` 渲染，留空回退 `DEFAULT_CONTEXT_RESET_SEED`；`{handoff_path}`/`{task_count}`/`{task_list}` 占位符替换，**无后台任务时含占位符的整行去掉**；`fillContextResetSeed(info)` 只填输入框不自动发送；分割线上的「填入种子消息」按钮（`data-fill-reset-seed`）读 `dataset.resetSeedInfo` 里的标记 JSON——**这是 DOM 属性赋值，不能 escapeHtml**（转义后的 `&quot;` 会让 JSON.parse 失败、路径变空，实测踩过））；**「编辑」与「重新生成」共用一次重发**（`resendFromUserMessage(userMessageId, text, {successText, errorPrefix, attachments})`：POST `/api/messages/edit` 截断该条 user 消息及其之后 → 把该轮附件放回 `state.pendingFiles`（**字段与「分支」同口径 `name/path/size/thumb_path`**；附件本体由 `/api/messages/edit` 回传，前端不猜）→ `openConversation` 重渲染 → 写 `#messageInput` 并 `notifyComposerChanged` → `sendMessage()`——**文本仍走输入框的正常管线**，`/ref` 技能引用与 `@` 工作区引用因此照常解析/剥离，不另开旁路）；**「重新生成」= 「编辑」的不改字版本**（`regenerateMessage(assistantMessageId)` 从 `state.messages` 往前找最近一条 `role === 'user'` 推导截断点与原文，原文取 `metadata.display_content`（含 `/ref` 与 `@`，**不能用剥离引用后的 `content`**）；截断点恒为**那条提问**而不是 AI 回复 → 反复点不累积重复提问、前缀消息字节级不动（前缀缓存命中）；**最后一轮一键直发不弹框**，非最后一轮或输入框有草稿时才 `window.confirm`）；放行口径与发送一致（文字或可用附件至少有一个，**纯附件轮次同样可重发**）；**编辑态把完整输入区移动到消息气泡**（`applyEditingState(row)` 标记 `body.is-editing-message` / `.message-row.is-editing`，把原 `.composer-wrap` 节点连同输入框、`@`/`/` 引用、附件列表、思考设置与 meta 行搬到该用户消息 `.message-body`，添加 `.edit-composer-header` 取消入口；底部只留隐藏占位 `composer-wrap edit-placeholder`。编辑气泡全宽显示，原消息附件进入待发送列表，可删除或继续添加；取消恢复原草稿与附件，确认按当前附件列表重发；**首次进入编辑与重渲染后复挂共用同一个 `mountEditComposer(row, {wrap, text, files})`**，且 `wrap` 必须由调用方传入——底部那个占位锚点也是 `.composer-wrap` 且在文档顺序里排在真 composer 前面，复挂时再查一次 `.composer-wrap` 会抓到那个空 div）、禁用 `#attachButton`，输入框与按钮仍由 `updateContextComposerLock` / `updateSendButtonState` 两个单点刷新；`confirmActiveEdit()` 供底部「重新发送」与回车调用——**编辑态下底部按钮不发新消息，改为确认上面那个编辑框**；**会话重渲染不再无条件退编辑态**（`renderMessages` → `prepareEditRerender(list, switched)` 三分支：①同会话刷新（轮询 `syncCurrentConversation`、后台任务写消息都会引爆）且被编辑消息仍在 → 只归位、保 `editComposerStash`，`replaceChildren` 之后按 `[data-message-id]` 找回新生成的行复挂（`resumeEditComposer`，懒加载窗口里没有它就先 `ensureMessageRendered`），编辑框 / 改到一半的字 / 附件 / 「重新发送」按钮全部原样；②被编辑消息已不存在、③换了会话 → `exitEditWithDraft()` 退出，并把进编辑前的底部草稿（文字 + 附件）还回输入框 + toast「编辑已取消，草稿已还原到底部输入框」）。**顺序是硬要求**：`replaceChildren` 会连坐销毁搬进气泡的 composer-wrap，必须先归位、再渲染、最后复挂。守门 `tests/test_regenerate_reply.py::EditComposerBridgeSourceTests`，冒烟 `verify/regenerate_smoke.py` 用例⑤⑥（反证过：退回旧行为时同步一到就是 `inRow=false` / `body.is-editing-message=false` / 发送键变回「发送」）） |；**活动流的助手气泡必须活过重渲染**（`renderMessages` 开头取 `liveRunRow`，`replaceChildren()` 之后 `container.append(liveRunRow)` 把同一个节点搬回末尾）：正在流式输出的那条回复，正文此刻只在 DOM 里（run 结束才落库），以前会被 `replaceChildren` 连根拔掉、重渲染后又没人复挂，于是「进行中的 AI 回复从屏幕上凭空消失」且此后一直不回来——典型触发是点任务面板里的某一行（`openConversation(当前会话)` 会整体重渲染），而 `resumeConversationRun` 见 `abortController` 仍在会直接返回、不会重连；复挂判据收紧到「确有活动流 + 行还挂在消息区 + 属于当前会话 + 库里还没有这轮的 **assistant** 消息」（**必须限定 `role === 'assistant'`**：这一轮的用户消息 metadata 里也带同一个 `run_id`，不限角色就会把正主一起挡掉）
| `05-bootstrap` | 认证、网络访问展示（**侧栏底部「手机访问」行内的 `#serverDot` 是唯一状态指示灯，由 `setServerStatus` 驱动**）、`initialize`、更新轮询 |
| `06-tasks-plans` | 后台任务/计划 UI；**`activeTaskStatuses` 含 `stopping`、`taskStatusLabel` 覆盖九个状态（`stopping` 曾缺失，界面上会直接漏出英文）**、`taskKindLabel`/`TASK_KIND_LABELS` 给后台作业类型中文名（`subagent` = 「AI 子任务」——面板组标题/完成 toast 用；组标题刻意不引用用户消息原文）、**`taskDisplayTitle(task)` 是面板行标题的唯一口径：优先显示落库任务名（`task.message` = `JobSpec.label`，如「子任务：整理素材并提交出图」），为空或命中后端兜底格式 `Job(kind)` 时回退类型名**；行标题的 hover title 固定给类型名兜底；**`.task-kind` 类名不得改名**——嵌套缩进的 `↳` 标记是挂在它 `::before` 上的（styles.css），行标题换成任务名后它仍要保留这个元素、这个类；`loadTasks` 走 `/api/tasks?jobs_only=1` 并把轮询失败原因与最后成功时间记进 `state.taskSyncFailed`/`taskSyncedAt`；**`maybeRecoverRunFromPoll` 的兜底重连只认顶层对话 Run**（按 `kind` + `parent_job_id` 过滤，与 `/api/runs` 同口径——后台子 Job 不得让整条会话被判成「回复进行中」） |
| `07-models-agents` | 模型/Agent/更新状态；顶部选择 API profile；**顶栏 Agent 选择器左侧的「?」说明按钮（`#agentHelpButton` → `#agentHelpPopover`）**——「Agent 是什么 / 三种预设怎么选 / 要用 ComfyUI 怎么办」这三件事在使用前就必要，而界面上只有三个名字和一个下拉（**能力边界「没勾的工具一律用不了」完全不可见**），故给一个**可点**的说明层：`toggleAgentHelpPopover`/`positionAgentHelpPopover`/`closeAgentHelpPopover` 与上下文用量弹层同一套规矩（`position: fixed` + 打开时挂 `document.body`（顶栏容器有 overflow 裁剪）、点外部关闭、`aria-expanded` 同步、resize/scroll 重定位）；正文里必须同时给出 **ComfyUI 两条路线**（HTTP 直连：套用「ComfyUI 联动」预设 = `comfyui_prepare_workflow` + `comfyui_batch` + `job_output`/`job_status`/`job_wait`，不需要 MCP；MCP 连接：设置 → MCP 连上后手动勾选引入的 comfy 工具）——**必要信息的通路不许只挂在 `title` 上**，触摸设备没有悬停，见 §九.87；**输入区模型下拉只列当前 API 的模型目录**（手动输入框 `#composerModelCustom` 与其 `saveCustomComposerModel` 已删除——不再有 `__custom__` 分支）；**该下拉的可视化层是「触发按钮 + 可搜索浮层」**（`#composerModelTrigger` / `#composerModelPanel`，中转站未分组时目录能返回 400+ 个模型，原生 select 只能靠滚动定位）：输入即过滤（对模型 id 与显示名做大小写不敏感子串匹配，面板头部显示「过滤后/总数」）、↑↓ 环绕移动 + Enter 确认、Esc 关闭并把焦点还给触发按钮、点浮层外部收起；**浮层 `position: fixed` 且打开时挂到 `document.body`**（与审批上拉框/思考强度/快捷消息同一口径，避免被 `.composer-wrap` 的 overflow 裁剪，也躲开「编辑消息时整个 composer 被搬进 `.message-row`」）；**高度上限取「该侧可用空间」、朝上展开时锚底边（`bottom`）而不是算 `top`**——只算 `top` 的话，过滤把候选从 400+ 缩到几条后面板会原地收缩、在按钮上方空出一大截（按钮离面板 300px，即用户报的「弹层到处飘」）；同理**不能只把 `top` 夹进视口**，400+ 条时面板比按钮上方还高，会被整块夹到视口边缘；锚底边后高度变化只往上长，面板恒贴按钮 8px，`maxHeight` 顶住空间后由候选列自己滚（`.composer-model-list` 必须 `flex: 1 1 auto; min-height: 0`，搜索框/刷新行/脚注/空态都是 `flex: none`）；**列是 flex column，候选项必须 `flex: none`**（否则 400+ 条时每行会被 flex-shrink 压成一条 10px 的细缝，实测踩过）；**重开浮层时光标落在「当前已选模型」上并 `scrollIntoView` 到位**（`filterComposerModelPicker` 里按 `item.selected` 定 activeIndex，避免一按 Enter 就换成目录第一项）；**原生 `#composerModelSelect` 仍在 DOM 中、仍是唯一值来源**，只是被 CSS 用「非空 bounding box + `opacity: 0`」隐藏——⚠️ **禁止改成 `display:none` / `visibility:hidden` / `hidden` 属性**（Playwright 会判定不可见，`verify/composer_model_smoke.cjs` 与 `verify/video_e2e_smoke.cjs` 的 `page.selectOption` / `el.options` 会全部失败），也**不要把它当死代码删掉**；触发按钮与浮层都只是它的投影，候选列表直接由 `select.options` 构建（排除空值占位项与刷新伪选项），选中与刷新一律经 `select.value = …` + 派发 `change` 回写，落库仍走 `saveComposerModelSelection` 这一条链路、**不开第二条保存路径**；且**末尾固定一项「↻ 重新检测模型」**（值 `COMPOSER_MODEL_REFRESH = '__refresh_composer_models__'`）：选中即 `populateComposerModels(keep, { force: true })` 强制重拉目录、完成后还原刷新前的选择（`composerModelLastChoice`），请求期间禁用下拉防重复触发；**该伪选项只是动作、不是模型**——`composerModelChoice()` 对它返回 `''`，`saveComposerModelSelection` 命中它时直接早退，绝不落库也绝不发给模型；**目录缓存的判据是「目录非空」而不是「查过」**（`(state.providerModelCatalogs[key] || []).length` 为 0 即视为未缓存——供应商 200 但列表为空时否则会在本页永久卡在「请先在设置中检查模型」，本地模型后 pull 上来也刷不出来），`force` 绕过该判据；`force` 路径失败 / 未配 API / 拿到空目录都会 toast（非 force 路径仍保持静默，只 `console.debug`）；**会话没有明确选择时显示禁用占位项「请选择模型…」，不再默认选中目录第一项**（目录顺序 = 供应商 `/v1/models` 返回顺序，没有任何语义）；历史会话保存的模型不在目录时置顶一项 `已保存：<原模型名>（请重新检查模型）` 并默认选中；会话分别保存 `model_key`（API）与 `model_name`（**会话实际使用的模型名，空值不可发送**——`run/chat.py` 在提交与 profile 解析两处直接拒绝），保存会话与提交对话一律发送 `composerModelChoice()` 的**原始选择**，绝不发送「解析后的具体模型名」（原 `selectedModelName` 因此退役）；`updateUnloadModelButton` 只管设置页的 `#unloadProviderModel`（**顶栏「卸载模型」按钮已移除**，本地模型卸载统一在「设置 → API 供应商」） |
| `08-conversations` | 会话侧栏（虚拟化）、工作区、**工作区重名靠目录路径分辨**（分组按**名字**归并，允许两个不同目录同名 ⇒ 界面每一处都补上目录这条补充信息：`renderComposerWorkspace` 里每个 `option.title` = 该工作区的 `dir`、「未分组」兜底一句说明、`select.title` = 「选择工作区（当前：<目录>）」；`sidebarRowHtml` 的分组头 `title` = 「名字（目录）」，未分组 / 空目录一律兜底为名字——**不许出现空白或 `undefined`**；§九.88）、**收藏与「⋯」菜单**、**Agent 页的快捷提示词（面板套用 / 另存 / × 删除）+ 角色卡导入**；`openConversation` 拉取首轮上下文（first_turn）供折叠卡渲染；切换会话/工作区时收起 @ 引用弹层与快捷消息面板并清空目录缓存。**会话条目**：五角星（`toggleConversationFavorite`，乐观更新 + 失败回滚；**收藏只提交 `favorite`，后端不推进 `updated_at`**，否则侧栏按时间重排、启动时"最新 5 条"被收藏项挤占）与「⋯」菜单（`openConversationMenu`/`closeConversationMenu`，fixed 挂 body 避免被侧栏 overflow 裁剪；含重命名/删除；`openRenameConversation`/`saveRenameConversation` 走独立对话框——pywebview 下不用 `window.prompt`）；**「已收藏」分组**（`SIDE_FAVORITES_GROUP='__favorites__'`，跨工作区聚合、固定追加在工作区分组之后、默认展开、不受 5 条上限影响；收藏的会话仍留在原工作区，两处都显示）；**新建工作区对话框**（`createWorkspace` 选目录 → `#newWorkspaceDialog` 填名称 → `saveNewWorkspace`，同样不用 `window.prompt`）；**当前会话定位用最小滚动**（`sidebarScrollForActive`：已可见就一动不动，不可见才把最近的一份——工作区与「已收藏」可能各有一份——刚好带进视口；**禁止 `st = offsets[idx]` 强制置顶**，点一下列表就整片重排会让用户找不回位置）；顶栏「新会话」按钮已移除（每个工作区分组自带「＋ 新会话」，列表为空时由空态兜底按钮 `data-action="new-chat"` 提供入口）；**收起按钮并入「工作区」操作行**（搜索/排序/新建工作区/收起同一行，不再单独占一行）；**会话条目整条可点**（点击判定不再只看中间的文字按钮：条目元素本身 `cursor: pointer`，`onSidebarTreeClick` 对条目内任意位置都调 `openConversation`——上下边缘/左侧留白/按钮间空隙都可点，判定区与视觉区一致）；`mergeAgentPromptText(existing, addition, limit)` 为**纯函数**（角色卡追加语义：空行分隔、超 12000 字符截断并自述）；**虚拟窗口渲染策略**：滚动上限取 `tree.scrollHeight - vh`（**不能用 `sidebarTotalH - vh`**，见 §九.31），且**只在「行区间 start/end 或总高变化」时才重写 innerHTML**（`sidebarWindowRange` 缓存；每帧重写是滚动迟滞的主因，实测 160px 滚动从 20 次重绘降到 ≤8 次），`renderSidebar` 重建行缓存时必须 `force: true`；**任务面板的派生分组按「可见父链锚点」**——`renderRunTasks` 沿可见父链上溯取锚点（父行被 `jobs_only` 过滤时用那个父 id 当锚点，同父兄弟不拆开；环状链用 `seen` 收敛），子行加 `task-item-nested`（`styles.css` 缩进 + `↳`），**父行也在面板里才缩进** |
| `09-settings` | 设置页：外观主题与皮肤（appearance）、**会话区字体**（外观页「会话字体」卡：字号滑杆 13-18 + 字体族 **两级显隐**——radio 4 选 1（跟随界面/衬线/圆润/**指定字体**，后者 `value="__pick__"` 只是前端占位）+ 展开后的 `#chatFontPick` **点选列表**（选项由 `CHAT_FONT_PICKS` 现建、**字体清单不进 HTML**，没装的字体由 `isFontInstalled()` 加「（未安装）」后缀——**只标注不禁选**，设置存服务端跨设备共享，这台没装不代表手机没装）+ 列表末项「手动填写…」才展开 `#chatFontManualRow` 手工串输入框 + 实时样例；`buildChatFontPickOptions()` **必须在 `syncAppearanceControls()` 之前**调用——select 里还没有对应 option 时赋 `value` 会被清空；`syncAppearanceControls()` 是 主题/皮肤/字号/字体族 **所有控件的唯一回显点**，`appearanceFormValues()` 把控件值转成可直接交给 `applyAppearance`/`saveAppearance` 的对象——**控件缺失时返回 `undefined` 而不是默认值**，否则旧 `index.html` 打开一次设置就会把用户已存偏好抹平，且 **`__pick__` 必须在这里翻译成 select 的真实值**（占位值落库会被后端白名单拒 = "保存失败"）；**交互口径**：`input`/`change` 只做本地预览（拖动是高频事件，绝不每像素打 API），落库统一由「保存外观」完成，与主题/皮肤同一套心智模型，`#chatFontStatus` 单独给字体卡一行反馈）、分组导航搜索、Skill/供应商/运行/视觉/搜索/MCP/工作区/Agent 管理（**运行设置**含「命令超时」、「**上下文提醒阈值**」（`#contextWarningPercent`，0-100，0=关闭，默认 80；由 `context_warning_percent` 持久化）与「**新会话种子消息**」（`#contextResetSeedTemplate`，多行模板，占位符 `{handoff_path}`/`{task_count}`/`{task_list}`，留空回退内置默认，最长 2000 字符；由 `context_reset_seed_template` 持久化））；**API 供应商页为卡片网格**（一行最多三张、末尾固定一张虚线「添加 API」卡；卡片压成两行（名称 + 脚行：类型标签/「当前」角标），右上角 × 删除——**不再展示模型名**（模型名改由 `#providerDialog` 弹层核对）；点卡片才弹出顶层 `#providerDialog` 并加载该供应商预设，点开即可编辑，Esc/右上角关闭/取消/保存成功统一经 `cancelProviderEdit` 复位并重绘卡片；`openVisionProviderForm` 仍复用 `addProvider`）；**Agent 页同为卡片网格**（一行最多三张、末尾固定一张虚线「新增 Agent」卡；卡片显示 名称/固定 Skill 数/**该 Agent 工具集对应的名称（`工具集：<预设名>`，由 `toolScopeLabel(agent.tool_scope)` 算出：命中内置预设用预设名、命中「我的工具集」用它的名字、空 scope 显示「未限制（全部工具）」、都不匹配显示「自定义 · N 个工具」）**/系统提示词两行摘要，右上角 × 删除——`built_in` 条目不给 ×；**默认 Agent 在卡片上不做任何标记**——没有「默认」角标、也没有 `.is-default` 高亮，因为卡片上任何强调色/角标都会被误读成「当前选中/正在编辑的那一个」，默认项只由顶栏 Agent 选择器体现；点卡片才弹出顶层 `#agentDialog` 并加载该 Agent，Esc/右上角关闭/取消/保存成功统一经 `hideAgentForm` 复位，`renderAgentManager` 不再顺手打开表单；**弹层内改为分区切换**（`.agent-tabs`：基本 / 系统提示词 / 固定 Skill / 工具集——四块面板都留在 DOM、只切 `hidden`，切页不丢勾选与已输入内容；标签带计数「固定 Skill N」「工具集 已选/总数」；打开表单复位到「基本」；**模块不再一段段往尾部追加**，弹层不再越加越长）；**ID 由后台自动分配**（表单无 ID 输入框，副标题只显示已有 ID）、**「自定义头像」按钮在「基本」分区**（紧挨名称；「导入角色卡 PNG」留在「系统提示词」分区，选图只做本地预览 → 保存 Agent 时随 FormData 上传）；**固定 Skill 与工具集同款卡片**（`.skill-card`：勾选框 + 名称 + 两行说明，两列网格；与技能页的 `.skill-item` 列表样式解耦），**两个列表各自占满分区高度、内部滚动**（`flex: 1; min-height: 0; overflow: auto`，不再有灰色标题块与固定高度——标题已由分区按钮承担，面板里只留一行 `.agent-tab-hint` 说明；工具集的计数/预设状态/「展开全部」搬进卡片区与搜索行）；**工具集分区是「卡片态 ↔ 编辑态」两态同框切换**（两态叠在 `.tool-stage` 同一格里，**弹层高度恒定 760px、不参与过渡**，所以切换时没有任何跳动；卡片态：4 张只读预设卡（只读 / 标准 / ComfyUI 联动 / 全能，显示名称 + 个数）+ 「我的工具集」卡片（右上角 × 删除）+ 末尾虚线「添加自定义工具集」卡，整块在框内居中，工具列表此时不展开；**点任意一张卡**（预设 / 我的工具集 / 添加）都先给该卡打上 `.is-picked`（按下 + 选中态）→ 卡片整块向上滑出 → 工具列表从下方滑入，`swapToolView(true)` 完成切换，`prefers-reduced-motion: reduce` 时直接切、不等待；**命名栏自动预填被点卡片的名字**（预设卡填预设名、「我的工具集」卡填它自己的名字、「添加」卡留空，且自动全选便于改名）；**「添加」卡不跟随当前选中项，固定以「标准模式」为底稿**（`DEFAULT_TOOL_SET_PRESET_ID = 'standard'`，用户新开一套时起点可预期）；预设卡载入的是该预设的工具集（走依赖闭包）但 `agentToolEditingId` 为空 → 「保存工具集」存成一份新的「我的工具集」，自定义卡则原地更新；`matchToolPreset()` 精确比对当前勾选与内置预设 → 命中则高亮对应卡、否则摘要显示「自定义（未保存）」，预设卡一律只读不给删除入口；保存走 `saveAgentToolSet()`（POST `/api/tool_sets`，写进 config.json 的 `tool_sets`，最多 30 条；**不再用 localStorage**——冻结版 pywebview 默认 `private_mode=True`，WebView2 的 localStorage 每次退出都会被清空，老数据由 `migrateLegacyToolTemplates()` 启动时一次性搬到后端））；/**工具集分组头一行呈现**（箭头 · 全选框 · 分类名 · 小字说明 · 计数，`head.append(caret, allCb, title, desc, count)` + 五列网格 `auto auto max-content minmax(0,1fr) auto`，说明超出省略，不再独占第二行；**分类名后跟风险徽标**（`.group-badge` + `data-tone` 配色，随标题同行、不额外占列）；展开区统一包在 `.agent-tool-group-body` 里）；**工具集搜索框**（`#agentToolFilter` → `renderToolScopeList()` 只重画列表：非空时切成平铺搜索结果视图、卡片带 `.tool-group-tag` 分类标签，清空恢复分组；表单打开时复位）；**工具目录短时效缓存**（`ensureToolCatalog({maxAgeMs=5000})` + `state.toolCatalogAt`；**打开工具集面板时 `maxAgeMs: 0` 强制取新**，MCP 连接状态变化经 `invalidateToolCatalog()` 作废——MCP 是按需连接的，永久缓存启动时那份目录会让面板永远看不到 `mcp__*` 工具，见 §九.55）；**MCP 二级分组**（`.tool-subgroup`：服务器名 + `subgroup-select-all` + `subgroup-count`，二级全选只作用于该服务器）；**展开后的工具卡片收紧并与父分组拉开层次**（`.permission-grid > label` min-height 54px / padding 8px 10px / 白底 + `--line-strong` 描边 + `--shadow-1`；**折叠时分组头保持原样（白底、悬停浅灰），只有展开的那一组分组头才变 `--surface-2` 浅灰条子，展开区 `.permission-grid` 保持白底**——层次只落在"当前展开的那一栏"上；说明两行截断、完整描述进 `title` 悬停））；历史数据管理（统计展示 + 压缩数据库）；`showAgentForm` 打开表单时收起并重建快捷提示词面板（`closeAgentPromptPresetPanel` / `renderAgentPromptPresetList` 由 08 提供）；**顶栏两处文案口径**：`updateSkillSummary` 只把**数字**写进 `#skillCount`（按钮自带「Skill」标签，别再拼前缀）；`renderMcp` 让 `#mcpStatus` 文字恒为「MCP」、**状态只走 CSS 类**（`connected` 绿 / `disconnected` 红 / `connecting` 黄 / `calling` 绿 / `error` 红 / 无服务为默认灰），具体状态放 `title`；**「聊天背景」卡的「文件暂不可用」提示**：`updateChatBackgroundControls` 是面板唯一写入点，由它渲染内联提示行 `#chatBackgroundMissingHint`（id 级红色，`hidden` + `:empty` 双保险）与缩略图文案（`点击调整` / `文件暂不可用`）；`populateChatBackgroundSettings` 打开设置时顺手 `refreshChatBackgroundImageStatus()` 重探一次（文件被清理或被补回都不必重启就看到准确状态） |
| `10-upload` | XHR multipart 流式上传（进度百分比/单文件取消/并发上限 3/80MB 前置校验/完成即显缩略图）与**待发送附件竖直列表**（`renderPendingFiles`：输入框上方固定高度、可纵向滚动的一行一文件；**行用 `--surface-2` 填充 + `--line-strong` 描边 + `--shadow-1`，缩略图/图标方块用白色内嵌**——白底 + `--line` 描边在 `--bg` 上几乎看不出边界，用户实测"区分度太差"；文件名 `ellipsis` 截断 + `title` 全名；图片行给预览缩略图 `img.pending-thumb`（带 `data-large-url` 沿用灯箱），非图片行给 `.pending-thumb-file` 占位图标；上传中显示 `%` 进度；无附件时容器 `hidden`；**缩略图加载失败两级兜底**（document 捕获阶段 `error`：① 缩略图不在但主图在 → 换主图，与消息气泡 `.thumbnail` 同策略；② 主图也没了 → 降级为 `.pending-thumb-file` 占位图标 + `title` 说明，并摘掉失效的 `data-large-url`，不再留破图）；`missingAttachmentPaths`（发送前调 `POST /api/uploads/check` 校验附件是否仍存在，见 11-run-stream）；`renderPendingFiles` 末尾调 `updateSendButtonState`（附件增减即时刷新发送按钮可用性） |
| `11-run-stream` | Run 流式消费、看门狗/重连/订阅；`sendChatMessage(textOverride, {skipContextWarning})` **允许空文字+附件发送**（文字与附件至少有一个才提交），**提交前先做上下文提醒判定**（`pendingContextWarning()` 命中则弹窗并 `return`，不清空草稿；「继续发送」带 `skipContextWarning` 重新进入），**清空草稿前先做附件落地校验**（`missingAttachmentPaths` → `POST /api/uploads/check`：命中丢失的缓存文件就剔除该项、保留文字草稿并 toast 提示重新上传；用模块内私有 `attachmentPrecheckPending` 挡连点——此处新增了 `await`，不再像以前那样同步无缝），发送前收起 @ 引用弹层（`@` token 原样提交，由后端解析）；**`SKILL_SCRIPT_RULES`（Skill 附带脚本规范：脚本放 `scripts/` 子目录 + 文件读写显式 UTF-8/GBK 风险）与 `SKILL_INSTALL_PRESET`**（安装入口发给 AI 的说明，末尾复用规范块）；**乐观插入的用户消息同步进 `state.messages`**（懒加载后刻度轨从数据收集轮次，不同步就会漏掉刚发的那一轮）；**`resumeConversationRun` 只认顶层对话 Run**（不再无条件取 `runs[0]`——那很可能正是一条卡住的子任务） |
| `12-chat-input` | 发送/确认、**交互选择面板（`#choiceButtons`：单选即跳题 / 多选「完成」收答案 / 答案块追加进输入框，行为与内存键见 §四）**、`handleChatEvent` 事件分发（type→handler 路由表，24 种事件各一函数（含插话的 `user_guidance` / `interjection_consumed`）；`tool_result` handler 把 `toolMediaMarkup(event)` 就地插到对应工具块之后——**流式期间媒体即可见**，终态全量重渲染由 `toolRunMarkup` 从同一份 `run.media` 重建同位置；`usage` handler 在流式尾端挂载/更新用量框并调 `setContextUsage` **即时刷新输入框旁的上下文圆环**——每次模型请求都刷，不再等整轮结束）；`setBusy` 只管图标与停止态样式，按钮 disabled/title 一律由 `updateSendButtonState` 决定、输入框 disabled/placeholder 一律由 `updateContextComposerLock` 决定（含编辑态——两处都不得再直写，否则会覆盖编辑态）；**开始页卡片**（内置预设与用户条目同在 `starter_prompts`，`renderStarterPrompts` 按 `icon`/`desc` 渲染并逐个挂编辑/删除，`restoreStarterPresets` 一键恢复默认；`openStarterPromptDialog(index, target)` 按 target 切换标题与接口）与**快捷消息面板**（独立列表 `quick_messages`，`toggleQuickMessagePanel`/`positionQuickMessagePanel`/`insertQuickMessage`/`handleQuickMessagePanelClick`/`removeQuickMessage`，按 `?sort=usage` 排序，加载态占位防闪旧序；**内置预设条目（带 `preset_id`）显示「默认」小标签**（`.quick-msg-tag`，标题行内 flex），与用户条目同权可编辑/删除，改动与删除都不会被版本更新覆盖）；**思考按钮**（`toggleDeepReasoning` 只开合强度列表——不再顺带切档；`REASONING_LABELS` 单一文案表；`updateDeepReasoningButton` 同步**输入框下方 meta 行（与「就绪」同行）的「思考 X」文字**与列表高亮；`setReasoningEffort` 落库会话设置；`positionReasoningMenu` fixed 定位）；**终态渲染统一走 `replaceWithMessage(row, message)`**（替换气泡并按需在其下方补「新会话」分割线：done / error(partial) / cancelled 三条路径）；**done 时若消息带 `session_start.source==="tool"` 就 `fillContextResetSeed(info)` 预填种子消息**（不自动发送，toast 提示确认后发送）；**`SKILL_EDIT_PRESET`**（编辑 Skill 入口的说明，import `SKILL_SCRIPT_RULES` 复用同一份脚本规范，并要求顺手修正旧脚本裸 `open()`）；**`cancelCurrentRun` 的复位不再以「服务端确认停止」为前提**——轮询 25 秒仍未确认时也照常清空 `chatRunId` / `runConversationId` 并 `setBusy(false)`，再按 `failureMessage` 提示一次失败原因；否则界面永久停在「回复进行中」，输入框与「分支 / 重新生成 / 新会话」三个救援入口全被挡住（2026-09-14 客户机实测）。解锁后若后端其实仍在运行，再次发送会被 `ACTIVE_RUN` 拒绝并给出明确提示——比界面永久冻结可诊断得多 |
| `13-skill-refs` | `/` 技能引用、`@` 引用 token 识别（`tokenizeFileRefs`/`composerRefTokens`）、输入镜像与气泡高亮（两类引用共用 `highlightComposerRefsHtml`/`renderUserContent`，`.file-ref` 换色区分）、`positionComposerPopup`（弹层通用定位，含视口兜底）、`insertTextAtCursor`（光标处插入，Skill 引用与快捷消息共用，改值后调 `notifyComposerChanged`）；`commitSkillSelection`/`prefillPresetSkillsInComposer`/`appendPresetSkillsToComposer` 同样在改值后通知输入管线 |
| `14-file-panel` | 文件面板、侧栏折叠；`NARROW_VIEWPORT_MAX = 760` 是「手机端」的**唯一 JS 阈值**（与 CSS `@media (max-width: 760px)` 同界）；`filePanelUsable()` 只判「有没有 `#filePanel`」——**不再按 `innerWidth > 760` 判死**，手机端靠 CSS 换成全屏抽屉形态而非丢能力；`sidebarDesktop()` 从同一常量取值 |
| `15-bind-events` | 全部事件绑定、Skill 安装/迁移设置、启动调用；**Agent 说明弹层**（`#agentHelpButton` 的 click → `toggleAgentHelpPopover`、弹层自身 click 停传播、`window` resize/scroll 重定位、document click 关闭——与上下文用量弹层同一组绑定，别另写一套开合规则）；**`#messages` 点击委托**（代码块复制 / 复制消息 / 打开文件 / 分支 / 「新会话」分割线 / 取消分割线 / 填入种子消息 / **「编辑」→ `startEditMessage`** / **「重新生成」→ `regenerateMessage`**）；**底部 `#composerForm` 提交与输入框回车在编辑态下改走 `confirmActiveEdit()`**（不再发新消息）；**文本右键菜单**（`contextmenu`：可编辑元素 → 编辑菜单，`.message-body` 选区 → 复制选中/快速发送；**触摸 / 手写笔长按一律不拦截**——`pointerdown` 捕获阶段把指针类型记进 `lastPointerType`，命中 `touch`/`pen` 就直接 return 把系统原生文本菜单（复制/粘贴/全选）让出来，手机经局域网 http 打开时那是唯一可行的粘贴通路，§九.85；**模态弹层内右键时选区菜单必须限制在同一弹层里**——模态弹层不清除 `window.getSelection()`，否则会拿底层会话的残留选区误弹会话页菜单）；输入框 `input` 事件与模块启动各调一次 `updateSendButtonState`（首屏即灰暗不可发送态）；输入框 input/click/focus 触发 `updateFilePopup`、keydown 先交 `handleFilePopupKey`、弹层点击委托 `handleFilePopupClick`；`快捷消息` 按钮开合、面板点击委托、点外部关闭、resize/scroll 重定位；灯箱左右按钮点击与 document keydown 里的 `handleImageLightboxKey`（先于文件面板 Esc 收尾）；**侧栏滚轮步进**（`wheel` 监听：`|delta| ≥ 40` 视为鼠标滚轮 → `preventDefault` + `scrollTop += delta * 1.5`；触控板小步进直接返回交给原生，避免过度加速）、**「⋯」菜单与重命名对话框**（菜单项点击/点击外部/Esc 关闭，Esc 挂 document——点击 ⋯ 后焦点在按钮上，菜单自身收不到键盘事件）、**Agent 表单的提示词下拉与角色卡文件输入**绑定；**API 供应商卡片事件委托**（`#providerCards` 的 click 分三支：× → `deleteProvider`、添加卡 → `addProvider`、卡片 → `openProviderCard`；keydown 的 Enter/Space 同样打开；`#providerDialog` 的 `close` 事件统一复位编辑态并重绘卡片）；**Agent 卡片事件委托**（`#agentCards` 的 click 分三支：× → `deleteAgent`、添加卡 → `showAgentForm(null)`、卡片 → `openAgentCard`；keydown 的 Enter/Space 同样打开；`#agentDialog` 的 `close` 事件统一复位）；**已删 `#composerModelCustom` 的 change/blur 绑定与 `saveCustomComposerModel` import**；**窗口跨 760px 边界不再关闭文件面板**——resize 处理器只在面板已打开时重算形态（`applyFilePanelOpenClass()`），手机抽屉与桌面侧栏共用同一份打开状态；**背景图可用性回调注册**（`bindChatBackgroundControls` 里 `onChatBackgroundMissingChange(() => updateChatBackgroundControls())`：启动探针是异步的，结果回来要重画设置卡——它不能在 01-core 里直接调，跨模块回调必须显式注册） |
| `16-file-refs` | **输入框 `@` 引用会话工作区文件/目录**：弹层状态与懒加载浏览（按目录请求 + 会话内缓存 + 并发合并 + 过期响应丢弃）、目录面包屑、↑↓ 选择、Tab 进入目录 / Shift+Tab 返回上级 / 文件项 Tab·Enter 引用、目录项右侧箭头与首项「返回上一级」（触屏）、目录行选中时左侧 `Tab` 键提示、`@` 后打字过滤当前目录；插入的 token 为 `@相对路径`（含空格加引号，目录带尾 `/`），解析交给后端；插入后调 `notifyComposerChanged` 刷新发送按钮 |
| `17-choice-groups` | **交互选项组的前端纯函数层**（与 `naiba/core/choices.py` 同口径、不持有状态、不碰 DOM）：`normalizeChoiceGroups(choices, choiceGroups)` 把实时事件 / 消息 metadata / 历史读取三条路径的数据规范成契约形态（旧版纯字符串数组 = 一个无题目的组；`mode` 只认 `single`|`multi`、缺省 `single`；原文不截断、空选项组丢弃）；`isChoiceFence`/`explicitChoiceGroups`/`choicePreviewMarkup` 处理回复里的 ```naiba-choices 块——有效块渲染成「题目 + 单选/多选标识 + 选项列表」的静态预览（不显示原始 JSON），无效块返回空、调用方原样保留为普通代码块（交互控件在 `12-chat-input` 的面板里） |
| `18-interjections` | **插话（运行中第二输入通道）的前端全部实现**（2026-09-20 补登记：本模块此前只出现在 §六 的冻结版自检里，漏在本表外）。导出 `isQueuedInterjection`（判「这条消息是插话行」：role 与 metadata 四键）、`runGuidanceElement` / `renderRunGuidance`（消息区顶部的插话面板：待引导行 + 操作按钮 + 「N 条待引导」计数；`settlePanel` 负责终态收口）、`queuedInterjectionStats`（计数口径，面板头部唯一来源）、`freezeQueuedInterjections`（终态把残留行冻结为 `stopped`）、`sendRunInterjection`（入队：`POST /api/chat/interject`，202 + 新消息 id）、`reuseInterjection`（把残留行文本取回输入框）、`guideInterjection` / `guideAllInterjections`（引导：转 `guided` 待模型消费）、`deleteInterjection`（**不要求 run 还活着**——终态后清理残留行才是常态，§九.100 ④）、`handleUserGuidanceEvent` / `handleInterjectionConsumedEvent`（两枚事件的 handler，挂 `12-chat-input.js` 的 type→handler 路由表） |
| `19-onboarding` | **首启引导向导**（还没有可用供应商时：选模板 → 粘 Key → 开聊）。`ONBOARDING_DISMISS_KEY = 'naibaOnboardingDismissed'`（跳过态记 localStorage——冻结版 WebView2 已是持久 profile，`launcher.py` 里 `private_mode=False`，不必为此再动服务端配置）；`maybeShowOnboarding()` 的判据是**「有空态」而不是「弹过没」**：`hasUsableProvider()` = 任一 profile 为 `kind==='local'` **或** `has_api_key`（**只建了卡、没填 Key 不算**），且先 `await loadProviderPresets()`、**名单拿不到（离线/旧服务）就不弹空窗**；`resetOnboarding` / `selectOnboardingPreset`（本地后端要地址不要 Key、在线大厂只要 Key、自定义要地址+Key+模型名）/ `testOnboardingConnection` / `saveOnboardingProvider`（保存前先本地预校验，服务端仍会再校验一次）/ `dismissOnboarding`（任何关闭路径都记一次）。**名单与卡片渲染都来自 `09-settings.js`**（本模块只负责「向导这个流程」）；向导自己的选中项是模块私有的 `selectedPresetId`，**不写 `state.providerPresetId`**——免得与设置弹层的表单来源互相污染 |
| `main.js` | 模块入口 |

## 四、关键概念与数据流

- **Run 与 Job 物理同源**：`background_tasks` 表按 `kind` 区分（run：chat/plan_execute；job：shell/check/http_poll/comfyui/subagent），`parent_job_id` 组成父子树、取消级联；事件同用 `run_events` 表（PK(run_id, sequence)）；**任务面板只列后台作业**（`/api/tasks?jobs_only=1`）：chat/plan_execute 那些行是「用户每发一句话就插入一次的回答记录」，`parent_job_id` 只是「我由哪次回答派生」的归属锚点，不是"那句话是一个任务"；面板按这个锚点分组、组标题取「类型 + 时间」（**不引用用户消息原文**），停止按钮直接打在单个作业上（`POST /api/jobs/{id}/cancel`，走 Job 的取消事件）；
- **历史数据管理（v14 起）**：推理流**流式期**保持 `reasoning_delta` 逐块即刻落库（逐词实时推送；落库即推送，`_stream_run` 每事件 flush）；run 进入终态后由收尾 finally 触发 `compress_run_events` 把该 run 的 delta 块**合流为整段 `reasoning` 事件**（2048 字符/1s 窗口，与迁移同口径，前端与 `_rebuild_partial_run` 均兼容两种形态）；run 进入终态（completed/failed/cancelled）时 `background_tasks.snapshot` 的 `conversation_messages` 键被收缩（仅运行期/中断恢复需要，interrupted 保留）；存量库由迁移 v14 一次压缩（迁移前自动备份到 `data/backups`）；`done`/`cancelled` 事件载荷的 message 仅用于终态即时渲染，历史回放依赖 `messages` 表；
- **一次对话的链路**：`POST /api/chat` → `submit_chat`（快照固化：模型/工具集/Skill 策略）→ run 线程 `_run_chat` → `build_model_history` 重放 → 图片占位改写（纯文本模型去图留路径引用，是否看图由模型按需调用 vision_analyze；多模态模型保留原图）→ `SkillAgent` 循环（模型↔工具，事件经 `_RunEventSink`/`emit` 落库；**每次工具产出即按声明采集媒体**）→ 前端 NDJSON 流式渲染 → 终态 `add_message`（metadata 落库：trace/reasoning/tool_runs/files/attachments）；
- **媒体采集与内嵌（P2/P3）**：媒体**在工具产出点**按 `ToolSpec.metadata["media"]` 声明提取并托管缓存（`storage/media_collect.py`），记录写回该次 `tool_run["media"]`（`{kind,name,source,thumb_path}`，仅路径与元数据），随 `tool_result` 事件与 `metadata.tool_runs`/`metadata.activity[].run` 下发——**不再等轮末扫结果文本**（原始 result 只在产出点可见，`display_tool_run` 已按工具脱敏）；因此取消/失败路径从事件重建时同样带媒体（旧实现这两条路径拿不到 vision 装载的路径）。**前端就地内嵌**：媒体块紧跟对应工具块之后（工具块保持折叠、媒体始终可见），流式期间 `tool_result` 一到即出现，终态 `done` 全量重渲染由 `toolRunMarkup` 从同一份 `run.media` 重建同一位置；消息级 `metadata.attachments` 是各 run 媒体的派生汇总（`union_run_media`：去重 + 分桶截断），末尾网格只渲染 `remainingAttachments`（没有就地归属的附件，即旧会话数据），因此**同一张图不会显示两遍**；截断信息（`media_truncated`/`attachments_truncated`）渲染为"共 N 个媒体，仅显示前 M 个"提示块。**候选先按分桶上限（图 20 / 视频 8 / 音频 8）预截断再落盘**，避免一次列目录把几百张图拷进 data 目录卡住；只对成功且非 `NEED_CONFIRM` 的结果提取（失败信息里的路径不是产物）；
- **媒体播放与传输（P4）**：用户气泡与助手气泡**同口径**渲染媒体——图片=缩略图+灯箱、视频/音频=`<video>/<audio>` 就地播放、其它=文件名 chip，**每种媒体都带 `figcaption` 文件名**（此前用户侧音视频只有文件名、助手侧连文件名都没有）。`/api/file` 支持**单区间 Range**（`206` + `Content-Range` + `Accept-Ranges: bytes`；越界 `416` + `bytes */size`；无 Range 保持 200；**分块 256KB 发送**）——`<video preload="metadata">` 与进度条拖动因此可用，大文件不再整包进内存。GIF 原图（动画）原样保留，但**生成首帧 WebP 缩略图**（`THUMB_SOURCE_SUFFIXES`）：否则前端按 `<主图 stem>_thumb.webp` 推导必然 404（破图）；前端另保留缩略图加载失败的 `onerror` 回退主图兜底（**待发附件区 `.pending-thumb` 同样兜底**：文件已被清理丢失时降级为文件占位图标并摘掉失效的 `data-large-url`，灯箱大图加载失败改为明确提示「图片文件已不存在」而不是静默关闭——此前表现为"点了没反应"）；
- **异步 Job 产物写回（P5）**：`comfyui_batch(wait=false)` / `run_in_background` 立即返回 job_id，产物要等 Job 跑完才出现——Job 进入终态（completed/cancelled/failed，部分产物同样要可见）时 `JobRegistry._finish` → `storage/job_media.py::JobMediaWriter` 把产物媒体挂回**发起该 Job 的那条助手消息**（归属链 `parent_job_id` → Run → `metadata.run_id` 对应的助手消息），并挂到"调用该 Job 的那次工具调用"上（按 result 里的 job_id 匹配 `tool_runs`/`activity`），因此**就地出现在原工具块下方**；写回时推进 `conversations.updated_at`，前端**既有轮询**（`loadTasks`/`syncCurrentConversation` 的快照比较）即会重渲染——**不新增事件通道、不刷新页面**；幂等按来源去重，消息已删/写回失败安全放弃（只记日志）；
- **纯附件轮次（输入框无文字只发文件/图片）**：前端 `sendChatMessage` 与后端 `submit_chat` 同口径放行（文字与可用附件至少有一个）；`messages.content` 存空串，模型侧文本由 `compose_user_content` 在历史重放时生成（`[用户未输入文字，只发送了以下附件]` + 附件引用行），因此**当前轮与历史重放逐字节一致**；发送按钮可用性由 `updateSendButtonState` 单点维护（无内容/上传中/上下文满 → 灰暗禁用，可发送时才是 accent 实心）；首轮无文字时会话标题取首个附件名；
- **快捷消息（独立列表，与开始页「自定义指令」互不干扰）**：输入区底部 meta 行的 `快捷消息` 按钮展开固定定位面板（`position: fixed` + JS 对齐按钮，避免被 `.composer-wrap` 的 overflow 裁剪）；**条目只有正文、不设标题**（列表首行作标题行、其余行折叠为一行预览；新增/编辑弹窗隐藏标题字段、正文标签为「正文」）；条目来自 `GET /api/quick-messages?sort=usage`（`quick_messages` 与开始页 `starter_prompts` 是两份数据），排序权重 `min(点击次数,50)×2 + 新鲜度加分`（7 天 +6 / 30 天 +3 / 90 天 +1，并列取新增时间倒序）——高频稳定靠前、新条目有曝光窗口；点击条目把正文插入输入框（不自动发送）并 `POST /api/quick-messages/{index}/use` 记一次使用，行内 `✎`/`✕` 复用同一编辑弹窗（按 `target` 区分标题、字段与接口）；开始页卡片仍按插入顺序展示全部条目（不做静默截断）；
- **交互选择面板（AI 回复里的可选项 → 输入框草稿）**：后端只提供数据（实时 `choice` 事件 / 消息 `metadata.choice_groups`），面板只做「收集答案 → 一次性填入输入框」，**不新增接口、不自动发送、不落库**；面板挂在 `.composer-wrap` 内、`#composerForm` 之前（`#choiceButtons`），**不进消息流**。状态以「会话 + 来源消息」为键存在前端内存（`choiceSelections`，`CHOICE_MEMORY_MAX=24` 按插入序淘汰、当前那条永不淘汰）：重渲染 / 重复事件 / 会话切换都不清空（切回来接着答），**整页刷新即从首题重来、不落半成品**；实时 `choice` 事件、`done` 事件 metadata、历史渲染三处汇入 `showChoiceButtons` 这一个同步入口。交互：**单选点完自动跳下一题**（末题停在原地等「完成」）、多选勾选不跳题、`▸/▾` 折叠只改显示（题面与进度保留、已选不丢）、运行中（`state.abortController`）除折叠外全部禁用；「完成」把答案块（一行一题 `题面：选项`，多选同行用「、」，无题目只写选项）**追加**到草稿末尾并 toast 提示确认后发送（不改写已有草稿、不挤掉待发送附件），完成后该条不再弹面板（重新加载历史也不弹）。答案块是普通文本——发不发、要不要改字都由用户决定。
- **开始页预设（内置与自定义同权）**：内置预设（通过 MCP/HTTP 调用 ComfyUI、设置本地 Comfy MCP、列出可用工具/文件、等待用户指令）并入 `starter_prompts`，与用户自建条目一样**可编辑、可删除**（副标题 `desc` 与图标 `icon` 随条目保留）。**升级语义**：每个内置预设带稳定 id、条目记 `preset_id`（改标题不影响识别）；用户删除的 id 记入 `starter_presets_dismissed`，**永不自动复活**；版本新增的预设（id 未被删除过）在下次启动**自动补齐**；「恢复默认预设」清空删除清单并补回缺失项（用户改过的同名条目保持原样）。「安装 Skill / 编辑 Skill」两张卡片是动作入口（无正文），保持静态不可编辑；
- **思考强度（会话级）**：输入框右侧的思考按钮**只负责开合强度列表**（auto/off/low/medium/high），不再顺带切档；当前等级显示在**输入框外的 meta 行**（`.composer-meta-right`，与「就绪」同行，文案「思考 X」——不挤在按钮下方），列表内高亮当前项（`REASONING_LABELS` 单一文案表）；选档走 `setReasoningEffort` 落库会话设置（无会话时先建会话）；列表用 `position: fixed` + JS 对齐按钮（挂到 body），避免被 `.composer-wrap` 的 overflow 裁剪（教训 §九.27）；
- **@ 工作区引用（输入框 `@` 引用文件/目录）**：前端弹层按目录懒加载（`GET /api/workspace/browse?conversation_id=…`，根 = 会话工作区，与工具层读取策略同根），插入 `@相对路径` token（目录带尾 `/`，含空格加引号）到输入框并高亮；**发送时前端原样提交 token**，由 `submit_chat` 用 `resolve_file_references` 解析成绝对路径后落库（`messages.content` 即模型可见文本，历史回放天然一致），`display_content` 与首轮标题仍取用户原文；只有"工作区内真实存在的路径"会被替换，邮箱（`a@b.com`）与越界/不存在的 token 原样保留；引用到的文件位于会话工作区内，模型 `read_file` 免越界确认；
- **系统提示词的唯一来源 = Agent**：会话级系统提示词已整体退役——`run/chat.py`、`plans.py`、`subagent.py` 三处注入点只读 `agent.system_prompt`（Agent 为空时回退全局 `agent_system_prompt`），`conversations.system_prompt` 作为**遗留列保留但不再注入**（历史值仍能在「首轮上下文」折叠卡里看到当时原文）；前端入口（顶栏「对话设置」按钮、会话条目齿轮、对话框本体）与角色卡导入全部移除/迁到 Agent 编辑表单（**系统提示词成块**：标题行右侧工具栏 = 快捷提示词面板（点按钮展开、条目右侧 ✎ 编辑 / × 删除、点条目套用）+「**存为快捷提示词**」按钮（弹窗含标题与正文，正文预填当前文本框）+ 导入角色卡**追加** + 自定义头像，下方是占满宽度的文本框）——**设置页的「快捷提示词」页已整体下线**（导航入口、列表、搜索、表单与旧绑定一并移除，增删改只走 Agent 表单），`stream_enabled` 也只剩遗留列（流式恒开）；**内置 Agent 预设已整体下线**——`built_in_agents()` 清单为空（机制保留：`built_in` 标记 / 不可删守卫 / 前端「内置」徽标），用户配置里遗留的旧内置副本（如 `dsh-standard`）启动时由 `_migrate_agent_builtin_flags()` 摘掉标记、变成普通可删 Agent（名称/提示词/工具集不动），引用已删 Agent 的会话回退 `default_agent_id`；**Agent ID 由后台自动分配**（`agent_<12 位 hex>`，用户不再手填；表单只保留隐藏的 `#agentFormId`）；**自定义头像按 Agent 存** `data/avatars/<agent_id>_<内容哈希>.webp`（上传图中心裁切 256px WebP），用该 Agent 的会话把助手气泡的「AI」圆标换成该图片（`object-fit: cover` 再兜一层）；
- **会话收藏（侧栏「已收藏」分组）**：`conversations.favorite`（迁移 v15，0/1）只用于侧栏归类；**写入口只有 `storage.set_conversation_favorite`**（`POST /api/conversations/{id}/settings` 收 `favorite` 布尔值）——它**不动 `updated_at`**，因为侧栏工作区分组按更新时间倒序且默认只显示最新 5 条，推进时间会让收藏把顺序顶乱、启动时"最新 5 条"被收藏项占满；列表/详情接口原样返回该字段；前端点五角星**乐观更新 + 失败回滚**，「已收藏」分组固定追加在工作区分组之后、跨工作区聚合、默认展开、不受每工作区 5 条上限影响，**收藏的会话仍留在原工作区**（两处都显示，避免"会话不见了"的错觉）；重命名/删除并入会话条目的「⋯」菜单（菜单 fixed 挂 body、Esc/点外部关闭）；新建工作区同样走对话框（选目录 → 填名称），不再用 pywebview 不支持的 `window.prompt`；
- **侧栏虚拟化（滚动丝滑的三条不变量）**：①**行高必须恒定**——行高由 `sidebarMetricsNow()` 启动时量一次并缓存进偏移表，任何 hover/选中态**不得改变行高**（`×` 删除按钮因此改绝对定位 + `visibility`，否则表头 28→32px、整个侧栏跟着抖）；②**只在行区间变化时重绘**（`sidebarWindowRange`：start/end/totalH 三者不变就只校正 scrollTop，不写 innerHTML）；③**滚动上限用浏览器真实 `scrollHeight`**（含容器 padding），并配 `overflow-anchor: none`（否则滚动锚定与重绘互相拉扯）；
- **取消链路**：`cancel` → 置 cancel_event + `cancelling` → `_run_chat` 双检 → 3 秒看门狗兜底（`_schedule_forced_cancel`）→ `_persist_aborted_message` 幂等重建"已中止"消息；
- **工具分类（Agent 编辑页）**：7 组单一维度（读取与检索 / 文件写入与编辑 / 命令与脚本执行 / 联网与外部服务 / 视觉与图片 / 任务与扩展 / 长会话），每组带风险徽标（只读·会改文件·高风险·联网·会写产物·会改动）；动态 MCP 工具归入「联网与外部服务」并**按服务器二级分组**（否则 40 个 `mcp__comfy-mcp__*` 挤在一层，要整组启用只能逐个点）；工具搜索框按名称/说明过滤成平铺结果。分类名只是**展示层**——Agent 保存的是工具名（`tool_scope`），重新分组不影响已有 Agent 的勾选；但预设里的 `group:` 引用要同步（现已改为显式工具名）。
- **工具三层**：内置（`registry` 单一定义 → `providers/*` 实现+策略 → 引擎确认/直调分发）｜Skill（领域知识，不授予工具）｜MCP（`mcp__<server>__<tool>` 同构 def，execute 绑 mcp.call、policy 读 annotations；call_mcp 网关已移除）；视觉工具单入口（`vision_analyze` 同名按会话模型能力分流 + `vision_image_ops`，所有模型同一工具集）；
- **工具结果三层语义**（`core/tool_results.py` 单一事实源）：原始 run（内存，宿主收尾用）→ 模型可见 model_run（`tool/success/脱敏+截断 result`；模型上下文三通道 native/兼容/历史兜底均以它为准）→ 展示 display_run（stream 事件与 `metadata.tool_runs`：模型可见 result + arguments/reason 仅供前端核对）；`arguments`/`reason` 是模型自产，不进模型上下文；截断必带"已截断"标记；已退役的 `tool_runs` 表与 `log_tool_run` 写入、`ToolSpec.summarize` 死 API 均已删除（迁移 v13 DROP）；
- **Plan**：`prepare → ready → building → finished/failed/cancelled`，SQLite 权威状态，执行托管在 Run 线程；
- **首轮上下文（first_turn）**：会话首轮（无历史消息，与 skill 冻结集同判据）run 在终态事件 emit 前把「第一轮发送给模型的完整上下文」落盘（完整 system 原文 + 工具 JSON 定义 + 生成参数 + 技能；请求消息图片 base64 摘要化），**同时写两处**——该 run 的 snapshot（那次运行的记录）与会话级 `conversations.first_turn` 列（这个会话的持久记录，迁移 v16）。前端经 `GET /api/conversations/{id}/first_turn` 在会话顶部渲染默认折叠的显示卡，**run 进入终态（done/error/cancelled）时由前端即时拉取并就地插入**（`refreshFirstTurnCard`），第一轮完成后立即展示；切换会话由 openConversation 拉取。**取数顺序**：会话列 → 老会话（v16 之前）回退最早 chat run 快照。**分支对话在分支点不是首条消息时继承该列**（分支点之前的首轮与源会话是同一轮；分支点为首条消息则不继承，按新首轮重新落盘）——只存 run 快照的话分支读不到、卡片不显示（用户报障）；「清空已结束任务」会删掉 chat run 行，会话级副本因此是唯一留存。trace 是增量设计（不含 system），系统提示词原文由 SkillAgent 经 `run_context["trace_system"]` 带出。老会话无此数据不显示，旧版落盘结构（prompt 字段）由 API 归一兼容；
- **上传链路（2026-09 优化后）**：前端 XHR multipart 流式直传（无 base64、真实进度、可取消、并发上限 3、80MB 前置校验）→ `POST /api/uploads`（http.py 按 Content-Length 精确读流、python_multipart 回调解析、超限 413 即断、临时 spool 落 uploads 根 .part、中断清理、中文文件名 UTF-8 还原）→ `app._upload_spooled` → `store_uploaded_file`（图片压缩/缩略图、内容级去重命中复用、分日目录 `uploads/YYYY-MM-DD/` 原子落盘）→ 上传后超 256MB 自动触发 `auto_clean_uploads`（**15 分钟保护窗口 + 本次落盘文件显式保护**，见 media.py；否则被引用缓存超阈值时会把刚落盘的待发附件一起删掉）；移除未发送 chip 调 `POST /api/uploads/delete`（仅未被消息/快照引用时可删，`store.upload_path_referenced` LIKE 转义子串匹配做引用保护）；`/api/file` 迁移回退递归适配分日目录；**发送前附件校验**：前端在清空草稿前调 `POST /api/uploads/check`（`app.api_check_uploads`），命中丢失的缓存文件时剔除该项、保留文字草稿并提示重新上传；服务端 `run/chat.py::submit_chat` 用同一判定 `missing_cache_attachment` 兜底（缺失 → 400「附件文件已丢失…请重新上传后再发送」，不再把幽灵路径喂给模型）；
- **图片查看**：点击消息里的缩略图打开大图灯箱；灯箱内可**左右切换当前会话的全部图片**（列表取 `#messages` 内所有 `img[data-large-url]`，按历史出现顺序去重）——左右按钮、底部 `N / M` 计数、首尾循环，`←`/`→` 键切换、`Esc` 关闭；**未缩放时点屏幕左右半屏也可翻页**；**滚轮缩放（1–6 倍、以光标为锚点）+ 缩放后拖动平移 + 双击复位 + 触屏单指平移/双指捏合/双击切换**（图片小于视口时不平移，避免拖出屏幕；换图/关闭自动复位）；单张图片或输入区附件/右侧文件面板的图片不显示切换控件；**灯箱控件层级（2026-09 修复，用户报障）**：`img` 带 `will-change:transform` 会自建层叠上下文（层级视为 0），而它在 DOM 里排在 `close`/`prev` 之后，按文档顺序绘制会盖住左箭头——手机上表现为「左箭头被图片吞掉、点击穿透成半屏翻页」而右箭头正常（左右不一致）；修法是给 `.lightbox-close`/`.lightbox-nav`/`.lightbox-counter` 统一加 `z-index:3`，控件恒浮在图片之上、左右对齐；**手机形态（≤640px）另设上下「控件安全带」**——`.image-lightbox` 的 padding 改为 `max(56px, env(safe-area-inset-top)+44px) / max(44px, env(safe-area-inset-bottom)+36px)`，并把图片尺寸改成按容器内容盒 `100%` 解析（原来的 `94vh` 不吃 padding，竖图铺满时叉叉会骑在图片上边缘），于是关闭按钮与计数完整落在图片外、且避开刘海安全区；回归断言见 `verify/lightbox_smoke.cjs` 第 ⑦ 段（390×844 视口下 hitTest 各控件未被图片遮挡 + 安全带布局 + 左箭头可点击翻页）；
- **PDF 处理链路**：上传的 PDF 附件在模型侧引用行带固定处理指引（`upload_reference_lines`，_run_chat 与历史重放共用、逐字节一致）；模型按需：`read_pdf` 提取文本层（页标记/截断续读）→ 扫描版（无文本层）用 `pdf_render_pages` 渲染页图 → 对页图路径调 `vision_analyze` 识别 → 细节不清时 `pdf_zoom_region` 局部放大（矢量源重渲）后再次 `vision_analyze`；页图/局部图缓存在 `data/uploads/pdf_pages/<sha16>/`（内容哈希键控幂等，受清理/统计覆盖），渲染产物经 `extract_attachments` 以消息附件卡呈现（用户可核对 AI 看了哪些页）；编排规则在系统提示「PDF 处理策略」段（**仅当会话工具集含 `read_pdf` 时注入**，与 `web_search` 引导、视觉段 `vision_prompt_sections` 同口径；附件引用行的 PDF 指引同样按该开关出现——工具集首轮固化，同一会话内恒定，不破坏前缀缓存）；`read_file` 对 PDF/图片/二进制明确报错并引导对应工具（二进制防护）；
- **视频处理链路**：上传的视频附件在模型侧引用行带固定处理指引（`upload_reference_lines` 的 `video_tools` 分支，与 PDF 分支同口径：类型判定读 `core/media_types.py::media_kind_of`，不二次维护扩展名名单）；模型按需：`probe_video` 读时长/帧率/分辨率 → `extract_frames` 抽帧（`times` 指定时间点 / `interval` 扫全片 / `keyframes` 找画面切换点，并生成**联系表**）→ 把联系表交给 `vision_analyze` 概览全片、可疑片段再抽单帧细看 → 局部细节用 `vision_image_ops` 裁剪（v1 不新增 `zoom_frame`：帧图落盘后裁剪不可能恢复更多细节，真高清要按时间点重抽，见 `naiba/video.py`）；帧图与联系表缓存在 `data/uploads/video_frames/<video_sha16>/<params_key>/`（两级上限 + 清单幂等），产物经媒体采集以消息附件卡呈现；编排规则在系统提示「视频理解策略」段（**仅当会话工具集含 `extract_frames` 时注入**，与 PDF 策略段、`web_search` 引导同口径）。

## 五、前后端契约（改这些要先改契约文件）

1. 事件 type：`core/contracts.py` `EventType`（前后端反查守门测试）；
2. 事件负载键：`EventPayload`（golden 反查守门）；`tool_result` 的 `media`（`[{kind,name,source,thumb_path}]`）与 `media_truncated`（`{total,shown,kinds}`）是宿主采集的媒体记录与截断自述，**模型不可见**（`model_visible_run` 不含）；
3. metadata 键：`core/messages.py`（读/写侧共用；`attachments` 为消息级派生汇总、`attachments_truncated` 为分桶截断自述）；**消息 metadata 可被异步 Job 就地更新**（`storage.update_message_metadata`，只改 metadata + `conversations.updated_at`，content/role/created_at 不动，消息顺序契约不受影响）；
4. run_context 键：`RUN_CONTEXT_KEYS` + 校验工厂（26 键，防裸键逃逸；`media_intent` 为枚举类工具的媒体门禁；`pull_interjections` / `mark_interjections_consumed` 是插话队列的读/消费回调，**在 `run/chat.py` 组装 run_context 时注入**——不在别处拼裸键）；
5. 工具 schema：`tools/providers/*` 单一定义（def.execute/policy 同源），`registry.py` 聚合声明与分发；
6. 会话字段：`conversations.favorite`（迁移 v15 新增，0/1；**只用于侧栏「已收藏」分组**，不参与模型上下文、不影响任何冻结快照）；`conversations.first_turn`（迁移 v16 新增，JSON 文本；**会话级首轮上下文**，只由 `set_conversation_first_turn` 写、`_first_turn_info` 读，不经 HTTP 暴露，分支对话会继承——见 §四）；`conversations.model_key`（API profile）与 `conversations.model_name`（迁移 v17 新增；**该会话实际使用的模型名，空值不可发送**——`run/chat.py` 的 `_profile_with_model_override` 与 `submit_chat` 两处都直接拒绝并提示在会话底部选模型）会同时写入 Run 快照，运行中的请求固定使用快照，后续切换不影响它；`POST /api/providers`（与 `/api/model-profiles`）保存**只改供应商配置本身**（`api_upsert_model_profile` → `config.upsert_provider`，仅在 `default_model_key` 为空时补默认），**不改写任何已有会话的模型选择**；`storage.clear_conversation_model_overrides`（「只清空覆盖值恰好等于旧默认模型名的会话」的定向清理实现）**当前没有生产调用点**，只有定义与单测；`system_prompt`/`stream_enabled` 为**遗留列**（会话级系统提示词已退役、流式开关已无 UI，读时保留、写入口只在 `/api/conversations/{id}/settings` 的兼容路径上）；**`conversations.branched_from_id` / `branch_message_id`（迁移 v18 新增，纯增量、默认空串）**登记「这个会话是从哪个会话的哪条消息分支出来的」——只由 `branch_conversation` 写入，`list_conversations` / `get_conversation` 随行带出 `branch_count`（同源分支数，一次 GROUP BY，不是逐行 COUNT）与 `branch_source_title`（源标题，源被删则为空），是侧栏分支徽标与分支链面板的**唯一数据来源**（列表与详情两处口径必须一致）；**v18 之前的存量分支明确不回溯**（按标题 `(N)` 弱推断不可靠，宁可显示为无关联）、**悬空引用（源被删）不是错误**（`branch_chain` 回 `source_deleted=true`，徽标降级为「源会话已删除」）；
7. HTTP 端点与 NDJSON 流格式：`http.py` + `EventType` — 端点路径与事件版式是前后端契约，改动需两端同步（前端 `api()`/`handleChatEvent`）；**`GET /api/file` 的 Range 语义同为契约**（单区间 → 206+`Content-Range`，越界 → 416+`bytes */size`，无 Range → 200+`Accept-Ranges: bytes`）；**Agent 头像两端口径同为契约**：`POST /api/agents/avatar`（multipart：`agent_id` + 图片文件，成功返回 `{ok, agent}`，Agent 不存在 404、坏图/超限 400）、`GET /api/agents/avatar/<文件名>`（`image/webp` + `Cache-Control: private, max-age=31536000, immutable`，文件名必须是 `<agent_id>_<12 位 hex>.webp`）；`/api/agents` 的 `id` 可留空（后端分配）；`GET /api/workspace/browse?path=&conversation_id=` 的响应字段（`root/path/rel/parent_rel/truncated/entries[{name,path,rel,kind,size}]`）同为契约——设置页目录树（不带 conversation_id）与 @ 引用弹层（带 conversation_id）共用；`/api/starter-prompts` 的条目字段（数组位置即 `index`；`title/text`，内置预设另带 `desc/icon/preset_id`）同样按契约对待——开始页卡片全部由这份数据渲染（内置预设与用户条目同权、可编辑可删除），响应还带 `missing_presets`（缺失内置预设数，前端据此显示「恢复默认预设」），恢复走 `POST /api/starter-prompts/restore`；`/api/bootstrap.media_exts`（`{kinds, exts:{image/video/audio}, limits:{image/video/audio}}`）是前端媒体判定的唯一数据源（`mediaKind`），改名单只改 `core/media_types.py` 一处；**`POST /api/messages/edit` 是「编辑 / 重新生成」的截断契约**（请求 `{conversation_id, message_id}`，只接受 `role == "user"` 的 id——非 user 400；删除该消息及其之后全部消息并回传该轮 `attachments`；两个入口复用同一接口，**后端零改动**）；**全文搜索 `GET /api/search/messages?q=&conversation_id=&limit=`**（`q` 空或纯空白 → 400、`limit` 非整数 → 400、`limit` 越界**夹紧**到 [1,50] 而不报错（它是 UI 的展示意图，不是契约）、`conversation_id` 给了但不存在 → 404（否则前端会把「这个会话没命中」误读成「搜不到」）；响应 `{query, conversation_id, total_hits, returned, truncated, limit, scope, hits[]}`，`hits[].snippet` 是命中词前后各 60 字的**连续原文**（前端据此高亮、不再回查正文），`in_context` 按**每条命中自己那条会话**的分割线判定；**`truncated` 必须显式标注截断，绝不静默丢弃**）；**分支链 `GET /api/conversations/<id>/branch_chain`**（只读；回 `{conversation_id, role: branch|source|none, source_deleted, source, items[]}`，`role=branch` 时链 = 源 + 全部兄弟（含自己）、`role=source` 时链 = 自己的全部分支（**自己不在链里**——用户已经在它上面了）；会话本身不存在 → 404）；**删除 / 撤销两条端点**：`POST /api/messages/delete`（`{conversation_id, message_id, mode: single|turn}`——`turn` 仅对 **user** 消息可用、`role='session'` 的遗留标记行一律拒绝（ValueError → 400，语义属 `delete_session_start`：误删会把上下文起点悄悄往前挪）；回 `removed[]` 完整快照**含 `metadata_json` 原始文本**）与 `POST /api/messages/restore`（`{conversation_id, messages: removed[]}`——按快照原 id/content/metadata/created_at 逐行插回，**已存在 id 跳过（幂等）**，因此重复点撤销不报错也不翻倍）；

8. 交互选项组（AI 回复里的「请选择」）：结构登记在 `core/contracts.py` —— `CHOICE_GROUP_KEYS`（`prompt`/`choices`/`mode`/`source`）、`CHOICE_MODES`（`single`|`multi`）、`CHOICE_SOURCES`（`explicit`=回复里的 ```naiba-choices 结构化代码块，有效块优先；`natural`=自然语言「请选择…」+ 编号/字母列表识别）、`DEFAULT_CHOICE_MODE`、`EXPLICIT_CHOICE_FENCE`（模型输出约定，**不是工具、不进工具目录**）；实时 `choice` 事件负载键为 `choices`/`choice_groups`/`message_id`（**`message_id` 是前端选择状态键的一半**：缺了它 `choice` 与随后的 `done` 会各算一个来源，面板会在两事件之间被重建、选择被清空）；消息 metadata 的 `choices`（旧版纯字符串数组、无题目）与 `choice_groups` 由同一条 `core.choices.normalize_choice_groups` 升级成组（实时事件 / metadata / 历史读取三条路径共用同一入口），**题目与选项原文一律不截断**（长度上限只作用于自然语言识别的准入）；消费方是输入框上方的选择面板（`12-chat-input.js`）与纯函数模块 `17-choice-groups.js`（同口径规范化 + ```naiba-choices 块的静态预览；无效块返回空、原样留作普通代码块）。

9. 插话（运行中的第二输入通道，§四 / §九.100）：**四条 POST 端点同为契约** —— `POST /api/chat/interject`（`{conversation_id, run_id, message, attachments?}` → 202 + 新消息 id）、`POST /api/chat/interject/guide`、`POST /api/chat/interject/edit`、`POST /api/chat/interject/delete`（三者收 `{run_id, message_id, …}`）；**错误口径与旧实现一致**：`LookupError`（行/run 不存在）→ 404、`ValueError`（状态不允许，如引导一条已 consumed 的行）→ 400。消息 metadata 四键登记在 `core/messages.py`：`interjection`（是插话行）/ `interjection_guided`（已引导、待消费）/ `interjection_consumed`（已被 agent 消费，此后才进模型上下文）/ `interjection_stopped`（终态冻结，**用 `COALESCE(...,0)=0` 判等，不能裸比 NULL**）；**`interjection_guided=1 && interjection_stopped=1` 的行仍然可删**（§九.100 ④）。事件 type 两枚登记在 `EventType`：`user_guidance`（负载 `{message_id, message}`）与 `interjection_consumed`（同键），`EventPayload` 同步登记——**改这两处必须同时跑 `tests/test_contracts.py` 的 golden 反查**。

10. **供应商预设（「只填 API Key」的接入名单）**——名单字段与两条端点都是契约：
    - `GET /api/provider-presets` → `{presets: [...]}`，条目字段 `id/name/abbr/kind/base_url/request_format/model/key_required/key_url/hint`。**唯一来源是 `naiba/llm/provider_presets.py`**，前端禁止复制第二份名单（设置弹层的模板网格与首启向导共用同一份）。
    - `POST /api/provider-presets/open` **只接受 `preset_id`**，地址由后端从预设表取（`provider_preset_key_url`）——**不接受任意 URL 传入**，否则就成了「打开任意网址」通道；没有注册页的预设（custom / 本地后端）返回 400。
    - `POST /api/providers`（与 `/api/model-profiles`）的请求体可带 `preset_id`：**`apply_preset_values()` 先把预设展开成连接字段，用户显式提交的非空值优先**；未知 `preset_id` → 400（不静默建成一张怪卡）。
    - **`preset_id` 会随卡片落库**（`config.upsert_model_provider`），但**只作前端反显与角标用途、不参与请求构造**；改成手填配置时清除。数据形状是配置项，改它要考虑存量卡片（没记来源就不高亮，等价「自定义配置」）。

## 六、验证资产与流程（按改动级别执行：默认只跑相关模块，重大重构才全量）

**解释器纪律（本机唯一正确用法）**：本仓库一切 python 命令——单元测试、静态检查器、浏览器冒烟、冻结版编译、源码起服务——一律用 **`项目根\.venv\Scripts\python.exe`**（Python 3.12，`pywebview`/`pystray`/`pillow`/`mcp`/`PyMuPDF`/`python-multipart`/`PyInstaller` 齐全）。**禁止用系统 `python`**：本机系统解释器为 Python 3.14 且未装 `PIL` 等依赖，直接跑全量测试会以 `ModuleNotFoundError: No module named 'PIL'` 报 **52 个 error**（环境问题，不是代码问题）。本节以下命令里写作 `python` 的，一律读作该 venv 解释器。

**目录分工（`tests/` vs `verify/`）**：两者都是验证资产，但**判据不同，别混放**——

| | `tests/` | `verify/` |
|---|---|---|
| 形态 | **断言式守门**（0/1，失败即红） | **诊断 / 冒烟 / 数据工具**（打印候选供人工判断，或需要真实环境） |
| 依赖 | 无外部依赖（纯函数 / 源码扫描 / 内存桩；`127.0.0.1` 只当字符串夹具） | 起源码 server、Playwright + Edge、真实 ComfyUI、真实数据库 |
| 跑法 | `python -m unittest discover -s tests` 一把跑完（1553 例 ≈ 68s） | 按本节分级按需单跑 |
| 语言 | Python | Python + Node（`.cjs` / `.mjs`） |

**新资产该放哪**：能在 unittest 里断言、且不碰外部环境 → `tests/`；需要起服务 / 浏览器 / 真实数据，或输出是"候选清单"要人看 → `verify/`。三处**已知灰区保持现状**（不要"顺手搬"）：① `verify/record_golden.py` 是 `tests/golden/` 基线的录制器（生成物在 tests/，生成器在 verify/，`tests/golden_replay.py` 引用它）；② `verify/media_markup_check.mjs` / `skill_prompt_check.mjs` 本质是断言，但语言是 JS（unittest 跑不了）且要真执行前端源码；③ `verify/p3/p4/p5/p6_*.py` 是 Python 但要起 server，属集成校验。

**验证分级（默认规则；不要"顺手全量"）**：

| 级别 | 判据 | 必做 | 不必做 |
|---|---|---|---|
| **A 轻** | 纯样式 / 文案 / 颜色 / 顺序 / 图标（CSS 值、说明文字、列表顺序、卡片外观） | 改动文件 `node --check`（JS）+ **一个只断言本次改动的浏览器检查**（可临时写 `verify/_check_*.cjs`，几十秒） | 全量 unittest、静态检查器、其它冒烟 |
| **B 中** | 前端接线 / DOM 结构 / 事件绑定 / 增删元素 / 前端调用新接口 | **相关守门测试单文件**（`python -m unittest tests.test_xxx`）+ **相关冒烟 1 套** + 编译 | 全量 unittest、其它冒烟 |
| **C 重** | 后端逻辑 / 契约（事件·metadata·run_context）/ 存储与 DB 迁移 / 协议 / 权限 / 启动装配 / 跨模块重构 | 全量 `python -m unittest discover -s tests` + 静态检查器 + 相关冒烟 + 编译 | — |

- **取最高级别**：一次改动里既有 CSS 又有后端字段/契约，就按 C 级；拿不准时按高一档。
- **只有重大重构才跑全量**：模块搬移、契约变更、迁移版本、依赖图调整、工具注册表结构变化等。普通功能、bug 修复、样式调整一律按 A/B 级**只跑相关部分**。
- **纯文档改动**（本文件 / README / release notes）：只查 UTF-8 无 BOM + 内容自洽，**不跑测试、不编译**（文档不进 exe）。
- **每轮只编译一次**（有代码改动时）；浏览器冒烟只跑相关的那一套，不要把全部套件串起来跑。
- **汇报要如实**：写清实际跑了哪一级、哪几个脚本；没跑的不要写成"已验证"。
- **CI 流水线（`.github/workflows/release.yml`，push master 即跑）**：全量 unittest → PyInstaller 编译 → 发 Release。本地全绿 ≠ 流水线全绿，这三条环境差异必须自己先对齐（否则只能等红）：① **依赖清单要覆盖 `naiba` 包的全部第三方导入**（当前 5 个：`PIL` / `av` / `mcp` / `pymupdf` / `python_multipart`）——`python_multipart` 是惰性导入，漏装**测试不会红、冻结版上传会坏**；② **库代码禁止裸 `print` 中文**（cp1252 控制台抛 `UnicodeEncodeError`；包导入期已统一 UTF-8，Job 线程一律走 `logging`）；③ **测试里比较路径必须两侧 `resolve()`**（runner 的 `TEMP` 是 8.3 短路径，`is_relative_to` 是纯词法判断）。详见 §9.53。
  - ③ 的**本地自检（推之前跑一次）**：`项目根\.venv\Scripts\python.exe verify\ci_short_path_check.py` —— 在临时目录建一个名字带 `~1` 的 junction 并把 `TEMP`/`TMP` 指过去，再跑与 CI 逐字相同的 `unittest discover -s tests`，跑完自动拆掉（可传测试模块只跑子集）。2.5.0-beta 首次发布就是栽在这一点上：新测试拿未 resolve 的临时路径去比产品 `resolve()` 后的存储路径，7 条断言在 runner 上红、本地却全绿（本地 `C:\Users\<name>\...` 没有短名）。（**已归档**，见上方归档说明）

**以下为验证资产清单（按上表级别取用，不要全跑）**：

**归档说明（2026-09-16 清理）**：`verify/` 做过一次清理——一批已完成使命的一次性脚本与产物（临时探针、截图 / 日志、旧冒烟）已归档到 `verify/_archive/verify_scripts_20260916-144726.zip`（清单 `deleted_manifest_20260916-144726.txt`，保留 71 个）。**下面清单里凡出现这些名字都按「已归档、不在 `verify/` 下」读**：`ci_short_path_check.py`、`css_content_bg.py`、`frozen_video_check.py`、`runtime_guard_smoke.py`、`choice_panel_smoke.py`/`.cjs`、`frontend_runtime_settings_check.cjs`、`run_frontend_check.sh`、`run_video_e2e.sh`、`_serve_video_e2e.py`、`video_e2e_smoke.cjs`、`migration_smoke.py`、`stop_cancel_smoke.py`、`_check_dark_soft_contrast.cjs`、`_check_bg_*.py`/`.cjs`、`_check_topbar_compact.cjs`、`_check_dark_inline_code_contrast.cjs`、`_link_merge_smoke.*`、`_probe_portrait.*`、`choice_custom_reply_check.mjs`、`choice_preview_check.mjs`。要复现这些历史验证：解压归档 zip，或从 git 历史取回（`git show <归档前的提交>:verify/<名字> > verify/<名字>`，归档提交见 `git log -- verify/<名字>`）。**例外：`_serve_tmp.py` 仍在 `verify/`**——它是隔离实例的公共 harness，被 `tasks_panel_smoke.py` 直接依赖，归档时会连工具链一起打断，故保留。

- **单元/契约/golden**：`项目根\.venv\Scripts\python.exe -m unittest discover -s tests`（1573 用例，含 **显示名品牌守门 `test_display_branding`**（28 例：显示名与内部标识各归各位——浏览器标题 / 欢迎标题 / 迁移与手机访问提示的目标准确、旧称呼不得残留在当前前端入口，托盘内部 name 与可见 title 用 AST 分开断言，`naiba.chat` AppUserModelID 与持久 profile 参数保留，`naibaChat*` 与 `lan*` 存储键保留且不得新建 cat 前缀键，目录选择器与 CLI 文案已换而 `server_version`、更新器三常量仍是原值，三份当前文档自称新名并写明「原名 + 不用搬数据」，见 §2.1）、**发版核验品牌守门 `test_release_watch`**（16 例：`release_watch.check_manifest` 的头条判据是「前缀 ∈ {`Cat Chat`, `Naiba Chat`}」——当前显示名与历史名都放行，非法前缀（含品牌出现在句中）、空说明、`release_notes` 缺失 / 空列表 / 类型不对一律判失败且**不抛异常**；另含 `_release_check.brand_hit` 的正负例，见 §九.113）、**供应商预设守门 `test_provider_presets`**（29 例：14 条预设表的顺序与字段齐全（含 `abbr`）、kind/`request_format` 必须走 config 词表且格式能在 `index.html` 下拉里选到、在线预设必须有 https 注册页且 hint 是三段式 ①②③、TE/摆烂两条实测参数、payload 是只读副本、**`provider_preset_key_url` 白名单**（custom/本地/未知 id/任意 URL 一律返回空——这是「打开注册页」不得变成「打开任意网址」的判据）、`apply_preset_values` 语义（只填空字段 / 用户显式值优先 / 未知 preset_id 报错）、preset→落库→改成手填后清除来源的往返、HTTP 路由与前端网格·向导接线、两份 JSON 合法；见 §九.112 同批）、**子代理双模式守门 `test_subagent_fork`**（27 例：runner 层 `fork=false ⇒ history == []` / 默认与缺省非空 / 字符串 `"false"` 真的走 spawn / 人设两模式都在 / 空历史父会话下两模式等价；handler 层**模型传的 `fork` 参数一律不作数**（以注册时固化的常量为准）+ 工具过滤·深度·数量闸门不受影响 + Job kind 仍是 `subagent`；schema 层两个工具**参数逐字同形且都没有 `fork`**、各自描述 ≤100 字 / ≤3 句且分工分明；常驻区文案用 AST 取字符串常量断言 + **真路径**断言「开哪个工具就只说哪条规则」、没开时一句不出现，见 §九.116）、**工具互斥归一见门 `test_tool_mutex`**（24 例：`normalize_tool_mutex` 只留组内排前者 / 单成员恒等 / 其余顺序逐字不变 / 重复项不吃掉 keeper；**三个入口全覆盖**（`resolve_allowed_tools` 固化集与 Agent scope 两条路径、`bake_session_tool_ids` 落库即归一且已固化会话只归一返回值不改写库、`enable_conversation_tools` 只加不删也归一）；`JOB_TOOLS` / `JOB_CREATOR_TOOL_DEPS` 覆盖新工具；**full 预设 = 全部 − 1**（显式 exclude）而常规四档不带子代理工具；`_clean_tool_set_tools` 后端兜底；**前端镜像**：`AGENT_TOOL_MUTEX_GROUPS` 与后端逐字一致 + 归一调用点接线（载入/保存/模板三条）+ 批量勾选关掉逐项互斥 + 单点勾选听用户的，见 §九.116）、**MCP 迁移守门 `test_config_migration`**（旧捆绑桥接按 `comfyui_mcp_server.py` 脚本签名剥离、用户自建同名 comfyui 服务保留、去重不变，见 §九.106）、**思考回放限长守门 `test_reasoning_replay`**（21 例：单条 4000 / 整轮 16000 双闸门、`_ReasoningReplayBudget` 由新到旧分配、只截回放不动落库、`_clip_reasoning_text` 是唯一实现、运行设置两项往返、三个 `build_model_history` 调用点透传、设置面板 UI，见 §九.103）、**思考强度预设守门 `test_thinking_presets`**（31 例：命中度量「卡片 > 模型名 > request_format 默认」、未声明档位发前失败、K3 方言映射、K3+codex_responses 不得命中、降档链按词表声明顺序下探，见 §九.104）、**思考三段治理守门 `test_stream_guards`**（18 例：推理预算熔断只在正文为空时降档、LM Studio 有推理零正文抛 `EmptyModelStreamError`、墙钟总时长超时进重试、0 值确实关闭闸门、**空流报错必须自报来源（LM Studio/Ollama/本地模型/在线模型）且源码不得写死「在线模型」**，见 §九.103）、**重复答复提示守门 `test_repeat_answer`**（9 例：只提示不拦截、不落 metadata、<20 字符不误伤，见 §九.105）、**会话字体守门 `test_chat_font`**（17 例源码级护栏：`.message-body` 必须消费 `--msg-font-*` 且不得再出现写死的 `font-size:15px`、`:root` 默认值必须与历史值逐字相同、变量不许挂到 `body`/`--font-sans`、只传 theme/skin 的调用点不得重置字体、滑杆 `input`/下拉 `change` 路径不得出现 `saveAppearance`、**点选清单必须覆盖后端全部字体键**、**`__pick__` 占位值不得出现在任何表单取值里**、安装探测必须是 canvas 度量且带缓存（判定"没调用 `queryLocalFonts`"要先剔除整行注释，见 §九.96），见 §九.95/96）、**外观设置守门 `test_appearance`**（15 例：默认值含字体三键 / 更新路径越界与非数值显式报错 / 加载路径静默 clamp / custom 串往返与截断 / 11 个预置键逐个往返 / 未知键回 system / 报错文案必须列全白名单 / 白名单没被改松）、**父任务取消闸门 `test_job_parent_gate`**（父被取消后不得派生新 Job、`resume`/`retry` 抛 `ValueError` 供 HTTP 层转 400、子 Agent 退出兜底取消它派生的 Job、启动扫尾对已取消父打接续标记、终态父仍可恢复，6 例）、**任务面板守门 `test_job_cancel_state`**（`cancel` 必须落 `cancel_requested`、worker 的状态更新覆盖不了 `stopping`、停止超时兜底强制终态且只对登记过的作业生效、**已终态父 Run 不得被改写成已取消**、store 三处自写状态列表必须消失）、**`test_tasks_aggregate`**（`exclude_kinds` 只丢回答记录、窗口能到 200、`/api/tasks?jobs_only=1` 路由契约、前端承诺：状态词表含 `stopping`、面板只渲染作业、组标题由「类型 + 时间」拼成且不引用用户消息原文、**派生链按可见父链锚点归组**、子行嵌套标记只在父行可见时出现）、**提示注入口径守门 `test_prompt_gating`**（视觉段按 `vision_analyze`/`vision_image_ops` 各自到齐注入、只开 ops 也给单独一句、**文案按模型视觉能力分分析/装载两种且与 schema 同口径**、ComfyUI 脚本编排句要求「有 comfy 工具（含 `mcp__comfy-mcp__*`）且 write_file/pwsh/run_in_background 至少一个可用」）、工具形态守门 `test_tool_registry_shape`、**长会话三件套守门 `test_history_tools`**（窗口函数按会话分区取每会话最新命中、上下文边界标注「在上下文中 / 已划出上下文」、截断显式、id 缺失报错不退回全库、provider 不得直接开数据库连接）、**消息懒加载守门 `test_lazy_messages`**（窗口以「轮」为边界、切换会话重置而同一会话刷新保留窗口与滚动、预渲染把高度加回 scrollTop、刻度轨数据驱动 + 点击未渲染轮次先渲染再跳）、**「重新生成 / 编辑」守门 `test_regenerate_reply`**（AI 操作区顺序 复制→重新生成→**删除**→新会话、用户操作区 编辑→分支→**删除**、两入口共用 `resendFromUserMessage`、原文取 `display_content` 而非 `content`、运行中守卫、纯附件轮次可重发、重发后输入管线必须刷新、截断点=那条提问且反复点幂等、附件随重发带出、`/api/messages/edit` 与 `_edit_message` 不得被当死代码删除、**编辑态下底部整体让位**（输入框锁住 + 提示指向编辑框、添加文件禁用、发送键变「重新发送」且可用性跟编辑框走、草稿附件列表收起、底部按钮与回车确认编辑、重渲染退出编辑态、`setBusy` 不得再直写输入框状态、**附件恢复的字段口径必须与「分支」一致（含 `thumb_path`，缺了会让缩略图退化成推导 `<path>_thumb.webp` 而 404）**、**编辑框必须带出原附件**（`class="edit-attachments"` 复用 `uploadedFileMarkup`、来源是同条消息的 metadata 而不是 DOM 反推））、**模型重置守门 `test_context_reset_tool`**（19 例：交接文档校验矩阵、后台任务快照排除本轮自身、成功后本轮收尾并落标记、种子模板契约；**含「工具经 per-call run_context 执行时，写入必须回落共享 Run 上下文」——真 Agent 循环的 A 级用例：单元级直接传同一个 dict 永远绕开 `_call_context`，抓不到 §九.111 那个 BUG**）、**顶栏样式守门 `test_topbar`**（溢出菜单已删且刷新回到操作区/**顶栏卸载模型按钮已移除且无残留绑定**/任务计数无胶囊样式/Skill 按钮不重复前缀/MCP 文字恒为 MCP 且状态只走 CSS 类/**文件按钮不得再用侧栏深色底**/**操作区不被压缩且按钮文字不换行、窄屏不得隐藏标签**/**无手动模型输入框 `#composerModelCustom`（及其绑定/死引用）、`#composerModelSelect` 与 `#turnJumpSelect` 在顶栏操作区**/**操作区按钮 SVG 必须有描边样式**/**index.html 不得有重复 id**）、**待发送附件列表守门 `test_pending_files`**（竖直行渲染/图片预览带灯箱入口/非图片占位图标/上传中进度/空列表隐藏/**网格列必须 `minmax(0, 1fr)`**（否则最长文件名撑宽整列）/文件名 ellipsis+nowrap+min-width:0/**行填充与描边必须与背景有区分度**/移除绑定仍在）、**暗色主题软色覆盖守门 `test_dark_soft_contrast`**（皮肤块里定义的每个 `--*soft*` 变量暗色块都必须有覆盖、覆盖值必须是 `color-mix` 重新混色而非照抄浅色值、`--violet-deep*` 这类「暗底上的文字色」也要重定义、提示条与「当前」API 卡片必须用变量取色）、**API 供应商卡片化守门 `test_provider_cards`**（卡片容器渲染供应商卡 + 末尾固定「添加 API」卡/卡片整张可点与右上角 ×/**卡片压两行（名称 + 脚行）、源码不含 `.provider-card-model`、min-height: 88px**/**表单整体搬进 `#providerDialog` 且字段 id 与标签逐字不变**/**旧下拉与添加/删除/编辑按钮连绑定一起消失（无死引用）**/**错误提示与按钮行固定在弹层底部、字段区独立滚动**/**一行最多三列且窄屏降级 2 列/1 列**/**× 删除必须二次确认**/`renderProviders` 不再顺手打开表单/重复 id 兜底）、**会话模型下拉守门 `test_composer_model_follow`**（会话模型必须来自设置页检测目录、供应商默认模型不再作为普通对话回退；`composerModelChoice()` 是原始选择而非解析后的模型名；**目录缓存判据是「目录非空」而非「查过」**（空数组不算已缓存）；**下拉末尾必须有「↻ 重新检测模型」且该伪选项不得落库 / 不得发给模型**（`composerModelChoice()` 对它返回空串、`saveComposerModelSelection` 命中即早退、保留 `force` 强制重拉通道）；**新会话不得静默选中目录第一项**（必须显示占位项））、**Agent 卡片化与内置 Agent 下线守门 `test_agent_cards`**（内置清单为空且机制保留/不再注入内置 Agent/遗留 `built_in` 标记摘除且名称·提示词·工具集不丢/旧内置 id 可删/`upsert_agent` 不再打标记/卡片容器渲染 + 末尾「新增 Agent」卡 + 整张可点与右上角 ×/**`built_in` 条目不得给 ×**/**表单整体搬进 `#agentDialog` 且字段 id 逐字不变**/**错误提示与按钮行固定在弹层底部**/**一行最多三列且窄屏降级 2 列/1 列**/**删除正在编辑的 Agent 会关掉弹层**/**固定 Skill 与工具集同款卡片且共用 `--agent-list-h` 等高**（`.skill-card` 卡片 + 两列网格 + 不再复用 `.skill-item`；`#agentToolScope` 必须用同一变量且 `grid-auto-rows: max-content`，否则分组行被均摊压扁）/**工具分类守门**（7 组顺序与设计一致、每组都有说明与风险徽标/配色、12 个旧分类名不得复活、每个工具**只落进一个分类**且成员名单逐项钉死、MCP 动态工具按服务器进 `subgroups` 且不进 `direct_tools`、预设引用的工具名/组名必须存在、**5 档且依赖闭包已显式列出**（只读 6 / 标准 13 / 长会话 17 / ComfyUI 18 / 全能 = 全部 − 1（互斥排除 `subagent_spawn`），标准含 PDF 三件套与视频两件套、只读只收 `side_effect=False` 的两件）、`_DEFAULT_SELECTED_TOOLS` = 标准模式、前端 `AGENT_TOOL_DEP_RULES` 与后端 `JOB_CREATOR_TOOL_DEPS` 逐项一致、未知引用告警、分组头 `head.append(caret, allCb, title, desc, count)` + 徽标随标题 + 五列网格 + `.group-desc` 不占第二行、前端徽标/二级分组/搜索框接线）/`renderAgentManager` 不再顺手打开表单/重复 id 兜底/**展开后的工具卡片紧凑且与父分组区分（白卡 + 强描边 + 投影；只有展开的那一组分组头变浅灰条子、折叠的保持原样、展开区白底；说明两行截断且完整描述进 title）**）、**Agent 自动 ID 与头像守门 `test_agent_avatar`**（id 留空 → `agent_<12 hex>` 且唯一·跨重启持久/显式 id 仍兼容/非法 id 拒绝/表单不带头像时 `avatar` 保留/**头像中心裁切为 256×256 WebP**/内容哈希命名且换图删旧文件/非图片·超限·空文件拒绝/文件名穿越拒绝/接口成功与 404·400 语义）、**文本框右键菜单守门 `test_context_menu`**（菜单必须挂进 `topLayerContainer()`/`topLayerContainer` 只认 `:modal` 并排除 toast/选区菜单限制在同一弹层内/编辑菜单七项齐全/number 输入框复制退化为整值/菜单 CSS 仍是 fixed 浮层）、**对话刻度轨守门 `test_turn_rail`**（`#turnRail` 绝对定位到消息区那一格/`--turn-rail-w` 统一宽度/消息列右内边距 = 左内边距 + 轨道宽（列左移 rail/2）/窄屏置 0 并隐藏/刻度与高亮尺寸（hover 与 active 都加长加粗、active 最深）/概要 1 行 + 2 行 clamp/`TURN_RAIL_MAX=30` 滑动窗口/高亮按视口中心/行区间不变不重写 DOM/点击跳转居中/`bindEvents` 调用 initTurnRail/重复 id 兜底/**手机端轮次下拉承接刻度轨能力（`#turnJumpSelect` 在顶栏操作区首位且带 `.mobile-only`、不足 2 轮 hidden、选项只在轮数变化时重建、跳转经 `scrollToTurn`）**）、**手机功能对等守门 `test_mobile_parity`**（760 块内无「按视口删能力」规则（文件面板不再 `display:none !important`、`.file-change-chip` 不再 `pointer-events:none`）、文件面板手机端是全屏抽屉形态、触摸目标 ≥44px、视口边界单一来源、顶栏标签不隐藏、跨 760px 不关文件面板）、**轮次下拉守门 `test_turn_jump`**（选择器位置/`.mobile-only`/`hidden`、逻辑留在 `04-messages` 内部不导出、options 只在轮数变化时重建、不足 2 轮隐藏与跳转抑制）、**侧栏收藏/会话级提示词退役守门 `test_sidebar_favorites`**（迁移 v15 加列且幂等/老库补列默认 0/收藏读写回路与"不动其他字段"、**收藏不推进 `updated_at`**、接口只收布尔值且未知会话 404/收藏只能走 `set_conversation_favorite`（通用设置更新不得再碰 favorite）/前端只提交 favorite 且排序口径不含它/**顶栏「新会话」按钮已删且空态有兜底入口**/**当前会话定位用最小滚动（禁止强制置顶）**/**收起按钮与工作区操作同行**/**会话条目整条可点且为手型**/**底部只留一个状态点且 api() 双路径刷新它**/**收起侧栏不得改 `--chat-max`**/后端三处注入点不得再读会话级提示词/前端旧入口不存在/Agent 表单拥有套用与导入/条目为五角星+⋯/收藏分组追加在工作区之后/滚动上限取真实 `scrollHeight`）、**Skill 说明守门 `test_skill_prompts`**（安装/编辑两处必须共用同一份 `SKILL_SCRIPT_RULES`：`scripts/` 子目录 + 显式 UTF-8 + GBK 风险说明）、**Skill 安装布局守门 `test_skill_install_layout`**（单文件 `.md` 导入必须落在独立子目录（托管目录根下不得出现 `SKILL.md`）、带顶层目录的文件夹上传不得多包一层、顶层散放 `SKILL.md` 的 zip 同样收进子目录、删除裸定义只回收该文件而托管目录与其它 Skill 保持不变、正常 Skill 只移动自己的目录）、**开始页预设守门 `test_quick_messages::StarterPresetSeedTests`**（内置预设并入/删除记入 dismissed 且不复活/改名不重复/新版本预设自动补齐/恢复清空清单/编辑保留 desc+icon/旧一次性标记迁移）、**Job 产物写回守门 `test_job_media`**（kind 声明门禁/终态与部分产物/归属链与工具调用匹配/匹配不到退化为消息级附件/幂等/消息已删安全放弃/`_finish` 接线）、**Range 解析守门 `test_http_range`**（单区间/后缀/越界夹取/416 语义/多区间忽略）、**媒体类型与采集声明守门 `test_media_declarations`**（唯一定义派生一致性/每个内置工具必须声明 media 策略/MCP 未声明走默认/前端不再硬编码扩展名名单/.bmp 等非媒体仍显示为文件 chip）、**媒体采集守门 `test_media_collect`**（只对成功结果提取/`NEED_CONFIRM` 不提取/声明门禁（never·none·intent_gated）/structured 只认 JSON/文本路径去散文后缀/**先截断再落盘**（25 张只落 20 份且 truncated 可自述）/内容级去重/data_dir 每次实时解析/消息级汇总去重与分桶/`display_tool_run` 带 media 而 `model_visible_run` 不带/Agent 生产点接线与采集异常不丢弃工具结果）、**本地模型四道闸守门 `test_local_model_guard`**（窗口探测三后端 + 失败返回 0 且不缓存失败；本地兜底 ≠ 在线 256k；闸门计数必须覆盖 reasoning_content 与 tool_calls/role=tool 与原生工具 schema；本地多模态大脑每请求图片总量上限（张数 + 字节，在线不动）；等本地锁可取消 + 周期性回报排队时长；profile 接线——探测写入且显式配置不被覆盖）、**运行状态口径守门 `test_run_state_guard`**（状态词表唯一真相：`ACTIVE_TASK_STATUSES` 含 `stopping`，`manager` 与 `jobs` 三份词表必须互相包含；**启动清理必须覆盖 `stopping`**——卡住的子任务不得活过重启；`list_primary` 只回顶层对话 Run 而 `list` 仍对任务面板可见；**流式读取可取消**：阻塞在等字节时 cancel 必须即刻把它拽出来、非取消异常原样上抛、三条流式路径都接了 `_iter_stream_lines`；前端：停止失败也复位界面 + 重连不认子 Job + `/api/runs` 走 `list_primary`）、**四道「把卡住变成可诊断/可收场」守门 `test_runtime_guards`**（①**首字节超时**：阻塞在等字节的流必须在超时内被 `close()` 拽出来并抛 `LocalModelFirstByteTimeout`（含可行动文案）、**只发 SSE 保活注释（`: keepalive`）不算「有输出」——否则闸门被静默解除**、正常流不受影响、取消优先于超时、只有 `is_local` 才给非零值且留 `options` 覆盖口、三条流式路径都收到该参数；②**锁占用者具名**：`_busy_notice` 说出 holder、无 holder 时回落文案、`_clear` 只清同名、**第一条等待状态就必须点名（不必等满一个周期）**、视觉/子代理/计划执行/计划整理/连接测试五处都声明 `lock_label`、占用期间记名且结束即清；③**步数上限**：`_resolve_step_limit` 优先级与脏值回落、循环到上限即停且发 `run_failed`、预算将尽先注入收尾提醒、默认值与 `config.AGENT_MAX_STEPS_DEFAULT` 一致；④**探测回写**：写进 `context_window_probed` 且对所有读取点可见、绝不覆盖显式配置、值未变不落盘、未知 provider/非法值忽略、`_profile_with_model_override` 端到端回写且 `local_probe` 来源仍重探、显式配置不触发探测、老键 `max_agent_steps` 继续被丢弃）、core 对拍 `test_core_tools_parity`、视觉占位守门 `test_vision_prepare_history`、**视觉分批守门 `test_vision_batch`**（每次调用独立成批 ≤4 张注入/loaded-shown-total_batches 元数据/文本大脑不注入/双形态默认 4）、描述瘦身守门 `test_tool_description_slim`、结果可见性守门 `test_tool_results_visibility`、工具设计守门 `test_tool_design_fixes`、反幻觉守门 `test_anti_hallucination_guard`、推理流守门 `test_reasoning_coalesce`（流式短窗/终态单 run 合流/snapshot 收缩）、存量压缩迁移守门 `test_storage_migration`（版本断言取 `CURRENT_SCHEMA_VERSION`，不再硬编码）、历史数据管理守门 `test_storage_maintenance`、事件契约守门 `test_events_contract`（AST 静态扫描全部发射点：type∈EventType、键⊆EVENT_PAYLOAD_KEYS 契约图；`# noqa: event-internal` 为 sink 内部形态显式豁免）、活动时间线守门 `test_activity_timeline`（物理序/最终答复后置/request_index/条目 ts/无时间戳省略键/**只留最后一段正文——工具调用前的过程播报一律丢弃**/`_rebuild_partial_run` 正文同口径/**前端 `activityMarkup` 同口径兜底（历史消息是落库数据）**，见 §九.97）、用量汇总守门 `test_usage_summary`（requests_detail 生成/旧数据兼容/汇总口径不变）、首轮上下文守门 `test_first_turn`（run 快照合并写回/最早 run 查询/无 run 返回 None/trace 图片摘要化/**会话级列读写/分支继承（分支点为首条消息不继承）/老会话回退 run 快照/清空已结束任务后不丢/`_first_turn_info` 取数顺序与旧 prompt 归一/v16 幂等迁移**）、消息排序契约守门（`test_contracts`：后端唯一决定消息顺序 + golden 反查豁免 metadata.activity 容器）、**上传系统守门 `test_upload_system`**（分日落盘/内容去重/图片压缩+缩略图/删除树内校验/引用保护/**B1 引用保护清理与手动无感知两口径**/清理组键分日）/ **二进制防护守门 `test_read_file_binary_guard`**（PDF/图片/通用二进制引导 + GBK/UTF-8 不误伤）/ **PDF 工具守门 `test_pdf_tools`**（文本提取页标记/范围/越界/扫描版引导/渲染幂等/预算/zoom 区域语法与越界/策略绑定） / **视频抽帧守门 `test_video`**（probe 元信息对硬编码基准、`_DEFAULT_SELECTED_TOOLS`/预设不被污染、times/interval 帧数与时间点、keyframes 恰好落在夹具切点、阈值语义、缓存幂等与参数分目录、联系表长边上限、两级缓存上限（单目录 200 / 同视频跨参数合计 600）、**缺 `av` 时给业务文案而非 `ImportError`**、损坏/非视频文件明确报错、**经真实 MediaCollector 的接线级断言**（帧图与联系表落进消息媒体区、缩略图齐备、画面完全相同的重复帧被宿主按内容去重合并）、策略绑定与分组/目录顺序）/ **纯附件轮次守门 `test_attachment_only_send`**（compose_user_content 逐字节口径/空文字与附件边界/附件无 path 拒绝/首轮标题回退）/ **@ 引用守门 `test_file_references`**（解析口径：文件/目录/引号含空格/尾标点/邮箱/越界/不存在；浏览边界：目录优先/隐藏项/越界拒绝/截断；提交链路：模型可见已解析 + 展示与标题保留原文）/ **快捷消息守门 `test_quick_messages`**（统计字段与旧数据兼容/编辑保留统计/index 稠密/权重公式与新鲜度阶梯/排序与并列口径/条目无 title 且旧 title 被丢弃/**两份列表互不干扰**）、**在线模型 5xx 重试与落盘守门 `test_llm_runtime_retry`**（HTTP 500 重试到次数上限且状态提示带状态码/500 后成功即返回内容/普通 400 不重试、有推理无正文的空流按空流重试到上限且重试成功后正文正常返回、**空流重试必须沿 high→medium→low 降档重建负载（codex+DeepSeek 映射 max→high→low、openai_chat+GPT high→medium、Kimi K3 max→high；low/auto 与 openai_chat+DeepSeek 画像原样重试；穷尽后报错带诊断文案，§九.102）**/5xx 落盘请求体且 base64 与 API Key 均不落盘/思考回传类 400 仍落盘、**Kimi K3 方言守门 `test_llm_protocols::test_reasoning_params_kimi_k3`（off→low/low→low/medium→high/high→max、auto 不发、判定只命中 K3 不命中 K2.x、codex_responses 不做 K3 特判）**、**上下文圆环与提醒守门 `test_context_ring`**（usage 事件必须立即刷新圆环/`state.contextUsage` 只有一个写入点/历史与终态路径复用同一入口/圆环百分比只由 `renderContextUsage` 写/**阈值取自运行设置、圆环渲染后必须做阈值判定、`armed` 保证每会话只提醒一次、弹窗与阈值输入框 id 唯一且弹窗有关闭入口、设置页回填与保存阈值**/**后端 `context_warning_percent` 默认 80、持久化跨重启、0 可关闭、越界与非数字被拒**）、**消息全文搜索守门 `test_message_search_api`**（判定层与片段口径：空 `q`/纯空白 → 400、`limit` 非整数 → 400 而越界**夹紧**到 [1,50]、`conversation_id` 不存在 → 404 而不带该参数是全库 200、`hits[].snippet` 必须是**原文连续子串**且含命中词（两端被裁时带省略号）、大小写不敏感、**`truncated` 必须精确等于「`total_hits` > 返回数」**（不静默丢弃的判据）、限定会话只扫该会话而全库跨会话、`_query_first` 既吃 `parse_qs` 的 list 值也吃裸串、`GET /api/search/messages` 路由已注册）、**分支来源与分支链守门 `test_branch_chain`**（迁移 v18 两列**纯增量 + 幂等**且**存量分支明确不回溯**（不得按标题 `(N)` 弱推断）、`branch_conversation` 写入「源 id + 分支点消息 id」且**非破坏性**（源会话消息一条不少）、`branch_count`/`branch_source_title` 在**列表与详情两处同口径**、`branch_chain` 三种角色（`role=source` 时**链里不含自己**）、**源被删后 `source_deleted` 降级不报错**（悬空引用不是错误）、只有会话本身不存在才 `LookupError`、链项是**扁平的**（无 `children`/`depth`——侧栏不画树的决策护栏）、`/branch_chain` 路由**必须排在 `/api/conversations/<id>` 兜底之前**）、**删除单条 / 整轮 + 撤销守门 `test_message_delete`**（`single` 只删该行且默认即 single；`role='session'` 的遗留标记行**拒绝**；跨会话 id 不误删；`turn` 仅 user 可用、**遇下一条 user 即停**并把途中遗留 session 行一并带走；非法 mode / 未知消息的异常类型映射 400/404；快照五字段（`id/role/content/metadata_json/created_at`）**逐字节完整**；**撤销后 `build_model_history` 与删除前逐字节一致**——这是前缀缓存最贵的一条回归；撤销**幂等**（重复点跳过已存在 id，不报错不翻倍）；删除与撤销都推进 `updated_at`；确认框粒度源码护栏——**「仅删这一条」对所有角色可见**（曾写成两个按钮都 `hidden = !isUser`，结果 AI 回复一条都删不掉）、「整轮」只对 user 开放、撤销窗口 10 秒、快照只存内存）、**路径纪律守门 `test_no_absolute_paths`**（扫已入库的代码/配置：不得出现盘符+用户目录或盘符+仓库名，占位 `C:\Users\user\…` 与测试桩假路径放行；§8.8）；共用桩 `tool_testkit.py`）+ `python -m unittest tests.golden_replay`（3 基线：取消防线/工具确认/多轮图片历史）。golden 基线在**行为变更时重录**（`verify/record_golden.py`）并人工核查后再固化；
- **冻结版启动迁移守门 `test_legacy_migration`**（**EXE 目录 ≠ 数据目录**的冻结版形态——`PathContext.local()` 会令两者相等而绕过该分支，必须直接实例化 `PathContext`：EXE 旁有旧 `config.json`/`data/` 时启动不抛异常、配置·数据库·uploads·普通文件完整搬运、运行期易变档（`server.json`/`server.lock`）不跨目录搬运、**目标已有 `data/server.json` 时迁移不得中断**（WinError 183 回归：uploads 排在它之后，中断即静默丢失）、非 OSError 失败只报告不阻断启动（`report["error"]` + `data-migration-warning.log`）、源码模式保持早退语义、`NaibaChatApp` 装配级不崩）；**真实进程级冒烟另跑 `verify/migration_smoke.py`**（17 项：frozen 形态真实子进程 + 真实 HTTP 服务，跨两次启动——首启搬运到位且旧配置真被应用层读到、第二次启动「数据目录已有 `server.json`」不中断且排在它之后的 uploads 新文件与末尾顶层项照样搬、注入 TypeError 后服务照常就绪且诊断落 `data-migration-warning.log`；失败才保留隔离目录供排查）；
- **代理策略守门 `test_net`**（`naiba/net.py` 出站网络层，纯内存桩、不起服务）：URL 归一（只接受 http/https 与裸主机、空白=清除、非法 scheme/空主机拒绝）、本地直连判定（回环 / 私有网段 / 本地后缀，公网不本地）、`configure` 使手动·系统·直连三种 opener 缓存全部失效并按新策略重建、非法代理 URL 回退直连且状态带错误、**手动代理绝不静默退化**（缓存被清后 `_opener_for` 必须按当前 URL 重建代理 opener，不能退回空 `build_opener()` 变成直连）、**重试丢弃的 opener 必须与 `_opener_for` 的选择逐分支对齐**（含「代理已开 + 未填地址 + 关系统回退」的降级直连——旧写法不匹配任何分支，重试会复用同一个失败 opener 空转）、非选中缓存不被扰动、幂等请求（GET/HEAD/OPTIONS）连接异常时按当前策略重建后重试一次、POST 不重试、HTTPError 不重试、本地主机绕过代理；
- **前端语法**：每个 js 文件 `node --check`（Node ≥22 限 `--input-type` 仅 stdin/管道会破坏 UTF-8：先复制为 `.mjs` 再检查，如 `Copy-Item public\js\X.js verify\chk.mjs; node --check verify\chk.mjs`；import/export 语义）；
- **前端渲染（无浏览器兜底）**：`verify/media_markup_check.mjs` —— 把 `03-media.js` 的纯渲染函数装进桩环境**真执行**并断言产出 HTML（媒体块紧跟工具块 / 截断提示文案 / 视频音频标签 / 用户侧音视频播放器与文件名标签 / 助手侧 figcaption / 末尾去重三态 / `mediaKind` 名单）。浏览器冒烟被沙箱拦（Edge spawn）时的替代防线；**桩缺口出现即补（2026-09-17）**：桩里缺 `localFileUrl`（`fileUrl` 依赖它），整支检查会在第一条断言前就抛 `ReferenceError` 静默失效——这类「看着在跑、其实早就崩了」的防线见到就修；**活动时间线只渲染最后一段正文（§九.97）**——三句过程播报只渲染最后一段、渲染的必须是最终答复且不残留任何播报、工具条目一个不少、单段正文照常、只有工具时无正文块、思考条目不被牵连；接线关键点另有 `tests/test_media_declarations.py::FrontendInlineMediaTests` 源码级守门；`verify/skill_prompt_check.mjs` —— 抽出两个按钮的说明常量并断言共用规范块（并打印规范全文供人工核对）；
- **冻结版实跑（打包后必跑；源码模式亦可）**：`verify/frozen_video_check.py` —— `dist
aiba-chat.exe --run-skill-script verifyrozen_video_check.py`（`launcher.py` 的隐藏入口，不起 GUI/HTTP/实例锁），在**打包好的解释器**里真解码夹具视频（`import av` + probe + times 抽帧 + 联系表 + keyframes 切点）。存在理由：`av` 漏装或 FFmpeg DLL 没收进产物时**测试不会红、只有冻结版会炸**（§九.53 / §九.67）；PyAV 的 DLL 由官方 hook（`hook-av.py` 收 `site-packages/av.libs`）负责，`.spec` 只加 `hiddenimports=['av']`，不要重复收集二进制。。**同一入口另有一条轻量自检 `verify/frozen_interrupt_check.py`**（几秒完成，断言运行期可达的东西：`ConversationRunMixin.recover_interrupted_runs`、`INTERRUPTED_TASK_REASON`、`_persist_failed_message(placeholder=…)`、`ChatStorage.interrupted_tasks`、`updater.APPLY_UPDATE_SCRIPT` 的两条 `Start-Process` 均不带 `-WindowStyle Hidden`、`Launcher._on_window_loaded`）——**冻结版把源码打进 PYZ、`_MEIPASS` 下没有可读 `.py`，所以这类检查只能断言运行期常量/签名，不能读源码文本**（源码级断言留在 unittest 里）（**`frozen_video_check.py` 已归档**，见上方归档说明；现行可用的是本条末尾那支 `verify/frozen_interrupt_check.py`）
- **视频抽帧真实反推（真模型端到端，2026-09-14 已验过）**：`bash verify/run_video_e2e.sh` —— 同一个 shell 生命周期里串「隔离源码实例（`verify/_serve_video_e2e.py`：从仓库 `config.json` 取真实供应商，默认 mimo / `mimo-v2.5`，多模态 + 工具调用）+ 无头 Edge + `verify/video_e2e_smoke.cjs`」，在**真实前端**里上传 mp4 → 选模型 → 发送 → 等模型**自行** `probe_video → extract_frames（keyframes 定位切点 + 联系表）→ vision_analyze` → 截图落 `verify/video_e2e_shots/`、结论落 `verify/video_e2e_result.json`。
  **两条纪律**：① 完成判据走**后端真值**（会话详情里出现非 `partial` 的助手消息），不靠 DOM 类名——类名是易变细节，用它判定"跑完没有"等于把脚本变成第二个待维护的前端；前端只用来验"渲染成什么样"，用截图与 `<img>`/`<video>` 事实说话。② 隔离实例的 `NAIBA_TMP_ROOT` **必须是绝对路径**，相对路径会被 `PathContext` 再拼一次，`data_dir` 变成 `.../_tmp_video_e2e/_tmp_video_e2e/data`。
  验收点（19 项断言）：工具调用序列含 probe/extract/vision、产物落在 `uploads/video_frames/`、**每帧都带 `thumb_path`**（缺了前端会推导 `<stem>_thumb.webp` 而 404）、消息区把上传的 mp4 渲染成 `<video>`、抽帧图**全部可加载且无破图**、最终答复确实在描述画面内容。（**已归档**，见上方归档说明）
- **传输侧校验（无浏览器）**：`verify/p4_transport_check.py` —— 起源码 server 后经 HTTP 校验 GIF 首帧缩略图（主图字节不变 + thumb 为 WebP + 200）、`/api/file` 的 200/206/416 与**字节级正确性**、`.m4v` 非 octet-stream；`verify/p6_comfy_extract_check.py` —— 用**真实 ComfyUI 产物 URL** 跑通采集链路（下载 → 托管缓存 → WebP 缩略图）并播种会话，配 `verify/p6_comfy_render_smoke.cjs` 在浏览器断言该图真实加载；
- **存量媒体补提取**：`verify/backfill_media.py`（默认 dry-run 只读报告；`--apply` 才写库）——按**修复后的口径**重新扫描历史助手消息的 `tool_runs`/`activity` 结果，只做加法（补 `media` + 重算 `attachments`），且只挂"落地可服务"的记录（本地文件存在 / ComfyUI URL 已成功缓存），不给老会话塞破图；
- **源码冒烟（五点）**：启动 `python server.py` 后 `GET / /api/health /api/bootstrap /api/tool_catalog` + `POST /api/skills/scan` 全 200；**停止命令返回 ≠ 端口已释放，复测前先确认旧进程退出**；纯附件轮次链路另可用 `verify/attachment_only_smoke.py`（五点半烟 + 真上传 + 空文字带附件被接受 / 空文字无附件 400 / 标题回退，**用 `--port` 起独立端口以免与冻结版串库**，跑完立即 kill 并还原 config.json 的 port）；
- **浏览器冒烟（含手机形态 `verify/mobile_shell_smoke.cjs`：五档视口 375/640/760/900/1440 实测手机/电脑形态切换——≤760 出轮次下拉、刻度轨隐藏、抽屉侧栏可开合、文件面板全屏抽屉可显示且无横向滚动、标签不隐藏、触摸目标 ≥44px；>760 反之）**：`verify/browser_smoke.cjs`（Playwright + 系统 Edge `channel:'msedge'`，`NODE_PATH=path to user workdir\node_modules`；沙箱下需放宽进程启动权限）：零 pageerror / console.error / DOM 警示条；**顶栏另有 `verify/topbar_smoke.cjs`**（「⋯」按钮与溢出菜单已删/刷新按钮在操作区且点击真的重载/**卸载模型按钮已移除**/任务计数为纯数字且无胶囊底色/按钮文本形如「8 Skill」/MCP 文字恒为「MCP」且状态类唯一 + title 带状态/**按钮均单行（white-space:nowrap + 标签单行 + 高度 ≤44px；不再用「≈34px」这类把「单行」绑死在具体像素上的脆弱断言）**/**文件按钮默认与 hover 样式与「任务」一致**/**900px 与 640px 窄窗口下按钮仍单行且不换行（≤760px 手机形态按 styles.css `.mcp-button, .control-button { min-height: 44px }` 长到 44px，故上限取 44px 而非 36px，与桌面 34px 形态并存）**/**打开设置页后顶栏计数仍是纯数字且历史数据统计独立**）；**待发送附件列表另有 `verify/pending_files_smoke.cjs`**（自建 2 图 + 1 超长名 + 6 普通文件并 `setInputFiles` 真上传：竖直堆叠/固定高度可纵向滚动/文件名 ellipsis 且 title 保留全名/图片预览加载成功且点击开灯箱/非图片占位图标/每行移除按钮/**行与页面背景色差 ≥12 且有描边与投影**/移除一行/清空后列表隐藏且发送按钮回禁用/空态初始隐藏，跑完清理临时文件）；发送按钮状态机与纯附件发送另有 `verify/send_button_smoke.cjs`（初始灰暗/有文字 accent/清空回灰暗/仅附件可发送/请求体 message 为空/气泡无正文/结束回到不可发送态）；@ 引用弹层另有 `verify/file_ref_smoke.cjs`（自建多层工作区会话：@ 触发/隐藏点号条目/目录优先/Tab 提示/进入与返回/点击箭头进入/点击目录行引用/Enter 引用文件/镜像与气泡高亮/落库为绝对路径/打字过滤/邮箱不触发）；灯箱切换另有 `verify/lightbox_smoke.cjs`（自建 3 图会话：点击第 2 张打开、计数 2/3、按钮切换与循环、←/→ 键、Esc 关闭、**点左右半屏翻页、滚轮放大、缩放后单击不翻页、拖动平移、双击复位、换图复位**、输入区单图不显示切换控件且点空白关闭、零页面错误）；用量速率另有 `verify/usage_rate_smoke.cjs`（先 `python verify/seed_usage_message.py` 播种带 requests_detail 的助手消息，再校验每行 `速率 %4d token/s` 的对齐与位置、总结行「本轮总耗时 X，平均 N token/s，共 M 次请求。日期」格式）；媒体就地内嵌另有 `verify/media_inline_smoke.cjs`（先 `python verify/seed_media_message.py` 播种"2 个工具块 + 3 张就地图 + 1 条旧格式消息"的会话，再校验：媒体块紧邻工具块且**不在折叠 details 内**、就地 3 张图、截断提示文案、新消息末尾网格为空而旧格式仍走末尾网格、灯箱计数、零页面错误/404；播种后的数据形态另有 `verify/p3_data_check.py` 经 HTTP 校验 `run.media`/`attachments_truncated`/`/api/file` 可服务性）；**P4 显示侧另有 `verify/p4_display_smoke.cjs`**（同一份播种数据额外含真实 mp4/mp3/动画 GIF 用户附件：断言 `<video>/<audio>` 就绪且**时长 > 0**（证明 Range 元数据拉取可用）、GIF 推导缩略图 `naturalWidth > 0`、助手侧 figcaption、零 404）；**P5 写回另有 `verify/p5_writeback_smoke.cjs`**（浏览器打开会话后由 Node 调 `verify/p5_writeback_apply.py` 用**真实 JobMediaWriter** 写回产物，再断言**不刷新页面**数秒内出现就地媒体、缩略图加载、零页面错误）；**API 供应商卡片化另有 `verify/provider_cards_smoke.cjs`**（自建 2 个在线 + 1 个本地供应商：一行三列网格/「添加 API」卡固定最后/点卡片弹出并加载该供应商预设且字段可编辑/Esc 只关最上层且关闭后卡片仍在/添加卡片新建后卡片 +1/× 删除走确认并回落/在线-本地 tab 过滤/**视觉页「添加 API」仍能切回供应商页开新表单、取消按钮同样关闭**/900px 两列与 640px 单列/零页面错误）；**Agent 卡片化另有 `verify/agent_cards_smoke.cjs`**（先 `python verify/seed_legacy_builtin_agent.py` 播种遗留内置副本：内置清单为空/dsh-code·minimal·cordis 不再出现/遗留副本摘标记、无「内置」徽标、有 × 且可删/三列网格与「新增 Agent」卡固定最后/点卡片弹出并回填名称·提示词·副标题显示已有 ID/**表单无手填 ID 输入框**/**固定 Skill 卡片化（两列 + 勾选）、两个列表各自占满分区高度（分区切换后各自切页再量，不再比等高）**/**每张卡片在「固定 Skill」下方显示「工具集：<预设名>」且与后端 `tool_scope` 逐张核对一致（空 scope 显示「未限制（全部工具）」、不匹配显示「自定义 · N 个工具」，且至少一张卡真的命中内置预设名）**/**卡片上没有「默认」角标、没有 `is-default`、所有卡片底色描边一致**/**工具集分组行未被压扁（行高 ≥ 分组头高度）**/**分组头一行呈现（小字与标题同行、计数在最后）且每个分组都有小字说明、视觉紧跟命令执行**/**固定 Skill 列表与工具集分类可见、可展开、可勾选**（防滚动容器被网格行裁掉）/Esc 只关最上层且设置页仍开/**新建不填 ID 保存后由后台分配 `agent_<12 hex>`**/× 删除走确认回落/900px 两列与 640px 单列/零页面错误）；**Agent 头像另有 `verify/agent_avatar_smoke.cjs`**（先 `python verify/seed_agent_avatar_chat.py` 播种带头像的 Agent + 绑定会话 + 助手消息：头像接口 200 `image/webp`/聊天气泡「AI」圆标换成该图且 30px 正方形 `object-fit: cover`/卡片缩略图/弹层预览/选图仅本地 `blob:` 预览/保存后**内容哈希改名且旧文件 404**/换普通 Agent 回退「AI」圆标/零页面错误与 404）；**文本框右键菜单另有 `verify/context_menu_smoke.cjs`**（会话输入框基线：菜单挂在 body 且盖在最上层、七项条目齐全；注入 `.message-body` 选区验证会话页仍是「复制选中/快速发送」；设置页文本框右键：菜单挂在 `#settingsDialog` 内部、命中测试通过、条目为编辑菜单；**弹层内右键不再误弹会话页选区菜单**；嵌套 `#agentDialog` 同样挂到最上层；「全选」真的选中全部、「复制」给出成功反馈、number 输入框复制退化为整值；零页面错误）；**移动端两处回归（顶栏收起形态 + 输入框粘贴通路）另有 `verify/mobile_ui_regression.cjs`**（**自建静态 `public/` 服务在 8799，不需要后端**——两处都只考前端形态与事件决策，起源码实例是浪费；20 项断言：展开态基线（API / Agent / 操作区都在）→ 真实点击折叠条 → **收起的判据是「还剩什么」**：`.topbar-actions` / `.model-control` / `.agent-control` / `#openSidebar` 全部不可见、**可见子元素恰好只剩折叠条一个**、顶栏高度 251px → 49px、条上「展开顶栏」文字可见、`aria-expanded=false` → 再点一次全部恢复；粘贴侧用**事件层真值**判定：触摸长按派发的 `contextmenu` 必须 `defaultPrevented === false`（不拦截＝系统菜单还能出来）且我们的菜单不弹，鼠标右键走真实右击、菜单仍是七项齐全，再把 `navigator.clipboard` 置 undefined 模拟局域网 http，点「粘贴」必须给出含 `Ctrl+V`/`长按` 的可行动作而不是「未授权」；零 pageerror）；**对话刻度轨另有 `verify/_check_turn_rail.cjs`**（先 `python verify/seed_turn_rail_chat.py` 播种 40 轮会话：刻度数 = 30 滑动窗口/同一时刻只高亮一条且 = 视口中心所在轮次/滚到顶与滚到中间窗口与高亮跟随/高亮刻度比普通刻度更宽/轨道贴会话区右缘且右内边距 > 左内边距/悬停弹出概要且用户 1 行 AI 2 行超长省略、弹窗在刻度左侧不出视口/点击刻度跳转后高亮切到该轮/640px 隐藏轨道并恢复居中/零页面错误）；**工具卡片紧凑度另有 `verify/_check_tool_cards_compact.cjs`**（设置 → Agent 弹层展开工具分组：折叠的分组头保持白底、只有展开的那一组变浅灰条子、子卡片白底 + 强描边 + 投影、父子背景色差 ≥ 8、卡片高度明显变小、零页面错误）；**工具分类改版另有 `verify/tool_groups_smoke.py`**（自编排：起 8793 源码 server → 跑 `tool_groups_smoke.cjs`，跑完还原 config.json 与 data/server.json；**先切到「工具集」分区**；**73 项断言**：接口与界面都是 7 个分类且顺序一致、每组都有说明与风险徽标且徽标与分类名同行、12 个旧分类名不复活、每个工具只归入一个分类、**卡片态 5 张预设（只读/标准/长会话/ComfyUI/全能）+ 1 张「添加自定义工具集」卡且工具列表不展开、弹层高度 >600px**、**点每张预设卡：立刻出现「按下」状态（`.is-picked` + aria-pressed）→ 进入编辑态（卡片收起、列表展开）→ 命名栏预填预设名 → 载入的工具个数 == 卡片显示个数 → 弹层高度与卡片态一致（±2px）→ 返回后该卡高亮且摘要带名字**（只读 6 / 标准 13 / 长会话 17 / ComfyUI 18 / 全能全覆盖，只读模式只含 6 个只读工具）、**「添加」卡同样有按下状态且命名栏留空、默认载入标准模式的 13 个工具（不跟随当前选中项）**、**保存「我的工具集」→ 回到卡片态出现自定义卡（名字 + 个数 + ×）→ 断言它落在后端 `GET /api/tool_sets`（且 localStorage 里没有残留）→ 点它回到编辑态（命名栏与勾选都还原）→ 返回时自定义卡高亮、内置预设卡不高亮 → × 确认删除后卡片与后端记录同步清掉**、展开与分类级全选/计数、搜索框过滤成平铺结果（卡片带所属分类标签）→ 无匹配空态 → 清空恢复 6 组、**拦截 `/api/tool_catalog` 注入假 MCP 工具**验证「联网与外部服务」内按服务器二级分组、平铺区不重复、二级全选只勾该服务器、**让第一次目录响应缺少 MCP 工具再打开面板 → 必须强制取新、MCP 工具回来（「MCP 工具消失」回归）**、零页面错误）；**分支对话继承首轮上下文另有 `verify/branch_first_turn_smoke.py`**（自编排：把 config.json 的 `data_dir` 临时指向 `verify/branch_first_turn_tmp/` 用**独立库**播种（4 条消息 + 会话级 `first_turn`）→ 起 8795 源码 server → 浏览器断言原会话顶部折叠卡含系统提示词原文、点第二条用户消息的「分支」后新会话顶部同样显示且内容一致、新会话历史为分支点之前 2 条消息、零页面错误，跑完还原 config.json 并删临时目录）；**「新会话」分割线另有 `verify/session_start_smoke.py`**（自编排：`data_dir` 指向 `verify/session_start_tmp/` 独立库播种 4 条消息（两轮问答）→ 起 8798 源码 server → 浏览器断言：每条完整 AI 回复的 actions 里「新会话」排在「重新生成」右侧（首个按钮仍是「复制」）、**在第一条 AI 回复后画线（中间位置）**后分割线紧贴该条下方且消息仍是 4 条 → 经 API 核对标记落在该消息 metadata（`session_start.source=manual`、**不新增行**、其余消息未被标记）→ **再在第二条 AI 回复后画线 → 两条并存且各自贴着自己的锚点** → **刷新后两条仍在**（持久化）→ 逐条撤销：剩 1 条时剩下的是第二条的线 → 全部撤销后回到 4 条消息 0 条线、后端标记清空** → 第二个会话（播种两条 `source=tool` 的分割线，一条带 2 个后台任务、一条没有）断言：**分割线标出「模型重置」与交接文档路径、都带「填入种子消息」按钮** → 点带任务的那条：输入框出现种子消息（含交接文档路径、`请先读取该交接文档再继续`、`当前仍有 2 个任务在运行` 与任务清单）且**发送按钮变为可用（未自动发送）** → 点无任务的那条：**含占位符的整行自动去掉**（无 `[后台任务]`、无残留 `{task`）→ 零页面错误，跑完还原 config.json 并删临时目录；模型侧截断由 `tests/test_session_start.py` 钉死，工具侧校验与种子模板由 `tests/test_context_reset_tool.py` 钉死）；**会话模型下拉另有 `verify/composer_model_smoke.py`**（自编排：独立数据目录 + 8799 源码 server + 2 个假在线 API（`base_url` 指向不可达域名，目录请求全部由浏览器侧拦截注入）+ 1 个「已绑定模型」的会话，跑完还原 config.json 并删临时目录；断言：**目录返回后选中的是会话已保存的模型而不是目录第一项**、下拉末尾固定是「↻ 重新检测模型」、点它**强制重拉**（目录请求次数 +1）且刷新后还原原选择、伪选项**没有被写进会话模型**、注入 502 时**界面出声**且不破坏已有目录、**空目录不算已缓存**（切到 B 拿到空目录 → 切回 A 用缓存不重拉 → 再切 B 必须重拉）、目录就绪但会话未选模型时下拉是禁用占位项且**发送被拦下、不产生消息**、显式选模型后落库到会话；**同步口径：切 API 一律等「API 已切换」toast**——它排在 `await populateComposerModels('')` 之后，直接 sleep 会读到上一次切 API 留下的旧下拉内容（实测踩过），零页面错误）；**发送按钮状态另有 `verify/send_state_smoke.cjs`**（空输入禁用 → 快捷消息插入后**必须可点** → 清空回禁用 → `@` 引用插入后可点，覆盖"程序化改值不触发 input"这一类 bug）；**上下文圆环与提醒另有 `verify/ring_usage_smoke.py`**（自编排：临时给 config.json 加一个 context_window=100000 的供应商并把 `context_warning_percent` 设为 50 → 起 8797 源码 server → **server 就绪后**才播种「仍在运行」的会话与 `usage` 事件（早于启动会被 `interrupted` 回收）→ Playwright 断言圆环在该轮**尚未结束**时就显示 8,200 / 8.2%、**「思考 自动」右侧的 `#contextPercentLabel` 显示「上下文 8.2%」且排在其右侧、无告警配色、title 带完整用量**且**未达阈值不弹窗** → 追加 usage #2（85,000 = 85%）断言百分比实时刷成「上下文 85.0%」并转告警色、`#contextWarningDialog` 弹出、文案含阈值 50%、**弹窗水平/垂直居中且高度 ≤240px、「知道了」按钮居中、正文为新文案**、可关闭 → 追加 usage #3（88,000 = 88%）断言**距上次提醒不足 5% 不再弹** → 追加 usage #4（91,000）断言**再涨 5% 以上重新弹窗**且百分比转危险色 → 再打开第二个**已结束且超阈值**的会话断言**打开不弹窗**、点发送才弹、「继续发送」可见、草稿未被清空、点「继续发送」后消息真的发出 → 收尾还原 config 与播种数据；本轮靠它抓到 §九.52 的「轮询重渲染擦掉实时圆环」）；**开始页预设与思考按钮另有 `verify/starter_reasoning_smoke.cjs`**（内置预设渲染/悬停编辑删除/编辑保副标题/删除后出现「恢复默认预设」并恢复；思考按钮点击只开合列表且等级不变、列表高亮当前档、**等级文字在输入框外的 meta 行（断言不在 .composer 内）**、点空白收起）；**侧栏收藏/⋯ 菜单/滚轮与紧凑化另有 `verify/sidebar_favorites_smoke.cjs`**（先 `python verify/seed_favorites.py` 播种 35 个跨工作区会话：旧入口全部消失/条目为五角星+⋯/条目 34px 与标题 12.5px/「已收藏」分组位于最下方且聚合跨工作区/滚轮一格 ≈150px 而触控板小步进不加速/**收藏不推进 updated_at 且工作区内顺序不变**/点星收藏后落库并进入收藏区、在收藏区取消后从收藏区移除但仍在原工作区/⋯ 菜单挂 body 且完整落在视口、含重命名与删除、Esc 与点外部关闭、重命名落库/**悬浮分组表头与会话条目高度不变**/**滚动 160px 的重绘次数 ≤8**/**点击视口中间的会话后侧栏滚动位置不变且该条目变 active**/**收起按钮与三个操作图标同一行、.sidebar-top 空行已删**/**条目上边缘点击即切换会话且整块为手型**/**底部只剩一个状态点：绿→红→绿三态 + 不弹调试条**/**收起侧栏后对话列宽仍 880px 且助手/用户跨度不变**/**新建工作区走 <dialog>（stub `/api/workspace/pick` 后断言预填与落库，跑完清理）**、零页面错误与 404；**虚拟列表只渲染可视窗口，断言前要先滚到目标区域；分组默认折叠，需先展开**）；**Agent 页提示词入口另有 `verify/agent_prompt_smoke.cjs`**（先 `python verify/make_test_card.py` 生成角色卡 PNG：**默认「基本」分区即含名称/头像/系统提示词**/**设置页导航已无「快捷提示词」入口**/**空提示词点「存为快捷提示词」时底部提示框挂在 `dialog[open]:modal` 内且 opacity=1——top layer 遮挡回归**/表单有「套用快捷提示词」与「存为快捷提示词」按钮/面板挂在 Agent 弹层内部/导入按钮位于「系统提示词」行下方/角色卡追加不覆盖且解析出姓名与描述/**点按钮展开面板、每条带 ✎ 编辑与 × 删除、点条目套用后自动收起**/**「存为快捷提示词」弹窗含标题+正文（正文预填当前系统提示词、标题默认取首行）→ 入库 → ✎ 编辑改标题与正文（不新增条目）→ × 删除后库与面板同步刷新**/跑完自动删除冒烟预设）；快捷消息面板另有 `verify/quick_msg_smoke.cjs`（轻量/富文本控件与文案已移除、规则条不存在、面板只列快捷消息且不含开始页指令、**行内显示正文且弹窗无标题字段**、排序（同权重取新增倒序、使用后置顶）、点击插入并累加次数、删除后开始页列表不受影响、开始页弹窗仍带标题字段、开始页只渲染自定义指令卡片、富文本默认渲染、**内置预设「写交接报告并 reset_context」出现在列表并带「默认」标签、同样可编辑/可删除**）；**消息懒加载另有 erify/lazy_messages_smoke.py**（自动编排：临时 data_dir + 8796 源码 server + 播种 30 轮、第 5 轮带分割线；断言默认只渲染最近 10 轮（≤21 行）且是末尾一段、初始停在底部、**刻度轨仍覆盖全部 30 轮**、滚到顶后往前预渲染且视口不跳、**窗口首行仍是 user 消息**、持续上滚后第 1 轮出现且总行数 60+1、**分割线紧跟在被标记的那条 AI 回复之后**、刷新后仍是末尾一段、点第 1 轮刻度先渲染再跳转、零页面错误）；**内置预设「写交接报告并 reset_context」出现在列表并带「默认」标签、同样可编辑/可删除**）；**`verify/regenerate_smoke.py`（+ `.cjs`）**（独立库 + 8796 源码 server + Playwright/Edge：假 `/api/providers/models` 目录与假 `/api/chat` NDJSON 流，**截断走真实后端**；播种三轮问答（6 条），其中第二问是**富消息** = `/ref` 技能引用 + `@` 工作区引用 + 一张真实图片附件；共 **39 项断言**——两处操作区顺序、**富消息的图片附件真的渲染出来**、中间轮弹确认且取消无副作用、最后一轮一键直发不弹框、**截断点=那条提问（后端只剩前两轮 4 条）且更早的消息逐字节保留**、重发原文含 `/ref` 而发给模型的正文已剥离引用、**编辑富消息**（回填逐字含 `/ref` 与 `@`、编辑框里带出原图片附件、**重发请求体里附件带回 `path` 与 `thumb_path`**、截断后前端两条仍在）、**编辑态下底部输入框锁住且提示指向编辑框 / 添加文件禁用 / 发送键变「重新发送」且可用性跟编辑框走（清空但仍有附件时保持可用）/ 草稿附件列表被 CSS 收起 / **点底部发送键**即确认编辑（而不是发新消息）/ 确认后编辑态退出**、编辑首条提问后后端清空、零页面错误，并留两张图 `verify/regenerate_smoke_actions.png`（富消息的 `/ref`、`@`、图片三条引用都在）与 `verify/regenerate_smoke_editing.png`（编辑框带出图片 + 底部让位））；
- **会话区字体（设置 → 外观 → 会话字体）冒烟 `verify/chat_font_smoke.py` + `.cjs`**（自编排：`tempfile` 全新隔离根 + 8798 源码实例 + Playwright/系统 Edge；播种 1 个在线 API + 一个带问答的会话；**23 项断言**）：默认正文 = 15px + `--font-sans`（与历史硬编码等价，老用户视觉零变化）→ **新插入的 `.message-body` 自动同字号**（流式增量插入无需在渲染循环里做任何事）→ 面板回显 → **真拖滑杆**即时改真实消息字号、**拖动期间 0 次 POST `/api/settings`** → 上下界 13/18 都可用 → 选「衬线」正文改宋体栈，**并用 canvas 度量证明真的换了渲染字体**（`current ≠ --font-sans` 且 `current ≈ SimSun`，不是只改了声明）→ 自定义串即时生效且带界面字体兜底 → 「保存外观」发出 1 次 POST 且带齐三键 → `/api/settings` 回读一致 → **只换主题不重置字号字体** → 刷新首帧即保存值（localStorage 缓存，移动端不闪）→ 清空 localStorage 后刷新仍生效（证明服务端持久化）→ 手机竖屏（390×844）滑块与自定义框均可用且拖动即时生效 → 零 pageerror / 设置接口零 4xx。**两条脚手架纪律**：① **每次鼠标动作前重新取 `boundingBox`**——状态文字与自定义框显隐会改变弹窗高度，居中 `<dialog>` 随之整体位移，沿用旧坐标会点到别处；② 窄屏 `#openSettings` 被顶栏裁出视口时 `isVisible()` **仍返回 true**、`page.click` 会一直重试到 30s 超时，必须按「坐标是否落在视口内」判断，并改走「☰ 打开侧栏抽屉 → 点设置」这条移动端真实路径。**打包后另跑 `verify/frozen_chat_font_check.py`**（`dist\naiba-chat.exe --run-skill-script <绝对路径>`，11 项断言，**前后端两侧都查**）：运行期真调用 `default_config()` / `normalize_appearance()` 证明设置契约进了 exe（冻结版源码在 PYZ 里，读不到 `.py`，只能靠运行期常量与函数），再核对打包资源里的变量默认值、`.message-body` 是否消费变量且不再写死 `15px`、`applyAppearance` 是否写变量、字体族映射与 custom 兜底、面板控件与 `input` 只预览不落库——源码模式读 `public/` 永远是新的，**不重编译换 exe，用户手上那一版前端就不会变**（§六 同类：`frozen_mobile_ui_check.py`）。
- **供应商预设（「只填 API Key」）+ 首启引导向导冒烟 `verify/provider_presets_smoke.py` + `.cjs`**（自编排：`verify/_serve_tmp.py` 隔离实例（独立 config / data_dir / 端口 8796）+ 系统 Edge 无头；**40 项断言**）：①**首启形态**——空供应商实例（`_serve_tmp.py` 播种的 `probe-*` 两张卡都**没有 api_key**，正好就是「一个可用供应商都没有」的首启态，不必另写播种）打开页面即自动弹向导：14 张模板卡、一行三张、**两行装不下时可滚动**、「还有 …，共 14 个模板」、入口卡是 DeepSeek 而末张是「本机 Unsloth」（在线在前、本地在后）、第 2 步内容初始隐藏、`select#providerPreset` 死控件已彻底移除、**步骤条在视觉上真的分得清（读两步的 `getComputedStyle().color` 断言互不相同——`.is-active` 只改颜色与字重、没有下划线，光断言类名等于没验「用户看得出来」）**；②**选 TE 中转 → 进第 2 步**——recap 写明域名、引导条三段式（①注册 ②充值 ③令牌建 Key）且写明 403 预扣费风险与 codex 切法、「打开注册页」按钮指向 `te`、**正常路径只剩 API Key 一个空**（URL/模型随模板走、输入框是隐藏的）、无 Key 点保存被当场挡下并给出原因；③**填 Key 保存**——向导关闭、卡片落库字段逐字来自模板（`name/base_url/request_format/model`）、`preset_id` 记住来源、`has_api_key=true` 且不回显明文；④**该不该弹**——已有可用供应商重开不弹、删掉卡片后仍不弹（跳过态已记）、**清掉 `localStorage['naibaOnboardingDismissed']` 后无可用供应商又重新弹**（判据是空态而不是「弹过没」）、「跳过，稍后自己配置」关闭；⑤**设置弹层是同一套网格**——「添加 API」渲染同样的 14 张、新卡不预选任何模板、标题副标题说明「选模板后只填 Key」、点智谱 GLM 回填名称/地址/格式/推荐模型且引导条有注册页按钮、切摆烂中转回填 `grok-4.6`/`api.bailan.store`、**自定义模板不填地址也不给注册页按钮**、**选 TE 模板 + 手改 codex 组合时用户值优先且仍记住来源模板**、未知模板被服务端挡下（400，不是静默建成一张怪卡）；零 pageerror / 零 4xx-5xx（`/api/providers/models` 是**已知取样噪音**：占位供应商 `example.invalid` 必然连不通、后端回 400，按 `agent_help_popover.cjs` 同款 URL 白名单排除，**但要求「被排除的条数」与「控制台报错条数」严格相等**，免得白名单把真报错一起吞掉）。留两张图 `verify/provider_presets_onboarding.png` / `provider_presets_dialog.png`。**三条脚手架纪律**：① **等待助手必须真 `await`**——写 `fn.await ? await fn() : fn()` 会让轮询恒真、断言集体假绿（§九.112）；② 两个网格同存于 DOM 时**点击选择器要按容器限定**（`#onboardingPresetGrid [data-provider-preset="te"]`），裸选择器取到的是文档序里的第一份，改一处 DOM 顺序就会悄悄点到另一个弹层；③ 收尾删除隔离目录会被沙箱批量删除护栏（>50 项/轮，§九.66）挡下——**启动前 `rmtree` 才是「每轮全新根」的保证**；但要注意**这一轮里已经删过 50+ 项之后，启动期的 rmtree 也会被拦，而那次拦截会直接把进程杀掉**（症状：脚本 1 秒退出、只打印一行 `[safe-delete][SAFE_DELETE_BULK_CONFIRM_REQUIRED]`，看起来像脚本坏了）。**关掉护栏再跑**：`CODEBUDDY_SAFE_DELETE_ENABLED=0 .venv\Scripts\python.exe verify\provider_presets_smoke.py`（与 §六 冻结版编译同一个开关），跑完隔离目录会真的被清掉。
- **「重置上下文后前端锁不解除」回归 `verify/context_reset_lock.py` + `.cjs`**（自编排：`verify/_serve_tmp.py` 隔离实例（独立 config / data_dir / 端口 8805）+ 真库播种 + 系统 Edge 无头；**14 项断言**：①A 真数据——满量会话（播种的 `usage.context_limit_source` 让前端不必配 `context_window` 也能算出 100%）打开即锁 → 点真实「新会话」按钮 → 圆环归零、输入框解锁且真能打字/发送 → 点「撤销」→ 满量重新生效、按需回锁 → 模型重置（`source=tool`）的分割线打开即解锁且「填入种子消息」真的落进**可用**输入框 → 分割线之下还有更小用量时按线以下算（10,000/100,000 = 10%）；②B 模块级注入（调真模块函数）——单消息入口带 `session_start` 按「无用量」处理、运行中「无 usage 的历史重渲染不得擦实时值」但**上下文已重置必须越权解锁**、遗留 `role=session` 标记行同样是分割线；零页面错误 / 零失败请求；留三张图 `verify/context_reset_lock_{1_locked,2_unlocked,3_seed_ready}.png`。**为什么不用静态 `public/` 服务**：这里要的正是「真库 → 真接口 → 真渲染」的形态（§九.92）；**反证过**——把 `updateContextUsage` 回退成旧写法重跑 ⇒ A② 立刻红（分割线已画出、圆环仍 100%、输入框仍禁用，`page.fill` 直接超时），正是终端用户报的死结）
- **插话（运行中第二输入通道）+ 流式状态「重连中…」滞留回归 `verify/interjection_smoke.py` + `.cjs`**（自编排：`verify/_serve_tmp.py` 隔离实例（独立 config / data_dir / 端口）+ 真库播种 + 系统 Edge 无头；**34 项断言**）：①**入队**——运行中真 `POST /api/chat/interject`（202）后**库里真多一条** `role=user` + `interjection_pending` 的消息（不只是面板上多一行）、头部计数同步；②**编辑**——就地展开输入框、提交后行内文本更新且**消息 id 不变**；③**引导**——状态转 `guided` 且落库一致、行标「已引导」；④**删除**——行移除且库里那条真没了；⑤**终态冻结**——done 之后残留行转 `stopped`、引导按钮消失、头部不再说「N 条待引导」，**且冻结行仍可删除**（§九.100 ④ 那类死数据）；⑥**「重连中」不得误报**（G1~G6）——向前端注入事件序列 `status → showElapsedStatus('重连中…','reconnect') → reasoning_delta`，断言 `#runtimeStatus` 文本**不含「重连中」**，且**负向断言**：「正在思考 · 已等待 X 秒」这种思考计时是**特性、不许被一起清掉**（两种计时共用同一条状态栏，清错一个就换一个 bug）；判据取**用户可见文本**，不断言内部变量；⑦**与「选择」面板共存**——同一会话里选择题与插话残留并存，两面板互不锁死、残留行可取回草稿；零页面错误 / 零失败请求。**两条脚手架纪律**：① **播种必须在服务就绪之后**——启动清理会把「仍在跑」的 run 改写成中断态，先播种再起服务等于把夹具自己毁掉（与 `tasks_panel_smoke.py` 同因）；② **合成 SSE 用 `page.addInitScript` 覆写 `fetch`**（`window.__streams`）并以 `window.__eventsOpenDelay` 放慢流——否则状态条那几帧还没来得及被人看见就过去了，断言会退化成空测（本仓「看着在跑、其实早就崩了」的防线同类，§六 media_markup 那条）。**为什么不用静态 `public/` 服务**：入队 / 引导 / 删除都要真接口真落库，静态服务下 POST 全 404、而面板与状态条看起来照样能画（§九.92）
- **冻结版「插话 + 不再误报重连中」自检 `verify/frozen_interjection_check.py`**（`dist\naiba-chat.exe --run-skill-script <绝对路径>`，**38 项断言，运行期真值 + 打包资源双查**）：运行期真调用——`EventType` 两枚事件与负载键、`RUN_CONTEXT_KEYS` 两个回调、四枚 metadata 键在权威清单里、manager 四方法与 storage 七方法存在；**真建库跑通状态机**（pending 不入可拉取队列 → 引导后可拉取且内容一致 → 消费后不可拉取 → **未消费/被冻结的插话都不进 `build_model_history`、消费后才进** → 冻结后 guided 行不可拉取 → **已引导+已冻结的行仍可删、且删除不需要活动 run**）；再核对打包资源里的 `18-interjections.js`（含 `sendRunInterjection`/`freezeQueuedInterjections`/`settlePanel`/`rowRunId`）、`index.html` 的 `runGuidanceList`/`interjectButton`、`12-chat-input.js` 的两个新 handler、**`11-run-stream.js` 用 `elapsedReconnectShown` 且 `elapsedKind` 已彻底消失**、`04-messages.js` 的 `isQueuedInterjection`、`styles.css` 的 `.run-guidance-list`。**存在理由**：后端部分打包进 PYZ、前端部分是打包静态资源，**用户跑的正是 exe**，源码模式读 `public/` 永远是新的（§六 同类 `frozen_mobile_ui_check.py` / `frozen_tool_prose_check.py`）。**脚手架坑**：顶层 run 互斥（`ACTIVE_RUN`），同一会话里要建第二个 run 必须先 `update_background_task(…, status='completed', finished=True)` 收掉前一个，否则整块断言链在 `create_run` 上抛异常、后面全绿的部分看起来像没跑
- **第一批三项（全文搜索 / 分支导航 / 删除单条消息）联动冒烟 `verify/batch1_smoke.py` + `.cjs`**（自编排：`tempfile` 全新隔离根 + 8802 源码实例 + Playwright/Edge；沿用 `regenerate_smoke.py` 的隔离模板（`PathContext.local` + 独立端口 + `--serve` 子进程），**全程不碰开发 `config.json` 与开发库**；**一次播种三个会话**——源会话（两轮问答，末条助手回复里埋 `MAGICTOKEN`）、它的分支（从第二问分支，只带「第一问/第一答」）、删除实验会话（两轮，专供删除，免得删掉搜索/分支的依赖数据）；**39 项断言**：①搜索（放大镜展开「标题｜全文」切换、默认标题模式、切全文后命中数/结果头/片段含命中词且有 `<mark>` 高亮/片段取自原文、**点命中切到源会话并把那条消息定位高亮**、**搜索只读**（会话消息条数不变）、Esc 退回会话树且当前会话不变）；②分支（分支行有徽标且 tooltip 带源标题、源行有 `⑂1` 计数徽标、点徽标开面板且标题为「分支链（源 + 兄弟）」、源项 `is-source` 与当前项 `is-current`、点源项跳回源会话后面板关闭、在源会话点计数徽标**只列分支且不含自己**）；③删除（user 确认框两粒度齐全且写明「整轮含义 / 缓存一次性成本 / 10 秒可撤销」、**整轮删除后后端恰好少两条**、DOM 同步移除那一行、撤销后**内容与顺序逐字复原**、撤销条收起、**assistant 只给「仅删这一条」**、单删 + 撤销同样复原）；④本批接口零 HTTP 错误 + 零 JS 错误（`/api/providers/models` 按既有做法桩掉，避免假供应商的 400 噪音）。留 6 张图 `verify/batch1_smoke_{1_search_results,2_hit_located,3_branch_chain_panel,4_delete_confirm,5_undo_bar,6_after_undo}.png`；**打包后另跑 `verify/frozen_batch1_check.py`**（`dist\naiba-chat.exe --run-skill-script <本文件绝对路径>`，24 项断言）：v18 迁移已注册、五个存储方法存在、`MESSAGE_DELETE_MODES` 口径、**真建库跑通「分支两列写入 / `branch_chain` 三角色 / 搜索命中且片段为原文子串 / 删一条 / 撤销逐字节还原 / turn 删除」**、`NaibaChatApp` 四个新 API 方法存在、**打包资源 `index.html` 含 `workspaceSearchRow`/`branchChainPanel`/`messageDeleteDialog`/`undoBar`/`messageDeleteTurn`**（源码模式读 `public/` 看不出打包缺失）。**注意：不能断言 `naiba/__file__` 指向的目录存在**——冻结版源码在 PYZ 里，`_MEIPASS\naiba` 是虚拟路径、盘上没有可读 `.py`（首跑就在这一条上假红过））
- **本地模型上下文溢出报错冒烟 `verify/q1_context_smoke.py` + `.cjs`（Q1）**（自编排：`tempfile` 全新隔离根 + 8803 源码实例 + **8804 假本地后端**（`POST /v1/chat/completions` 直接回 400 + llama.cpp 的 `exceed_context_size_error` 响应体，含 `n_ctx: 4096`）+ Playwright/Edge；播种一个**本地**供应商（`context_window` 显式写 32768 → 来源 `local_config`，不触发窗口探测）与两个会话；**20 项断言**：①静态形态——三条带 `metadata.truncated` 的助手回复各渲染一条 `.truncation-notice`，`cause=context` 说「上下文窗口已耗尽 + 新会话」且保留「已自动续写一次」事实、`cause=output` 说「已达模型输出上限」且**不得误报窗口耗尽**、**没有 cause 的老数据**仍按输出上限显示（向后兼容），再回读 `GET /api/conversations/<id>` 确认 cause 是库里真值而不是前端自己编的；②**真链路**——在浏览器里对绑定该假后端的会话真发一条消息 → 真 run → 消息区出现可读失败文案（点名后端 + **后端自报的 4096 tokens**（而不是配置里的 32768）+「新会话」+「设置 → 模型」），且**不得出现原始 JSON / `exceed_context_size_error`**；③零 JS 错误 + 零 4xx/5xx；留两张图 `verify/q1_context_smoke_{1_notices,2_overflow_error}.png`；**打包后另跑 `verify/frozen_q1_check.py`**（`dist\naiba-chat.exe --run-skill-script <绝对路径>`，14 项断言：特征串判定与窗口解析、`context_overflow_message` 文案、Ollama 流内错误 / SSE 内嵌错误 / HTTP 400 三条路径都转 `ContextOverflowError`、`_truncation_info` 的 cause 口径、**打包资源 `public/js/03-media.js` 含 `cause === 'context'` 与「上下文窗口已耗尽」**（源码模式读 `public/` 看不出打包缺失）；**冻结版没打 `unittest`**，脚本里不能用 `unittest.mock`，手写替身）；
- **冻结版「进度播报不再堆积」自检 `verify/frozen_tool_prose_check.py`**（`dist\naiba-chat.exe --run-skill-script <绝对路径>`，9 项断言，**运行期真值 + 打包资源双查**）：运行期真调用 `_build_activity_timeline`（三句播报 + 三个工具 + 最终答复 ⇒ 只留一段且等于最终答复、工具条目一个不少、答复仍在末尾）与 `ConversationRunMixin._rebuild_partial_run`（工具之后的 delta 才算正文；停在工具步时正文为空），再核对打包资源里的 `03-media.js` 含 `lastProseIndex` 过滤、且旧的无脑遍历写法已消失。**存在理由**：这条修复横跨 Python（打包进 PYZ，盘上读不到 `.py`）与前端静态资源两层，而**用户跑的正是 exe**——用户报障时原话就是「当前生成的 EXE 仍会出现这类废话」；源码模式读 `public/` 永远是新的，不重编译换 exe 就到不了用户手上（§九.97；§六 同类 `frozen_mobile_ui_check.py`）。
- **冻结版前端资源自检 `verify/frozen_mobile_ui_check.py`**（`dist\naiba-chat.exe --run-skill-script <绝对路径>`，7 项断言：打包内的 `01-core.js` / `15-bind-events.js` / `styles.css` / `index.html` 含「长按判定 `lastPointerType`」「触摸不拦截」「`PASTE_UNAVAILABLE_HINT` 且含 Ctrl+V」「旧死文案已消失」「收起隐藏全部子元素」「旧『并回一行』规则已删」「`topbar-toggle-label` 文字入口」。**存在理由**：这两处修复全是打包的静态资源，源码模式读 `public/` 永远是新的——用户手机上跑的是 `D:\<安装目录>\naiba-chat.exe`，不重编译换 exe，改动一行也到不了手机；本轮就是靠它确认「用户手机看到的旧前端」与「修好的包」的差别）
- **Agent 说明弹层 + 工作区提示回归 `verify/agent_help_popover.py`（自编排）+ `verify/agent_help_popover.cjs`（浏览器侧）**（**跑真后端**：`.py` 用 `verify/_serve_tmp.py` 起**隔离源码实例**（独立 config / data_dir / 端口 8801，每轮全新根）→ 等 `/api/health` → 把 `NAIBA_SMOKE_BASE` 与 `NAIBA_EXPECT_STARTERS` 交给 Node 检查 → 收尾 kill 服务并删隔离根。**为什么不是静态 `public/` 服务**：断言确实不需要后端，但**截图需要**——静态服务下 `/api/starter-prompts` 404，开始页只剩 index.html 里写死的那两张 Skill 卡，配置驱动的 6 张全没了，截图看起来"卡片少了一大半"而断言照样全绿（§九.92）。**23 项断言**：①开始页形态——空态可见、`.custom-starter` 张数 = `NAIBA_EXPECT_STARTERS`（由 `.py` 从 `naiba.config.BUILTIN_STARTER_PRESETS` 现算，改预设张数不用动脚本）、卡片总数 = 预设 + 3（两张 Skill + 「自定义指令」）；②「?」按钮可见 → 点击后弹层打开且**已挂到 `document.body`**（顶栏容器有 overflow 裁剪，挂错地方就是被裁）→ 文案含 ComfyUI **两条路线**（`HTTP 直连` / `MCP 连接`）、含「ComfyUI 联动」预设与 `comfyui_prepare_workflow`/`comfyui_batch`、含三种预设 Agent（通用/编程/短剧）→ `aria-expanded` 同步 → **点弹层外部真的关闭**（落点取右下空白：**别踩开始页卡片**，点卡片会往输入框塞文字）；③工作区提示用**注入 `state.workspaces`（两条同名不同目录）后调真实渲染函数**的办法考：`#composerWorkspaceSelect` 每条 `option.title` = 该工作区目录、「未分组」也有一句说明、`select.title` 带当前目录，`sidebarRowHtml` 的分组头 `title` = 「名字（目录）」、未分组兜底为「未分组」；④**会话所属分组没登记在本实例配置里时，下拉不许静默退化成「未分组」**（§九.89）：把 `state.workspaces` 清空、令会话属于 `naiba-chat`（源码模式实测形态）→ 下拉必须**保住那一项**、`value` 仍是真值 `naiba-chat`、该项标签标出「未注册」、`select.title` 说明「未在本实例注册」并给出「新建工作区」这条恢复路径；再把注册表补齐 → 同一会话立刻回到正常形态（无「未注册」标记）；⑤零 pageerror + 零失败请求（`/api/providers/models` 是**已知取样噪音**：隔离实例的占位供应商 `https://example.invalid/v1` 必然连不通、后端回 400，脚本里按 URL 白名单排除；其余 4xx/5xx 照旧算失败）。**注入状态前必做**：`state.syncPolling = false` 并清 `state.syncTimer`——会话同步 10s 一跳会按服务端真实状态重渲染下拉，把注入的状态冲掉，那就又是"拍到的不是被断言的状态"（§九.90）。留三张图 `verify/agent_help_{button,popover,orphan_workspace}.png`——`popover.png` 是**真后端整页**（开始页 9 张卡全在），`orphan_workspace.png` 截的是**下拉本身**（原生闭合 select 显示选中项文字，正好是「naiba-chat（未注册）」），不是顶栏切片（§九.90）；**限度写清楚：这条脚本只证明"属性写对了"，不证明"用户滑得到"**——`title` 在原生 `<select>` 下拉项里是否呈现由浏览器决定，真机确认才算数（§九.88）
- **软件更新「目标版本」下拉另有 `verify/update_version_smoke.py`**（自编排：`data_dir` 临时指向 `verify/update_version_smoke_tmp_<pid>/` 以避开本机实例锁 → 起 8801 源码 server → 跑 `verify/update_version_smoke.cjs`（Playwright + Edge，浏览器拦截 `/api/update**` 注入固定版本表：2.1.26…2.1.0 共 27 项 + 一个必须被过滤的 2.0.0）→ 收尾 kill 整棵 server 进程树、还原 config.json、删临时目录与日志；**跑前先探端口，被占用直接失败**——否则健康检查会打到残留 server 上，等于用旧代码验证新改动；等就绪期间同时盯自己起的进程有没有提前退出，避免「对着别人说话」还报绿）；断言：只渲染可自动更新的版本（2.0.0 被过滤）且**历史版本不被截断为 20 条**、原生 `select` 同步全部可安装值、**渲染后默认聚焦「最新可更新版本」**（不是列表第一项、也不是此前手选值）、展开高度 ≤20 行且完全落在视口内、内容超出可滚动、**菜单顶/底用 `elementFromPoint` 命中测试证明未被祖先裁剪**（只看 `getBoundingClientRect` 对裁剪无感）、滚到底最后一项仍可见、键盘（↑/↓ 移动选中且与触发标签同步、Escape 关、Space 开、Enter 收）、鼠标点击选项写回原生 `select` 并同步标签、**点「检查更新」且已是最新时聚焦当前版本**、零 pageerror / console.error；
- **四道运行闸真机冒烟 `verify/runtime_guard_smoke.py`**（自编排：独立 `data_dir` + 8799/8800 双端口 + 假本地端点按阶段切换行为 `reply`/`hold`/`loop`；**13 项断言**：①探测窗口回写 provider（且只问一次 `/props`）②`hold` 阶段状态必须在首字节闸门内转 `failed`、耗时远小于 1800s 总超时、假端点观察到对端关闭、错误列含可行动文案（**关键词必须与 `local_first_byte_timeout_error()` 逐字一致**）③`loop` 阶段模型恰好被调用 `agent_step_limit` 次后停下且错误列写明「步数上限」；**取 Run id 必须用「该会话最新一条」而不是 `active_only=1`**——秒回的 Run 在首次轮询前就结束，会让后续断言全废）；（**已归档**，见上方归档说明）
- **前端「运行设置」检验 `verify/run_frontend_check.sh` + `verify/frontend_runtime_settings_check.cjs`**（runner 把「隔离源码实例 → 等健康 → 跑 cjs → 收尾」串在同一 shell 生命周期内，每轮用全新隔离根 `NAIBA_TMP_ROOT`；**25 项断言**：两个新输入的可见性/label 文案/取值范围/未被裁切、显示值 = `bootstrap.settings` 真值（**DOM id 与配置键必须显式映射**，两者不同名）、hint 保留「保活注释不算」与「0 = 不限制」、全新安装默认 120/200、**填 0 → 保存 → 重载页面仍是 0**（防前端 `|| 默认` 吞掉合法值 0）、改回 120/200 生效、走页面自身 change 处理真实切夜间主题并截浅色/暗色两张图、零 pageerror）；（**已归档**，见上方归档说明）
- **工具调用前的「进度播报」不得累积进助手消息 `verify/tool_prefix_prose_smoke.py`**（自编排：全新隔离目录 + 8806 源码实例 + 8807 本地假模型；**无浏览器**——本轮修复全在后端展示层与提示词，前端一行未改）：假模型**每轮都先吐一句进度再吐 K3 `<|open|>tools…` 协议**（正是缺陷形态），真 `POST /api/chat` 跑完 4 轮工具调用 + 最终答复；**双向断言**——前提侧：run 事件流里确实出现过那 4 句播报（否则负向断言是空断言）；结论侧：`activity` 只剩 1 段 prose 且等于最终答复、工具条目 4 个全部成功、`content` 与 `activity` 里**一处都没有**播报原文。**反证过**：把 prose 过滤回退成修复前的写法重跑 ⇒ 「prose 5 段」「activity 残留 4 句播报」立刻红，正是用户报障的形态（§九.97）。
- **流式回复撞上「点任务面板」的回归冒烟 `verify/stream_rerender_smoke.py` + `.cjs`**（自编排：全新隔离目录 + 8798 源码实例 + 本地假 SSE 模型（10 段 × 400ms，`/v1/chat/completions`）+ 系统 Edge 无头）：**真 `POST /api/chat`、真 run、真事件流**，任务面板那一行由浏览器侧拦 `/api/tasks` 注入；流程＝发送 → 等第一段 delta → 点那一行（断言 `renderMessages` 日志确实新增了一条，证明触发条件成立）→ 断言**同一条气泡仍在 `#messages` 里**（DOM 标记 + `isConnected`）→ 断言后续 delta 继续落进它 → 跑完断言后端只有一条助手回复且正文首尾俱全 → 零页面错误。**反证过**：把复挂那行改成 `if (false && …)` 后，「气泡仍在消息区」等 3 项立刻红。
- **任务面板（重做后的口径）三条资产**：`verify/tasks_panel_smoke.py` —— 自编排（**全新隔离根 + 先播终态数据、服务起来后再播活动作业**——否则会被启动清理改写成 `interrupted`；独立端口 + 源码实例 + Playwright 用**系统 Edge**），断言 `/api/tasks?jobs_only=1` 只回后台作业、不带该参数时向后兼容、`POST /api/jobs/{id}/cancel` 落 `stopping` + `cancel_requested`；它调用 `verify/tasks_panel_smoke.cjs`（**22 项**浏览器断言：面板只列作业、按回答分组、组标题为「类型 + 时间」、停止按钮只对活动态出现、**两层派生链同组 + 嵌套缩进/`↳` 由真实 CSS 生效**、零 pageerror / console.error / 4xx、430px 不横向滚动；模型目录请求按 `composer_model_smoke.cjs` 的做法桩掉）；`verify/tasks_panel_render_check.mjs` —— 无浏览器兜底（抽 06/08 的面板渲染函数真执行并断言产出 HTML，**21 例**，含「父行被过滤时兄弟不拆组」与「环状父子链必须收敛不卡死」）。前置：仓库根 `node_modules/` 内有 playwright（`npm install --no-save playwright`，跳过浏览器下载，用系统 Edge）。
- **交互选择面板端到端 `verify/choice_panel_smoke.py`**（自编排：隔离 `data_dir`（默认 `verify/choice_panel_tmp/`，`NAIBA_SMOKE_ROOT` 可改）+ 源码实例 + 系统 Edge 无头；py 用 `ChatStorage` 播种三条会话（两题＝单选 + 多选 / 单题 / 长题目长选项）与一个冒烟 API profile，跑 `verify/choice_panel_smoke.cjs`，截图落 `verify/choice_panel_shots/`（每次先清旧图），收尾 kill 整棵进程树 + 删隔离根且不改开发 `config.json`；**55 项断言**：面板挂在 `.composer-wrap` 里紧贴输入框上方（不进消息流）、`role=group` + `aria-label` 用题面、绑定「会话 + 来源消息」（换消息 / 切会话不串题）、题面 / 单选多选标识 / 进度 / 底部提示行、选项是 radio/checkbox 且初始未选、未选时「下一题」禁用且首题没有「上一题」、**单选点完自动进下一题**、多选不跳题（勾选 · 追加 · 反选 + 「已选 N 项」摘要）、末题按钮变「完成」且未勾选时仍禁用、改题后选中态与答案同步、折叠只藏选项与操作区（题面 / 进度 / 已选都在，展开不重置）、「完成」把答案块（一行一题 `题面：选项`、多选同行用「、」）**换行追加**在草稿末尾（不覆盖草稿、不挤掉待发送附件、不自动发送）、已答会话重新加载历史不再弹面板且不重复填答案块、未答会话切走再切回面板与已选仍在（内存键）、长题目 / 长选项 / 摘要 / 答案块**一律不截断**、零 pageerror / console.error；**两条防串台**：`POST /api/chat` 全程被拦截并计数（从打开面板到点完成，一次聊天请求都不能发）、`/api/providers/models` 桩成固定目录（模型下拉可用但不真的联网）；**两条脚手架坑**（侧栏锚点要取落库真值 / 附件夹具别预塞 `uploads`）见 §九.70；（**已归档**，见上方归档说明）
- **发版工具（`verify/` 下，可复用；只读）**：`_release_check.py`（发布清单同步自检——两份 release_notes 是否**严格一致**、README 的四个版本串是否已跟到 `naiba-chat-update.json` 的 `version`、工作流 `RELEASE_VERSION`/`RELEASE_TAG`/`PACKAGE_NAME` 与 Release 标题/发布步骤名是否同步，最后一节做本地绝对路径审计；**路径审计的范围＝§8.8 守门的范围**——只扫 `git ls-files` 里扩展名属于守门 `CODE_EXTS` 的文件、排除 `docs/`，正则与占位白名单**直接加载 `tests/test_no_absolute_paths.py` 复用**（判据不写第二遍，见 §九.115）；**版本号一律从更新清单派生，不硬编码**；README 标题与工作流 Release 标题的品牌**按已知品牌表判定**——`brand_hit(text, "# {brand} …")` 逐品牌代入，当前显示名 `Cat Chat` 与历史名 `Naiba Chat` 都放行，但必须是整个前缀命中，不接受「句中出现品牌」）；`release_watch.py`（**本机没有 `gh` CLI**，故走匿名 GitHub API 跟 workflow run：打印每个 step 的结论 → 成功后核 manifest 的版本/commit/说明条数 → 把 exe 下回来算 SHA-256 与 manifest 比对；**头条品牌判据是 `BRAND_PREFIXES = ("Cat Chat", "Naiba Chat")` 的前缀命中**（历史条目仍以旧名开头，两个都算正常）——`release_notes` 缺失 / 空列表 / 非字符串一律**明确判失败而不是抛 `IndexError`**（守门 `tests/test_release_watch.py` 16 例）；用法 `release_watch.py [sha] [tag]`，流程见 `.codebuddy/rules/版本更新说明.mdc`。**匿名额度 60 次/小时是硬约束**：额度耗尽时 API 一律回 403 `X-RateLimit-Remaining: 0`，脚本识别到就**立即降级**到「静态口核验」（`git ls-remote --tags` 看 tag → `releases/latest/download/` 取 manifest 与 exe 对哈希，全程不用 API），**绝不把「问不到」报成「没成功」**——2026-09-17 就因此把一次成功的发布误报为失败（旧实现把 403 当网络抖动、重试到 20 分钟超时）；降级时拿不到逐步骤结论，且**必须带 tag**，否则无法定位资产）；
- **冻结版编译**：`$env:CODEBUDDY_SAFE_DELETE_ENABLED="0"; $env:NAIBA_BUILD_VERSION="X.Y.Z-beta"; 项目根\.venv\Scripts\python.exe -m PyInstaller --noconfirm --clean naiba-chat.spec`（版本号取当前版本，见 §二，**不要写死成某个旧版本**）——**`CODEBUDDY_SAFE_DELETE_ENABLED=0` 必须带**：`--clean` 一次要删 `build/` 里上百个文件，会撞上本沙箱的 safe-delete 批量护栏（`SAFE_DELETE_BULK_CONFIRM_REQUIRED`，门槛 50 项/轮）直接在 `Building EXE` 那步失败，见 §九.66；**每次修改完成后默认编译一次**（用户约定：交付即验证，无需等询问）；**编译前 kill naiba-chat 进程**（编译遇 lock 直接 `Stop-Process -Force`，不用等待确认）；`build_info.json` 每次编译滚动、**勿提交**；
- **静态检查器清单（`verify/` 下，可复用；均为无浏览器、秒级）**：
  - `scan_undef_all.py`：全包未定义名（75 文件 0 候选）；
  - `scan_unused_pkg.py`：未使用导入（保留有意 noqa re-export）；
  - `esm_cross_assign.py`：前端跨文件赋值（ESM 只读绑定事故防线）；
  - `esm_unused_imports.py` / `esm_graph_check.py`：前端图一致性与未使用导入；
  - `tdz_check.py`：前端 Top-level 求值序（输出 7 处候选，均为属性键/子串误报，需人工评估）；
  - `dup_ids.py`：`index.html` 重复 id 速查（当前 307 个 id 无重复）。
  - `css_content_bg.py`：对话区内容「实底」守门（聊天背景图只铺对话区、不做蒙层，可读性全靠内容自带底色——`REQUIRED_OPAQUE` 清单里的内容块缺 `background` 或被写成 `transparent` 即退出码 1，豁免项与「顶栏仍半透明」的风险登记一并打印；实测能报出修复前的 `.starter-grid .starter-add`）。（**已归档**，见上方归档说明）

## 七、Git 提交规范

- 按**逻辑单元**提交（一次功能/修复/重构），中文信息：`<类型>: <改动内容>`（feat/fix/refactor/docs/perf/test/build/chore）+ 一行理由；
- 显式 `git add <文件>`，**禁用 `-A`**（`build_info.json` 永不入提交；`verify/` 只提交 §六 清单里的可复用脚本——见 §六 末尾「目录纪律」）；
- 无改动跳过空提交；提交失败必须报错查明原因；每提交可独立回滚（`git revert`）。

## 八、工作守则（修改代码前必读 · 源自 coding-workflow skill）

### 8.1 执行链路

```
0. 理解需求 & 消除歧义（必要时询问）
1. 读上下文（先读后改）
2. 分级风险排查（高风险先等确认）
3. 制定实现计划
4. 实现代码
5. 异常处理与日志
6. 验证（**按 §六 分级：只跑相关模块的测试/冒烟；重大重构才全量**）
7. 提交 git（按逻辑单元）
8. 汇报（文件名：内容+理由；验证状态；风险；下一步）& 询问是否继续
9. 会话收尾（用户表示结束会话时）：按 §8.7 **主动询问是否清除遗留的一次性测试脚本/产物**
```

### 8.2 分级风险拦截

- **高风险**（破坏构建/运行、大范围重构、改核心数据结构/DB schema/公共 API、删除数据、无测试覆盖、涉敏涉密）→ 先列「影响面 / 测试覆盖 / 可否回滚 / 是否破坏 API / 是否涉密」，给 2~3 个预案，**等确认再动手**；
- **低风险**（注释/文案/日志/格式/小范围加法/有测试保护）→ 简述计划直接动手；
- 排查至少覆盖：影响面 · 可回滚 · 测试覆盖 · 接口/行为兼容 · 敏感数据。

### 8.3 触发即停

出现以下任一 → 带着上下文停下询问（描述冲突/歧义 + 证据 + 影响面 + 2~3 个可行动选项），不要猜：指示不明确/有歧义；发现需求冲突或可能引发 bug；多解而无法判断。等待期间把已自洽的改动按逻辑单元先提交，保持可回滚。

### 8.4 异常处理与日志

- 区分可预期（业务分支正常处理）与非预期（明确报错，不吞）；
- 禁止 `except:` 裸捕获与"吞错返回假成功/None 无日志"的兜底——一律「记录错误 + 明确报错」；
- 日志带上下文：错误信息 + 关键输入 + 堆栈 + 时间戳/级别；不打印密钥/令牌（必要打码）；
- 区分永久失败（快速失败明确报错）与瞬时失败（可重试/退避）。

### 8.5 架构与兼容

- 单一职责、关注点分离、面向接口、依赖注入、组合优于继承；
- 不过度设计：不为想象的需求预堆抽象；为已知演化留扩展点；
- 向后兼容：API 增量演进、保留弃用路径、配置驱动、避免硬编码；
- 避免上帝对象与过高耦合。

### 8.6 汇报格式

```
文件名：修改内容 + 修改理由
（多文件按相关度分组）
额外说明：验证状态（跑过哪些测试/构建）；警告与未改动内容；剩余风险。
末尾：提议下一步修复方向，并询问是否继续（呼应 8.2/8.3）。
```

### 8.7 会话收尾清理

用户表示结束会话（「结束 / 收工 / 今天就到这里」这类）时，**主动问一句**：要不要清除本轮遗留的一次性测试脚本与临时产物？——既不静默留着，也不擅自删除，等用户确认后再动手。

- **先报清单再动手**：报出「将删 N 个一次性脚本 + M 个产物 + K 个临时目录（约 X MB）/ 保留 §六 资产」，用户同意才执行；
- **只清一次性**：临时排查/复现脚本、`node --check` 副本（`chk*.mjs`）、`.out/.log/.png/.txt` 产物、`commit_*.txt`、测试临时目录（`tmp*`/`naiba-*`）；
- **不动可复用资产**：§六 清单点名的检查器/冒烟/播种器/传输校验/数据工具，以及 `record_golden.py`、`backfill_media.py`、`media_markup_check.mjs`、`verify_doc_encoding.py` 等；
- **删除前先打包**：待删脚本压进 `verify/_archive/verify_scripts_<日期-时刻>.zip`，并写同名的 `deleted_manifest_<日期-时刻>.txt` 记录清单（**文件名必须带时刻**——同一天多次清理不能用同一个文件名，否则会覆盖上一次的备份）——可复用脚本已入库（§六「目录纪律」），一次性脚本不在 git 里，删掉无法回滚；
- **一键脚本**：`.venv\Scripts\python.exe verify\cleanup_verify.py`（默认 dry-run 只打印计划，加 `--apply` 才备份+删除；**保留集合 = `KEEP` 名单 ∪ `git ls-files verify`**——受版本控制即保留。2026-09-17 修的：此前只认 `KEEP`，实测漏登记 7 个受控脚本会被误删，其中 `_serve_tmp.py` 正是文档点名必留、且被 `tasks_panel_smoke.py` 依赖的公共 harness）。

### 8.8 路径纪律：禁止本机绝对路径

**凡是要提交进仓库的内容——文档、脚本、测试夹具、配置样例、Release body——一律不得出现本机的盘符/用户目录绝对路径**（形如 `D:\<某人>\项目名\…`、`C:\Users\<某人>\…`）。两条理由：① 其他维护者 clone 后**直接跑不起来**（本轮实测：`scripts/*.mjs` 与 `verify/*` 里 20+ 处硬编码路径，只能在本机跑；`upload_smoke.py` 还硬编码了本地访问口令）；② 会把本机用户名与目录结构**泄露进仓库**。

| 场景 | 正确写法 | 禁止 |
|---|---|---|
| 文档 / Release body / 提交信息 | 语义写法 `项目根\data\chat.db`、`%LOCALAPPDATA%\NaibaChat`；必须举例盘符时用省略或占位并注明是示例（§一.5） | 真实盘符路径 |
| Python 脚本 / 测试 | `Path(__file__).resolve().parents[N]` 派生项目根（`verify/x.py` → `parents[1]`） | `Path(r"…")` 字面量 |
| Node 脚本（`.mjs` / `.cjs`） | `path.resolve(path.dirname(fileURLToPath(import.meta.url)), '..')`（ESM）/ `__dirname`（CJS） | 盘符字面量 |
| 测试夹具 | 占位（`C:\Users\user\…`）或 `tmp_path` / `tempfile` | 真实用户名 |
| 运行产物（截图 / 日志 / 缓存） | 项目根下的 gitignore 目录或 `data/` | 绝对路径 |

**自检**：`git grep -n -E '[A-Za-z]:[\\/]'` 逐条人工判断（测试桩里的假路径如 `C:\a.png` 属正常）；本机指纹（如 `git grep -n '<用户名>'`）必须为 **0**。守门 `tests/test_no_absolute_paths.py` 会扫已入库的代码/配置类文件（`docs/` 下的手册生成物不在扫描范围内，其示例按 §一.5 处理）。

## 九、本项目维护要点（实践总结 · 血泪教训）

1. **先读后改**：编辑前确认调用方与既有约定；缩小改动面，不顺手重排文件；
2. **大块搬移/重构守则**：
   - 三步验收：① 完整未定义名扫描（AST 收集全部 Store 绑定）→ ② 全量 unittest → ③ 五点半烟（bootstrap 必测，只测 health 会漏 NameError 类装配故障）；
   - 顶层定义集 diff（搬出 == 目标 ∪ 剩余），防误搬/漏搬；
   - **禁"凭印象拼导入清单"**：优先原样复制源文件头部导入，新增依赖由扫描兜底（注意混合大小写命名）；
   - **改写型手术（方法→函数/正文变换）风险远高于整块搬移**：能整类搬移就整类搬；
3. **静态检查的盲区清单**（每类都发生过真实事故，均有固化检查器）：
   - 装饰器-绑定错配（`@staticmethod` + `self`）→ `tests/test_static_decorators.py`；
   - `self.app.` 替换的残留（含 `getattr(self.app, …)` 无点形式）→ 同守门测试；
   - 前端 **ESM import 只读绑定**（跨文件赋值）= `esm_cross_assign.py`；
   - 顶层求值序（TDZ）与正则/属性键误报 = `tdz_check.py` + 人工复核；
   - 词边界类启发式会误报（正则字面量/属性键/注释），报表中的每条候选都需人工确认；
4. **环境纪律**（本 DSH 沙箱特有；正常本机环境不需要）：
   - 测试需 `$env:TEMP=$env:TMP=$env:PYTHONPATH="项目根\verify"`（`verify/sitecustomize.py` 补丁：沙箱拒绝 chmod/mkdir(0o700)）；临时目录会落在 `verify/` 里，已被 gitignore 覆盖，收尾用 `verify/cleanup_verify.py` 清掉；
   - **PowerShell 内联 `python -c` 会破坏转义字符串（多次假警报根源）**——验证脚本一律写成 `verify/*.py`（或 `.cjs`）再执行；
   - 浏览器冒烟需放宽进程启动权限（沙箱拦 Edge spawn；`spawn EPERM` 时按规则原样重试并申请放宽，实测放宽后 Edge 可正常拉起、冒烟可跑）；
   - `verify/` 目录纪律：**默认整体 gitignore**（`verify/*`，临时产物/日志/截图/备份包一律不入库），但 **§六 清单里的可复用脚本（检查器 / 冒烟 / 播种器 / 传输校验 / 数据工具 / `cleanup_verify.py`）在 `.gitignore` 里逐条 `!` 放行并随仓库发布**——其他合作者 clone 后可直接调用，无需自己重写。新增可复用脚本时：① 加进 `verify/cleanup_verify.py` 的 `KEEP`；② 在 `.gitignore` 补一行 `!verify/<文件名>`；③ 在 §六 清单登记。脚本内**禁止硬编码本机绝对路径**（见 §8.8）——一律 `Path(__file__).resolve().parents[1]`（Python）或 `fileURLToPath(import.meta.url)` / `__dirname`（Node）派生项目根；
5. **每阶段收尾**：冻结版编译 + 用户实测（源码跑通 ≠ 分发形态跑通）；编译前确保无 naiba-chat 进程；
6. **契约先行**：任何事件/字段/键的新增先改 `contracts.py`/`messages.py`，再写使用方；
7. **用户侧工具**：Playwright 由用户本机安装（`npm install playwright`），脚本经 `NODE_PATH` 解析；
8. **golden 基线是行为等价的裁判**：行为变更必须重录并人工核查内容后固化；
9. **策略/判定禁止闭包捕获可变状态（工具系统重构实测教训）**：权限确认逻辑从引擎
   （`self.workspace` 随会话快照更新）搬到 def 级 policy 后，闭包误捕获**装配期配置工作区**，
   会话级工作区切换/绑定时工作区内子目录被误判越界。教训：**判定必须使用当前运行上下文
   （引擎传入参数），禁止闭包捕获可变状态**；守门 `tests/test_tool_registry_shape.py::
   test_policy_uses_engine_workspace_not_assembly_config`；
   **扩展（工作区判定修复，2026-09）**：① 判定与执行必须同源——policy 侧 `policy_workspace`
   与执行侧 `ctx_for_run` 都取 `run_context.workspace_dir`（Run 快照），只修一侧会出现
   "判定按新工作区、执行按旧工作区"；② 越界判定**不得**用"词法路径命中"放宽（工作区内
   链接指向外部必须仍判越界，需要信任时把真实目标显式加入允许根）；③ 大小写不是误判来源
   （Windows `relative_to` 天然不敏感），排查先看"工作区根是否同源"；
   守门 `tests/test_path_matrix.py`、`tests/test_tool_permissions.py::
   AutoRunWorkspaceSourceTests`/`DocumentsRunWorkspaceSourceTests`；
10. **协议分隔符必须与用户数据正交（NEED_CONFIRM 盘符冒号截断）**：协议以半角 `:` 分三段，
    Windows 盘符 `C:\…` 命中分隔，确认描述被截断为「读取工作区外路径：C」。golden 未暴露——
    录制/回放走同一路径只保行为等价，不保解析健壮性。教训：**含用户可输入文本/路径的协议段，
    生成端必须对分隔符做转义/归一**；守门 `tests/test_tool_executor.py::
    test_need_confirm_reason_survives_colon_split`；
11. **动态 HTML 事件禁内联 onclick 引用 ESM 函数（前端 `rejectTool is not defined`）**：确认卡
    按钮 `onclick="rejectTool(...)"` 在全局作用域查找模块内函数必然 ReferenceError。教训：
    **动态注入 HTML 的事件绑定一律 `data-*` + 模块内事件委托**（import 只读红线内，不做跨文件
    赋值）；本沙箱浏览器冒烟被拦（Edge spawn），此类回归靠静态审查 + 用户实测兜底；
12. **第三方协议字段形态必须实测确证（DeepSeek reasoning_text 回传血泪，三轮反转）**：
    Responses API 思考模式 + tools 必须回传历史轮次 reasoning，形态认知经历三次反转：
    - `reasoning_text` 字段（藏进 assistant 消息）→ 服务端不识别（400 must be passed back）；
    - `content` 为明文字符串 → serde 直接拒 **`invalid type: string, expected a sequence`**
      （09-07 用户 19:28 实测：明文形态第一轮就 400，彻底证伪"明文归并"说）；
    - `content` 为 `[{type:"reasoning_text",text:…}]` 块数组（**无 id**）→ 单轮工具链实测通过，
      但 **12 轮长工具链实测 400（must be passed back）**——单轮通过 ≠ 全场景通过；
    - 官方 API Schema（用户提供）：reasoning item 的 content 为 **`reasoning_text` 内容块列表**
      （"以明文承载思维链内容"），**输出 item 带唯一 id**（OpenAI 规范 input 侧 `reasoning.id` 必填）
      → 最终形态：**块数组 + 唯一 id**；id 优先服务端真实值（流式 `output_item.added` item.id /
      `reasoning_text.delta` 的 `item_id` 捕获；非流式从 output 提取），旧持久化历史无 id 时
      合成确定性 `rs_h_*`（sha1 内容前缀，跨轮字节稳定）。
    教训：**协议 wire 形态以"目标场景（多轮工具链）实测"为准，官方 Schema 是最终裁决者；
    每修一轮必须真机多轮复验（单轮通过不可信）**；守门 `tests/test_llm_protocols.py::`
    `test_responses_input_*`（块数组+id/合成兜底/无 CoT 占位/顺序/无 reasoning 不产 item）与
    `tests/test_llm_stream.py::test_read_sse_captures_reasoning_item_id`；
    **最终根因（09-07 19:56 payload 取证）**：服务端可能返回"无 CoT 的工具调用轮"
    （reasoning_content 为空），该类 assistant 消息若不回传 reasoning item，下一轮请求必然
    400——修复：**带 tool_calls 的 assistant 消息无条件产出 reasoning item**；失败 payload
    自动落盘（`naiba-model-error-payload.json`）是此类问题的取证通道。
    **09-10 再犯（用户「为什么这次又出现 400」）+ 真机矩阵定论**：当时的实现给空 CoT 轮回传
    `text: ""` 的 reasoning item，长链（7 轮工具调用）第 8 次请求又 400——**空文本同样被拒**。
    直接拿用户 config 里的 key 打真机（`verify/_probe_*.py` 手法，用完即删），4 轮工具链矩阵：
    | 该轮形态 | 结果 |
    |---|---|
    | 带 tool_calls，不回传 reasoning item | 400 |
    | 带 tool_calls，`text: ""` | 400 |
    | 带 tool_calls，`text: " "` 或占位文本 | 200 |
    | 无 tool_calls（普通回复轮），不传 / 空文本 | 200 |
    规则精确化：**只有"带 tool_calls 的轮"必须回传 reasoning item，且 text 必须非空**；
    无工具轮不受约束。修复：空 CoT 轮用常量占位 `NO_REASONING_PLACEHOLDER =
    "(no reasoning content)"`（常量串跨轮字节稳定、利前缀缓存），**合成 id 改用工具调用签名
    做 key**（占位文本对所有空 CoT 轮都一样，拿它做 key 会撞 id）。
    复验方式（本次）：用 `_responses_input()` 重建"7 轮 + 链尾空 CoT + 图片 + stream:true"
    的请求真机打过去，`200` 且收到 `response.completed`；单轮通过仍不可信，**必须复刻真实长链**。
    守门 `tests/test_llm_protocols.py::test_responses_input_reasoning_passback`（非空占位 +
    空 CoT 轮 id 逐轮唯一）。
13. **诊断输出纪律（windowed exe / 无 logging handler）**：应用未配置 logging handler 时
    `logger.info` 被静默吞掉；PyInstaller `runw` 无控制台、`stdout/stderr` 为 None。
    教训：**诊断一律写文件**（如 `<cwd>/naiba-model-debug.log`，进程 cwd 有不确定性，
    可加环境变量覆盖路径），且与用户约定好读取位置；`AllocConsole` 重定向方案实测不生效。
14. **golden 场景的占位符替换必须结构化（Tool 结果 JSON 假象教训）**：`<TMP>` 被直接
    `str.replace` 进 json 字符串时，Windows 路径的裸反斜杠（`\t`/`\.`）会破坏 JSON 合法性，
    使 `json.loads` 静默失败、脱敏路径永远不生效——**行为变更基线"未漂移"其实是假象**。
    教训：**含 JSON 字符串字段的测试数据，替换占位符前先解析、结构化替换后再 dumps**；
    基线重录前人工核查改动点确实发生（本项目 `tests/golden_replay.py` 的 `substitute` 已按此修正）。
15. **文档/交接文件禁止用 PowerShell 管道写（GBK/UTF-8 固化 mojibake 坑，两次事故）**：
    `Get-Content -Raw | ... | Set-Content -Encoding utf8` 在 zh-CN 控制台会把 UTF-8 内容
    按 ANSI(GBK) 解码后再写回，产出"乱码固化"文件，且 git diff 可能只显示少量行（被提交后难察觉）。
    教训：**含中文的仓库文件一律用 edit/write 工具或 Python（显式 `encoding="utf-8"`）写入**；
    用 write 工具（UTF-8 无 BOM）重写后提交，并用 python 读首字节抽查（`b"# \xe5"` 且
    `decode("utf-8")` 正常 = 无 BOM UTF-8）。
16. **反幻觉由"事实回环"承担，勿再加逐轮守卫（用户实测 + 全库取证教训）**：ComfyUI/任务
    描述类防幻觉守卫已整体移除——它在全库 253 会话中正确拦截 0 次、误判 2 次：把模型
    **复述历史真实 Job ID/连接证据**判为"本轮编造"，撤回回复 + role=user 校正消息冒充用户
    （模型误以为用户质疑）+ 自动 retry（界面"回复一闪、自动下一轮"、正常轮次被拖去执行真实提交）。
    教训：**模型无从编造成功**（ID/状态只能来自工具返回），编造会被 job_status/job_output/job_wait
    的"不存在/无从访问"事实戳穿——**先模型后工具的事实闭环就是防幻觉**；系统提示只承担
    "引用历史 ID 前先核实、未经验证的状态不得声称"的纪律法规，不做前置拦截
    （守门 `tests/test_anti_hallucination_guard.py`：守卫函数/校正文案必须不存在 + 法规存在）。
17. **契约键"只见 snapshot 不见 RunContext"的隐性断链（crop 落盘错位教训）**：会话工作区
    解析值在 run snapshot 里冻结了，但 **RunContext（工具层 ctx）没有 `workspace_dir` 键**——
    产物类工具（vision crop/pixel_diff）按 `ctx.get("workspace_dir")` 取会话工作区时永远拿到
    空值，静默退到程序默认工作区（`exe_dir/workspace`），用户实测 crop 产物落在
    `dist\workspace\.naiba-chat\` 隐蔽目录、模型还得绕道复制才能交付。
    教训：**跨层传递的上下文必须登记进 `RUN_CONTEXT_KEYS` 契约（先契约、再生产方写入、
    消费方读取），不得依赖"某处字典里有"的隐式假设**；守门
    `tests/test_contracts.py::test_run_context_default_factory_and_validation`（键集合一致性）。
18. **Windows 管道子进程输出编码必须双向对齐（中文乱码坑）**：PowerShell/Python 子进程在
    **管道（非控制台）**下默认按 locale 编码（GBK/ANSI；PS5.1 的 `$OutputEncoding` 甚至默认
    ASCII）输出，而父进程一律按 UTF-8 解码 → 中文路径/报错信息变 `�`（run_skill_script 实测：
    `can't find entry file C:\...\�临时提示词\...`，文件其实存在，纯显隐乱码）。
    教训：① **PS 命令**必须前缀 `POWERSHELL_UTF8_PREFIX`（`[Console]::OutputEncoding=$utf8` +
    `$OutputEncoding=$utf8`），pwsh 工具 / shell Job（jobs.py `_run_shell`）两处均已对齐；
    ② **Python 子进程**（run_skill_script、冻结版 `--run-skill-script` 隐藏入口）env 强制
    `PYTHONIOENCODING=utf-8` + `PYTHONUTF8=1`；③ argv 经 `CREATE_PROCESSW` 传参本就是
    Unicode，**真正的坑在 stdout/stderr 解码侧**；④ 排查时按"spawn 点清单"全量过一遍
    （core.py×2 / jobs.py / updater.py×2，git/Git for Windows 输出默认 UTF-8 可豁免）；
    ⑤ **env 不是万能**：冻结版（PyInstaller `runw`）下 `PYTHONIOENCODING` 未必生效，
    子进程仍按 locale(GBK) 输出——用户实测 `run_skill_script` 打印的
    `C:\...\临时提示词\...` 变 `C:\...\??ʱ??ʾ??\...`（ASCII 部分正常、只有中文坏，正是
    "子进程 GBK 输出 + 父进程 UTF-8 解码"的指纹）。处置：**隐藏入口显式
    `stream.reconfigure(encoding="utf-8", errors="replace")`**（`launcher._force_utf8_stdio`），
    源码路径子进程加 `-X utf8`；⑥ **乱码路径的次生伤害**：父进程把乱码路径当媒体产物，
    前端渲染成**破损图像**——采集侧因此增加"落地校验"（本地路径不存在即丢弃，URL 才保留），
    守门 `tests/test_media_collect.py::test_mojibake_path_is_dropped`；
    守门 `tests/test_tool_design_fixes.py::RunSkillScriptEncodingTests`（中文参数子进程往返）；
    ⑦ **把规范写进"发给 AI 的说明"**：宿主修好了 stdio，但 AI 现场写脚本时仍会犯同样的错，
    因此「安装 Skill / 编辑 Skill」两个按钮的用户消息末尾共用一份 `SKILL_SCRIPT_RULES`——
    脚本统一放 Skill 目录的 `scripts/` 子目录（`run_skill_script` 的 script 就是相对该目录的
    路径）、文件读写显式 `encoding="utf-8"`（并说明冻结版默认 GBK 的后果）、编辑入口还要求
    顺手修正旧脚本的裸 `open()`；守门 `tests/test_skill_prompts.py`（两处必须共用同一份文案）。

20. **multipart 上传三连坑（2026-09 上传系统优化实测教训）**：① `python_multipart.parse_options_header`
    返回的参数字典**键是 bytes**（`b'boundary'`/`b'filename'`），用 str 键取值为空 → 误报"缺少 boundary"；
    ② multipart header 的 filename 按 latin-1 字节流解析，**中文文件名（UTF-8 字节）被解成乱码**
    （`冒烟文档.pdf` → `xa3.pdf_`）——必须还原原始字节后再按 UTF-8 解码（失败回退原值）；
    ③ **请求体必须按 Content-Length 精确消费**：无界 `rfile.read(65536)` 读循环会与"发完体等待响应"
    的客户端死锁（HTTP/1.0 下客户端不关写端则服务端永远等不到 EOF）——冒烟 60s 超时抓获。
    教训：**流式协议先想清楚"何时停止消费"，再写 while 循环**；守门既有冒烟脚本
    （`verify/upload_smoke.py`：真上传/去重/删除/.part 残留）与 `tests/test_upload_system.py`；
21. **Provider 循环闭包陷阱（documents 域实测）**：`for spec in specs:` 循环内 `def _execute` 直接
    捕获循环变量 `impl`，**三个工具的 execute 全部绑定到最后一次迭代的实现**（测试抓到
    pdf_render_pages 报"region 格式无效"—执行的其实是 zoom——而不是空转）。教训：**循环内
    造闭包必须用工厂函数传参捕获**（`_make_execute(impl)`），或默认参数绑定；守门
    `tests/test_pdf_tools.py`（每个工具独立执行语义断言）；
22. **PyMuPDF Pixmap.save 无 `format` 参数、按扩展名推断格式**：临时文件名 `*.png.part` 保存报
    "Image format part not in (...)"——用 `pix.save(path, output="png")` 显式指定；
23. **工具描述守门细则（test_tool_description_slim）**：模型可见描述 ≤100 字、≤3 句、禁内部术语
    （Harness/ToolRegistry/ToolSpec/宿主）与跨工具编排句式（先用/再调用/随后用…）——**编排规则
    一律放系统提示（常驻段，或**按会话固化工具集条件注入**的段，如「图片处理策略」仅在
    `vision_analyze` 到齐时出现、「PDF 处理策略」仅在 `read_pdf` 到齐时出现——缺工具就不提，
    避免让模型去调不存在的工具），描述只答"干什么/何时用/关键
    参数"。新增工具未过此守门会被整库回归拦下（2026-09 read_pdf 131 字/「宿主」两连拒）。
    **2026-09-20 复审（`subagent` 成本提示）**：给 `subagent` 描述补成本规则时撑到 **256 字 / 4 句**、
    两例直接红。按本条既定分工处理：描述只留一句钩子（`成本随父会话历史重发而放大，简单任务不要开。`
    ⇒ 全描述 98 字 / 3 句），完整计费形态（缓存命中按缓存价 vs 无缓存/预扣费中继全价重放）与两条硬约束
    （简单任务不要开、单父最多 4 个且不可派生）搬到 `naiba/skills/agent.py` 的系统提示常驻区，新增
    `if "subagent" in allowed:` 条件注入块（与 `register_mcp` / `comfyui_batch` 同款写法；
    子 Agent 自己的工具集里没有 `subagent`，所以不会重复注入）。
    **踩到的坑（可复用）**：改完只跑了 `-k subagent`（4 例 OK）就以为过——而本守门的测试名里
    **没有** `subagent` 字样（方法名是 `test_descriptions_within_budget` 一类，工具名只是 `subTest`），
    定向筛选**根本筛不到它**。**凡动 `registry.py` 的 `description=`，直接跑
    `tests.test_tool_description_slim`（或全量），不要用工具名筛。**
24. **给模型看的截断一律带标记（静默截断 = 幻觉误导源，多图批/附件实测教训）**：`_extract_step_images`
    曾 `return parts[:4]`——两个 PDF 各 5 页页图时每批第 5 页**静默消失**，且两批注入消息文本完全相同，
    模型（DeepSeek）推理出"只显示了第一批图、第二批图片应该已加载"的自相矛盾（浪费多轮自愈）。
    教训：**任何"给模型看"的截断必须携带"共 N 张/已展示前 M 张/其余可继续"标注**；多批内容
    必须带批次序号（「图片批 1/2」），"静默缺省"会让模型误判数据不完整；且一次注入的图片
    数量上限 4 张（多模态模型对多图注意力衰减是业界共识）；守门
    `tests/test_vision_batch.py`（分批/元数据/不静默）。

19. **冒烟/测试 server 忘记 kill = 端口占用 + 串库事故（实测：用户"历史记录全消失"虚惊）**：
    源码 `python server.py`（后台 job）连的是**源码开发库**（`项目根\data\chat.db`），
    而冻结版 exe 连的是 `%LOCALAPPDATA%\NaibaChat\data\chat.db`（用户真实数据）。
    冒烟 server 漏 kill（持续运行）→ 它占着 8765——用户启动 exe 时连到
    **错误服务**：界面显示的是源码库里那个空会话（标题 `????`、0 消息），用户以为
    "所有历史记录不显示"；实际用户库（281 会话）完好无损。
    教训：① **每个后台 server 启动必须在当轮 kill 并验证**（`job_kill` + 确认
    `Get-NetTCPConnection -LocalPort 8765` 无监听者、进程消失）；② 排查串库时
    **先看服务连的是哪个库**（数据目录 = PathContext 语义：frozen→LOCALAPPDATA、
    源码→仓库 data/；两者内容可能天差地别），不要先怀疑数据库损坏；③ 判"数据丢失"
    前先查真实数据目录副本（复制 chat.db 只读查询 conversations/messages 计数）；
    ④ 编译前的 `kill naiba-chat` 只处理 naiba-chat 进程，**不覆盖源码 server**（Python 进程）；
    ⑤ 用 `--port` 起冒烟服务会**把端口写回 `config.json`**（`main_entry` 保存 host/port），
    收尾必须还原 port 并删除 `data/server.json` 残档；
    ⑥ **改配置的临时脚本，还原必须覆盖「改写之后的全部代码路径」**：`verify/branch_first_turn_smoke.py`
    首次运行把 `config.json` 的 port/data_dir 改成 8795 + 临时目录后，`seed()` 却写在
    try/finally 之外——播种一抛异常（`sys.path` 少项目根）就把开发配置留在临时值上，
    源码 server 之后会连向一个已被删除的临时数据目录（正是 ① 的"看起来像历史丢失"）。
    教训：**改配置与还原之间不夹任何可能抛异常的步骤**，或把播种也纳入 finally 覆盖范围；
    排查源码 server "读不到历史"时先看 `config.json` 的 `data_dir`/`port` 是否被临时改过；
    ⑦ **kill 要杀整棵进程树，且跑冒烟前先探端口**：Windows 上 `Popen.terminate()` 只杀直接子进程，
    `.venv\Scripts\python.exe` 派生的孙进程仍占着端口——"已退出"是假象，下一轮要么直接失败、
    要么打到旧进程上；收尾一律 `taskkill /PID <pid> /T /F`（POSIX 侧 `start_new_session=True` +
    `os.killpg`）。前置校验同样关键：**端口已被占用就直接失败退出**——残留 server 会接管
    `/api/health`，让"等就绪"通过，整轮冒烟其实在对着旧代码说话（假绿）；等就绪循环里还要
    同时 `poll()` 自己起的进程有没有提前退出（端口被占时它会立刻退出）。参考实现
    `verify/update_version_smoke.py`。

25. **前端"可用性判定"与后端"放行判定"必须同源同口径（发送按钮教训）**：原实现里发送按钮的
    `disabled/title` 有**两个写入点**（`setBusy` 与 `updateContextComposerLock`），而"能否发送"
    的真实判定却在 `sendChatMessage` 里以 `if (!text) return` 静默丢弃——空输入时按钮可点、
    点了没反应；后端 `submit_chat` 又要求 `message` 非空。三者口径不一致时，任何一处改动都会
    造出"按钮能点但无响应"或"前端放行、后端 400"的错位。
    教训：① 一个控件只留**一个写入点**（现为 `updateSendButtonState`，其余调用点只负责触发刷新）；
    ② 放行判定在前后端**同一口径**（文字或可用附件至少有一个）并各自守门测试；
    ③ 模型可见文本用**共享函数**（`compose_user_content`）生成，避免"当前轮"与"历史重放"
    两套拼接漂移（前缀缓存与逐字节一致依赖它）；④ UI 类改动必须真机浏览器验证
    （`verify/send_button_smoke.cjs`：断言 `disabled` 与 `getComputedStyle` 背景色，
    单靠源码审查抓不到"颜色没变"这类回归）；⑤ **程序化改输入框不会触发 `input` 事件**——
    快捷消息、`/` 技能引用、`@` 引用、选择按钮回填、编辑重填都是直接写 `#messageInput.value`，
    发送按钮因此不会刷新（用户实测：快捷消息插入后按钮仍是灰的）。凡是代码写值的地方，
    改完必须调 `01-core.notifyComposerChanged(input)` 派发一次合成 `input` 事件，让输入管线
    （resize/镜像/两个弹层/发送按钮）统一处理；真机守门 `verify/send_state_smoke.cjs`。

26. **用户输入里的"引用"必须由后端校验存在性与边界（@ 引用设计教训）**：`@` 在用户文本里
    天然有歧义——邮箱 `a@b.com`、普通 @ 提及、工作区外的路径都可能出现。把"解析成绝对路径"
    放在前端（凭 token→路径映射表替换）会引入三类风险：映射表与会话工作区脱钩（切工作区后
    相对路径指向别处）、分支/编辑重发时映射丢失（token 原样进模型）、以及无法校验边界。
    教训：① **前端只负责"插入 token 与展示"，解析交给后端**（`resolve_file_references` 在
    `submit_chat` 落库前执行）——工作区根、存在性、越界判定都在后端一处收口，分支/编辑/
    其它客户端天然一致；② **解析失败必须原样保留 token**（不报错、不静默替换），邮箱与
    不存在的路径因此无害；③ token 语法要能表达"目录"（尾 `/`）与"含空格的路径"（`@"…"`），
    目录结果保留尾分隔符，模型才分得清"文件"与"整个目录"；④ 展示与模型可见文本分离：
    `display_content` 保留 `@相对路径`（气泡高亮、可读），`messages.content` 落解析后的
    绝对路径（历史回放与当前轮一致），首轮标题另取用户原文（`create_chat_run(title_text=…)`），
    否则标题会变成一串绝对路径；⑤ 弹层浏览复用同一份目录语义（目录优先、隐藏项、越界拒绝），
    并**按目录懒加载**——一次性递归整个工作区在真实项目里会卡死。

27. **祖先 overflow 会裁剪绝对定位弹层（快捷消息面板实测）**：面板最初照抄 meta 行内联弹层的
    `position: absolute; bottom: 30px` 挂在 meta 行内，短面板正常，条目一多就**被
    `.composer-wrap { overflow-y: auto }` 裁掉上半部分**——元素仍"可见"（Playwright 也认为
    可点），但实际点击落在被裁剪区域外的 `#messages` 上，表现为"按钮点不动"。
    教训：① 高度不可控的弹层一律 **`position: fixed` + JS 按触发按钮定位**（本项目
    `@`/Skill 弹层与 `positionComposerPopup` 就是这一套路），fixed 元素的包含块是视口，
    不再受祖先 overflow 影响（祖先若有 transform/filter 则会失效，需留意）；
    ② 定位函数必须**夹在视口内**（优先向上展开、贴边时下翻），并挂 resize/scroll 重定位；
    ③ **异步刷新列表前先渲染加载态**：面板每次打开都重新取排序结果，若沿用上一轮的
     DOM，会先闪一帧旧顺序（既是视觉瑕疵，也让"读 DOM 断言顺序"的冒烟测试出现假失败）；
    ④ 冒烟脚本清理数据要**按 index 倒序删除**——正序循环删除会因索引左移误删/漏删，
    残留数据会污染下一次运行（本轮真实踩到，排查了半天）；
    ⑤ **同一坑第二次踩到**：思考强度列表原为 `position: absolute; bottom: 42px`（贴在按钮
    上方，仍在 `.composer-wrap` 盒内所以没事），加上"按钮下方显示等级"后列表被顶出盒子 →
    立刻被 overflow 裁掉、点击落到 `#messages`（Playwright 报 "intercepts pointer events"）。
    处置与①一致：挂到 `body` + `position: fixed` + `positionReasoningMenu()` 夹在视口内。
    教训：**弹层位置一改就要重估祖先 overflow**，别假设"原来能用就永远能用"。

28. **"同一份数据两个入口"= 功能重复与互相污染（快捷消息血泪）**：快捷消息面板最初直接复用
    开始新对话页的「自定义指令」列表（`starter_prompts`），结果**开始页的两条用户指令直接
    出现在会话面板里**，还被我误记了使用次数——用户一眼看出"这就是功能重复"。两个入口看似
    都是"常用提示词"，但语义不同：开始页是**新对话的开场指令**，快捷消息是**会话中的插入
    模板**，生命周期与展示位都不同。教训：① 新增入口前先问"**是不是同一件事**"，而不是
    "能不能共用一份数据"——共用只有在两处语义完全等价时才成立；② 共享一旦出错，代价是
    **用户数据被另一功能改写**（使用次数/排序），比 UI 重复严重得多；③ 拆开后各自的字段
    形状也应各随其需（开始页保持 `{title,text}`，快捷消息带 `index/count/added_at/used_at`），
    **不要为了复用把两边都塞成超集**；④ 守门测试要显式断言**互不干扰**（增删一边后另一边
    不变、开始页条目不含统计字段），而不只是各自的行为。

29. **派生文件名必须由同一基准推导（缩略图命名血泪）**：`store_uploaded_file` 给主图加
    `naiba_chat_<ts>_<rand>_` 前缀，缩略图却按**原始文件名**命名（`灯箱A_thumb.webp`），
    于是三处按 `<主图 stem>_thumb.webp` 推导的逻辑全部失配：① 前端缩略图兜底 404
    （控制台报错、图裂）；② 去重复用路径 `_thumb_path_for` 返回空 → 复用方拿不到缩略图；
    ③ `remove_uploaded_file` 删不掉缩略图（缓存残留，统计/清理口径失真）。
    教训：**同一实体的多个派生文件必须共用同一个命名基准**（本项目统一为"主图 stem +
    固定后缀"），并在写入处就按最终落盘名生成——不要用"用户原始名/入参名"当基准；
    新增派生文件时把**所有消费方**（前端兜底、去重、删除、清理、统计）列一遍再定名；
    守门断言要覆盖"重复上传命中去重后仍返回同一份缩略图"（本轮 `test_upload_system` 补上）。

30. **按工具名硬编码的集合会与声明表各写各的——且"清退役名"要分清两类集合**：工具单入口
    重构后，几处按名字硬编码的集合没人跟着改：`attachments.ENUMERATION_TOOLS` 里长期躺着
    `glob_files`（已并入 list_directory）、`glob`（已退役）、`find_files`（**从未存在**）；
    而 `run/session.py` 的视觉工具集反过来**只剩 8 个退役名、漏掉了
    `vision_analyze/vision_image_ops`**。教训：
    ① **区分两类集合**——「当前轮次分发」用（枚举门禁、allowed_tools 系统集）必须只含声明表
    里的活名；「历史重放」用（`CONTENT_READ_TOOLS`、`model_visible_result` 的视觉分支）
    **必须保留退役名**，因为旧会话落库的 `metadata.tool_runs` 里就有它们，删掉会让老会话内容
    在重放时静默消失——本轮先删后 golden `history_images` 立刻漂移，已回滚并加注释；
    ② 工具新增/退役时**全局搜索旧名**，逐处判断属于哪一类，不要一把梭；
    ③ 守门把两类都钉死：`test_tool_registry_shape::ToolNameSetFreshnessTests` 既反查
    "当前集合 ⊆ 声明表"，也显式断言"重放集合保留 vision_read_folder"，避免后人再把兼容名
    当遗留清掉；
    ④ **扩展名/媒体名单同理，且已经出过事故**：前端 `FILE_CHIP_MEDIA_EXTS` 比后端
    `MEDIA_PRODUCT_EXTS` 多算了 `.bmp`/`.svg`——前端把它们从「修改文件」里滤掉、后端又不当
    媒体，`.bmp`/`.svg` 产物在气泡里**两头都不显示**（静默消失）；`.m4v` 前后端都当视频、
    却漏了 MIME 兜底（可能不播）。处置：名单收归 `core/media_types.py` 唯一来源，后端各处
    import、前端经 `/api/bootstrap.media_exts` 取同一份，媒体判定统一走前端 `mediaKind()`；
    工具侧「是否显示/如何提取媒体」改为 `metadata["media"]` 声明（policy/extract），
    **不再按工具名硬编码集合**。守门 `tests/test_media_declarations.py`。

31. **从工具文本结果里取路径/URL 必须分别做边界处理（两次真实事故）**：
    ① **Windows 路径的散文后缀**：`write_file` 返回 `已写入 C:\...\a.png（12 字符）`，用
    `[^\r\n"']+` 抓盘符路径会把中文括号与字数一起吞进"路径"，末尾扩展名判定失败 → 这张图
    既没有"修改文件"chip（媒体名被过滤）、也没有媒体卡，产物彻底不可见（与 §九.30④ 的
    `.bmp` 隐形同属"两头都不显示"）；
    ② **URL 的查询串**：`job_wait` 返回 `Job 已完成。{…"files":["http://127.0.0.1:8188/view?filename=a.png&subfolder=&type=output"]}`，
    若把 `?` 也当散文终止符，URL 会被截成 `…/view` → 判定非媒体 → **Job 跑完图片不显示**
    （用户实测：任务完成、图片"不见了"）。
    教训：**URL 与路径的边界规则必须分开**——URL 只去尾部标点、绝不按内部标点截断（`?&=` 是它的一部分），
    并以"首个 CJK 字符"为结束边界（`见 https://x/a.png，谢谢`）；路径则在散文终止符处截断，
    但终止符不含 `:`（盘符）与空格（路径可含空格）。另：**能用结构化结果就别用文本扫描**
    （采集按声明分流 `extract=structured`/`scan`）。守门
    `tests/test_media_collect.py::TextSourceBoundaryTests`（含真实 `job_wait` 结果回归）；
    存量消息可用 `verify/backfill_media.py`（默认 dry-run）按修复后口径补提取。

32. **跨轮次的状态变化先复用既有轮询，别急着加事件通道（Job 产物写回教训）**：异步 Job 在
    Run 已经结束**之后**才产出媒体，直觉是"新加一个事件 type 推给前端"，但前端早已停止
    轮询 `/api/runs/{id}/events`——新事件要么得另开一条长连接，要么得让前端重新订阅，
    改动面与失败面都不小。实际做法：写回时**只推进 `conversations.updated_at`**，
    前端既有的 `syncCurrentConversation`（每 1.8s/10s 比较 `updated_at|消息数|末条`）与
    `loadTasks`（任务终态 → 主动同步当前会话）**自动完成重渲染**——零新增契约、
    零新增通道，真机断言（`verify/p5_writeback_smoke.cjs`）验证"不刷新页面即出现"。
    教训：**先问"现有通道能不能感知"，再考虑加通道**；顺带记住 `conversationSnapshot`
    只看 `updated_at`/条数/末条——**就地修改已有消息必须推进 `updated_at`**，否则前端永远
    发现不了（本项目的 `update_message_metadata` 就承担这件事）。

33. **缓存键必须是"内容"，不能是"来源"（ComfyUI 复用文件名 → 显示旧图）**：ComfyUI 每次
    生成都用同一个文件名（`lumine_cute_00002_.png` 被新图覆盖），`/view?filename=…` 的 URL
    因此**完全不变**。媒体缓存最初以"来源字符串的哈希"为键（`sha256(source)[:16]_<name>`），
    于是第二次生成命中了上一次的缓存文件 → 用户看到的**是过去那张图**（实测："Job 刚跑完，
    显示的却是旧图"）。教训：**只要来源不是内容寻址（文件名复用、路径被覆盖、URL 指代可变
    资源），缓存键就必须落在内容上**——流式读取来源、边算 sha256 边写临时文件、原子改名
    `<内容哈希16>_<name>`；同内容复用、内容变化自然落到新文件（顺带让 `/api/file` 的
    `max-age` 浏览器缓存不会命中旧内容）。守门
    `tests/test_media_collect.py::ContentAddressedCacheTests`（同名同路径改内容 → 不得复用；
    重复引用未变内容 → 复用同一份）。

34. **虚拟列表的滚动上限必须取浏览器真实 `scrollHeight`，不能自己拿行高求和（滚轮"滚不动"
    的真根因）**：侧栏虚拟化把可视行渲染进 `.sidebar-virtual`（高度 = 行高之和
    `sidebarTotalH`），滚动时用 `sidebarTotalH - clientHeight` 作为 `scrollTop` 的上限。
    但滚动容器 `.conversation-list` 有 `padding: 4px 8px 12px`——**内容高度比
    `sidebarTotalH` 多 16px**，于是每次滚动都被"夹紧"回一个偏小的值再回写：表现为
    **滚轮一格只走几像素、越滚越慢、列表底部永远差 16px**（用户实测："滚轮翻动速度太慢"）。
    教训：**滚动边界只有浏览器知道**——上限一律用 `tree.scrollHeight - tree.clientHeight`
    （自动含 padding/border），自己算的行高之和只配用来定位窗口起点；另外这类"每帧重写
    innerHTML"的容器要 `overflow-anchor: none`，否则 Chrome 的滚动锚定会与重绘互相拉扯。
    守门 `tests/test_sidebar_favorites.py::test_sidebar_scroll_clamp_uses_real_scroll_height`
    + 浏览器冒烟实测"一格 ≈150px、触控板小步进不加速"。

35. **键盘事件要挂在能收到它的元素上（Esc 关不掉菜单）**：会话条目「⋯」菜单最初把
    `keydown` 监听挂在菜单元素上，但用户点 ⋯ 后焦点留在按钮/body 上，**菜单自己收不到
    键盘事件**，Esc 形同虚设（冒烟实测 FAIL）。教训：**浮层的"全局"关闭键一律挂 document**
    （`if (menu.hidden) return` 守卫即可），只有"菜单内导航"这类必须菜单持有焦点的行为
    才挂菜单本身。同理：点击外部关闭要排除触发按钮本身，否则"点按钮开→立刻被外部点击关"。

36. **虚拟列表里"hover 改样式"必须不碰布局（侧栏闪烁根因）**：工作区分组表头的删除按钮
    原本 `display: none → inline-grid`，hover 时出现一个 20px 高的按钮，把表头从 28px 撑到
    32px。虚拟列表的行高是**启动时量一次**并缓存进偏移表的，实际高度一变，窗口定位与真实
    内容就错位——表现为**鼠标划过时整列抖动/闪烁**（冒烟实测 `28 → 32`）。教训：虚拟列表
    里所有 hover/选中态**只能改颜色、透明度、visibility**，绝不改 display/尺寸/内外边距；
    需要"占位但不显示"就用 `visibility` 或绝对定位（本项目 `×` 按钮改绝对定位 + 常驻
    `padding-right` 预留位置）。守门 `sidebar_favorites_smoke.cjs` 的"悬浮高度不变"两条。

37. **每帧重写 DOM 是滚动迟滞的主因——虚拟列表只在"行区间变化"时重绘**：侧栏滚动监听
    原先每帧 `innerHTML = 窗口切片`（20 帧滚动 = 20 次解析 + 整棵子树布局），实测手感发滞。
    行高固定时，视口内滚过一行才需要换窗口（34px/行），因此缓存 `{start, end, totalH}`
    三者不变就只校正 `scrollTop`、不动 DOM——实测 160px 滚动从 20 次重绘降到 ≤8 次。
    注意：`renderSidebar` 重建行缓存后必须 `force: true` 强制重绘（索引可能恰好相同而内容已变）。

38. **"把当前项滚到最上"是反模式——定位用最小滚动（点一下就整片重排，用户找不回位置）**：
    侧栏每次 `openConversation` 都 `st = offsets[activeIndex]`，把被点的会话顶到列表第一行；
    用户明明是在列表中部点的，结果整片内容跳走、上下文全丢（实测反馈："视觉定位上造成了
    极大困难"）。教训：**"滚动到目标"必须区分"已可见"与"不可见"**——已可见就一动不动；
    不可见才用最小位移把它带进视口（贴边即可，不必居中、更不必置顶）。本项目实现为
    `sidebarScrollForActive(rows, offsets, currentScrollTop, viewHeight)`（同一会话可能在
    工作区与「已收藏」各有一份 → 取离视口最近的那一份）。顺带：入口重复也要清理——
    顶栏「新会话」与每个工作区的「＋ 新会话」功能重复，删前者并给"空列表"留兜底按钮。

39. **点击判定区必须覆盖整个视觉条目（条目边缘点不动 = 体验割裂）**：会话条目是 flex 行，只有中间的文字按钮绑了点击（`event.target.closest('.conversation-open')`），而按钮高度
    （padding 6px + 行高）小于行高 34px——鼠标落在条目上下边缘那 2~3px 时**既不变手型也不切换**
    （用户实测："此时鼠标没有从指针变成手"）。教训：**列表项这类"整行即一个目标"的交互，
    点击判定与光标必须落在条目容器上**（`onSidebarTreeClick` 对 `.conversation-item` 内任意
    位置都触发；`.conversation-item { cursor: pointer }`），按钮只负责键盘可达性；子按钮
    （收藏/⋯）用 `[data-action]` 分支先行拦截并 return。同理：单个按钮不要独占一行
    （收起按钮并入「工作区」操作行）。

40. **状态指示只要一个点，且状态必须由"唯一出口"驱动**：侧栏底部原本有两处状态——
    「● 服务已连接」文字块 + 「手机访问」行前的小圆点，语义重复且文字意味不明。改为**只留
    一个点**（并入地址行，`#serverDot`），并把状态切换收进 `01-core.js` 的 `setServerStatus()`：
    `api()` 在 **fetch 网络失败**时点红、**成功**时点绿（HTTP 4xx/5xx 说明服务在线 → 仍绿）。
    教训：①**状态灯要在唯一的请求出口上驱动**，否则各调用点各写各的、必然漂移；②指示灯必须
    独立于所在按钮的禁用态（地址按钮未开手机访问时是 disabled，用 `.address-button:disabled
    { opacity: 1 }` 保住圆点、只调暗文字）；③顺带修掉"服务端不可用时点击会话弹 PROMISE REJECT
    调试条"——UI 事件里的 Promise 必须 `.catch()` 成 toast，调试条只应留给真正的编码错误
    （它还会挡住点击，冒烟因此暴露）。

41. **"侧栏收起"只该改侧栏占位，不该改内容列宽（收起后整体错位）**：`.app-shell.sidebar-collapsed`    原本除了 `--sidebar-col: 0px` 还把 `--chat-max` 从 880px 改成 `min(1400px, calc(100% - 240px))`。
    收起后对话列从 880 跳到 ~1300px（实测 1549 视口：助手块左缘 530→179、用户气泡右缘
    1341→1419），观感就是"助手左移、用户右移、整体不在中间还错开"。教训：**折叠类布局切换
    只应改变被折叠区域的占位，内容列宽/列内相对布局必须恒定**（收起=把同一列在更宽区域里
    重新居中）；守门 `sidebar_favorites_smoke.cjs` 断言"收起后 `--chat-max` 仍 880px 且
    `用户气泡右缘 − 助手块左缘` 的跨度不变（±2px）"。

42. **grid/flex 的默认列宽会被"不可断行的长文本"撑破（附件列表实测）**：待发送附件列表
    改成竖直列表后，`.pending-files { display: grid }` 的默认 `auto` 列由**最长的文件名**
    决定宽度——实测行宽 1338px > 容器 870px：文件名不截断、整列横向溢出、每行都被撑到
    同宽。教训：**凡是"一行 + 可截断文本"的容器，网格列必须写 `minmax(0, 1fr)`**
    （flex 同理要给文本 `min-width: 0`），否则 `overflow: hidden / text-overflow: ellipsis`
    永远不会生效。守门 `tests/test_pending_files.py` + `pending_files_smoke.cjs`（真上传
    超长文件名，断言 `scrollWidth > clientWidth` 且 `title` 保留全名）。

43. **从弹层搬回页面的按钮必须补齐样式上下文（裸 SVG 渲染成黑块）**：顶栏「刷新」「卸载模型」
    原本在「⋯」菜单里，SVG 的线性描边样式写在 `.topbar-overflow-menu svg` 上；搬回
    `.topbar-actions` 后该规则不再命中，**SVG 用默认 `fill: black` 渲染成一团黑块、图标与
    文字还因按钮不是 flex 而换行**（截图才发现，冒烟只断言"按钮存在"是抓不到的）。教训：
    **移动 DOM 位置时要连"它依赖的样式上下文"一起看**；容器级兜底样式（本项目
    `.topbar-actions .control-button svg { fill:none; stroke:currentColor }` +
    `display: inline-flex`）比逐按钮写更稳。同理：**一个按钮只写一次标签**——计数只填数字
    （按钮自带「Skill」文字），状态灯只用颜色（文字恒为「MCP」，状态进 `title`）。

44. **顶栏按钮不能被压缩、也不能靠"隐藏文字"省空间（竖排文字 + 裸数字）**：`.topbar-actions`    默认 `flex: 0 1 auto` 会被顶栏压缩，窗口一窄按钮就变成竖排文字（实测「卸载模型」「刷新」
    竖着排、「文件」拆成两行）；而 761–1100px 的媒体查询又把「任务 / Skill」的文字标签
    `display: none` 掉，只剩裸「0」「6」——两种做法都让顶栏不可读。教训：**操作区一律
    `flex: none` + 子项 `flex: none` + 按钮 `white-space: nowrap`**，宽度压力交给可压缩的
    模型/Agent 下拉框；**宁可让按钮整行换行，也不要隐藏文字标签**。另外同一排按钮的默认/hover
    样式必须同源——文件面板按钮曾单独用侧栏深色底（`--sidebar`），在浅色主题里格格不入、
    hover 也跟别人不一样，已回归 `.control-button` 通用样式。守门
    `tests/test_topbar.py` + `topbar_smoke.cjs`（含 900/640px 窄窗口单行断言与 hover 同款断言）。

45. **HTML 里绝不允许重复 id（`$('#x')` 只命中第一个，另一处会被悄悄写坏）**：顶栏任务徽标与
    「设置 → 数据与迁移 → 历史数据管理」的运行记录**都叫 `id="taskCount"`**，
    `loadStorageStats()` 打开设置页就 `$('#taskCount').textContent = '0 条（已结束 0）'`
    ——写的是顶栏那个（文档顺序靠前），于是顶栏任务按钮变成「0 条（已结束 0） 任务」
    （用户截图报障）。修法：设置页改用 `storageTaskCount` 并同步读取点。教训：**改 UI 时
    顺手扫一遍 `index.html` 的重复 id**（本文件 264 个 id 只有这一处重复，脚本 `verify/dup_ids.py`
    可秒查）；守门 `tests/test_topbar.py::test_index_html_has_no_duplicate_ids` +
    `topbar_smoke.cjs`（打开设置页后断言顶栏计数仍是纯数字）。

46. **把常驻表单搬进弹层：关闭路径必须收敛到一个函数（API 供应商卡片化实测）**：供应商页从
    「下拉 + 添加/删除按钮 + 常驻表单」改为卡片网格 + 点开弹层后，表单整体搬进第二个顶层
    `<dialog id="providerDialog">`（与设置页 dialog 并列；原生 top layer 支持叠加，Esc 只关
    最上层）。真正的坑不在嵌套，而在**状态残留**：`state.providerEditing` 既表示「表单可编辑」，
    又被 tab 切换与 `addProvider` 当门禁，只要有一条关闭路径忘了复位，关掉弹层后点
    「本地 API」就会被门禁静默拦住（按钮点了没反应、无任何报错）。
    教训：**弹层关闭的每条路径（Esc、右上角关闭、取消、保存成功）必须收敛到同一个复位函数**
    （本项目 `cancelProviderEdit`：清定时器 → 复位 `providerEditing` →
    `dialog.close()` → 重绘卡片），并挂 `#providerDialog` 的 `close` 事件兜住所有原生关闭方式；
    守门 `tests/test_provider_cards.py`。

47. **冒烟脚本两条纪律：`fetch` 不序列化对象、断言不能靠固定 sleep**：本轮冒烟的两处假失败
    都出在脚本自身——① `fetch(url, { body: {...} })` 会把对象发成 `"[object Object]"`
    （播种静默失败、卡片数为 0，看着像前端 bug），**传 JSON 必须 `JSON.stringify`**；
    ② 保存/删除是异步收尾（`saveProvider` 在缺上下文参数时会先探测模型端点，不可达端点要走
    重试退避），固定 `waitForTimeout(1200)` 会抢跑，断言到「弹层还开着」。
    教训：**真机冒烟里凡「等待某状态出现」一律写带超时的轮询**，不要用固定睡眠；两条已固化进
    `verify/provider_cards_smoke.cjs` 的 `apiJson`/`waitForClosed`/`waitForCardCount`。
    ③ **别手写 base64 当测试图片**：头像冒烟里我硬编码了一段 base64 PNG，实际是坏图 →
    后端 400、断言误报"头像没换"，排查后改为**用页面 canvas 现造 PNG 再
    `setInputFiles({name, mimeType, buffer})`**——测试素材要么真造，要么从磁盘读。
    ④ **显式平滑滚动会拖长滚动过程**：`.messages` 默认瞬时定位（见 §九.56），但**用户主动跳转仍是平滑的**
    ——刻度轨点击在自己的调用点显式传 `behavior:'smooth'`。脚本里点完刻度立刻断言「悬停弹窗显示」会被
    这段动画期间的滚动事件收掉弹窗、误报"弹窗不显示"。处置：断言前等滚动结束，
    或程序化滚动前临时把 `container.style.scrollBehavior = 'auto'`。

48. **下线"内置"实体只删定义不够——遗留标记会让前后端口径分裂（内置 Agent 退役实测）**：
    把四个内置 Agent 从 `built_in_agents()` 移除后，用户配置里**曾经编辑过**的副本仍然带着
    `built_in: true`（本项目实测：`%LOCALAPPDATA%\NaibaChat\config.json` 里就躺着
    `dsh-standard` + `built_in: true`）。后果是前端继续显示「内置」徽标并隐藏 ×，而后端
    `built_in_agent_ids()` 已为空、`delete_agent()` 早已放行——用户看到的是"删不掉"，
    日志里却什么都没发生。教训：**退役任何带"内置/受保护"标记的实体时，必须同时清理用户
    数据里的遗留标记**（本项目 `_migrate_agent_builtin_flags()`：只摘 `built_in`，名称/
    提示词/工具集原样保留，用户内容不丢），并让前后端判定回到同一份清单
    （`built_in_agent_ids()`）；守门 `tests/test_agent_cards.py::BuiltInAgentsRetiredTests`
    + 真机冒烟 `verify/agent_cards_smoke.cjs`（播种遗留副本 → 断言无「内置」徽标、有 ×、可删）。

49. **网格 `auto` 行高会把滚动容器裁成"表头高度"（固定 Skill / 工具集点不开的根因）**：
    Agent 弹层把表单字段区做成 `display: grid`，其中 `.agent-skills` 内部是
    `.skill-list` / `.agent-tool-scope` 两个 `overflow: auto` 的滚动容器。`auto` 行的尺寸取
    **最小内容贡献**，而滚动容器的最小内容高度是 0 → 行高只剩表头（实测 70.75px），
    列表内容（220px / 320px）被父级 `overflow: hidden` 整块裁掉——**既看不见也点不到**
    （用户报"固定 skill 和工具集无法展开选择"）。
    教训：**网格里放"可滚动子块"必须显式 `grid-auto-rows: max-content`**（或让子块
    `overflow: visible`），否则行高按最小内容算、内容被裁；排查时先量
    `getComputedStyle(容器).gridTemplateRows` 与子块 `getBoundingClientRect()` 的高度差，
    别只看"DOM 里有没有内容"。守门 `tests/test_agent_cards.py::test_scrollable_lists_are_not_clipped`
    + 真机冒烟（断言列表高度 ≥ 内容高度、点表头能展开、能勾选）。**两个列表要"等高"时用一个共享变量**
    （本项目 `.agent-skills { --agent-list-h }`，`.skill-list` 与 `#agentToolScope` 同时 `height`/`max-height`
    取它）——各写各的高度必然漂移；注意 `#agentToolScope` 的旧规则是 **ID 选择器**（`#agentToolScope { max-height: 320px }`），
    权重高于类选择器，改高度必须直接改那条 ID 规则。
    **同一坑的反向形态（同日实测）**：给 `#agentToolScope` 设了固定 `height: 320px` 后，网格把 320px
    **均摊**给 12 个分组行 → 每行只剩 19.58px，而分组头本身 59px，被 `.agent-tool-group { overflow:hidden }`
    裁成一条条"压扁的标题"（用户截图报障）。`align-content: start` **无效**（行基尺寸仍被算成均摊值），
    必须 `grid-auto-rows: max-content` 让行按内容定高、由容器内部滚动。教训：**给网格容器设固定高度前，
    先确认行是"内容驱动"的**；凡是"滚动容器 + 固定高度"的组合，`grid-auto-rows: max-content` 是标配。

50. **模态 `<dialog>` 在 top layer：body 上的 fixed 浮层永远盖不过它（设置页右键菜单"消失"）**：
    文本框右键菜单是 `document.body` 上的 `position: fixed; z-index: 1000`。会话页正常，但设置页
    是模态 `<dialog>`——**top layer 高于一切 z-index**，实测 `document.elementFromPoint(菜单中心)`
    命中的是弹层内容（菜单在背后，用户看到的是"右键菜单被禁用"）。第二个症状同源：**模态弹层
    不会清除 `window.getSelection()`**，弹层里右键时选区还停在底层会话的 `.message-body` 上，
    于是弹出「复制选中 / 快速发送」这类会话页菜单。
    教训：① 需要盖在模态弹层之上的浮层，**要么挂进最上层那个弹层内部**（本项目
    `topLayerContainer()`：`dialog[open]:modal` 取最后一个，无则 body），要么用 Popover API；
    别只调 z-index；② 弹层内的右键/选区逻辑必须**按弹层作用域判定**，不能直接读全局
    `getSelection()`（它跨弹层存活）；③ `input[type=number]` 等不暴露 `selectionStart`，
    复制要退化为整值，否则右键复制永远提示"没有可复制的内容"；④ **底部提示框（`#toast`）是同一个坑**：
    它虽然是 `<dialog>` 但用 `show()`（非模态）打开，**不在 top layer**——在设置/Agent 弹层里点按钮
    （如空提示词点「存为快捷提示词」）时提示被弹层整块盖住，用户完全看不到。处置与 ① 一致：
    `toast()` 先 `topLayerContainer()` 再 `append`（无模态时回 body）；守门
    `tests/test_context_menu.py::test_toast_is_reparented_into_top_layer_container` + 冒烟
    `verify/agent_prompt_smoke.cjs`（断言 toast 的父节点是 `dialog[open]:modal` 且 opacity=1）。
    教训：**凡"临时浮层"都要先问它在不在 top layer**——用 `show()` 的 `<dialog>`、`position: fixed`
    的 body 子元素都不在；守门 `tests/test_context_menu.py` + 真机冒烟 `verify/context_menu_smoke.cjs`。

51. **供应商 5xx 是瞬时故障：既不能直接终止整轮，也不能不留取证（2026-09-09 实测）**：
    一次 `/v1/responses` 返回 **HTTP 500**（响应体只有 `Internal Server Error`、无结构化错误），
    直接终止了整轮对话；用户 18 秒后重发**同样的请求**即成功。排查时才暴露两处缺口：
    ① 重试矩阵只有 `{429, 502, 503, 504}`——**500 不在其中**，一次抖动就毁掉整轮；
    ② 请求体落盘只在「思考回传类 400/422」触发，5xx 不留任何原件，只能靠离线重建
    （`build_model_history` + `_responses_input`）反推，而重建不是逐字节原件。
    教训：**5xx 与 429 同等对待**（API 无状态、重发模型请求安全：退避 1.5s/3s、上限 3 次），
    且**任何供应商侧失败都要落盘请求体**（超长字段截断、总量封顶、不含 API Key）。
    另一条排查经验：**「裸 500 + 无 JSON 错误体」与 schema 违约可区分**——DeepSeek 对 schema
    问题一律返回带 `error.message` 的 400（本项目库里就有 21 条），因此 500 应先怀疑服务端/
    瞬时故障，而不是继续改 payload；守门 `tests/test_llm_runtime_retry.py`。

52. **"实时值"必须有主，不能被周期性重渲染擦掉（圆环冒烟抓到的真 bug）**：把上下文圆环改成
    随每次 `usage` 事件刷新后，冒烟出现**间歇性**失败——圆环时而显示 8.2%、时而回到
    「暂无模型用量数据」。根因不是测试抖动：本轮尚未落库，`messages` 里当然没有带 `usage`
    的助手消息，而会话/任务轮询触发的 `renderMessages → updateContextUsage(messages)`
    会把 `state.contextUsage` 覆盖成 `null`——**实时值被历史快照擦掉**。
    教训：**运行期实时数据与"从历史重建的数据"必须区分主次**——历史重建只能在没有实时值
    或会话已切换时生效（本项目：`updateContextUsage` 在 `state.chatBusy` 且
    `contextUsageConversationId === state.conversationId` 时直接返回，写入点
    `setContextUsage(usage, conversationId)` 记录实时值所属会话）。
    **唯一例外（2026-09-18 补）**：`contextReset` 是「上下文已被显式重置」的确证（最新一条
    「新会话」分割线就落在这条消息下方）——收尾瞬间 `chatBusy` 尚未落下，此刻必须越权归零，
    否则重置后满量锁会一直挂在屏幕上（见 §九.94）。
    附带教训：**间歇性失败的冒烟不要当抖动重跑**——它往往指向真实竞态；守门
    `tests/test_context_ring.py::test_live_usage_survives_history_rerender_while_busy`
    + `verify/ring_usage_smoke.py`。

53. **发布流水线红：三处"本机全绿、CI 必挂"的环境差异（GitHub Actions 实测）**：一次 push 后流水线
    报 12 处失败，逐条看**没有一处是业务逻辑问题**，全是本机环境比 CI"宽松"造成的：
    - **缺依赖**：`naiba/tools/providers/documents.py` 顶层 `from naiba import pdf`，而
      `naiba/pdf.py` 顶层 `import pymupdf` —— 工作流只装了 5 个包时 `test_pdf_tools` 直接全 error。
      教训：**安装清单必须覆盖包的全部第三方导入**，别按"测试要不要跑"挑；扫描一次就够
      （AST 遍历 `naiba/**/*.py` 的 `Import`/`ImportFrom` 减去 `sys.stdlib_module_names`，当前恰好 5 个：
      `PIL` / `av` / `mcp` / `pymupdf` / `python_multipart`）。
      其中 `python_multipart` 与 `av` 是**惰性导入**（`http.py` 里函数内 `from python_multipart import …`、
      `video.py` 里函数内 `import av`），漏装时**测试照样全绿、但冻结版上传/抽帧直接坏**
      ——这类"只在运行时炸"的依赖必须显式装。
    - **控制台编码**：`naiba/jobs.py` 工作线程里的中文 `print("…产物已写回消息…")` 在 cp1252 控制台
      抛 `UnicodeEncodeError: 'charmap' codec can't encode`，把用例打成失败。教训：**库代码不得假定
      宿主控制台编码**——包导入期统一 `ensure_utf8_stdio()`（`naiba/__init__.py`，复用
      `core/diagnostics.py` 的唯一实现，无控制台时静默跳过），且 Job 等"可能在任意宿主里跑"的线程
      **禁止裸 `print`**，一律 `logging`（`logger.exception` 还能带上栈）。守门 `tests/test_stdio_encoding.py`。
    - **8.3 短路径**：CI runner 的 `TEMP` 是 `…\RUNNER~1\…`，生产代码 `resolve()` 后是长名
      `…\runneradmin\…`，而测试里 `assertIn(str(tmp), out)`、`is_relative_to(tmp)` 都是**词法比较**，
      必挂。教训：**比较路径两侧都 `resolve()`**（`PurePath.is_relative_to` 不解析 8.3 短名）；
      生产侧同理——`_read_roots` / `_make_write_policy` 现在把工作区根也 `resolve()`，否则调用方
      传入未解析的工作区会把**工作区内**的文件误判成越界。
      **本地复现手法**（本机 8.3 已关，靠 junction 造出"词法路径 ≠ 解析路径"）：
      `cmd /c mklink /J %TEMP%\naiba_junc %TEMP%\naiba_junc_target`，然后
      `$env:TEMP=$env:TMP="%TEMP%\naiba_junc"` 再跑全量——这一步在本地抓到了 CI 日志之外的第 5 处
      （`test_pdf_tools` 的越界策略用例），比"等流水线红"划算得多。
    附带教训：工作流的 `$attempts = 2` 重试对**确定性失败**毫无意义，只会让日志变成两份——
    看到流水线红先按上面三条自查，别指望重试。
54. **pywebview 的 `private_mode` 默认 True = localStorage 每次退出被清空（用户"我上一轮自定义的
    工具集呢？"实测）**：`webview.start()` 的 `private_mode` 默认 `True`，EdgeChromium 后端会把
    profile 放进**临时目录**并在进程退出时 `shutil.rmtree`（`platforms/edgechromium.py` 的
    `clear_user_data`）。于是前端所有 localStorage 写入都是"一次性"的：用户保存的「我的工具集」
    （当时存 `naiba.agentToolTemplates`）在**关掉应用/重编译重启后凭空消失**，侧栏宽度、
    文件面板宽度、顶栏 Skill 勾选、交互模式同理每次启动重置——而代码注释一直写着"localStorage
    全局持久化"，说明这是**无声失效**，不是设计。
    两条正确做法，按"数据性质"选：
    - **用户创作的数据（工具集、提示词等）→ 后端 config.json**：与 Agent/快捷提示词同源，
      随数据目录备份迁移、换设备/换浏览器一致。已按此把工具集改成 `tool_sets`
      （`get_tool_sets`/`upsert_tool_set`/`delete_tool_set` + `/api/tool_sets` + bootstrap 带回），
      前端 `migrateLegacyToolTemplates()` 负责把老 localStorage 数据一次性搬过来。
    - **本机界面偏好（侧栏宽度等）→ `private_mode=False` + `storage_path`**：`launcher.py` 现在
      显式传 `private_mode=False, storage_path=app_dir/webview`（**不能放 data_dir**，否则会被
      数据目录迁移/备份当成用户数据搬走），目录不可写时回退私有模式并留痕。
    排查手法：这类"重启就没了"的 bug 先看**存储层是不是临时目录**——`webview.start` 的参数、
    进程退出时的清理逻辑，比怀疑自己的业务代码快得多；另注意静态资源已带
    `Cache-Control: no-cache, no-store`，所以开启持久 profile 不会带来旧前端缓存问题。
55. **含"运行时才出现的内容"的接口不能永久缓存（「MCP 工具消失了」实测）**：`/api/tool_catalog`
    的内容取决于**当前已注册的工具**，而 MCP 服务是**按需连接**的（`app.py`：configured services
    stay stopped until a run explicitly needs them）——应用刚启动时目录里没有 `mcp__*` 工具。
    前端原先"整个页面会话只拉一次"（`if (!state.toolCatalog) fetch()`），本意是省一次请求；
    但把首次拉取**提前到启动时**（为了让 Agent 卡片显示「工具集：预设名」）之后，就变成
    "永远用启动那份"：用户重启应用 → 打开 Agent 工具集 → **MCP 工具一个都没有**，
    而顶栏 MCP 明明显示已连接。
    正确口径：**目录带短时效缓存 + 关键入口强制取新**——`ensureToolCatalog({maxAgeMs})`
    （默认 5s，卡片渲染用），打开工具集面板时 `maxAgeMs: 0`；`pollMcpStatus` 发现
    `connected` 变化就 `invalidateToolCatalog()`。冒烟里的回归用例：让**第一次**目录响应
    缺 MCP 工具，再打开面板，断言 MCP 工具必须回来（`tool_groups_smoke.cjs` ⑨）。
    一般化：**缓存"会随运行时状态变化"的接口前，先问一句"它的内容会不会在我缓存之后变"**；
    会变就加时效或显式作废，别用"只拉一次"。
56. **懒加载的窗口必须以「轮」为边界；容器禁止挂 `scroll-behavior: smooth`（程序化滚动"读回旧值"并连发 scroll 事件）**
    （2026-09-10 消息列表懒加载两个真机坑）：
    - **边界**：窗口按"消息条数"切会让「新会话」分割线跟它的锚点消息分到两个渲染块里
      （分割线渲染在锚点**之后**，切在中间就只剩线没有消息）。正确口径 = 以 `user` 消息下标为轮起点
      （`turnStartIndexes`/`clampTurnStart`/`initialRenderStart`），逐条 `append(messageElement) +
      sessionDividerAfter`，分割线天然与锚点同进同出；用户特别提醒过这条，冒烟里也钉了
      「分割线紧跟在被标记的那条 AI 回复之后」。
    - **滚动行为**：`.messages` **不挂 `scroll-behavior: smooth`**。挂了之后程序化写 `scrollTop` 会**动画化**——
      读回来还是旧值（实测 `container.scrollTop += delta` 之后立刻读，值没变），而且动画期间会连发
      scroll 事件，把"用户滚到顶"的判定和"补进来的高度加回 scrollTop"全部打乱（表现为打开会话就多渲染一段、
      预渲染后视口跳 190px）；流式期间更严重——每个 delta/工具事件都贴底，动画排队追不上内容，肉眼就是持续晃动。
      正确做法 = 容器保持瞬时；程序化定位/补偿一律经 `withInstantScroll(container, fn)` 临时把 `style.scrollBehavior`
      改成 `auto` 再写（用时还原，双保险），贴底跟随 `scrollToBottom()`/`forceScrollToBottom()` 内部也走它；
      渲染/程序化滚动引发的 scroll 事件要有一个抑制窗（400ms）+ `!stickToBottom`
      门槛，否则"内容刚填进去、scrollTop 还在 0 附近"会被当成用户滚到顶。
      唯一的平滑滚动是**用户主动跳转**：刻度轨点击在自己的调用点显式传 `behavior:'smooth'`。
    教训：**凡是"程序化改滚动位置 + 监听滚动事件"的地方，先确认容器的 `scroll-behavior`；容器默认一律瞬时。**
57. **DOM 属性赋值（`dataset`）不能过 `escapeHtml`（种子消息路径变空实测）**：分割线上要把标记
    JSON 传给「填入种子消息」按钮，写成 `row.dataset.resetSeedInfo = escapeHtml(JSON.stringify(info))`
    ——`dataset` 是**属性赋值**不是 `innerHTML`，`&quot;` 会被原样存下来，读出来 `JSON.parse` 直接抛错、
    被 `catch` 吞成 `{}`，表现为"种子消息里交接文档路径是空的"（其余占位符都正常，很容易误判成模板 bug）。
    正确做法：`dataset` 直接赋原始字符串（浏览器会自己处理转义）；只有拼 `innerHTML` 时才 `escapeHtml`。
58. **"最新 N 条命中"的候选窗会静默挤掉老会话（历史检索实测复现）**：旧版 `recall_history` 是
    `ORDER BY created_at DESC LIMIT 400` 再按会话聚合——某个会话近期命中多，就把候选窗吃满，
    **更老的会话一条都进不来**（实测：410 条噪音命中让另一会话唯一的结论完全消失，而"用户问以前说过什么"
    恰恰最需要它）。正确做法 = 在 SQL 里按会话分区再截断：
    `ROW_NUMBER() OVER (PARTITION BY conversation_id ORDER BY created_at DESC, rowid DESC) <= 3`
    再按会话 `updated_at` 排序取前 N（本机 SQLite 3.49 支持窗口函数）。配套口径：检索范围（会话数/消息数）
    必须原样告诉模型、截断必须显式（`total_hits`/`truncated`）——**隐性上限是最难发现的错**。
59. **手机端「不删能力、只换形态」**：手机（局域网访问）不是缩小版电脑——**凡桌面能做的手机都要能做**，
   放不下的控件换等价形态而不是删掉：刻度轨 → 轮次下拉（`#turnJumpSelect`，复用 `collectTurns`/`scrollToTurn`，
   **只在总轮数变化时重建 `<option>`**，见 §九.37）、侧栏 → 抽屉、文件面板 → 全屏抽屉（`filePanelUsable()`
   不再按视口判死）、弹层 → 近全屏。判定源**只有视口宽度 `≤760px` 一个**（CSS `@media (max-width: 760px)`
   与 `NARROW_VIEWPORT_MAX` 同界），不要引入设备/UA/参数第二套判定（iPadOS 桌面站点 UA 与 Mac 相同，判不准）。
   即使窄屏也**不得隐藏控件文字标签**（§九.44）：靠分两行 + 图标承载压缩宽度，宁可整行换行也不隐藏。
   守门：`tests/test_mobile_parity.py`、`tests/test_turn_jump.py`、`verify/mobile_shell_smoke.cjs`。
60. **"散装"上传必须收进独立目录，否则删除会端掉整个托管目录（数据丢失级事故）**：单文件
    `.md` 导入走 `POST /api/skills/install_folder`（前端提交 `path="SKILL.md"`），后端按相对
    路径直接写进托管 Skills 目录根 → 该 Skill 的 `root` 等于托管目录本身；删除时
    `delete_skill` 的 `shutil.move(root, recycle)` 于是把**整个托管目录**搬进回收目录，
    目录下所有 Skill 一起消失（用户报障："删一个 Skill，全部 Skill 都没了"）。教训：
    ① **"实体的归属目录"必须由安装环节保证**——凡是有文件直接落在导入根的上传（单 `.md`、
    顶层散放 `SKILL.md` 的 zip），一律先建 `<name>/` 子目录再写入
    （`app._skill_subdir_prefix`，目录名取 frontmatter `name`），带顶层目录的文件夹上传保持
    原样、不多包一层；② **删除是不可逆操作，路径解析必须带归属校验**——移动前确认目标严格
    位于托管目录之下，`root == managed` 时退化为"只回收这一个定义文件"（历史遗留数据也能
    安全清理，不必引导用户手删），启动时还会把历史裸 `SKILL.md` 迁入同名子目录，并同步 Agent
    固定 Skill 引用与隐藏 ID；③ 这类"父目录 == 实体"的形态肉眼看不出来（UI 只显示目录
    路径），必须有用断言钉住的守门测试（"托管目录根下不得出现 `SKILL.md`"）；
    ④ 同类风险适用于任何"把用户上传按相对路径直接展开到共享根目录"的入口（附件、模板、
    技能），展开前先确认顶层归属。守门 `tests/test_skill_install_layout.py`。

61. **模型目录是「唯一真相来源」，它的缓存就不能"查过就算数"（JS `[]` 是 truthy）**：会话级模型
    选择的**根本动机**是"一张卡片 = 一个 API（地址 + key）、模型维度下移到会话"——供应商新上架
    的模型必须**马上能用**，而不是先去设置里再存一张卡片。所以**目录的新鲜度是一等需求**，不是
    nice-to-have。而 `state.providerModelCatalogs[key]` 的守卫原先写成"有没有查过"
    （`if (state.providerModelCatalogs[key]) return`），JS 里 **`[]` 是 truthy**：供应商返回 200
    但列表为空（本地模型还没 pull/加载、中转只放行部分模型）之后，本页会**永久**当作已缓存，
    下拉一直卡在「请先在设置中检查模型」，模型后来上架也刷不出来，唯一逃生口是进设置页点一次
    「检查模型」——而那个入口埋在设置页最里面，会话里的用户够不到。教训：① 缓存判据必须是
    **内容**（`(catalog[key] || []).length`）而不是"是否存在一次查询记录"；② 唯一真相来源必须配
    一个**会话内可发现的刷新入口**（下拉末尾的「↻ 重新检测模型」：选中即强制重拉 + 还原原选择，
    且该伪选项要在取值出口 `composerModelChoice()` 就屏蔽掉，绝不落库、绝不发给模型）；③ 反过来
    **不要**在目录不可用时静默回退到供应商配置里那个手填的 `provider.model`——它是死字符串
    （用户被 `deepseek-v4.1-flash-expires-on-0910` 钉住后长期发错模型），宁可明确报错。
    守门 `tests/test_composer_model_follow.py` + 冒烟 `verify/composer_model_smoke.py`。

62. **"先选后弃"的两张分支表必须逐条对齐，否则"重试一次"只是空转（代理层实测）**：`naiba/net.py` 的
    `_opener_for`（选择当前 opener）与 `_refresh_opener`（丢弃旧实例、重建后重试）是两张各自独立的
    if/elif 表。选择侧有"代理已开启 + 未填地址 + 关系统回退 → 降级直连"这一档，重试侧却只匹配
    "未配置 / 系统回退 / 有 URL"三档——落到这一档时**不丢弃任何 opener**，`_refresh_opener` 把刚失败的
    同一个实例原样返回，"连接异常时按当前策略重建后重试一次"形同虚设，用户只能手动重来。同源隐患还有
    一处：手动分支写成 `return self._manual_opener or urllib.request.build_opener()`，缓存一空
    （`configure` 与重试都会清缓存）就**静默退回空 ProxyHandler 直连**——"以为在走代理"比直接报错更危险。
    教训：① 这类"选择/丢弃"成对的判定，能合并成一个产出点就合并（重建统一走 `_build_manual_opener`），
    否则必须在守门测试里**逐分支对拍**（`tests/test_net.py::RefreshOpenerTests` 把每一档"该丢谁、
    不该动谁"都钉死）；② **回退只能显式发生**：拿不到代理 opener 不能退成直连，宁可报错或重建；
    ③ 两张表不同步的症状是"重试无效"而不是"报错"，排查超时/重试类问题时先核对这两张表。

63. **对话框里的下拉要同时过两道关：祖先 overflow 裁剪 + 模态 dialog 的 top layer**：设置页「目标版本」
    下拉最初留在 `#updateVersionCombobox` 内用 `position: absolute`，先被 `.update-status{overflow:hidden}`
    切掉一半选项，又被设置面板的 `overflow:auto` 拖得不跟手（§九.27）；照 §九.27 改成 body 下
    `position: fixed` 之后**仍然盖不住**——设置页本身是模态 `<dialog>`，整块在 top layer 之上
    （§九.50 同一个坑的第三种形态）。处置：**挂载点取"能进 top layer、又能当定位容器"的宿主**——
    `#updateVersionMenuHost` 是设置 dialog 内的 `popover="manual"` 元素，`showPopover()` 让它进
    top layer 且**晚于** dialog，菜单 append 进去后祖先 overflow 管不到（fixed 的包含块是视口，
    宿主不产生盒子）、层级也压得住。宿主自身必须 `display: contents`、**不得产生任何可交互/可绘制
    区域**，否则它会在 dialog 之上铺一层看不见的拦截（菜单之外的点击全被吞）；宿主缺失或浏览器不支持
    popover 时退回 body（`updateVersionMenuHost()`）。教训：① 弹层"看不见/点不到"的诊断顺序是
    "先看祖先 overflow，再看 top layer"——两者常同时命中，只修一处会误以为没修好；② `:popover-open`
    **不能当存在性判据**（未 `showPopover()` 时匹配不到任何元素），判据要用
    `typeof host.showPopover === 'function'`；③ 这类层级/裁剪断言必须用 `document.elementFromPoint`
    命中测试，`getBoundingClientRect` 对裁剪完全无感——"位置算得对"与"真的能点到"是两件事。
    守门 `verify/update_version_smoke.cjs`（`verify/update_version_smoke.py` 自编排起服务与收尾）。

64. **「谁算在运行」必须只有一份词表，否则僵尸状态会锁死整个界面；而「停不掉的读」只能靠关连接打断
    （2026-09-14 客户机：学 16 小时后永久停在「等待本地模型资源」）**：用户报的三个现象——点「停止」无效、
    鼠标移到 AI 回复上「新会话」只弹「请先等待当前回答结束或停止后再划分割线」、重启应用乃至重启电脑
    都一样——**没有一处在前端**，是四处独立缺陷叠成的死锁：
    - **状态词表分裂**：`jobs.cancel()` 会把子任务置为 `stopping`，`jobs.JOB_ACTIVE` 也认它，但 `store` 的
      **启动清理**只扫 `queued/running/waiting/cancelling`、`manager.ACTIVE` 同样漏掉 `stopping`
      ⇒「已取消但线程还没退出去」的子任务**重启也清不掉**，成了永久僵尸。教训：**「未结束」这类状态集合
      必须有唯一一份定义**（本项目收归 `storage.ACTIVE_TASK_STATUSES` / `TERMINAL_TASK_STATUSES`，
      启动清理、活动列表、取消判定全部引用），并让守门测试断言**几份词表互相包含**——任何一侧新增状态
      都要同步；这类分裂的症状是"重启也没用"，比报错难查得多。
    - **一份数据被两个语义共用**：`/api/runs` 回答的是「这条会话在不在回答」，却直接返回了
      `background_tasks` 的全部记录（run 与 job 共用一张表）⇒ 一个卡住的**子任务**让整条会话被判成
      运行中，而「分支 / 重新生成 / 新会话」的守卫都是 `if (state.abortController || state.chatRunId) return`，
      三个救援入口一起被挡住。教训：**接口要么拆开、要么在服务端按语义过滤**（本项目新增
      `manager.list_primary`：`kind ∈ PRIMARY_RUN_KINDS` 且 `parent_job_id` 为空，与 `storage.active_run`
      的顶层语义一致；任务面板继续用 `/api/tasks`），前端不要在"取第一条"上附加语义。
    - **「停止」送不到正在阻塞的读**：三条流式路径都是 `for raw_line in response:`，阻塞在 socket
      `readline`（1800s 超时透传），**循环体不看 `cancel_event`**——本地模型 prefill 一个字节都不吐时，
      点停止只是往库里写了个标记，读取线程根本看不见。教训：**要打断阻塞读，只能关掉它正在读的连接**
      （本项目 `_iter_stream_lines`：看门狗线程在 cancel 置位时 `close()`，并把随之而来的
      `ValueError/OSError` 翻译回「任务已取消」，避免被误判成网络故障而进入重试）。附带修正一条认知：
      「可取消 IO」原先只挂在**非流式**分支（`_read_response_cancelable`）上，流式那条路一直是裸的——
      **别以为有了"可取消"工具就等于所有路径都可取消**。
    - **前端的「失败即冻结」**：`cancelCurrentRun` 的复位写在 `finally` 里却带条件
      `if (terminalConfirmed && …)`——停止没被确认（本地模型卡住时必然如此）就跳过复位，界面永久停在
      「回复进行中」。教训：**收尾动作（解锁 UI、清运行态）必须与"操作是否成功"解耦**：成功与否都要
      恢复可操作，失败另给一次诚实提示。宁可让下一次发送被后端用 `ACTIVE_RUN` 明确拒绝，
      也不要让界面永久冻死。
    配套守门 `tests/test_run_state_guard.py`（21 例）。

65. **「卡住」要能被自动收场、也要能被归因——四个边界都别等用户来发现（2026-09-14，接 §九.64）**：
    §九.64 修的是「停不下来」，这一轮修的是「为什么卡住无人知晓、以及没人踩刹车」：
    - **超时必须匹配"卡住的形态"，不能只有一个总超时**：本地后端 prefill 时**一个字节都不吐**，
      这与「正在慢慢算」在界面上完全无法区分；而总超时是 1800 秒 ⇒ 用户要干等半小时才知道这轮没戏
      （客户机就是这样「永久卡死」的观感）。处置：对**首个字节**另设短超时
      （`LOCAL_FIRST_BYTE_TIMEOUT_SECONDS`，默认 120s，可配/可关），由**已有的那个看门狗线程**多管一件事
      ——「取消」与「首字节超时」共用同一个 `close()` 动作。教训：**「一直不返回」与「返回得慢」是两种
      故障，要给两种计时**；而且新计时优先复用既有的中断机制，不要另起一套线程/信号。
    - **不可归因的等待 = 不可诊断**：本地锁是全进程唯一类属性，占用它的调用未必有 status 通道
      （视觉识别、子代理都传 `status=None`），于是界面只会说「等待本地模型资源」，**不知道是谁在用、
      也就无从判断该等还是该杀**。处置：占用者登记一个角色标签（`options["lock_label"]`），等待方
      在状态里**指名道姓**（且**首次发现忙碌就立刻回报**，不先沉默一个周期）。教训：**跨组件的
      互斥/排队，除了"锁"本身，还要传"谁持有"**——只传"忙/闲"的锁在长链路里等于没有可观测性；
      同理，凡是有 `status=None` 这种"静默模式"的调用点，都要给它一个外部可见的身份。
    - **没有出口的循环必须自己设上限**：Agent 循环是 `while True`，出口只有「模型给出最终答复 /
      两条熔断 / 取消」——模型一旦进入「一直调工具但拿不到结论」，一轮可以无限烧下去（还一直占着
      本地锁）。而 `max_steps` 这个参数**早就存在、也一路透传，就是从来没人读**。处置：`_resolve_step_limit`
      真正生效（默认 200，0=不限制），并在**预算将尽时先提醒模型收尾**（尾部追加一条 user 指令，
      不动 system 前缀 ⇒ 缓存不受影响）。教训：**"参数存在"不等于"功能存在"**——透传但未消费的参数
      比没有参数更危险（它会让人以为已经兜住了）；审计这类参数时直接搜"谁读了它"。
    - **"探测到的真值"必须落回配置，否则只有一条路径受益**：本地窗口探测值原先只挂在
      会话 profile 的**副本**上，设置页（`public_providers`）与其它读 `_infer_context_window` 的地方
      仍拿 0 → 又退回在线 256k 兜底（等于 §九.64 的病根换个地方复发）。处置：回写到 provider 的
      **独立字段 `context_window_probed`**，取值优先级「显式配置 → 探测值 → 未知」，且**值没变不落盘**。
      教训：①「运行时算出来的真值」要问一句**"它该存哪、谁是权威"**，只活在临时对象里就等于没算；
      ② 新增字段而不是覆盖用户字段（**"探测值"不能伪装成"用户配置"**，否则设置页会撒谎）；
      ③ 顺手记一个**键名陷阱**：本项目老版本遗留过一个 `max_agent_steps`（启动时显式丢弃），
      新设置**故意换了 `agent_step_limit`**——沿用同名会让老配置里的残值突然生效、把用户的步数卡死在
      一个随手填过的小数字上。**复用一个曾经被丢弃的键名 = 复活一批不可预期的脏值。**
    配套守门 `tests/test_runtime_guards.py`（30 例）。
    - **主题变量分「中性层 / 换肤层」，暗色只覆盖中性层 ⇒ 近白底 + 近白字**：用户截图两处看不清——
      Run 运行中提示条 `.active-task-bar`（正文）与设置里 API「当前」卡 `.provider-card.is-default`（卡片名）。
      组件本身没写错颜色，全部用变量取色；病根是 `--accent-soft` / `--accent-soft-2` / `--violet-*`
      这些**服务于浅色主题的软色**由 `:root` 与 `html[data-skin=…]` 定义（`#EDECFF`、`#d9efff` 一类），
      而 `html[data-theme="dark"]` 只重定义了 `--neutral-*` / `--surface` / `--text`，**没碰软色层** ⇒
      `--text` 已近白、底色仍近白，浏览器实测对比度 **1.05**（1.0 = 完全同色）。处置：在暗色块里把软色
      重新混色（软底 `color-mix(in srgb, var(--accent) 28%, var(--surface))`、violet 的「深色字」反向
      混白），**消费方一行不改**，对比度回到 16.92。教训：①换肤体系新增/改动变量时，要按「层」核对暗色
      覆盖完整性，而不是只看显眼的中性色；②暗色块里别引用 skin 会改写的浅色字面量（`var(--brand-100)`
      就是浅色陷阱，`color-mix` 里要用 `var(--accent)` + `var(--surface)` 这类**语义**变量）；
      ③组件一律用变量取色，暗色适配才能收敛成「只改一个块」。
      配套守门 `tests/test_dark_soft_contrast.py`（4 例）+ 浏览器检查 `verify/_check_dark_soft_contrast.cjs`
      （暗色/浅色两套断言实际对比度，隔离源码实例上跑）。

66. **「验证本身」也会骗人——这一轮的坑全在测试脚手架，不在产品（2026-09-14）**：
    四道闸的代码一次通过，反而是**验证环节**连出六次假信号。凡是「跑绿了/跑红了」的结论，先怀疑脚手架：
    - **本机 safe-delete 批量删除保护会污染测试结果**：本沙箱把删除改道回收站，且一次超过 **50 项**就
      `SAFE_DELETE_BULK_CONFIRM_REQUIRED`/`FAIL_CLOSED`。后果有两个：跑全量单测会多出**一个假 error**
      （`test_skill_catalog` 的 `TemporaryDirectory` 清理被拦 → `SystemExit(1)`，产品断言其实全过），
      并在仓库根落下 `tmpXXXX/` 垃圾（过去一直以为是「某测试忘了清理」，其实是环境拦的）。
      处置：跑单测与清理产物时带 `CODEBUDDY_SAFE_DELETE_ENABLED=0`（`sitecustomize` 的开关），
      比过去「把 `build/` 改名再编译」更干净——**PyInstaller `--clean` 同理，不必再改名绕**。
    - **冒烟脚本取 Run id 不能用 `active_only=1`**：假端点秒回时整个 Run 在**第一次轮询（0.1s）之前**就
      结束了，`active_only` 永远看不到它 ⇒ `run_id` 空串 ⇒「Run 已创建」FAIL +「等待终态」白等到超时。
      处置：改用「该会话**最新一条** Run」（`active_only=0` 取首条）。教训：**用"活跃"作过滤条件时，
      先想清楚被测行为会不会比一次轮询还快**。
    - **断言里的关键词必须从被测文案复制，不能手打**：`find_text("没有返回任何内容")` 打不中
      `local_first_byte_timeout_error()` 实际写的「没有输出任何内容」，于是「可行动文案」这条断言
      在任何情况下都过不了。教训：**断言字面量要贴着源码，改文案时用同一处搜索把断言一起改**。
    - **模态 `<dialog>` 里别用 `locator.screenshot()`**：弹层在 top layer 且内容比视口高，Playwright 的
      分块拼接会截出**叠影 + 串到下层聊天界面**的图（照样是几十 KB 的"正常"文件，肉眼才发现）。处置：
      「把目标滚到视口中间 + 视口截图」。
    - **DOM id ≠ 后端配置键**：前端检验第一版直接拿 `#localFirstByteTimeout` 去读 `bootstrap.settings`
      （后端键是 `local_first_byte_timeout_seconds`），断言拿到 `undefined` 又回落到默认值 120 ——
      **两边都"恰好对"的断言等于没断言**。处置：显式建 id→键映射表并断言键存在。
    - **验前端必须用隔离源码实例，且「服务+浏览器+脚本」要在同一个 shell 生命周期里**：8765 上常驻的是
      打包版（读内置资源），拿它验仓库 `public/` 等于验空气；三者分开启动则会随调用结束被回收，
      整批截图退化成同一张空白帧。处置：`verify/run_frontend_check.sh` 统一编排；
      `verify/_serve_tmp.py` 支持 `NAIBA_TMP_ROOT` 每轮用**全新隔离根**（否则「全新安装默认值」断言会被
      上一轮写进 `config.json` 的值污染）。
    - **「0 是合法值」这类约定要端到端钉，不能只钉后端**：后端单测过了不等于前端不吞它——
      只有「填 0 → 保存 → **重载页面** → 仍是 0」这条来回链路才算验过（`?? 默认` 是很容易写出的一行）。
    配套：`verify/runtime_guard_smoke.py`（13 例）+ `verify/run_frontend_check.sh` / `verify/frontend_runtime_settings_check.cjs`（25 例）。

67. **视频抽帧（PyAV）的五个专属坑——都是"看起来对、跑起来不对"那一类（2026-09-14）**：
    解码本身只有几十行，但下面五条各踩一次，且**前两条会静默把错误形态漏给模型**：
    - **`av.error.FFmpegError` 继承的是 `ValueError`，不是 `OSError`**（PyAV 9+ 实测：`InvalidDataError
      → FFmpegError → ValueError`）。所以"业务错误继承 ValueError，于是写 `except ValueError: raise`
      先放行、再 `except Exception` 包一层"的写法**恰好把 PyAV 的原始报错放行出去**——模型会收到
      `[Errno 1094995529] Invalid data found when processing input: '<视频路径>'` 这种带 Errno 与裸路径的
      字符串。正解：先用 `_av_error_types()` 取出 `av.error.FFmpegError` 等类**先捕获**，再放行业务
      `ValueError`。守门：`tests/test_video.py::test_missing_and_wrong_type_and_corrupt_files`
      （断言拿到的是中文业务文案，不是原始异常）。
    - **`av` 必须惰性导入**：`naiba/pdf.py` 顶层 `import pymupdf` 的老事故（缺依赖 → `test_pdf_tools`
      整组 error）不能重演——`video.py` 顶层不 import `av`，函数内导入并在失败时抛
      「视频解码依赖未安装（av）；…`pip install av`」。**但"测试不会红"正是危险所在**：漏装时只有
      冻结版会炸，所以 §6.3 的"冻结版实跑"是硬要求。
      **冻结版实跑入口用现成的**：`dist\naiba-chat.exe --run-skill-script verify\frozen_video_check.py`
      （`launcher.py` 的隐藏入口，不起 GUI/HTTP/实例锁，直接在打包好的解释器里真解码一次）。
    - **联系表在工具结果里是"裸路径"**：`MediaCollector` 只对 dict 记录取 `thumb_path`，字符串来源拿到
      的是空 thumb，前端于是按 `<stem>_thumb.webp` 推导 —— **不显式生成这张 WebP 就是 404 破图**
      （与 `storage/media.py` 对 GIF 的同一条教训，且产物在 `uploads` 内时采集器**不会**补缩略图）。
    - **宿主按内容去重会合并重复帧**：`_dedupe_records` 是 sha256 字节级去重，静止画面逐秒抽出的
      多张**完全相同的帧只留第一张**。于是"工具返回 8 个路径 / 消息区只挂 N 张卡"是**预期行为，不是丢帧**，
      验收时别把它当 bug（联系表本身也不参与这层去重以外的判断）。
    - **缓存键不能抄 PDF**：`pdf.py` 用"全文件 sha256"，视频动辄几 GB，照抄会让每次调用多花几十秒；
      改用 `sha256(绝对路径 + size + mtime_ns)`。**上限也必须两级**：`params_key` 进目录名 ⇒ 换一组
      抽帧参数就是一个新目录，只按目录计数（200）拦不住总膨胀，必须再加"同一视频跨参数目录合计 ≤600"。
      守门：`tests/test_video.py::test_cache_limit_across_param_dirs`。

68. **一张表两种语义，界面就会撒谎；而"停止请求"与"当前状态"是两件事（2026-09-14/15，接 §九.64）**：
    用户看着任务弹窗问"我这条对话里哪儿来的子任务"——不是他看错了，是面板把两类记录混在一起了。
    - **`background_tasks` 同时装着"每次回答的记录"与"后台作业"**：`chat`/`plan_execute` 那些行是
      "用户每发一句话就插入一次"的运行记录，`comfyui`/`shell`/… 才是真正的后台作业，`parent_job_id`
      只是"我由哪次回答派生"的归属锚点。旧面板平铺两者且不显示 `parent_job_id`，用户看到的是
      "我发过的每一句话都是一个任务"；顶栏徽标又只统计活动状态，全部终态时显示 `0`，于是
      "库里有 13 条记录"与"0 任务"同时成立。处置：面板只列后台作业
      （`/api/tasks?jobs_only=1`，**过滤放在 SQL 层**——默认 limit 是 50 且回答记录占绝大多数，
      先捞再筛会把较早的作业挤出窗口；`limit` 一并放到 200），按 `parent_job_id` 分组、
      组标题取「类型 + 时间」（**刻意不引用用户消息原文**：那可能含不该复述的内容），
      徽标在没有活动任务时回落为总数并弱化。教训：**同一列表里混有多种语义的记录时，
      要么拆接口、要么在服务端按语义过滤**（§九.64 的 `/api/runs` 是同一坑的另一种形态）。
    - **"请求已下发"与"当前状态"是两件事**：`cancel` 只写 `stopping`，而 `_set_status` 的冻结条件
      读的是 `cancel_requested`——标记没人写，那段防护就是空转的，worker 下一次状态更新会把
      `stopping` 打回 `running`（用户看到的是"点了停止它还在跑"）。教训：**写完一个状态就问一句
      "谁会读它"**；冻结类判断必须挂在"请求已下发"这类**持久标记**上，而不是状态值本身。
    - **终止条件要匹配"卡住的形态"**：`stopping` 属于活动状态，worker 若卡在网络等待/退避里
      （`_run_check` 退避上限 60 秒）不退出，它就永久占着活动名额——正是 §九.64 那次"三个救援
      入口全被挡住"的成因。处置：90 秒 watchdog 强制置 `cancelled` + `finished` 并摘掉线程句柄；
      且**只对登记过取消事件的作业生效**——被误传到 Job 接口的顶层 Run 不得被强行标成已取消。
    - **"父已结束"是常态而非异常**：后台任务的家常便饭是"回答早已发出、只剩子 Job 在跑"，
      此时父行只是归属锚点；`manager.cancel` 原先会把父行从"已完成"改写成"已取消"，
      等于篡改历史。教训：**级联取消要先问"被级联的那一层是不是本次操作的对象"**。
    - **验证脚本自己也会骗人（本轮又中两次）**：① 播种 `running`/`stopping` 行**之后再**起服务，
      会被启动清理改写成 `interrupted`——活动状态的行必须在服务起来之后再插（`stop_cancel_smoke.py`
      的既有做法）；② 隔离实例里的假供应商 `base_url` 指向 `example.invalid`，打开会话时模型目录
      请求必然 400——按既有做法把该请求桩成空列表（`composer_model_smoke.cjs` 同款），
      **而不是把 4xx 断言放宽**，否则真正的接口回归会被这层噪声盖住。
    - **「等状态变化」不是「等线程退出」（流水线实测，2026-09-15）**：这一轮新增的
      `test_stop_timeout_watchdog_forces_terminal` 本地全绿，CI 两次尝试都红——`FAIL:
      assertNotIn(job_id, registry._cancel)` 加 tearDown 的 `PermissionError [WinError 32]
      …chat.db 被另一个进程占用`。根因是测试只轮询「状态变了没」：兜底线程的收尾顺序是
      「落库 → 发事件 → 摘取消事件/线程句柄」，状态一变就跳出循环去断言，正好抢在线程释放
      SQLite 连接之前；本地线程调度快，恰好盖住了这个竞态（§九.66 的同一类坑）。
      教训：**测试里起了后台线程，「收尾」必须有「线程真的做完」的屏障**——等到它摘掉句柄
      再断言、再删临时目录，别用「sleep 一会儿看它没出事」或「状态变了就算完」当同步手段。
      复现/验证手法：把 `JobRegistry._forget_job_thread` 打桩成「先 sleep 1.5 秒」再跑，
      旧写法必红、新写法必绿（探针验证完即删，不入库）。
    守门：`tests/test_job_cancel_state.py` + `tests/test_tasks_aggregate.py`（合计 21 例，含
    "已终态父 Run 不得被改写"与"store 三处自写状态列表必须消失"）；冒烟
    `verify/tasks_panel_smoke.py`（自编排隔离实例，6 项接口/取消断言）+ `verify/tasks_panel_smoke.cjs`
    （17 项浏览器断言：只列作业、按回答分组、组标题=类型+时间、停止按钮只对活动态、
    零 pageerror / console.error / 4xx、430px 不横向滚动）；无浏览器兜底
    `verify/tasks_panel_render_check.mjs`（抽渲染函数真执行并断言产出 HTML）。

69. **「能上传」不等于「能显示」——UI 素材要走「按图片内容判定的格式白名单」（2026-09-15）**：
     聊天背景图（只铺对话区，见 `public/styles.css` 的 `.chat-backdrop` 与其上的注释）第一版
     直接吃了 `/api/uploads`：那个通道**只校验大小（80MB）、不校验类型**，非图片格式原样落盘。
     于是选到 TIFF / HEIC 时是「上传成功、设置保存成功、背景一片空白」——真正决定能否渲染的
     是另外两件事：`/api/file` 给出的 MIME（`mimetypes` + `core/media_types.MIME_BY_EXT` 兜底 +
     `nosniff`）与 WebView2/Chromium 的解码能力。TIFF 有 MIME 但 Chromium 不解码；HEIC 连 MIME
     兜底都没有（`application/octet-stream` + nosniff → 直接拒渲染）。
     教训：**凡是「用户选一张图当 UI 素材」的功能，都要在保存设置处按图片内容（读出的 format）
     过一遍白名单**（`config.CHAT_BACKGROUND_IMAGE_FORMATS`：PNG / JPEG / WEBP / GIF / BMP / AVIF），
     并把路径限定在 `data/uploads` 内（设置里的路径会被前端拼进 `/api/file?path=…`）。前端
     `accept` 与扩展名过滤只是 UX，判定必须在服务端且**不信扩展名**（TIFF 改名 `.png` 实测被抓）。
     同轮的另一条：**「乐观应用 + 落库」必须能回滚**——背景图先写 CSS 变量再发 `/api/settings`，
     服务端拒绝后不回滚就会留下「设置失败、背景却已变空白」的假象；像素断言顺手又炸出它的
     连带伤害：清除背景去回收文件时，回收的是那张已被删掉的坏图，真正在用的旧图留在了盘上。
     守门：`tests/test_chat_background.py`（默认值 / 透明度 clamp 与 NaN / 目录穿越 / 改名绕过 /
     TIFF 拒绝 / 清除语义）；静态检查器 `verify/css_content_bg.py`（对话区内容必须实底，能报出
     修复前的 `.starter-grid .starter-add`，见 §六 清单）。

 70. **浏览器冒烟的锚点要取「落库后的真值」，夹具要让产品管线自己生成（2026-09-15）**：
    选择面板冒烟（`verify/choice_panel_smoke.py`）第一版点不动会话：脚本按 `create_conversation`
    返回的 `title` 去侧栏找行，而首条用户消息落库时会把会话自动改名——锚点永远匹配不上；正确取法
    是**落库后的 `sidebar` 字段（首条用户消息）**。同处的第二坑：侧栏是**虚拟列表 + 分组默认折叠**，
    `reveal` 必须每轮从 `scrollTop = 0` 重扫、滚到底没出现才再展开一个分组，否则上一轮滚到底后会
    直接判「到底了」、漏掉新行。另一半是附件夹具：**不能把文件预塞进 `data/uploads`**——
    `store_uploaded_file` 有内容级去重，会命中这份手写文件，而它没有缩略图（`_thumb_path_for`
    回空串），前端再按 `<stem>_thumb.webp` 推导必然 404；夹具放在 uploads **之外**，让附件在浏览器
    里走真实上传管线（分日目录 + 自动缩略图）。
    教训：**读界面状态的断言，锚点只能取产品自己写出来的值；走管线的输入，不要替产品预置中间产物。**
    守门：`verify/choice_panel_smoke.py`（55 项断言，见 §六 清单）。

 70. **「预览」必须与真实渲染同源，否则调的和看到的是两回事（2026-09-15，背景图编辑器）**：
     聊天背景图第一版只有一个 116px 的细条预览（比例 ≈2.85:1，真实对话区 ≈1.4:1），只能看透明度、
     根本判断不了取景；用户报障「无论上传什么图都被截断」的根因也不是渲染坏了，而是
     `background-size: cover` 的固有语义（铺满 + 裁掉溢出）**缺一个能自由取景的入口**。
     这一轮补的做法值得抄：把渲染规则抽成一个共享类 `.chat-bg-surface`，由「真实背景层
     `.chat-backdrop`」「编辑白板 `.chat-bg-board-layer`」「设置卡缩略图」三方同挂，只消费
     `:root` 上同一组变量（唯一写入点 `applyChatBackground()`）→ 三个视图不可能漂移。
     三条实测得到的硬规则：
     ① **尺寸用百分比、别用像素/transform**：`--chat-bg-size = <coverW×zoom>% <coverH×zoom>%`
     （coverW/H 由图片比例 q 与盒子比例 r 推出）。百分比相对元素自身盒子解析，所以大盒子（真实
     对话区）与小盒子（白板/缩略图，同比例）取景**逐像素一致**——像素尺寸或 `transform: scale`
     都做不到这点，还会逼你拆出"裁剪窗口"层。
     ② **缩放下限必须按比例推导**（`min(q,r)/max(q,r)` = contain/cover 之比），写死 0.5 这种常数
     在长图上够用、在宽图上就不够，需求（"任何图都能看全"）会悄悄不成立。
     ③ **"取消回滚"与"中途落库"不可并存**：编辑器里所有交互只改本地 CSS 变量，「完成」才
     `POST /api/settings`；中途落一次库，用户按取消就只回滚了本地，刷新又变回中途那个值。
     关闭路径仍按 §九.46 收敛到一个函数（Esc / × / 取消 / 完成 都经 dialog 的 `close` 事件兜底）。
     顺带三个**测试侧**教训（都是先假红、查了才发现是判据错，不是产品错）：关掉的 `<dialog>` 是
     `display:none`，量它子元素的矩形全是 0，别把"没布局"误判成"比例不对"；背景层默认强度 0.35，
     相邻色块叠上页面底色后色差可能只剩 ~18，像素判据的阈值定高（40）就找不到边界；flex 行里
     `width:auto + aspect-ratio` 会解析成 **0 宽**（flex 基础尺寸按空内容算），缩略图要显式写宽度。
     守门：`tests/test_chat_background.py`（17 例：默认值 / 旧配置补齐 / 脏值与 NaN 归一化 /
     越界 clamp / `fit` 被拒 / 兄弟字段不丢 / 持久化）；临时像素级校验
     （`verify/_check_chat_bg_editor.py/.cjs`，跑完即删）用「白板 vs 关掉弹层后的真实对话区」
     降采样逐格比色（容差 6）+「拖动 30px → 画面色块边界位移 30px（±3）」+「完整显示时留白处
     = 页面底色」三条判据，把"所见即所得"和交互数学都变成可观测量。

 71. **「看不见」等于「不会用」：取景类 UI 必须把整张图亮出来（2026-09-15，背景图编辑器第二轮）**：
     上一轮交付的编辑器是「白板 = 最终画面」，能拖能缩，但**从不显示整张图**——竖图在横框里
     要么被 `cover` 裁成一条、要么缩成中间一条纸带，用户既看不到被裁掉的部分、也无从"选一段"，
     体感就是「没做裁剪」。实测数据（600×1500 竖图 / 对话区 1008×737）：zoom=1 时只有图片高度的
     **29%** 露在画面里（`--chat-bg-size = 100% × 342%`），完整显示时图片只占框宽的 **29%**。
     几何事实：竖图要填满横框，能显示的最大高度就是 29%，"既填满又显示更多"在不变形的前提下
     不存在——所以问题不在数学，在**心智模型**。这一轮改成「概览（整张图）+ 取景框（锁定对话区
     比例、框外压暗）」：拖动取景框 = 选段，拖右下角 / 滚轮缩放框，框比图大时框会明显超出图边
     （那正是"两侧要靠模糊底补"的状态，用户一眼就懂）。同轮还补了：**模糊填充**（没铺满的两侧
     用同一张图的模糊放大版补满，不再是两条空白——比"露页面底色"好看得多）、**旋转 90°**
     （在服务端把图真的转成新文件，见 §九.70 的单源思路：改文件比在每个渲染路径叠 transform
     干净得多），以及「效果预览」从大板缩成小预览（与真实对话区同源，像素级比对实测最大差 1）。
     顺带三个**布局/交互**教训：① 内容比弹层高时**按钮行必须固定在滚动区之外**（实测「完成/取消」
     被裁在可视区外，功能等于不可用）；② 可滚动躯干**重开时要回到顶部**，否则停在上次的位置，
     概览被滚出可视区 →「拖框抓不到」（表现为"拖了没反应"）；③ 反解取景时把**范围数组**当成
     **跨度标量**去做除法，会让 position 变 NaN 再被 clamp 静默回落成 50%——同样是"看着没坏、
     其实没生效"，靠"拖动 30px → 框位移 30px"这条像素判据才被抓出来。
     守门：`tests/test_background_presets.py`（内置图存在/格式/幂等/补回/降级）、
     `tests/test_image_rotate.py`（顺时针方向由像素判定、长宽对调、落 uploads、去重、坏图报错）、
     `tests/test_chat_background.py`（取景三键的归一化/clamp/`fit` 被拒）；静态检查器 `css_content_bg.py`
     仍要求对话区内容实底。

     72. **「自由度恰好为 0」的预置态 = 用户眼里的「坏了」；裁剪工具必须能改形状（2026-09-15，背景图编辑器第三轮）**：
     上一轮（§九.71）把取景框比例**锁死成对话区比例**，只暴露「缩放 + 位置」两个自由度。
     数学上自洽，但它注定有两个死方向：点「填满」时图片在某个方向正好等于框（那个方向挪不动），
     点「完整显示」时反过来。用户连报三次同一个根因——"取景框锁死、只能缩放""只能横向切割，
     不能竖向切割""取景框不能调整大小，这完全失去了自由裁剪的意义"：**一个不能改形状的裁剪框，
     在用户眼里根本不是裁剪框**（另外"挪不动"的另一个来源是指针状态污染，见下）。
     定案：取景状态换成**自由矩形** `crop = {x, y, w, h}`（图片内相对坐标）。
     `clampChatBackgroundCrop` 保证框永远在图片内 → 两个方向永远有可动空间，"挪不动"只可能
     出现在框已经贴到图片边界时，而那时边界是**看得见**的（旧版是"框在图片中间却拖不动"）。
     编辑器给**八向手柄**（角改两向、边改单向），显示改为"取景块等比铺进对话区"（形状不匹配
     就留白，由同图模糊底补——这套视觉 §九.71 就有）。**迁移**：`zoom/position_*` 降级为
     只读的升级输入（`crop` 缺失时按旧公式换算等价区域；对"填满/完整显示"逐像素等价，
     "缩得比对话区还小、四周全靠留白"的特例收敛成"整张图等比铺"，观感只变好）。
     同轮第二个坑：**指针状态必须能自愈**。`pointerdown` 登记指针、`pointerup` 删除，一旦某次
     释放没收到（`setPointerCapture` 抛错 / 窗口外松手 / 切窗口回来），残留的"幽灵指针"会让
     下一次按下被误判成**双指捏合** → 表现正是"只能缩放、拖不动"。修法四条：主指针按下先清表、
     捏合只在 ≥2 个**非鼠标**指针时成立、`lostpointercapture` + window 级 `pointerup/cancel`
     兜底、`setPointerCapture` 用 try/catch 吞掉（**捕获失败不该让手势起不来**——实测合成指针
     会在这里抛 NotFoundError，把整个 pointerdown 处理器打断）。
     守门：`tests/test_chat_background.py`（20 例，crop 的加载期收敛 / 写入严格校验 / `crop: null`
     语义 / 旧字段并存）；临时浏览器实测（`verify/_probe_crop.py/.cjs`，跑完即删）20 项断言覆盖
     八向切割、两轴拖动、「填满/完整显示」两态、**幽灵指针残留后仍能拖**、`完成` 落库 round-trip，
     以及"旧配置换算后的取景尺寸与旧公式逐字相同"。
    发布现场还补了一条：这次 CI 红在 `Run tests` 第 5 步，根因不是产品代码，而是新测试拿**未
    resolve** 的临时路径去比产品 `resolve()` 后的存储路径（§六 ③）——已固化成
    `verify/ci_short_path_check.py`，推之前跑一次即可照出这一类问题。**注意脚本自身**：它的说明文字里不能出现「盘符 + 用户目录」字面量（本机账户目录改用 `%USERPROFILE%` 表述），否则会被 `test_no_absolute_paths` 反咬一口（2026-09-16 实际发生，已改）。

   73. **冻结版启动路径上的「重构漏改」：EXE 旁有旧目录的用户 100% 崩、官方包用户 100% 不崩（2026-09-16，外部用户报障反推）**：
    症状是双击 exe 弹错误框后退出——`.spec` 为 `console=False` + `disable_windowed_traceback=False`，
    且 `launcher.py` 在**主线程**、窗口创建之前就构造 `NaibaChatApp`，启动迁移抛出的异常直穿 `__init__`。
    根因：`_copy_legacy_data` 在 2.0.0-beta 的后端模块化重构中把签名改成
    `(source, paths: PathContext, replace_empty_target=True)`（新增**必需位置参数**），
    而 `migrate_legacy_data` 里的调用仍写 `_copy_legacy_data(paths.exe_dir)` → `TypeError`
    （同文件 `app.py:391` 的 `_copy_legacy_data(source, self._paths)` 写法是对的，典型漏改一处）。
    **判据（决定"有人崩有人不崩"的唯一变量）**：`migrate_legacy_data` 的两处提前返回必须都过不去才会走到
    崩溃点——冻结版 `app_dir` 固定 `%LOCALAPPDATA%\NaibaChat`、`exe_dir` 是 exe 所在目录，第一处（源码模式）
    必不命中，于是只看 **`exe_dir` 旁是否存在 `config.json` 文件或 `data/` 目录**：有则每次启动必崩，没有则
    永远不崩。官方 zip 只有 exe + 更新清单 + README（发布工作流从未打包数据文件），所以官方全新解压的用户
    踩不到；踩雷的是 `build-35` 及更早（`2ea24cf` 之前，那时冻结版 `APP_DIR` 就是 exe 所在目录）用过、
    并把新 exe 一直覆盖进同一老目录的用户——启动迁移是"只复制、不删旧文件"，那两样会永远留在原地。
    同批用户还挂着第二处缺陷：合并循环把 `data/` 下的**普通文件**（`server.json`）当目录喂给
    `_merge_data_tree`（首句就是 `target.mkdir`），目标已有同名文件即 `WinError 183`，被 `except OSError`
    吞掉 → 迁移自该项起**静默中断**（uploads、skills、收尾全丢）——所以修好第一处会立刻撞上第二处。
    修法三条：① 调用补 `paths`；② 合并循环区分文件与目录（目录递归合并 / 普通文件"目标不存在才复制"，
    与数据目录搬迁 `app.py:518-524` 同口径），并把 `server.json` 加入跳过名单（运行期状态档：pid/host/port/token，
    每次启动重写、产品代码无读取方）；③ `except OSError` 放宽为 `except Exception`——启动路径上的迁移失败
    只记录（`logger.exception` + `report["error"]` 进 bootstrap 的 `data_location.migration` + 数据目录
    `data-migration-warning.log`，窗口版无控制台，文件是唯一通道）绝不阻断启动，否则下一次任何非 OSError
    都会重演"弹框退出"。
    **为什么旧测试与本机全绿**：`PathContext.local()` 令 `app_dir == exe_dir`，既有全部用例与源码模式都命中
    第一处提前返回，这段代码**零覆盖**——漏改因此躲过了 2.0.0 → 2.5.0 的全部单测与 CI。
    守门：`tests/test_legacy_migration.py`（6 例，直接实例化 `PathContext` 造出 `exe_dir != app_dir` 的形态，
    含"目标已有 server.json 仍要继续搬迁"与"非 OSError 只报告不抛"两条事故回归）。

    74. **父任务取消闸门：取消之后仍能派生新 Job（2026-09-16，聊天里点停止）**：用户点停止（父 Run 落 `cancelled`）后，已在跑的 subagent 仍继续派生 comfyui Job 并跑完 6/6。根因：取消级联 `_cancel_children` 只抓「取消那一刻已存在」的子 Job，**晚于取消创建的派生 Job 没人管**；缺口的另一面是 `resume()`/`retry()` 能在已取消父下把作业复活——等于复活用户明确不要的工作。修法三条：① `JobRegistry.start()` 前置闸门，父已 `cancel_requested` 或处于 `stopping`/`cancelling`/`cancelled` 时抛 `TaskCancelled`（**不拦终态父**：回答完成后派生/恢复的后台 Job 都以终态 Run 当归属锚点，`resume()`/`retry()` 依赖它重建 Job）；② `resume()`/`retry()` 把 `TaskCancelled` 翻成 `ValueError`，HTTP 层映射 400 + 原因（不再静默 404 让人以为记录丢了）；③ 子 Agent worker 的 `finally` 兜底 `_cancel_children` 收网，并让启动扫尾对已取消父打接续标记（否则每轮启动重复尝试、刷错误日志）。守门：`tests/test_job_parent_gate.py`（6 例）。

    75. **「有推理无正文」的空流被判成整轮失败（2026-09-16，mimo-v2.5）**：推理模型长思考后偶尔整轮只回 `reasoning_content`、正文为空——用户侧看到「气泡空白」，日志里只有一句「在线模型流式响应中没有文本内容」。原口径把可重试空流（`EmptyModelStreamError`）**只留给 codex_responses**，其它在线格式直接 `RuntimeError` 判死；而实测退避后再试一次大概率拿到正文，本质是上游抖动，不该升级成整轮对话失败。修法：`reasoning` 非空而正文为空时同样抛 `EmptyModelStreamError`，走既有退避 + 可取消等待 + 带次数提示重试；**既无正文也无推理的空流保持原 `RuntimeError` 不重试**（那是真·上游拒绝，重试无意义）。守门：`tests/test_llm_runtime_retry.py` 新增 2 例（重试到上限且带状态提示 / 重试成功后正文正常返回）。

    76. **任务面板把一条派生链拆成两组（2026-09-16，面板重做后的回归）**：真实链路是 chat Run → 子 Agent → ComfyUI，而面板数据来自 `/api/tasks?jobs_only=1`（回答记录行被过滤）：按**单层** `parent_job_id` 分组时，子 Agent 与它派生的 ComfyUI 各占一组——用户看到「一个子 Agent 又变成两个任务」，且子任务没有任何从属标记。修法：`renderRunTasks` 沿**可见父链**上溯取分组锚点（父行不在面板里就用那个父 id 当锚点，同父兄弟不拆开；无父作业仍归一组），子行加 `task-item-nested`（`public/styles.css` 缩进 + `↳`），**父行也在面板里才缩进**，环状父链用 `seen` 收敛不卡死。守门：`test_tasks_aggregate` 2 例（锚点口径 + 嵌套标记，防回退成单层分组）、`verify/tasks_panel_render_check.mjs` 21 例、`verify/tasks_panel_smoke.py` 浏览器冒烟 22 例（真实 DOM + 真实 CSS）。

     77. **「探针失败」不等于「用户不要了」：失效兜底不得改写用户设置（2026-09-16，用户报障"更新后背景图丢失"）**：
     症状是升级/重启后聊天背景消失、设置卡只剩控件。DevTools 里 `<html>` 的 `element.style` 只有
     `--chat-bg-opacity`、没有 `--chat-bg-image` —— 根因不是渲染，而是 `chat_background.image` **被清空了**：
     `syncChatBackgroundFromBootstrap()` 的探针（`new Image()` 请求 `/api/file?path=<背景图>`）只要失败
     （文件已被缓存清理删掉 / 数据目录迁移后路径失效 / 启动期网络抖动）就立刻 `clearChatBackgroundSetting()`
     —— 把用户设置写回空串并**落库**。于是一次探测失败 = 永久丢失，且用户没有任何补救入口。
     同日同源的第二个坑：前端每次保存都提交**整份** `state.chatBackground`，于是"改一下背景强度"也会把
     image 一起送审，文件没了就变成「保存失败：背景图文件不存在」——**背景坏了以后连滑杆都调不动**。
     修法四条（**失效兜底只标记、不改写设置**是总原则）：
     ① 探针失败只置 `state.chatBackgroundImageMissing`（运行时标记，不落库）+ toast + 设置卡内联提示
     「背景图文件暂不可用（设置已保留）…点「更换图片」重新选择」；**唯一**把 image 写回空串的入口
     只有用户点「清除背景」；
     ② 服务端校验同步放宽：文件不在磁盘上时**不再抛错**，先按文件名在当前受管目录（含 `uploads/YYYY-MM-DD/`
     分日目录）兜底找回并迁移路径（数据目录迁移/换机器，与 `/api/file` 的既有兜底同口径），确实找不到
     就**原样保留路径**——安全边界（必须落在 uploads/backgrounds 内）一步不放宽；
     ③ 前端保存改为**只提交本次增量**（按键白名单 `CHAT_BACKGROUND_WRITABLE_KEYS`）：文件缺失不再挡住
     强度/取景等其它字段，运行时标记也不会混进请求体（多一个键服务端整份 400）；
     ④ 清理侧把背景图算作「在用」（`app._upload_path_in_use`，见 `storage/media.py` 行）——它只写在
     config.json 里，不补这条判定就会被自动清理当无人引用的旧图删掉。
     教训：**"文件暂时不可用"是可恢复状态，绝不能当作"用户不再需要"**；凡是"探测/校验失败 → 顺手清理用户
     数据"的兜底都要改成"标记 + 保留 + 给出路"，并且**探测失败永远不该触发写入**。
     守门：`tests/test_chat_background.py`（30 例：缺失文件保留路径且不挡其它字段 / 同名兜底迁移 / 兜底也要过
     格式校验 / 背景图计入「在用」且带缩略图成组保留 / 前端源码断言"探针失败不得调用清空"+ 增量提交白名单 +
     提示行 id 唯一 + 回调注册存在）；真机冒烟 `verify/_check_bg_missing.py`+`.cjs`（隔离 data_dir + 两阶段：
     缺失 → 断言接口路径保留、卡片提示「暂不可用」、背景层变量仍在、强度仍能保存；补回文件刷新 → 提示消失、
     背景自动出现；两阶段均零页面错误）。

     ---

     78. **重渲染不得顺手清掉「用户正在编辑的东西」；占位锚点别复用会被查询的类名（2026-09-16，前端 UX 排查 P1）**：
     `renderMessages` 以前无条件把编辑态退掉（注释还写着"重渲染会换掉编辑框 → 编辑态必须同步退出"），
     而它的调用方不只用户操作：`syncCurrentConversation` 的轮询（后台任务写消息、流式落库都会引爆）、
     `openConversation`、`renderMessages([])` 三处都算。后果是编辑被静默取消、改到一半的字掉回底部输入框、
     附件丢失、发送键从「重新发送」变回「发送」（此时再按回车就变成发新消息），全程零提示。
     正确形状是**分流**而不是退出：同会话刷新且被编辑消息还在 → 先归位（`restoreInlineComposer({resume:true})`：
     只搬节点、保 `editComposerStash` 与占位锚点，不还草稿）→ `replaceChildren` → 按 `[data-message-id]` 找回
     新生成的行复挂（懒加载窗口里没有就先 `ensureMessageRendered`）→ 回填文字/附件/选区；只有消息真没了或换会话
     才退出，且**必须把进编辑前的底部草稿还回去 + toast 告知**。
     **顺序是硬要求**：`replaceChildren` 会连坐销毁搬进气泡的 composer-wrap，归位必须发生在渲染之前。
     两个派生坑：① 首次进入编辑与复挂必须共用同一个 `mountEditComposer`，否则复挂漏一步就是"输入框在、附件没了"的半残态；
     ② **占位锚点复用了 `.composer-wrap` 类名、且被插在真 composer 前面**，复挂时再 `querySelector('.composer-wrap')`
     会抓到那个空 div（现场表现：气泡里只有一个空 wrap、真输入框还留在底部）——一律写 `:not(.edit-placeholder)`
     或由调用方把 `wrap` 节点传进来。
     守门 `tests/test_regenerate_reply.py::EditComposerBridgeSourceTests`；冒烟 `verify/regenerate_smoke.py` 用例⑤⑥
     （拦 `/api/conversations/<id>` 造出真实的一次 `syncCurrentConversation`：append → 保现场，drop → 还草稿；
     反证过：退回旧行为时同步一到就是 `inRow=false` / `body.is-editing-message=false` / 发送键变回「发送」）。
     测试侧同一处的教训：编辑态里**不能用 `:has-text(原文)` 定位消息行**——正文已被编辑框替换、清空编辑框后连输入
     镜像层也只剩零宽字符 → 一律按 `message id` 定位（`rowSel()`）。

     ---

     79. **重渲染还会拆掉「正在写的那条回复」；DOM 是流式期的唯一真相（2026-09-16，用户实测紧急缺陷）**：
     同一次 `renderMessages` 重构只盯住了编辑态（§九.78），漏了另一种同样被 `replaceChildren` 连坐的东西——**活动流的 run 行**。它承载着正在流式输出的正文，而正文在 run 结束之前**只存在于 DOM**（落库发生在终态）。触发路径极短：后台任务跑着时点任务面板里的任一行 → `openConversation(当前会话)` → 整体重渲染 → 气泡被拔掉且无人复挂；更隐蔽的是 `resumeConversationRun` 见 `abortController` 仍在会**直接 return**（同会话无需重连），于是一次重渲染就永久切断了「流」与「屏幕」的联系——流还在写，写进的是一个已经脱离文档的节点。用户看到的就是「AI 回复消失，只剩下前一条用户侧消息」，等不到、刷新也没用；若此时重启，这条 in-flight Run 被判 `interrupted`，**已产出的正文也从没人重建落库**，于是「重启、刷新都不管用」（用户那轮的库里真的只剩一条用户消息）。三处一起补：
     ① 前端复挂活动流气泡（判据见 §3.4 `04-messages`；**限定 `role==='assistant'` 这一条是踩坑得来的**——初版不限角色，而这一轮的**用户**消息 metadata 里同样带 `run_id`，于是复挂被自己挡掉，冒烟第一跑就红）；
     ② 启动时把中断轮次已产出的内容重建为 partial 消息（`recover_interrupted_runs`，见 §3.3 `naiba/run/chat.py`）；
     ③ 更新脚本重启不得隐藏主窗口（见 §3.3 侧翼）。
     方法论：**这条是靠「真链路冒烟」逼出来的，不是靠读代码**——单测只能断言源码里有 `container.append(liveRunRow)`，真正证明「同一节点活过重渲染 + 后续 delta 仍落进它」的只有 `verify/stream_rerender_smoke.py`（并且必须做反证跑一次）。顺带记一笔：`renderMessages` 的调用方远不止用户操作（轮询、终态收口、任务面板跳转都会引爆），**任何「重渲染」都不能假设屏幕上那些非消息节点（composer / 活动流气泡 / 浮层）可以随手重建**。
     守门 `tests/test_interrupt_recovery.py`；冒烟 `verify/stream_rerender_smoke.py`。

    80. **任务卡片整块可点 = 误触切走会话；同一处还让日志在每轮重绘里被静默擦掉（2026-09-17，用户实测）**：
    用户报障两件事，根因是同一处设计——`#taskList` 的 click 兜底分支把**整张卡片当切换入口**
    （`closest('[data-task-id]')` 命中任意空白）：
    ① **误触即切走**：读任务名 / 选中文本 / 点日志区边角都算「点卡片」，于是 `openConversation(...)`
    + `$('#tasksDialog').close()`，正在看的日志连带被打断；且那次 `openConversation` 没接 `.catch`
    （侧栏路径有，这条路径没有），会话取不到时是一次未捕获的 Promise 拒绝。
    ② **日志展示缺陷**：`renderRunTasks` 每轮轮询（活动任务 1.5s）都整体重建 `#taskList`，
    而日志只按 `?after=<cursor>` 增量续拉、`taskLogCursors` 只增不减 —— 新 DOM 里没有任何日志行，
    续拉又只带回「上一轮之后」的新行，于是展开着的日志每轮重绘都被擦空（hint 跟着跳回「暂无输出」）；
    `cursor` 不回退还让「收起 → 再展开」同样只剩新行，历史行彻底拿不回来。
    早先还能靠"别重绘"绕过，但任务的终态收口、会话同步、清理都在调 `renderRunTasks`，
    重绘不可能避免（§九.79 同一条教训：**DOM 不是这些非消息内容的真相**）。
    修法（`public/js/08-conversations.js` / `15-bind-events.js` / `styles.css`）：
    ① 切会话收窄成显式「跳转」按钮（`data-task-open`，固定在「详情」左侧）：卡片本体不再是动作区，
    点任务名/说明/空白一律无副作用；跳转失败按侧栏口径 toast 出声；无 `conversation_id` 时只提示。
    ② 日志改为「缓存是唯一真相、DOM 只是投影」：`taskLogLines` 留最近 200 行，重绘/重开后先按缓存
    回填再续拉增量；行文本**先写缓存再落 DOM**（拉取期间的重绘不会丢这批行）；`taskLogLoading`
    防同一 cursor 被并发消费两遍；`taskItemById` 改为在面板内按 `dataset` 逐个比对（不拼属性选择器：
    id 含引号/反斜杠会拼出非法选择器，全局选择器还可能命中面板外的同名 data 属性）。
    ③ 贴底策略：`taskLogStick` 由 `#taskList` 捕获阶段的滚动委托实时上报（scroll 不冒泡），
    用户上翻读历史时新行到来与整表重绘都不把他拽回底部；任务从列表消失时回收展开态与三张 Map。
    ④ 「详情/日志」入口与「有没有结构化详情行」解耦：入口与日志容器常驻（`taskRowMarkup`），
    否则 ComfyUI 那类 result 整块是对象、没剩标量字段的任务永远打不开日志。
    守门：`verify/tasks_panel_render_check.mjs` 新增 4 条 markup 断言（跳转按钮每行都有、位置在详情前、
    详情入口常驻、日志容器常驻）；`verify/tasks_panel_smoke.cjs` 新增 5 条浏览器断言（跳转按钮、
    **点任务名不跳转且面板保持打开**、日志按 cursor 拿到 5 行、等到一次真实重绘、**重绘后行数不减少**）；
    `verify/stream_rerender_smoke.cjs` 的触发点改为点「跳转」按钮（仍是 `openConversation(当前会话)`
    → 重渲染，§九.79 的活动流复挂回归照旧有效）。日志那两条断言做过反证：把回填改成空数组后必红。

81. **第一批三项（全文搜索 / 分支导航 / 删除单条消息）落地纪要 + 一个「只看源码看不出来」的交互事故（2026-09-17）**：
    三项一次做完（理由：前两项无 schema 变更、分支导航只带一次 v17→v18 迁移，三者都动侧栏/消息操作区，守门与冒烟可合并跑一轮）。落地口径：
    - **搜索**：**不复用 `search_history`** —— 那是给模型用的「按会话分桶 + 上下文边界」口径（bucket、每会话最多若干条、`in_context` 供模型自述），侧栏要的是「一串按最近使用排序的命中 + 命中片段」，两者语义不同，硬合并会把模型侧口径泄漏给 UI。新 `search_messages` 一次扁平查询 + 一次 GROUP BY 量级的分区序号，**`truncated` 显式回传**（绝不静默丢结果）；**片段是原文连续子串**（前端高亮不再回查正文）；`q` 空 400 而不是「返回空」——空查询会白扫全库且结果无意义。
    - **分支导航**：**侧栏不画树**（树与 `updated_at` 排序根本冲突，多级分支还会打乱虚拟列表的行索引），改为「平铺 + 徽标 + 点开面板」。`branched_from_id`/`branch_message_id` 是**纯增量登记**，**存量分支明确不回溯**（按标题 `(N)` 弱推断不可靠，宁可显示无关联）；**源被删不是错误**，`branch_chain` 回 `source_deleted=true` 让徽标降级，只有「会话本身不存在」才 404。`/branch_chain` 路由**必须排在 `/api/conversations/<id>` 兜底之前**（否则会被当成会话 id 吃掉），路由顺序有守门测试钉死。
    - **删除**：`single` / `turn` 两个粒度，`role='session'` 的遗留标记行**拒绝**（那是 `delete_session_start` 的语义，误删会把上下文起点悄悄往前挪）；`turn` 仅 user 可用且**遇下一条 user 即停**。撤销走「快照 + 原字节插回」，**已存在 id 跳过（幂等）**，所以判据是「删了又撤销 → `build_model_history` 逐字节一致」——这是前缀缓存唯一真正重要的财产。
    - **事故（靠冒烟抓到，源码级看不出来）**：`askDeleteScope` 里两个按钮写成同一个条件 `hidden = !isUser`，于是**assistant 消息的确认框只剩「取消」，AI 回复一条都删不掉**。单测当时只断言了「按钮 id 存在」，照不出来；是浏览器冒烟走真链路点下去才红的。修法：`singleButton.hidden = false`（「仅删这一条」对 user/assistant 都合法）+ `turnButton.hidden = !isUser`，并补 `tests/test_message_delete.py::DeleteDialogSourceTests` 把**可见性条件**（不只是「存在」）钉死。教训重述：**「按钮存在」≠「用户能用」，DOM 动作类的护栏必须断言到可见性/可用性那一层**。
    - **测试适配（预期内的红）**：新增「删除」按钮插进 AI 操作区（复制→重新生成→**删除**→新会话），两处**源码级**断言（`test_regenerate_reply` / `test_session_start`）因旧顺序串失配而红——这是计划要求的顺序变更，**更新断言**而不是回退功能；§六 已同步改写这两条的描述。
    - **写新单测时的坑（记录以免重犯）**：`create_conversation(title=...)` 的标题会被**首条 user 消息顶掉**（`add_message` 的既有行为，`message_count <= 2` 且未自定义时改写标题），所以断言「源标题」「会话标题」必须取**库里的当前值**，不能拿 create 时的字面量；另 `branch_conversation` 回的是 `{conversation, branch_message}` 而**不是**会话本体（取字段要先取 `.conversation`）。
    守门：`tests/test_message_search_api.py`（17 例）/ `tests/test_branch_chain.py`（23 例）/ `tests/test_message_delete.py`（37 例）；冒烟 `verify/batch1_smoke.py` + `.cjs`（39 项断言，含 6 张截图）；冻结版实跑自检 `verify/frozen_batch1_check.py`（24 项断言，证明新接口与新 DOM 钩子真的进了 exe）。

82. **「请求已经超出窗口」被说了三遍，用户一次也没听见（2026-09-17，Q1）**：
    本地模型因上下文不够而失败时，后端其实**明确说了原因**，但三条路径各吞一次，用户在界面上
    看到的分别是「流式响应中没有文本内容」「请求失败：HTTP 400: {原始 JSON}」「永久停在等待本地
    模型资源」：
    ① **Ollama 把错误整块放在流里的 `chunk["error"]`**——流解析循环从头到尾**没读这个字段**，
       错误随 chunk 一起被丢掉，最后以「没有文本内容」收场（`naiba/llm/stream.py`）；
    ② llama.cpp / Unsloth 的 400 走 `_summarize_http_error` 后**原文透传**，用户拿到的是
       `HTTP 400: {"error":{"code":400,"type":"exceed_context_size_error",…}}`——字段名它都认不全，
       更别说「该怎么办」；
    ③ 4xx 溢出的语义还很容易被**下面那串兼容链吃掉**：`stream_options`/`tools`/`reasoning`
       三级「剥离字段后重试」只要有一个正则匹配上，就会白打一次注定失败的请求，再把错误冲淡一遍
       ——所以溢出判定必须放在 HTTPError 分支的**最前面**（`is_context_overflow`）。
    修法（口径统一在一处）：`ContextOverflowError`（定义在 `stream.py`，`runtime.py` 再导出以免
    循环导入）+ 特征串判定（只作用于**服务端错误体**，不碰用户正文，无误伤面）+ 一份文案
    （`context_overflow_message`：后端名 + 当前窗口 + 「新会话 / 设置 → 模型」两条动作）。
    **窗口取后端自报的真值**：llama.cpp/LM Studio 回 `n_ctx`、DeepSeek/OpenAI 回
    `maximum context length is N`，它比本机配置/探测值可信——用户对着真值才知道该往设置里填什么
    （冒烟里配置写 32768、后端说 4096，文案必须显示 4096，这条是断言）。
    教训：**「静默吞掉错误字段」和「原文透传」是同一种故障的两个极端**，都在把「后端说了什么」
    变成「用户猜吧」。凡是后端明确回报的失败，都要有**一处**负责把它翻译成「为什么 + 下一步」。
    守门：`tests/test_context_overflow.py`（27 例）；冒烟 `verify/q1_context_smoke.py` + `.cjs`
    （20 项，含真链路假后端）；冻结版 `verify/frozen_q1_check.py`（14 项）。

83. **`except` 处理块里抛出的异常，不会被同一个 `try` 的其它 `except` 子句接住（2026-09-17，Q1 实测踩中）**：
    溢出文案我本来只想写一处——在 `_complete_online` 的重试循环里加一个
    `except ContextOverflowError: raise <补好文案的同类异常>`，让所有抛出点（流解析、HTTPError 分支）
    都过这道漏斗。实测只有流解析那条生效，HTTPError 分支抛出来的照样是原始 detail。
    原因不是顺序问题，而是 Python 语义：**异常是在 `except urllib.error.HTTPError` 块里抛的，
    它根本不会回到同一个 `try` 的子句表里找处理器**（那些子句只覆盖 `try` 体）。
    修法：把「拼文案」抽成 `_context_overflow_error(exc, profile, ...)`，两个抛出点都显式调用它，
    并在 HTTPError 分支就地注释写明这个语义坑。
    教训：**想用「统一出口」收拢异常时，先分清算它到底经过几次 `try`**——`except` 块里的 `raise`
    属于「往外走」，不属于「往上走」。这类 bug 单测会红得很直白（断言文案里有供应商名，实际是原文），
    但**读代码时几乎看不出来**：漏斗看起来明明在那儿。

84. **`finish_reason="length"` 有两种相反的成因，界面却只给一种建议（2026-09-17，Q1）**：
    本地模型的窗口是**总窗口**（prompt + 生成共用 n_ctx），生成到上限时后端报的也是 `length`
    ——和「撞 max_tokens 输出上限」在 wire 层长得一模一样。旧实现一律按输出上限处理（自动续写一次
    + 提示「已达模型输出上限」），于是**窗口真的满了的时候，用户照着提示继续写/续写，只会让请求
    更大、更快撞死**。
    修法：`_truncation_info` 增加 `cause`——**本地**模型且 prompt+completion ≥ 窗口 95%
    （`_context_saturated`，用 `last_usage` 的 total 或 in+out）判 `context`，其余 `output`；
    前端 `truncationNotice` 按 cause 分文案（「上下文窗口已耗尽，建议『新会话』后继续」 vs
    「已达模型输出上限」）。**老数据没有 cause 键时按 output 显示**（不猜、不误报），前端因此必须
    写 `cause === 'context'` 这种显式判定，不能写成 `cause !== 'output'`。
    为什么只对本地判：在线的 `max_tokens` 会先截住输出，总量离窗口很远；把近窗口当唯一判据
    对在线也适用，但会动到长期验证过的在线续写行为，故本轮明确不动（要动另开一轮，见计划 Q1 §D）。
    守门：`tests/test_context_overflow.py::TruncationCauseTests` + 真链路冒烟的两条提示行断言。

85. **手机端「粘贴」是个死按钮，而且是我们自己把系统菜单关掉的（2026-09-17，用户手机截图报障）**：
    症状是输入框里长按 → 弹出**我们自己的**右键菜单 → 点「粘贴」→ 「粘贴失败：浏览器未授权」。
    根因是两层叠加，缺一层都不会出事：
    ① **网页读不到剪贴板是常态**：`document.execCommand('paste')` 被所有现代浏览器禁用；
    `navigator.clipboard.readText()` 只存在于**安全上下文**（https / localhost / 桌面内嵌壳），
    而手机经局域网访问的是 `http://192.168.x.x:8765` ——非安全上下文里 `navigator.clipboard`
    **对象根本不存在**（不是"权限被拒"，是压根没有）。
    ② **系统长按菜单才是手机上唯一通路**，而我们的 `contextmenu` 处理器对可编辑元素无条件
    `preventDefault()` ——把浏览器的原生文本菜单（复制/粘贴/全选）一起关掉了。
    于是用户面前只剩一个不可能成功的按钮。教训：**凡是"网页天生做不到、但平台菜单做得到"的能力，
    绝不能用 `preventDefault()` 把平台通道堵上**；判定要在 `preventDefault()` **之前**做。
    修法：`pointerdown`（捕获阶段）记下 `lastPointerType`，`contextmenu` 命中触摸/手写笔时不拦截，
    交给系统菜单；程序化两条通道都不可用时，提示必须给**可行动作**
    （`PASTE_UNAVAILABLE_HINT` = 「请按 Ctrl+V，手机可长按输入框用系统菜单」），
    不许只说"未授权"——那句话用户念十遍也找不到下一步。
    守门：`tests/test_context_menu.py`（长按判定必须排在 `preventDefault()` 之前 + 提示含 Ctrl+V/长按）
    + 冒烟 `verify/mobile_ui_regression.cjs`（用 `ev.defaultPrevented` 的真值判定触摸不拦截、
    鼠标右键仍走自定义菜单、非安全上下文下点「粘贴」给的是可行动作）。

86. **「收起顶栏」只藏了一半，用户看到的还是「API 栏 + Agent 栏」（2026-09-17，用户手机截图报障）**：
    旧实现把收起定义成「隐藏 `.topbar-actions`，并把 API / Agent 两个下拉**收拢到第 1 行**」——
    想省的是那一行操作区的高度。但用户的期待是"收起 = 只剩那条箭头"：他看到的仍是一行 API 下拉
    + 一行 Agent 下拉 + 一条细箭头，于是报「收起的时候还有 api 栏和 agent 栏」。
    教训：**"收起"的判据是"还剩什么"，不是"藏了哪一块"**；省高度是按行算的，只藏半行不会让用户感知到，
    只会让他觉得按钮没生效。修法：`.topbar.compact > *:not(.topbar-collapse-toggle) { display: none; }`
    一条规则盖掉全部子元素（API / Agent / 操作区 / 侧栏按钮），单列一行；同时把「展开顶栏」文字
    亮出来（收起态它成了整个顶栏唯一的入口，裸 18px 箭头认不出来）。
    护栏：隐藏规则必须有**文字入口**作补偿（§九.44 的"不许靠隐藏文字省空间"说的是不许把功能藏在
    无字标签里，不是不许收起），且点一次要全部恢复——冒烟实测顶栏 251px → 49px、可见子元素恰好 1 个。
    守门：`tests/test_mobile_topbar.py`（`test_compact_hides_every_child_but_the_toggle` +
    `test_toggle_label_only_shows_when_collapsed`）+ `verify/mobile_ui_regression.cjs`。

87. **「Agent 能干什么」在界面上无处可问：必要信息不许只挂在 `title` 上（2026-09-17，用户提需求）**：
    用户要问的三件事（Agent 是什么 / 三种预设怎么选 / 要用 ComfyUI 怎么办）都是**使用前就必要**的信息，
    而界面上只有三个名字（通用 / 编程 / 短剧）和一个 Agent 下拉；更要命的是**能力边界不可见**——
    「能干什么、不能干什么完全由工具箱决定，没勾的工具一律用不了」这条硬规则，用户只能靠猜。
    修法：Agent 选择器左侧挂一个「?」圆钮，点开是可点的说明层。
    教训：**判断一个信息入口该不该"可点"，看它有没有第二条能读到的通路**——
    ① 必要信息（Agent 是什么、要用 ComfyUI 怎么起步）→ 必须**可点**：手机没有 hover，`title` 在触摸设备上
    等于不存在，而这条说明恰恰是新用户唯一能问到的地方。开合规则直接沿用上下文用量弹层那一套
    （`position: fixed` + 打开时挂 `document.body`——顶栏容器有 overflow 裁剪，挂原地会被切）。
    ② 补充信息（重名工作区的目录路径，§九.88）→ 可以留 `title`，因为它只在"同名歧义"时才需要看一眼。
    反过来也成立：**别把必要信息塞进图标按钮里**——18px 的 `?` 靠 `aria-label` 服务读屏，
    但用户找不找得到入口靠的是位置（紧贴 Agent 下拉左侧）+ 悬停 `title` 提示。
    这跟 §九.44「不许靠隐藏文字省空间」不冲突：那条说的是**功能入口**不许无字，不是每个按钮都得写满字。
    **本轮明确不做**：Agent 编辑界面的工具页不加逐工具手写说明文字——工具说明取自工具目录的
    `description`（`buildToolCard` 已渲染两行 + `title` 全文），手写一份必然与目录漂移，
    这也是弹层正文能写「Agent 的每个工具在设置页都有说明」的依据。
    守门：`verify/agent_help_popover.cjs`（按钮可见、点击开且挂 body、两条 ComfyUI 路线与三种预设
    都在文案里、`aria-expanded` 同步、点外部关闭）。

88. **工作区允许重名：「名字」被当成标识，用户就分不清在往哪写文件（2026-09-17，用户提需求）**：
    工作区分组按**名字**归并，于是两个不同目录都可以叫「素材4」——下拉里两条一模一样的「素材4」、
    侧栏里合成一组。名字对得上、目录对不上，而**工作区决定工具能碰哪些路径**，选错了就是往错的目录写东西。
    教训：**凡是把"用户看得见的名字"当键用，就要问一句"重名会怎样"**；这里后端归并规则不能动
    （历史数据已按名字分组），那就必须在界面把区分手段补上——每处都带上目录这条补充信息：
    下拉每个 `option.title` = 该工作区目录、`select.title` = 「选择工作区（当前：<目录>）」、
    侧栏分组头 `title` = 「名字（目录）」，未分组 / 空目录一律**兜底为名字**（不许出现空白或 `undefined`）。
    另有一条限度要写明：**`title` 不算"已验证"**——原生 `<select>` 的下拉项由浏览器决定是否呈现 `title`，
    脚本能断言的只有"属性写对了"，"用户真的滑得到"必须真机确认；所以真正的兜底是 `select.title`
    （悬停闭合态的下拉一定可见）与侧栏分组头这两处。
    守门：`verify/agent_help_popover.cjs`（注入两条同名不同目录的工作区 → 断言 option.title /
    select.title / 分组头 title 三个口径，含未分组兜底）。

89. **源码模式与冻结版各持一份 `config.json`、却共用一个数据库 ⇒ 工作区注册表会分叉（2026-09-17，用户报「工作区不能切换了」）**：
    症状：源码模式里「本来是 naiba-chat 的会话，现在不显示 naiba-chat」，输入框旁的工作区下拉只有
    「未分组」，切无可切。根因（**与前端改动无关，三条都有实测证据**）：
    ① `workspaces` 注册表住在 **config.json**（`config.data["workspaces"]`，库里没有这张表），
    而 config.json 属于 **APP_DIR**——`PathContext` 语义：源码模式 = 仓库根、冻结版 = `%LOCALAPPDATA%\NaibaChat`
    （§九.41③、§九.67 已记录该语义，此处是它第一次真的咬人）；
    ② 两个模式 **共用同一个 `data_dir`** ⇒ 库里会话的 `workspace_group`（实测 17 条 `素材4` / 6 条 `naiba-chat`）
    照旧是真值，只是**源码模式那份注册表是空的**（`workspaces: []`）；
    ③ 于是下拉只剩「未分组」一项——更糟的是 `select.value = currentGroup` 赋值失败会**静默落成 `''`**，
    界面把「属于 naiba-chat 的会话」显示成「未分组」：状态显示错，且一旦切走就切不回来（下拉里根本没有那一项）。
    排查口径（可复用）：**先分辨"哪份 config、哪个库"**——`%LOCALAPPDATA%\NaibaChat\config.json`（冻结版，
    自带 port 8765）还是 `<仓库根>\config.json`（源码模式，port 8799）；再把 `workspaces` 与
    `SELECT workspace_group, COUNT(*) FROM conversations GROUP BY 1` 对一遍。用户这次是"exe 里有、源码模式里没有"。
    处理：两份注册表对齐即可（逐字复制 `workspaces` 数组；`data_dir` 相同 ⇒ 指的是同一批目录）。
    **`workspaces` 在 `update_settings` 的键白名单里，改完不会被应用自身的保存抹掉**（这点必须先确认，
    否则修了等于没修）。改完必须**重启实例**——`workspaces` 只在启动时从 config 读进内存。
    **已实现（2026-09-17，用户拍板「做」）**：下拉遇到"当前分组没注册"时不再静默退化成「未分组」——
    `renderComposerWorkspace()` 先算出 `orphan`（`currentGroup` 不在候选 `options` 里），把它**补进下拉**
    并标「未注册」，`select.value` 照旧赋真值（保住"这条会话属于谁"的显示），`select.title` 改成
    「当前工作区「X」未在本实例注册（可在侧栏「新建工作区」登记同名工作区）」。
    边界（**改这里必须守住**）：孤儿分组**只用于显示，绝不回写 config.json**——它只证明"这份注册表看不到
    它"，不代表用户想登记它；一旦回写，源码模式的空注册表就会被库里的历史分组悄悄填满，两边反而更难对齐。
    补登记的入口永远只留给用户自己点「新建工作区」。
    待定（未做，用户明确否决）：两个模式共用库却各持注册表，是否加"库里出现过但未注册的分组"启动对账提示
    ——**会影响其他用户**，先不做。
    证据：临时探针（真实前端）实测注册表为空 ⇒ `options=['']`、`value=''`；把两条注册表填回 ⇒
    `options=['','素材4','naiba-chat']`、`value='naiba-chat'`、`select.title` 带当前目录。
    守门：`verify/agent_help_popover.cjs` 第 ③ 组断言；反证过——把 `public/js/08-conversations.js` 换回
    改动前的版本重跑 ⇒ **9 项失败**，其中「未注册分组没进下拉」直接打出 `values:['']`、`labels:['未分组']`。

90. **截图必须拍在"被断言的那个状态"上，坐标不能复用（2026-09-17，自查回归脚本时发现）**：
    `verify/agent_help_popover.cjs` 里那张"未注册工作区"的截图连出两个错，**而断言全 PASS**——
    ① 复用了顶栏那条 `clip: {x:0,y:0,width:1280,height:64}`，于是拍到的其实是顶栏，与
    `agent_help_button.png` **字节完全相同**（`md5sum` 一比就露馅）；② 更隐蔽的是，状态回正
    （把注册表补齐）那步和"未注册"断言写在**同一个 `page.evaluate` 里**，等脚本走到截图那一行，
    页面早已是恢复后的正常形态——**拍的是"改好之后"的样子，而不是"要证明的那个样子"**。
    教训：**留证截图要跟断言同生死**——① 坐标/元素必须指向被断言的那个节点（用 `locator.screenshot()`
    让它自己算，别手写 clip）；② 需要"先坏后好"两态的用例，**每态各自 evaluate + 各自截图**，
    回正步骤永远排在截图之后；③ **截图不参与断言，所以错了也没人喊**，只能靠人看：
    批量出图后核一次"有没有两张图 md5 相同"（相同基本就是坐标复用的信号），关键截图自己打开看一眼。

91. **"该保留什么"有两份名册就必然漂移：清理脚本差点删掉自己依赖的 harness（2026-09-17，自查发现）**：
    `verify/cleanup_verify.py --apply` 只认模块里的 `KEEP` 常量，而"哪些是复用资产"另有一份口径在
    `.gitignore` 的 `!verify/*` 白名单里；两份名册各记各的，于是**7 个受控脚本压根没进 `KEEP`**
    （`_serve_tmp.py`、`q1_context_smoke.py`/`.cjs`、`stream_rerender_smoke.py`/`.cjs`、
    `frozen_q1_check.py`、`frozen_interrupt_check.py`）——其中 `_serve_tmp.py` 是文档点名必留、
    还被 `tasks_panel_smoke.py` 依赖的隔离实例公共 harness，删掉等于当场打断工具链。
    更值得注意的是：脚本 docstring 明明写着"不碰仓库任何已跟踪文件"，实现里**根本没有这层判断**。
    补完兜底**还要把名单本身对齐**：那 6 个已补进 `KEEP`（`_serve_tmp.py` 更早就显式写在里面），
    否则"名单漂移"会一直挂着、每次清理都靠兜底救。
    守门：`tests/test_verify_assets.py`（三份名单互相对齐——白名单里存在的文件必须在 `KEEP`、
    `KEEP` 里的名字必须在白名单、`_serve_tmp.py` 必须在 `KEEP`；只有三层一致才谈得上"可复用资产"）。
    教训：**同一件事有两份名册，要么合成一份、要么让一份成为另一份的兜底**——现在保留集合是
    `KEEP ∪ git ls-files verify`：`.gitignore` 白名单（= §六 登记口径）是唯一真相，`KEEP` 退化成
    可读性声明，再漏登记也没有破坏力。配套两条：**声明式的安全边界必须落到代码里**
    （docstring 里的承诺不算数），以及**破坏性脚本改完必须 dry-run 看一眼"将删清单"**
    （本轮正是看到 `_serve_tmp.py` 出现在示例里才揪出来的）。

92. **图省事的取样环境会静默改写"真实形态"：截图里卡片少了一大半，断言却全绿（2026-09-17，用户看图时发现）**：
    用户一句「这截图你不看的吗，`agent_help_popover.png` 快捷卡片怎么少了那么多」点中要害。查下来：
    `verify/agent_help_popover.cjs` 为了省事**自建静态 `public/` 服务**（理由写在脚本头：这两处只考前端
    形态与事件决策，起真实例是浪费）——但静态服务没有后端，`/api/starter-prompts` 404，开始页就只剩
    `index.html` 里写死的那两张 Skill 卡，**由 `BUILTIN_STARTER_PRESETS` 驱动的那 6 张全部消失**：
    真实界面 9 张，截出来 3 张还被我那张弹层挡掉两张。断言一条都不受影响（它们考的是弹层与 `title` 属性），
    于是**测试全绿、证据全错**，而且我连图都没打开看就交出去了——这不只是 §九.90 那个"拍到错状态"的技术问题，
    **更是"把产物当成了任务的终点"**：留证的截图，本身也是要被人看的产物。
    教训：① **凡是"给人看"的产物，交付前必须自己看一眼**（md5 只能证明两张图不一样，证明不了图是对的）；
    ② **证据截图的取样环境必须与用户所见一致**——配置驱动的 UI（卡片 / 供应商 / 工作区）只有在真后端下才存在，
    所以"图省事用静态页"这条路对**要留证的**脚本是禁的；宁可多花 3 秒起 `verify/_serve_tmp.py` 的隔离实例
    （真后端 + 独立 config/data_dir/端口，不碰用户数据）；③ **意识到静态取样会丢数据后，就该把"该有的东西在不在"
    也写成断言**——现在开始页卡片数（`BUILTIN_STARTER_PRESETS` 张数由 `.py` 现算传给 Node）是永久护栏，
    再退回静态取样会立刻报红；④ 真后端带来的新麻烦要一并处理：**会话同步轮询（10s）会按服务端状态重渲染并冲掉
    注入的 state**，跑前必须 `state.syncPolling = false` + 清 `state.syncTimer`，否则又变成"拍到的不是被断言的状态"。

93. **「问不到」被当成了「没成功」：发布门禁误报（2026-09-17，`release_watch.py` 在发布**成功**之后报失败）**：
    现象：脚本跑满 20 分钟超时、退出 1，但 GitHub 上 tag / Release / 三个资产 / 哈希**全都正常**。
    根因：脚本走**匿名 GitHub API**（60 次/小时），额度耗尽后 API 一律回 403 `X-RateLimit-Remaining: 0`；
    而 `wait_for_run` 的 `except Exception` 把它当「网络抖动」每 15s 重试一次，从头到尾**没有区分
    「拿不到答案」与「答案是失败」**——一个发布门禁把自己拿不到信息表达成了坏消息。
    教训：① **门禁脚本必须把「无法判定」与「判定为失败」分开表达**，前者既不能吞掉、更不能冒充后者；
    现在额度耗尽会**立即降级**到静态口核验（`git ls-remote --tags` 看 tag + `releases/latest/download/`
    取 manifest 与 exe 对哈希，全程不吃额度），真正的网络中断则退出 **2**（语义 = 没核完，不是失败）；
    ② **匿名额度真的会被吃满**：排查 GitHub 状态优先用静态口与 `git ls-remote`，别反复重试 API
    （这次就是重试到额度见底、随后每个后续调用全 403）；③ **这台机器到 GitHub 的 TLS 会偶发
    `UNEXPECTED_EOF_WHILE_READING`**（额度探测和 84MB 的 exe 下载都撞到过）⇒ 任何取数都要带重试，
    且**大文件重试必须整体重下**——断点续传会把两段不同内容拼起来算出一个假哈希，比重新下载危险得多。

94. **「有效用量」不认分割线：后端早已按分割线重算历史，前端却把满量锁留在屏幕上（2026-09-18，终端用户卡死在交接流程的最后一步）**：
    现象（在线 API）：会话聊满 → 输入框锁成「上下文已满，请新建对话后继续」；让模型写交接文档，
    `reset_context` 成功、分割线落库、种子消息也填进了输入框——**输入框仍是禁用态，种子发不出去**；
    改点回复上的「新会话」按钮，同样圆环 100%、锁不解除。用户卡死在交接流程的最后一步。
    根因：`build_model_history` 从最新分割线重算上下文（后端**正确**，轮首闸门也只数线以下的消息），
    而前端取「有效用量」的 `03-media.js::updateContextUsage` 只找「最后一条带 `usage` 的 assistant」，
    **从末尾向前扫时不停在分割线上** ⇒ 分割线之后有效上下文≈0，圆环与锁却按线**以上**的满量算。
    更致命的是叠加态：`context_full` 事件置的 `state.contextAtCeiling` 唯一的清除点是
    `renderContextUsage` 的「无 usage」分支，而这条路径**永远能找到旧 usage** ⇒ **永久锁**。
    教训：① **「有效量」的取数口径必须与「重算口径」同源**——后端以分割线为界裁剪历史，前端算用量
    就必须以同一条线为界；同一个概念在两处各写一套判定，是这类 bug 的温床（改一处必须回头核对另一处）；
    ② **把「确证已重置」与「扫不到数据」分开表达**：`contextReset`（分割线已落库）要能越过
    「运行中不得用历史值覆盖实时值」的保护（§九.52）——`done` 那一刻 `chatBusy` 还没落下，若只改扫描、
    不放这条保护，`updateContextUsage(null, event.message)` 会被守卫直接 `return`：种子消息被填进一个
    **仍然禁用**的输入框（用户看到的正是「填好了却发不出」），得等收尾重渲染才恢复。两种写法最终都能
    通，但「重置 = 立刻归零」不该依赖收尾时序，而且"扫不到 usage"必须继续受该保护约束；
    ③ **从末尾向前扫到「最新一条」分割线即停**，不要"先收集再过滤"——任何一条旧线存在时后者都会算错；
    ④ **用户卡住的功能，验证要落在「能不能继续打字」上**：本项目 `updateContextComposerLock` 是输入框
    disabled/placeholder 的唯一写入点，`updateSendButtonState` 是发送键的唯一写入点，所以判据取
    「输入框 disabled + placeholder + 圆环百分比 + 打字后发送键可用」四件事，而不是"函数被调用了"；
    ⑤ **修完必须反证**：把 `updateContextUsage` 回退成旧写法重跑 `verify/context_reset_lock.py`
    ⇒ A② 立刻红（分割线已显示、圆环仍 100%、输入框仍禁用，`page.fill` 超时）。注意断言里
    **控制项（满量必锁）与修复项（重置必解锁）缺一不可**——只有后者的话，"夹具有效性"没被证明，
    测试就成了空测。
    守门：`tests/test_context_ring.py::ContextResetLockTests`（分割线判定必须排在 usage 判定之前、
    `!contextReset` 给运行中保护让路、遗留 `role=session` 行同口径）、冒烟
    `verify/context_reset_lock.py` + `.cjs`、打包资源核对（冻结版入口读 `public/js/03-media.js`
    ——前端修复不重编译就换不了用户手上的 exe）。
    关联：与《报告-新会话误报上下文已满.md》（本地探测窗口 8192 失真）是**两个独立根因**；
    那份的 P2（锁只由后端事件触发）若实施会落在同一函数附近，review 时一并看，但分两笔提交。

95. **「字体族写进 CSS」不等于「用户看到新字体」：字号好验，字体族必须验到渲染度量（2026-09-18）**：
    会话区字体设置（`appearance.chat_font_*`）里，字号读一次 `getComputedStyle().fontSize` 就能证明生效；
    而字体族**声明与渲染是两件事**——栈里靠前的字体没装时，`fontFamily` 照样原样返回，渲染却已经
    回退到界面字体。只断言 `fontFamily.includes('Source Han Serif SC')` 是一条**永远为真的空断言**
    （本机就没有思源宋体）。正确做法：用同一段文字量三次宽度——把 `--font-sans` 与 `--msg-font-family`
    分别挂到同一个隐藏 `<span>` 上取 `getBoundingClientRect().width`，再用 canvas `measureText` 量
    候选字体，于是 `current ≠ sans` 证明"真的换了"，`current ≈ SimSun` 证明"落在预期的那个上"。
    教训：① **凡"跨一层间接"的用户可见效果（CSS 变量 → 字体 / 主题 / 单位换算），断言要落在最终
    渲染结果上**，别停在"属性写对了"（§九.94 ④ 讲的是同一件事的另一个面：判据要取用户能不能用，
    不取函数被没被调用）；② 度量对照是"浏览器不提供'当前实际用了哪个字体'查询"的通用替代手段；
    ③ **同一个面板里不许有两套心智模型**：字号滑杆若"拖动即落库"而主题/皮肤要按「保存外观」，
    用户就得同时记两种规矩——本实现统一为「拖动只本地预览（高频事件绝不打 API）+ 保存外观统一落库」，
    一次把性能和一致性都解决；④ **旧 `index.html` 兼容分支只存在于代码里，验证时别忘了它能被"顺手
    读成默认值"毁掉**：控件缺失时表单取值必须返回 `undefined`（= 这次没传），返回 `'system'`/`15`
    就等于"打开一次设置把用户偏好抹平"——测试里专门钉了这两行。
    守门：`tests/test_chat_font.py`（17 例源码级护栏：`.message-body` 必须消费变量且不许再出现写死的
    `font-size:15px`、`:root` 默认值必须与历史值逐字相同、变量不许挂到 `body`/`--font-sans` 上、
    只传 theme/skin 的调用点不得重置字体、滑杆 `input`/下拉 `change` 路径不得出现 `saveAppearance`）、
    `tests/test_appearance.py`（15 例：默认值 / 更新路径越界与非法枚举显式报错 / 加载路径静默 clamp /
    custom 串往返与截断 / 预置键逐个往返）、冒烟 `verify/chat_font_smoke.py` + `.cjs`
    （39 项断言，含"字体族真的换了渲染度量"的两两对照）。
    **后续演进**：字体族的「自定义串」文本框已升级为**点选列表**（文本框降级为列表末项的高级兜底），
    见 §九.96。

96. **挑字体不该靠打字：点选列表 + 安装探测的三个坑（2026-09-18）**：
    §九.95 交出的「自定义字体串」文本框在手机上是最难受的形态——要用户手敲 `"LXGW WenKai"` 这种
    CSS 字体名，等于把"知道字体名"的前置知识转嫁给用户。改成**点选列表**（原生 `<select>`，手机上
    直接弹系统选择器）后，三个坑值得记下来：
    ① **安装探测不能用 `window.queryLocalFonts()`**：它要权限弹窗，且**只在安全上下文可用**——
       手机走 `http://192.168.5.x` 是非安全上下文，直接不可用。可用的手段是 canvas 双基线度量：
       把候选字体与 `monospace`/`sans-serif`/`serif` 依次拼成 `font-family` 量同一段文字，
       **任一基线下宽度不同** ⇒ 该字体真的参与了排版 ⇒ 已安装。**必须多条基线**：单一基线可能恰好
       就是探测字体本身，那样"没装"会被误判成"装了"。结果要 `Map` 缓存（一次面板构建 12 次探测 ×
       3 条基线，不缓存就是每次开设置都重算）。
    ② **探测只用于标注，不许当门禁**：设置存在服务端、多端共享，本机没装不代表手机没装；把缺失项
       禁选会造成"电脑上选不了、手机上明明能用"。文案后缀「（未安装）」，`<option>` 照常可选。
    ③ **`__pick__` 这类"只活在 DOM 里的占位值"是静默失败高发区**：radio 需要第四项表达"我要在列表里
       选"，但那个值不属于后端白名单。占位值一旦流到表单取值里，后端会 400，用户看到的是"保存失败"
       而完全不知道原因。做法：radio 用 `__pick__`、`appearanceFormValues()` 里翻译成 select 的真实值、
       测试直接断言"任何一次 POST 体里都不含 `__pick__`"。
    另外两条是验证层踩到的，都是"断言写成了空测"的同一个病根：
    ④ **"某个 API/字符串不存在"的负向断言要区分注释与代码**：本仓 `01-core.js` 的注释里专门写了
       "为什么不用 `window.queryLocalFonts()`"，于是 `assertNotIn('queryLocalFonts', core)` 被注释
       搞红。正确判据是**先剔除整行注释（`line.lstrip().startswith('//')`）再断言**——注释是文档，
       不是调用。
    ⑤ **"（未安装）标注正确"不能只读 DOM 文案**，那是自己验自己（文案错、断言也错）。冒烟里改成
       **现场复算**：动态 `import('/js/01-core.js')` 取回 `CHAT_FONT_PICKS` 的探测名，在本页重新度量
       一遍，与 DOM 里每条 `<option>` 的文案**逐项**比对（同时校验个数与顺序），不一致就报出具体 key。
    ⑥ 顺带一条冒烟脚本的坑：`openSettings()` 必须**幂等**。弹窗已经开着时再"点一次设置按钮"会先去找
       侧栏按钮，而侧栏被已打开的 `<dialog>` 挡住，Playwright 的 `click` 会一直重试到 30s 超时
       （报错信息是 `intercepts pointer events`，很容易误读成布局问题）。判据用
       `document.querySelector('#settingsDialog')?.open === true` 提前短路。
    守门：`tests/test_chat_font.py` 的 `test_pick_list_covers_every_font_key`（清单 key 集合必须等于
    后端 `APPEARANCE_CHAT_FONT_FAMILIES` 去掉结构三档）、`test_pick_placeholder_never_reaches_the_backend`、
    `test_install_probe_is_measurement_based_and_cached`；`tests/test_appearance.py` 的
    `test_font_family_error_message_lists_every_option`；冒烟 `verify/chat_font_smoke.cjs` 的
    `crossCheckPickAnnotations()`；冻结版 `verify/frozen_chat_font_check.py`（运行期逐个预置键走
    `normalize_appearance` 往返，证明 exe 里的白名单不是旧的）。

97. **工具调用前的"进度播报"不是重复消息，是"每一轮都被保留的中途正文"（2026-09-19，用户报障「十句进度堆在一条回复里」）**：
    现象很误导——用户先怀疑"网络卡顿""前端重复提交"，实际数据库里那十来句近义进度**就是同一条助手消息**。
    链路是三层叠加：
    ① 模型每调用一次工具，先在正文里说一句"我已核对…/根因已经锁定…/现在补协议…"，再吐 K3 的
       `<|open|>tools…` 协议；
    ② `naiba/llm/stream.py::_forward_guarded_text` 检测到协议时**把协议前面的正文照常发出去**
       （`visible = pending[:offset]` → `delta` 事件）——这是"中途正文"的既有设计，本身没错；
    ③ `naiba/run/stream.py::_build_activity_timeline` 只判断"本轮有没有工具调用"，有就把**每一段**
       delta 记成 `prose` 条目；前端 `04-messages.js` 见 `activityHasProse` 为真就**隐藏底部正文**
       （正文只在内联时间线里展示）⇒ 十来段重复话术摞满一条回复。
    修法（三层，缺一不可）：
    - **展示层认准"哪一段才是答复"**：一段正文之后若还有工具条目，它必然属于某次工具调用前的预告；
      只有后面不再有工具调用的那一段才是最终答复。`_build_activity_timeline` 只保留**最后一段** prose
      （`activityHasProse` 仍为真，前端"内联展示、末尾不再重复渲染"的既有契约保持不变）；
      `chat._rebuild_partial_run`（取消 / 失败 / 重启恢复）同步改成"**工具事件之后的 delta 才算正文**"——
      否则中止的消息看起来也是一堆播报。
    - **前端渲染层再兜一次**（`public/js/03-media.js::activityMarkup`）：**activity 是落库数据**，
      后端改口径重算不了已经存在的老消息；不在渲染时过滤，用户打开老会话看到的还是那十来段
      （会直接判"没修"）。判据与后端逐字同源：只渲染最后一个 prose 条目。
    - **源头补一句系统提示**：调用工具前不输出过程预告，直接发起调用；只在真实阻塞、需要用户决策或
      长任务时说明一次。提示词只降低发生概率，正确性由展示层兜住（不依赖模型自觉，§九 一贯口径）。
    **为什么不在流式层"拦下来"**（重要决策记录）：正文是**逐字实时发出**的，协议标记出现在正文**之后**——
    等判定出"这是工具轮"，那些字早已落库、早已渲染。要拦就只能把整轮正文缓冲到响应结束才发，
    那等于废掉流式（`_InlineReasoningParser` 里那条"固定 tail 会让每个短答案 burst"的教训同理）。
    故接受"流式期间短暂闪现、run 收尾后按正确口径重渲染"，代价远小于牺牲流式体验。
    **验证教训**：断言必须**双向**——只断言"activity 里没有播报"是空断言（假模型可能压根没吐播报，
    或协议根本没被识别）。前提侧先证明"run 事件流里确实出现过那几句播报"，再证明"展示面上没有"；
    反证也要真跑（回退过滤 ⇒ 立刻红），本轮实测回退后红出「prose 5 段 + 残留 4 句播报」，
    与用户报障形态一致。
    守门 `tests/test_activity_timeline.py`（新增 3 例 + `PartialRunRebuildTests` 2 例）；
    冒烟 `verify/tool_prefix_prose_smoke.py`。

98. **版本号形态必须能被 `_release_version_key` 解析，否则更新链路会静默死掉（2026-09-19 实测）**：
    想"只改版本号后缀"来表达小修补时最容易踩的坑。`updater.UpdateManager._release_version_key`
    的正则是 `v?(\d+)\.(\d+)(?:\.(\d+))?(?:[-._ ]?(beta|b)(?:[-._ ]?(\d+))?)?` 且用 **`re.fullmatch`**：
    - **只认三段数字**，第四段数字一律不认 ⇒ `2.6.9.1-beta`、`2.6.9.1` 都返回 `None`；
    - beta 序号必须**紧跟在 beta/b 之后** ⇒ `2.6.9-beta2` / `2.6.9-beta.2` / `2.6.9b2` 都是
      `(2,6,9,0,2)` ✔；而 `2.6.9-1-beta` / `2.6.9-2-beta` 还是 `None`（数字在 beta 之前，正则吃不下）；
    - 不带 beta 段 = 正式版，第 4 位记 1 ⇒ `2.6.9` = `(2,6,9,1,0)`，比 `2.6.9-beta` = `(2,6,9,0,0)` 新。
    解析失败的后果**不是报错，而是静默失效**（最难查的那类）：`_is_auto_update_version` 为假 ⇒
    ① 该版本被 `_normalize_releases` 直接从版本列表里过滤掉（设置页「目标版本」下拉里根本不出现）；
    ② 客户端侧 `latest_release is None` ⇒ 两个比较分支都进不去 ⇒ `update_available=False` ⇒
    **永远不会提示更新**，且没有任何日志。用户端表现就是"发了新版但没人收到"。
    规则：**版本号一律沿用 `X.Y.Z-beta`**；确要表达"同一版本的小修补"用 `X.Y.Z-betaN`
    （如 `2.6.9-beta2`），**禁止** `X.Y.Z.N-beta` 与 `X.Y.Z-N-beta` 两种写法。
    改完版本号后必须复核一次：`UpdateManager._release_version_key("<新版本号>")` 非 `None`、
    且大于上一版的 key（本条就是"发版清单"里除版本串一致性之外的第二道自检）。

99. **`release_watch.py` 的哈希核验会栽在「隧道代理封了资产域名」上，绕行口是 API 资产端点（2026-09-19 实测）**：
    发 2.6.10-beta 时 run 结论、job 每步、资产清单都拿到了（`#176 conclusion=success`），
    但核 sha256 那一步 **4 次重试全是 `URLError: Tunnel connection failed: 502 Bad Gateway`**，
    脚本最后打一句 `!! 网络中断，没能核完` 就退出。**这步不能当作"跑过了"**——哈希不符时
    应用内「检查更新」是**静默拒绝安装**（不报错、不提示），是本项目最贵的静默失效之一。
    根因不在脚本逻辑，而是本机出口走隧道代理，`objects.githubusercontent.com`（Release 资产的
    真实下载域名，即 `browser_download_url` 重定向后的落点）被代理封了，`api.github.com` 却是通的。
    绕行办法（已实测可行，`codeload`/`raw` 那条路不通）：
    - **资产内容**走 `GET https://api.github.com/repos/<owner>/<repo>/releases/assets/<asset_id>`
      + 头 `Accept: application/octet-stream`（**必须带这个 Accept**；不带只会拿回资产元信息 JSON）；
      `asset_id` 从 `releases/tags/<tag>` 的 `assets[].id` 取；
    - **exe 的权威摘要不必下 80MB**——同一响应里每个资产的 `digest` 字段就是 GitHub 侧
      `sha256:<hex>`（2025 年起 GitHub 对上传资产自动计算），拿它跟 manifest 里声明的 `sha256`
      逐字比即可，等于把"下载 83MB 再本地算哈希"换成"读一个字段"；
    - 顺带还能一眼核到：`assets[].size` 与 manifest 取回字节数是否相等、`tag_name` / `name` /
      `prerelease` / 发布 body 是否正确。
    本次结论：manifest 的 `version=2.6.10-beta`、`commit=2a93bf6…`、`sha256=862d3d46…` 与
    GitHub 侧 `naiba-chat.exe`（83,587,378B）的 `digest` **逐字一致**，208 条说明首条为 2.6.10。
    **待办（下次发版顺带做）**：把这条 API 回退路径并进 `verify/release_watch.py`，让哈希核验在
    代理环境下不再靠运气——本次先不动它，避免在刚发完版时再推一次 master 触发同版本重发。

100. **插话（运行中第二输入通道）复活：append-only 是缓存契约的硬约束，队列必须在「每一个终态」冻结（2026-09-19）**：
    按 `计划-复活插话功能.md` 执行，底稿从 git 历史（`ead85d9^`）取回后按包内结构重做。设计定调是
    **彻底手动引导**：运行中在输入框打字 → Enter 入队（`POST /api/chat/interject`，落库为一条真
    `role=user` 消息 + metadata `interjection_pending`）→ 面板三个动作（引导 / 编辑 / 删除）→ 用户点
    「引导」才注入 agent 下一步；**绝不做自动 follow-up**（旧设计的 `claim_interjections_for_followup`
    明确不恢复：终态带选择题时它会把刚弹出的选择面板锁死、用户点选项又变成新插话，两个输入通道的意图打架）。
    八条必须钉住的约束：
    ① **消费点只能 append**：`consume_interjections()` 把插话作为**新 user 消息追加到 `messages` 末尾**，
       不改写、不重排已有历史——DeepSeek 前缀缓存按前缀逐字节命中，往中间插一条会让其后全部失效。
       「最终答复与插话同一步到达」的分支也必须先把答复落位、再把插话排回末尾，相对上次请求仍是 append-only。
    ② **未被消费前不进模型上下文**：`build_model_history` 按 `interjection && !interjection_consumed` 过滤。
       插话是「用户写了但还没按下引导」的草稿态，提前进上下文等于绕过用户的手动决定。
    ③ **状态机 `pending → guided → consumed`，外加 `stopped`（终态冻结、永不发送），冻结要挂在每一个终态上**：
       只挂 `cancel()` 的话，「正常跑完」的残留队列会停留在可引导态——用户点引导却永远没有下一步来消费它。
       前端同样要冻（`freezeQueuedInterjections()` 由 done / cancelled / run_failed 三个 handler 调用，
       并记进本地 `frozenRunIds`），否则 10s 会话轮询重渲染会把行翻回 pending 态，等于冻结白做。
    ④ **终态之后队列仍要能清理**：`_finish` 冻结后 `interjection_guided=1 && interjection_stopped=1` 的行
       最初既不能删也不能取回（`delete_run_interjection` 拒 guided、`edit_run_interjection` 拒 stopped），
       成了死数据。正确口径是「**stopped 行即使已引导也可删**」（`guided=0 OR stopped=1`），且
       `delete_interjection` **不得要求 run 还活着**——run 结束后的清理才是最正常的用法。
    ⑤ **面板要自带 `run_id`**：`state.chatRunId` 在 run 结束后被清空，此时拿它去删/引导那条残留行会打到
       空 id 上（行还画着、动作全失效）。做法是渲染时把 `metadata.run_id` 写进 `row.dataset.runId`
       （面板的生命周期比 run 长），所有动作只读行自己的 id。
    ⑥ **面板头部计数必须跟着本地变更走**：删除/引导只重画了一行，头部那句「N 条待引导」还是旧值——
       用户按头部判断还剩几条，读到的就是假信息。抽出 `settlePanel()`（过滤缓存 + 头部计数 + 显隐 +
       引导可用性）作为唯一收口点，任何行级变更后都调它。
    ⑦ **程序化写输入框后必须手动刷发送键**：入队成功后 `input.value = ''` 是脚本写的，**不触发 `input`
       事件**（浏览器只在用户输入时触发），发送键与插话键仍停在「有内容」的可用态。凡程序化改写输入框
       （入队成功、失败回滚、取回草稿）都要显式调一次 `updateContextComposerLock(state.chatBusy)`。
    ⑧ **与「选择」面板共存的规则**：两者时间窗天然互斥（插话 = 运行中，选择题 = run 结束后），运行中
       `syncChoicePanelLock` 的 busy 判据一行不动；选择提交路径不读队列、队列里的插话也不并入答案轮。
       唯一交叠的接缝是「运行结束那一刻」，靠「①无自动 follow-up + 不合并」把两个意图分开。
    守门：`tests/test_interjections.py`（19 例，含消费点 append-only 前缀逐字节一致、guide 拒 pending 确认、
    cancel/finish 冻结、终态后可删）；冒烟 `verify/interjection_smoke.py` + `.cjs`（34 项）。

101. **状态条上的「重连中…」必须由连接状态机拥有，且与计时器寿命分离（2026-09-19 用户报障「明明还在输出，右下角一直说重连中」）**：
    现象很反直觉：内容还在逐字流出，状态栏却写着「重连中… · 已等待 X 秒」并一直走秒，用户读成「掉线了」。
    根因不是真掉线，是**计时器的生命周期漏管**，三层叠加：
    ① 计时器由 `enterReconnectCoolDown` / `scheduleRunReconnect` 经 `showElapsedStatus('重连中…')` 启动，
       同时 `setConnectionState('reconnecting')`；
    ② **重连成功后没有任何人清它**——`resumeRun` 重连成功只做了 `setConnectionState('connected')`，
       全程不碰计时器；
    ③ 恢复后最先到的事件也不清：「会清计时」的事件只有 status / delta / reasoning_start / tool_start /
       tool_confirm / cancelled / run_failed / done / error，而**长思考流的首发事件是 `reasoning_delta`**
       （连同 reasoning_end / reasoning / tool_progress / tool_result / usage 共六个 handler 都不清）。
       于是「重连成功 → 模型正在长思考」这个最正常的组合，恰好让陈旧计时独占状态栏直到终态。
    修法的关键是**把「横幅在不在」与「计时器活没活」拆成两件事**：
    - 状态里新增 `elapsedReconnectShown`，**它才是「横幅是否显示」的唯一真相**。原先判据是 `elapsedKind`，
      而 `detachRunConnection()`（`resumeRun` 开头就会调它）里的 `clearElapsedStatus()` 会把 `elapsedKind`
      一起抹掉 ⇒ 等重连真的成功、该清横幅时，**判据早已是 false**，横幅永远留在屏幕上。
      一句话：**计时器的寿命比横幅短**，用计时器的字段记录横幅状态必然失效。
    - **横幅由 `setConnectionState` 单点托管**：离开 `'reconnecting'` 时调 `settleReconnectStatus()`。
      状态机是「连接状态」的唯一出口，比去 N 个事件 handler 里逐个补调用可靠得多。
    - `handleChatEvent`（事件总入口）顶部再调一次 `settleReconnectStatus()`——**尾缓冲里那批事件的派发
      绕过了主循环**，只挂在主循环里就会漏掉「最后一批才到达」的形态。
    - `detachRunConnection()` 只在**当前不是 `reconnecting`** 时才 settle：`resumeRun` 紧接着要重连，
      在状态被打成 reconnecting 之前先 settle，会把刚举起的横幅抹掉。
    教训（可迁移）：**凡是「显示给用户的状态」都必须有一个明确的归属者与完整生命周期**——谁举起它、
    谁在什么条件下必须放下，两者要能对齐；用另一个短命对象（这里是计时器）的副产物去记录它，
    就是埋一个「看起来永远在重连」的地雷。**新增事件 handler 时顺手问一句：它该不该清这个横幅？**
    本条的答案是不用逐个清——收口在总入口，此处「少写几行」比「写对六处」更可靠。
    守门：冒烟 `verify/interjection_smoke.cjs` 的 G 组（G1~G6，含**负向断言**：「正在思考 · 已等待 X 秒」
    是**特性、不许被清**，而「重连中」必须消失——两种计时共用同一条状态栏，清错一个就换一个 bug）。

102. **「有推理零正文」空流重试必须降档（high→medium→low），原样重发 = 必然重演（2026-09-19 用户报障「ds 突然弹执行失败」）**：
    现象：deepseek-v4.1-flash（codex_responses、思考=高）跑多图网络排查长会话，整轮 9 步里前 6 步
    工具调用全正常，第 7 步模型陷入约 **10 万字符**的「hmm, wait, actually…」推理循环，正文/工具
    调用全为零；App 抛 EmptyModelStreamError 退避重试 2 次，**三次尝试推理量几乎相同（10 万→10.2 万
    →9.3 万字符）、正文全空**，整轮 Agent 任务失败；上游中继日志三条 ¥0、无 token 计数（响应不完整
    不计费），与本地三次尝试一一对应。根因不是上下文窗口满（输入才 46.9%），是**输出额度被思考烧光**：
    高档思考 + 长上下文 + 复杂题诱发分析瘫痪，max output tokens 全耗在 reasoning 上，流在正文开始前
    被截断。而旧重试策略**原样重发同一负载**——对这种失败模式等于白重试（同一提示必然掉进同一循环）。
    修法（`runtime.py` 的 EmptyModelStreamError 捕获处 + `protocols.py` 的 `_lower_reasoning_effort`）：
    - 重试前按 **high→medium→low 逐级降档**重建负载（只改本次请求的重试负载，**不改会话设置**），
      状态提示明说「自动降低思考强度重试」；无档可降（off/low/auto，或 openai_chat+DeepSeek 画像——
      其端点拒收 reasoning_effort、本就不发思考字段）时保持原退避重发，应对真正的上游瞬时抖动。
    - 降过档仍全部失败时，报错带诊断与可操作建议（最长一次思考约 N 万字 + 「请把思考强度调低或
      精简上下文后重新生成」），不再只有一句看不懂的「没有文本内容」。
    教训（可迁移）：**重试策略要问「重发同一负载有没有意义」**——确定性失败（推理循环、负载本身
    是诱因）必须改变负载再重试，否则重试只是把一次失败复制成三次；**排障先看双方留痕**：本地
    `run_events`（逐事件流）与上游计费日志（¥0 + 无 token = 截断未计费）能对上时，根因就跑不掉。
    守门：`tests/test_llm_runtime_retry.py` 六条降档用例（逐级降档 max→high→low、降档后成功、
    low/auto 原样重试、openai_chat+DeepSeek 不降档、openai_chat+GPT 降档、穷尽后诊断文案）。

103. **思考要管三段：流式期（预算熔断）、回放期（双闸门确定性截断）、计费期（推理按输出计费，失控=烧钱）；`urllib` 的 timeout 是「每次读的空闲」不是「总时长」（2026-09-19 实测 MiMo 长会话「每次总结都是同一条文字」）**：
    现象：长会话里模型一份总结反复产出同一段文字、换提示词也不变。根因不在提示词，在**历史**：
    上一条失控思考（数万字符的自我循环）被**原样全量回灌**进下一轮请求，模型被自己的循环锚定，
    每一轮都复读同一结论；同时推理 token 按**输出**计费，死循环等于无限烧钱。一句话：**思考是容器，
    不是日志**——它既要有上限，也不能无条件继承。
    修法（三段各自有闸门，互不替代）：
    - **流式期 = 预算熔断（§A）**：`runtime.py` 的 `REASONING_STREAM_BREAK_CHARS=48000`，`stream.py`
      三个流解析器（`_read_ollama_stream` / `_read_sse_response` / `_read_lm_studio_stream`）都接
      `_break_on_runaway_reasoning`，**只在正文仍为空时**抛 `EmptyModelStreamError`（带上
      `reasoning_chars`）⇒ 汇入 §九.102 的降档重试，而不是整轮失败。本地 LM Studio 的「有推理零正文」
      一并改成抛 `EmptyModelStreamError` 并纳入降档白名单 `{"codex_responses","openai_chat","lm_studio"}`
      （此前直接 `RuntimeError`，等于把一次可自愈抖动升级成硬失败）。
    - **回放期 = 双闸门确定性截断（§B）**：`core/history.py` 两个常量——单条
      `MODEL_REASONING_REPLAY_MAX_CHARS`(4000) + 整轮 `MODEL_REASONING_REPLAY_TURN_CHARS`(16000)，由
      `_ReasoningReplayBudget` 按「由新到旧」分配额度（新思考更相关，且旧的真被截断了也无所谓）。
      **只截回放，不动落库**（库里永远是原文），因此截断是**确定性**的——同一历史每轮得到同样字节，
      前缀缓存不会被打散。`_clip_reasoning_text()` 是唯一实现，`message` 与 `trace` 两条出口共用它
      （守门断言「省略标记只在它内部拼装一次」）；运行设置项 `reasoning_replay_max_chars` /
      `reasoning_replay_turn_chars`（0 = 明确关闭，留空回落默认）。
    - **计费期 = 按输出计费**：这是「为什么必须管」的理由，不是开关。诊断时用上游计费日志与本地
      `run_events` 的推理字符量对账（§九.102 已用过同一手法）。
    - **超时语义**：`ONLINE_MODEL_TIMEOUT_SECONDS=180` 是 `urllib` **每次读操作的「空闲」超时**——
      只要还有 delta 到达就不断被重置；一段思考死循环可以这样无限流下去。所以另加**墙钟**兜底
      `ONLINE_STREAM_TOTAL_TIMEOUT_SECONDS=900`（§D，`_iter_stream_lines(total_timeout=…)`，超时抛
      `StreamTotalTimeout`，文案「单次请求超过 N 分钟，已停止等待」），只挂在线请求（本地已有首字节
      超时 + 1800s 上限）。**同一个 180 秒在别处指的是别的东西**，改这块前先确认自己看到的是哪一个。
    教训（可迁移）：**「没报错/还在收数据」不等于「在正常干活」**——空闲超时永远救不了持续输出的
    死循环，必须另有一条与「有没有输出」无关的墙钟上限；**给用户看进度的指标不能和「是否卡住」的
    判据共用**（本地版本已踩过同类坑）；**治理「模型复读」要先查回灌内容，别先改提示词**。
    **真实数据复测（2026-09-19，`verify/_probe_reasoning_gate.py`，只读真实库）**：目标会话
    `239e93ba…`（50 条消息）落库思考合计 **1,533,638 字符**、单条最大 **431,726 字符**（9 个块，
    最大块 102,622）；开启闸门后回放 **4,024 字符**（保留 4000 + 标记），关闭闸门时原样回放
    431,734 ⇒ 对照组证明探针命中的是真数据；两次构建**字节完全一致**；请求体 448,745 → 14,325 字节；
    库里思考总量前后不变（只截回放）。**记账口径要记住**：闸门约束的是**保留内容**长度，
    省略标记（约 24~30 字符/条）另计，所以被截断的那条必然比 `single_max` 长一点、整轮也略高于
    `turn_max` —— 这是「标记始终非空」的存在性契约换来的，**不是 bug**，`_clip_reasoning_text`
    与 `_ReasoningReplayBudget` 的 docstring 已写明，别去"修"。
    **另有两条真实路径复测**：`verify/_probe_reasoning_break.py`（流式熔断阈值：12000 字正常思考
    与 47000 字下沿都不误伤、52000 字零正文在 50000 处停并提示「已主动中断并准备降档重试」、
    正文一旦出现即解除）与 `verify/_probe_a0_wire.py`（真实请求体：gpt-4o/auto 顶层无 max_tokens
    且只 1 次请求、DeepSeek codex 落 `max_output_tokens:32768` + `reasoning:{effort:"max"}`、
    Claude 落 `max_tokens:8192`、K3 不填但 `reasoning_effort:"max"`、Gemini 落
    `generationConfig.maxOutputTokens:8192`）。
    **复测当场抓到一处用户可见缺陷**：本地 LM Studio 画像（`kind=local`）跑到「有推理零正文」时，
    最终诊断却写「**在线**模型流式响应中没有文本内容」——本机模型报错说自己是云端，用户按这句话
    根本找不到该去哪个设置页排查。修法：新增 `_empty_stream_label` / `_empty_stream_phrase`
    （LM Studio / Ollama / 本地模型 / 在线模型，Latin 标签后留空格、中文标签不留），三处空流抛错
    统一走它，守门 `tests/test_stream_guards.py::EmptyStreamSourceLabelTests`（含负向断言：源码里
    不得再出现写死的 `"在线模型流式响应中没有文本内容"`）。教训：**报错文案也是产品的一部分**——
    「谁在报错」必须来自画像，「在线模型」这种写死的措辞在本地后端上就是误导。
    守门：`tests/test_stream_guards.py`（18 例：熔断提前中断并降档、LM Studio 降档、总时长超时重试、
    0 值确实关闭闸门、空流来源标签）+ `tests/test_reasoning_replay.py`（21 例：双闸门本体 + 配置 +
    三个调用点透传 + 设置面板 UI）。

104. **不填 `max_tokens` 不等于「有兜底」；思考强度识别必须数据驱动，子串嗅探会因中继改名失效（2026-09-19）**：
    两件事同源：**「协议默认」只是「我不发字段」，不等于上游替我设了上限**。
    - **A0 默认输出上限**：`max_tokens` 是「思考 + 正文」的**总量**闸门。不填 = 上游默认值（多数近乎
      无限），**只有 Claude 有 4096 硬兜底**——而 4096 会被一段高档思考一次吃光（§九.102 的
      「有推理零正文」正是这个形态）。因此注入 `min(32768, ceiling)`（`DEFAULT_THINKING_MAX_OUTPUT_TOKENS`，
      `ceiling` 由预设表给出），并在负载里打 `injected_max_tokens` 标记以便自愈：上游拒绝
      `max_tokens` 时**只撤掉自己注入的那一个值**重试一次（`_drop_injected_max_tokens`），
      **绝不把用户显式配置的 max_tokens 一起删掉**。判据必须是**模型级**证据（真的不支持思考）：
      `auto` 档只命中协议族预设**不构成依据**——否则 gpt-4o 这类模型每个请求白打一次 400 往返。
      Claude 的 4096 兜底要抬到 `min(32768, ceiling)`；注入时 `attempts` 相应 +1（自愈要占一次尝试）。
    - **G1 思考强度数据层**：`protocols._reasoning_params` 原先靠 6 条 `if` 分支 + **子串嗅探**判断
      画像，中继只要把模型名改一下（`deepseek-v4.1-flash` 走 `codex_responses` 中继、`kimi-k3` 经
      OpenAI 方言转发）就全部失配，静默发错字段甚至发空。改为声明式预设表 `THINKING_PRESETS`
      （新模块 `naiba/llm/thinking.py`，10 个预设：deepseek_codex / deepseek / kimi_k3 / kimi_k2 /
      ollama / lm_studio / openai_codex / openai / claude / gemini），命中度量 =
      **卡片 > 模型名 > `request_format` 默认**；**未声明的档位在发请求前就报错**（不再静默丢字段）；
      带**资格约束**（如 `kimi-k3` + `codex_responses` 不得命中 K3 预设——中继按 OpenAI 方言转发时
      由中继自己翻译）。降档链（§九.102）也改为走词表：按**声明顺序**下探到上一档，而不是「应用层
      四档统一减一」——K3 的 low/high/max 退档是 max→high→low（不是「high→medium」）。
      `_reasoning_params` 保留旧签名（`strict=False` 容错），老 config 零迁移。
    教训（可迁移）：**「默认」和「兜底」是两回事**——写代码的人以为默认值安全，实际是上游说了算，
    只要没有显式上限，一个失控的思考就能吃光全部预算；**方言识别必须集中在一张数据表里**，
    散落的条件分支 + 子串匹配会在中继改名/换协议时**静默失效**（失配不发错字段，比发错还难查）。
    守门：`tests/test_thinking_presets.py`（31 例：命中规则、未声明档位发前失败、§九.102 方言映射、
    降档链顺序、`thinking_active` 粒度洞）+ `tests/test_llm_runtime_retry.py::ThinkingOutputBudgetTests`
    （A0 共 12 例，含「auto 档不发 max_tokens 且无 400 往返」的粒度断言，以及
    **`test_cap_lands_on_each_protocols_own_key`：四种协议的落点键名逐一钉死**——
    openai/claude 是 `max_tokens`、codex_responses 是 `max_output_tokens`、
    gemini 是 `generationConfig.maxOutputTokens`；真实请求体复检时正是因为按 `max_tokens`
    去断言，一度把「已注入」误读成「没注入」）。
    **G1 实测清单回收（2026-09-19，teynex 中继真实端点，`verify/_probe_k3_ceiling*.py` /
    `_probe_k3_stream_effort.py`）**：`kimi_k3` 预设的 `max_output_ceiling` 由 0（待实测）回填为
    **1048576**——官方文档口径 `max_completion_tokens` 上限即 1048576，实测 4096~1048576 全阶梯
    HTTP 200（1M 真实成交）；10M 撞的是**计费墙**（New API 系中继按 max_tokens **预扣费**，
    约 ¥9/百万 token，预扣 ¥94 > 余额 ¥32 被 403 拒绝）而非协议拒绝；int32 上限（2147483647）才
    真正被端点判 `max_tokens is invalid`（HTTP 500）。**坑：预扣费口径意味着 A0 注入的
    32768 在这类中继上每次请求先预扣约 ¥0.29，与用户实际烧多少无关**——大 ceiling 无成本，
    但大注入值在余额紧张时会被 403 拦下，这是中继行为、不是 K3 协议墙。思考方言侧：官方
    `reasoning_effort` 只认 low/high/max（预设表原本就是这个词表）；中继对**任何**取值（含
    `none`/`minimal`/`xhigh` 甚至 K2 的 `thinking` 字段）都 HTTP 200 不校验；**非流式响应不返回
    `reasoning_content` 且 usage.reasoning_tokens=0**——不能据此判断字段没生效，**流式**下
    `reasoning_content` 真实出现且 reasoning_tokens 随档位变化（low=15 / high=108 / max=51）。
    教训：**验证「思考字段是否生效」必须走流式**；非流式的零推理读数是中继聚合形态，不是方言失效。

105. **「同一句话答两遍」只提示、不拦截：检测必须落在终态正文，且不进 metadata（2026-09-19）**：
    §九.103 讲的是「回灌毒化」的因，这条是它的**表现层探针**：终态答复与上一轮 assistant 正文
    规范化后（`strip` + 折叠空白）**逐字相同且 ≥ 20 字符**时，发一条 `status` 提示
    （「本次答复与上一轮完全相同，模型可能陷入重复；建议换个说法或开新会话」）。
    三个边界必须守住：① **不拦截**——用户完全可能真的要求复述（「再说一遍」），拦下来就是 bug；
    ② **不落 metadata**——消息契约与 `build_model_history` 的字节稳定性都靠它，为一条提示改动
    落库结构不值当（§九.100 插话那次已经吃过「metadata 一动缓存就散」的教训）；
    ③ **阈值 ≥ 20 字符**防误伤短应答（「好的」「收到」重复是正常的）。
    守门：`tests/test_repeat_answer.py`（9 例：只提示不拦截、不同则不提示、端到端 `status` 事件）。

106. **「退役清理」按 id 一刀切 = 删用户数据：迁移过滤必须认「签名」不认「名字」（2026-09-19）**：
    用户反馈「2.6.x 设置的 MCP，覆盖更新两次都没了」。真相：`ConfigStore.__init__` 的 MCP 去重段里
    有一条「退役旧版捆绑 ComfyUI 桥接」的过滤——**凡是 `id == "comfyui"` 的 mcp_servers 条目一律丢弃**，
    且 `__init__` 末尾 `self.save()` 每次启动都落盘 ⇒ 用户每次启动（不只是更新）配置都被重写、
    条目永久消失；用户只在覆盖更新时重启，于是表现为「一更新就没」。旧桥接（已删的
    `skills/comfyui-mcp` 的 configure_mcp.py）当年注册的条目恰好也叫 `comfyui`，过滤时没区分
    「我们的旧资产」和「用户自己的服务」——用户装官方 comfy-mcp 并取名 comfyui 是再自然不过的事。
    修复：新增 `_is_retired_comfyui_bridge()`，只按**脚本文件名签名**（args/command 含
    `comfyui_mcp_server.py`）识别旧桥接；**不能按路径片段 `comfyui-mcp` 匹配**——真实事故里用户的
    venv 就叫 `comfyui-mcp-env`，按目录名匹配照样误伤（守门测试就是按这个形态写的）。
    教训：**退役清理的判据必须窄到「只有我们的旧资产能命中」**；凡是「启动时剥离 + 立即 save」
    的组合，过滤条件写宽一格就是静默删用户数据。另注意 `mcp.py` 的别名路由：当 `comfy-mcp`
    连接存在时，`call("comfyui", …)` 会被改路由到 `comfy-mcp`——同名自建服务在共存时会被遮蔽。
    守门：`tests/test_config_migration.py`（旧桥接条目剥离、用户自建 comfyui 服务保留、去重不变）。

107. **「已受理的流」断线 ≠「提交被拒」：两条失败路径必须分开，草稿只在真正没发出去时才回填（2026-09-20）**：
    用户反馈「手机版每次切后台，上一次提问都会自己回到输入框」。根因：`sendChatMessage` 的
    catch 对所有非 AbortError 一视同仁——**连 `consumeRunStream` 中途断线也走
    `restoreRejectedSubmit()` 把整段提问塞回输入框**。手机切后台时浏览器挂起网络、杀死
    进行中的 fetch 流是系统常态，于是每次回来都看到「请求失败 + 旧提问回填」，诱导用户
    再发一次撞 ACTIVE_RUN。修复：用 `requestAccepted` 标志区分——拿到 2xx（流开始）之后
    的断线一律按「掉线」处理：**不回填草稿**（后端 Run 还在跑），有 runId 走
    `scheduleRunReconnect` 退避重连（`finally` 里配 `reconnectScheduled` 跳过
    `finishRunSubscription`，与 `resumeRun` 的既有收尾约定一致）；还没拿到 runId
    （`run_started` 未到达）时先 `detachRunSubscription()` 再 `openConversation` +
    `resumeConversationRun` 兜底（顺序同 409 分支；`resumeConversationRun` 要求
    `abortController` 为空，漏了 detach 会被它自己的守卫静默早退）。
    判据：**恢复草稿的正当性只存在于「服务端可能没收到这条消息」的窗口内**；一旦流被
    受理，前端对这条消息的唯一职责是把进度接回来，不是把它还给用户。

108. **回放事件里的确认卡必须带「当前仍否待决」标注；确认失败的出路是卡片不是 toast（2026-09-20）**：
    用户反馈「已经允许过的 AI 行为会要求再次允许，点了允许又说未授权」。三个叠加根因：
    ① 页面重载/重连后 `/api/runs/<id>/events?after=0` 会把**已处理过**的 `tool_confirm`
    事件原样回放，前端照单渲染出带「允许执行」按钮的活卡；点下去后端
    `owns_confirmation` 为 False → 409「确认请求不属于该运行或已失效」（用户口中的
    「未授权」）。修复在**流出口**（`_stream_run`）：回放/实时的 tool_confirm 事件统一查
    `owns_confirmation`，不再待决就标注 `confirm_resolved: True`——实时事件此刻必在
    pending（executor 先登记后发事件），不受影响；前端据此渲染「确认已结束」的历史回执
    （无按钮），并按 confirm_id 去重防同一事件二次插卡。② `approveTool`/`rejectTool`
    的 catch 只 toast、不改卡片——卡片永远停在「正在执行...」，用户以为已批准。修复：
    失败原因写回 `.tool-confirm-actions`（`confirmEl` 必须提出 try 外才能被 catch 用到）。
    ③ 确认等待超时 300s：手机切后台超过 5 分钟是常态，回来后确认早已被自动拒绝。
    修复：`wait_for_confirmation` 默认与调用点统一提到 1800s，超时文案按 timeout 动态
    拼分钟数（不再写死「5分钟」）。取消信号仍即时中断等待，不受影响。
    教训：**凡是从存储回放出来的「可操作 UI」，都要回答「这个操作现在还成立吗」**——
    事件是历史，按钮是现在，两者不能共用一份渲染。

109. **插话队列预览整段折行，不用单行省略（2026-09-20）**：
    `.run-guidance-preview` 原为 `nowrap + ellipsis`，长插话挤成一行省略号、用户看不清
    自己排了什么（窄屏手机更明显）。改 `pre-wrap + overflow-wrap:anywhere`，卡片改
    `align-items:flex-start` 并给图标/徽标 3px 顶部补偿对齐首行。队列是用户要逐条审阅
    管理的对象，「一行一句话省地方」在这里是错误取舍。

110. **移动端「文字只显示一半/排版偶尔会变」先查 FontBoosting：`text-size-adjust:100%` 是全站底线（2026-09-20）**：
    用户反馈手机端输入框打了很多字却只能看到一半。根因：移动 Chrome 的字体提升
    （FontBoosting）会放大**普通 div** 的文字、却不动**表单控件**——输入框是
    「透明 textarea + div 镜像层」结构（`.composer-input textarea` color:transparent /
    `.input-mirror` 负责显字），镜像层被 boosting 放大后两层字号与换行点错位，
    可见文字就只剩一部分；boosting 是否触发取决于布局启发式，所以「有时候才会」。
    修复：`body` 加 `-webkit-text-size-adjust:100%; text-size-adjust:100%`（全站字号
    一律以我们的 CSS 为准）。**凡是「透明输入框 + 镜像层显字」的组件，移动端都必须
    假设两层会被不同策略缩放**——该结构本身没错，缺的是把浏览器的自动缩放关掉。

111. **per-call 上下文派生不能是「写完就丢」的浅拷贝：工具写进 Run 上下文的状态必须回落共享对象（2026-09-20）**：
    用户实测：模型写完交接报告、调用 `reset_context` **成功**（工具卡片「已执行」、返回
    `{"ok": true, "handoff_bytes": 9288}`、模型也宣称「上下文已按要求重置」），可上下文**一点没变**——
    消息没有 `session_start`、界面没画分割线、圆环照旧 66.9%、种子消息没填进输入框。
    根因是**回归**：`reset_context`（09-10 `09a541d`）置位 `run_context["context_reset"]`；而
    09-16 的 `eca4a61`（工具实时输出 / per-call seq）引入的 `_call_context()` 用
    `{**run_context, "event_sink": _seq_event_sink(...)}` 派生 per-call 副本（原注释写「其余键共享
    （零拷贝语义）」）。浅拷贝只共享**嵌套对象**，**顶层新增/更新落在副本上就永久丢失**：工具置位写进
    副本，而宿主收尾（`skills/agent.py` 的 reset 收尾分支、`run/chat.py` 落 metadata）读的是原 dict
    ⇒ 读到空 ⇒ 静默吞掉。09-10~09-16 之间是好的，之后**工具路径从未成功过**——库里 3 条
    `session_start` 全是 `source=manual`（手动点「新会话」按钮），没有任何一条来自工具。
    修复：新增 `_CallRunContext(dict)` 作 per-call 视图——**读**=派生快照（语义与原来一致）、
    **写**除 `event_sink` 外一律同步写回 `_base`；必须一并覆写 `update` / `setdefault`，
    因为 `dict.update` 走 C 实现、**不经过被覆写的 `__setitem__`**（「只在子类里改 `__setitem__`」
    是经典漏网）。注意**前端 §九.94 的分割线感知逻辑本身没错**：症状在前端，病根在后端
    （分割线压根没落库）。
    教训（可迁移）：**「共享可变容器」的浅拷贝不是共享**——它只保住嵌套对象的引用，顶层新增/更新
    写进副本就等于丢掉，而且**调用方看到的是成功返回**、没有任何报错；凡是「工具/子模块写、宿主读」
    的状态，要么写进**预先存在的嵌套可变槽**（浅拷贝天然共享），要么在派生对象上显式回写。
    另两条：**单测必须覆盖真实调用链**——`tests/test_context_reset_tool.py` 原先 7 例全绿，因为它们
    直接调 `_tool_reset_context` 并传**同一个** dict，永远绕开 `_call_context` 这一层；
    **模型自述 ≠ 系统事实**（模型的「已按要求重置」只是在复读工具返回的 message 字段），判「副作用
    有没有发生」必须查落库/落盘证据（消息 metadata、文件），不要信工具卡片或模型的复述。
    守门：`tests/test_context_reset_tool.py`（`CallContextWriteBackTests` 两条：per-call 出口隔离 +
    「下标/update/setdefault」三条写路径回落；`AgentLoopResetTests` 一条 A 级：真 Agent 循环 +
    真 `reset_context` 工具，断言宿主收尾读得到置位、本轮只请求模型一次）。**已核验护栏会红**：
    把 `_call_context` 临时换回浅拷贝旧实现，3 例立刻失败并打出
    `'context_reset' not found in {...}`——即根因的字面复现。

112. **轮询等待里的 `fn.await` 恒为假 ⇒ 等待空转、断言假绿（2026-09-20）**：
    新写的 `provider_presets_smoke.cjs` 里 `waitFor(page, fn)` 抄了 `if (fn.await ? await fn() : fn()) return true;`
    这行——**`Function` 上没有 `await` 属性**，判定恒为假，于是走 `fn()` 分支拿到的是**未决 Promise**，
    而 Promise 恒真 ⇒ 第一次轮询就 `return true`。后果不是报错而是**静默变成空测**：脚本打印
    「首屏到向导弹出的耗时：157 ms」，可当时的截图快照是 `open:false` / `cardCount:0`——向导
    根本还没渲染；**同一函数还让两条「不该弹」的负向断言一起变成空断言**（它们本来就只会 `open===false`，
    等待失效后连「有没有等够」都无从判断）。修法：`fn` 一律传 async 函数并老实 `if (await fn())`。
    教训（可迁移）：① **「等待」类助手一旦恒真，防线就从「断言」退化成「断言 + 空转」，而它照样全绿**——
    与 §六 里 `media_markup_check.mjs` 桩缺口那个「看着在跑、其实早就崩了」是同一族；
    修没修好的判据也很直接：同一条打印从「157 ms」（假）变成「600 ms」（真等了，向导确实在那之后才画出网格）。
    看门狗式的自查方式：**新写的等待助手先拿一个「必定不发生」的条件跑一次，它会超时才算活着**。② **负向断言必须有正向对照**：三条「不该弹」之所以能站住，
    靠的是同一页面、同一段启动序列里第 3 次重开**确实弹出来了**——没这条对照，负向断言写多少条都是装饰。
    ③ **`page.click` 会自己等到元素可见**，所以「点击居然成功了」**不能**当作「此刻它已经在那儿」
    的证据——正是这个错觉掩盖了快照与点击之间的时间差。

112. **comfyui_batch 三连修：负 seed 随机上限 / shots 必须生效 / 提交失败透传原因（2026-09-20）**：
    用户要 10 张图，内置 comfyui_batch 当天 6 次提交全部「第 1 段提交失败」秒败，模型连盲试
    6 次后只能自写 Python 脚本绕道。排查（只读 sqlite + 1:1 复刻提交路径 POST 实验）抓到两个
    根因 + 一个放大器：
    ① **负 seed 替换爆上限**：工作流里 rgthree「Seed (rgthree)」节点 `seed=-1`（-1=随机，该节点
    声明范围 ±2^50，合法）；`_normalize_comfyui_runtime_workflow`（原 app.py 抽迁）把负 seed
    替换成 `randbelow(2^63)` 的巨数 → 必超该节点 max → ComfyUI 整单 400
    `value_bigger_than_max`。同一文件 MCP（comfy-cli）**不做替换**、原样提交即成功——20 秒内
    一胜一败是定位的关键对照。修复：随机上限改为双方节点范围的交集
    `_COMFYUI_SEED_RANDOM_MAX = 2**50`（核心 KSampler 0~2^64-1 ∩ rgthree ±2^50）。
    教训：**单方面改写用户工作流的参数值，必须落在「所有常见节点声明范围」的交集里**；
    不知道各节点的 min/max 时取保守上限，而不是想当然用 int 全域。
    ② **shots 被静默吞**：`shots` 只在单 `workflow` 分支生效，`workflow_paths` 分支直接无视
    → `workflow_paths + shots=10` 返回 `total=1`（模型自己发现："total=1 — but shots=10"）。
    修复：shots 提前解析，paths 分支每路径重复 shots 次；显式 `workflows` 数组 + shots>1
    **明确报错**（数组已逐条列出，复制元素来表达重复）而不是装没看见。教训：**参数组合
    不支持就报错，静默忽略 = 模型按返回值自检一轮、用户白等一轮**。
    ③ **放大器——提交失败吞原因**：`_comfyui_submit` 任何异常只回 None，任务上只剩一句
    「第 N 段提交失败」，400 的 node_errors（节点号/类型/具体校验错误）全部丢失 → 模型
    无法自诊只能盲试。修复：改回 `(prompt_id, reason)` 元组，`_summarize_comfyui_reject`
    把 400 响应压成「HTTP 400 ...；节点 20（Seed (rgthree)）：Value ... bigger than max ...」
    写进 job error 与 result.errors（上限 600 字符防整段回显灌库）。教训：**外部服务拒绝时
    必须把「服务端说了什么」透传到模型可见的层面，否则每次失败都是一次盲试循环**。
    注意改名后的坑：`_comfyui_submit` 返回值从 str|None 变元组，**调用点共 3 处**
    （首提 2 处 + 丢失重提交 1 处）——改签名时漏了重提交那处，tuple 被整个写进
    checkpoint.submitted（守门测试当场抓红）。守门：`tests/test_comfyui_provider_tools.py`
    （seed 上限/连线引用不动/shots 三分支/数组+shots 报错/每段独立规范化）、
    `tests/test_job_resume_guard.py::ComfyBatchSubmitRejectTests`（400 透传断言到
    「Seed (rgthree)」「bigger than max」字样）。工具 schema（registry.py）与系统提示
    指引（skills/agent.py「同一次调用带 shots=N」）已同步新口径。
    排查手法备忘：job 的 `snapshot.params` 记录了每次提交的实参、`run_events` 的
    tool_start/tool_result 有完整调用链——「模型到底传了什么、工具回了什么」先查这两处，
    不要靠截图猜。

113. **显示名 Naiba Chat → Cat Chat：只换人看得见的称呼，机器认的标识一个都不动（2026-09-20）**：
    需求是「把应用显示名改成 Cat Chat」。最省事的做法是全仓库把 `naiba` 换掉——那会同时改掉
    仓库名、EXE 名、更新资产、AppUserModelID 和**全部浏览器存储键**，等于把老用户设置连同
    自动更新一起作废。真正的判据只有一句：**这个词是给人看的，还是给机器认的**（边界表见 §2.1）。
    - 人看的（改）：`index.html` 的 `<title>`、欢迎向导标题、设置面板里的说明句（外观 / 手机访问 /
      数据迁移 / 软件更新）、`launcher.py` 的托盘悬停标题与窗口标题、启动失败消息框、
      `filedialog.askdirectory(title=…)`、实例锁报错、argparse 描述、启动横幅、`start.bat`。
    - 机器认的（不改）：仓库名 `yc883123/naiba-chat`、`naiba/` 包、`naiba-chat.exe`、更新器三常量
      与「不是受支持的 naiba-chat Git 仓库」报错、`naiba.chat` AppUserModelID、**托盘内部 name**
      （`pystray.Icon` 第 1 参数，改了就成另一个应用）、`%LOCALAPPDATA%\NaibaChat`、
      `app_dir/webview` 持久 profile、`server_version = "naiba-chat/1.0"`、
      **全部 `naibaChat*` / `naiba.agentToolTemplates` / `naibaOnboardingDismissed` / `lan*` 兼容键**、
      `application/x-naiba-file-path`、`naiba_chat_` 上传前缀、`naiba.*` logger、`NAIBA_*` 环境变量。
      最容易被「顺手全替换」误伤的两处恰是这两类：`index.html` 的防火墙放行对象（实际 EXE 仍叫
      naiba-chat）与更新来源链接——**改了就是说假话**。
    守门 `tests/test_display_branding.py`（28 例）。最值得复用的手法：**托盘那处用 AST 分开断言
    第 1 参数（内部 name）与第 3 参数（可见 title）**——按源文本扫描根本分不出这两个位置；
    再加「不得新建 cat 前缀存储键」的负向断言。另跑真后端浏览器定向检查（临时 `verify/_check_*.cjs`，
    不长期入库）验首启向导标题、设置四个面板文案、**真点「启用手机访问」**后状态说明与 toast 两处、
    mock 迁移接口的完成提示插值。
    因为不搬迁数据、不改 `config.json`/`webview` 路径链，本次**没有任何数据库迁移**。
    - **发布准备（2026-09-20 已做，随 2.7.6-beta 一起）**：改名会连带坑到**自己的核验工具**——
      `verify/release_watch.py::check_manifest` 原本写死「头条以 `Naiba Chat` 开头」、
      `_release_check.py` 写死 `# Naiba Chat {版本} Beta`，**首次以 Cat Chat 发布时它们会把一次完全正常的
      发布判成异常**（改名计划 §6.3）。现在两个脚本都改成**品牌表判定**（`BRAND_PREFIXES` /
      `brand_hit`，当前名 `Cat Chat` 与历史名 `Naiba Chat` 都放行，但必须整个前缀命中、不接受
      「句中提到品牌」），并给核验器补上「空说明 / 空列表 / 类型不对一律明确判失败而不是抛
      `IndexError`」的处置。**这条必须与版本串一起改、且必须有离线守门**
      （`tests/test_release_watch.py` 16 例）——否则「核验器说发布异常」和「发布真的异常」从此分不清，
      而核验器本身不会因为改错而红。同一批还把 `_release_check.py` 那节路径审计的**扫描范围**收到
      与 §8.8 守门一致（原先会把 `verify/` 下的 `.exe`/`.log` 当文本读、742 处噪音淹没真问题），
      见 §九.115。

114. **任务面板「显示不对」先分清是**写入口脏**还是**展示口径缺**：`message` 是标题位、`current_step` 是进度位，两者不得互相顶替（2026-09-20）**：
    用户截图：一格里并排两条行，一条「已中断 | ComfyUI 批量生成 | 服务重启，运行已中断」，
    另一条「已完成 | **完成 8/10** | 完成 10/10」，汇总行还写着「共 3 · 运行中 0 · 失败 0 · 已完成 2」
    （3 ≠ 0+0+2）。三处毛病，根因分属三层，**分开修**：
    - **写入口脏（后端）**：`jobs.py::resume()` 用 `label = job["current_step"]` 当新 Job 的任务名。
      `current_step` 是**进度文字**（`完成 8/10` / `提交第 3/10 段`），而 `JobSpec.label` 会落进
      `background_tasks.message`——它**直接顶在面板的标题位**。于是同一次重启恢复产生两条行：
      源行是原名、恢复行是进度，用户读到的是一句没有主语、也看不出是恢复件的进度。
      修法：原名按可信度取 `run snapshot.label` → `源 Job 的 message`，加「（恢复）」后缀
      （已带后缀不叠加），都取不到才留空。
      **两个坑**：① `get()`/`_snapshot()` **刻意不带 `message`**（它是展示字段，不在 Job 快照契约里），
      所以回退路径必须**回库取** `storage.get_background_task(job_id)["message"]`——用 `job.get("message")`
      会静默拿到 `None`、标题退化成 `Job(comfyui)`；② 占位名 `Job(kind)` **不算原名**，必须一起判掉
      （`_PLACEHOLDER_JOB_LABEL_RE`），否则得到 `Job(comfyui)（恢复）`——它绕过了前端按类型名兜底的
      正则判定，反而更难看。
    - **展示口径缺（前端）**：`renderTaskSummary()` 只统计 `failed` 与 `completed`，`interrupted`
      与 `cancelled` 既不算失败也不算完成 ⇒ 汇总行凭空少一条，**用户从汇总行完全读不出「发生过一次中断」**。
      修法：终态各自成桶、计数为 0 不显示，保证「共 N」恒等于各项之和且不堆噪音。
      改这里**必须保留「最后成功更新」兜底**（`test_tasks_sync_failure_is_surfaced_with_last_success_time` 会守）。
    - **可读性缺（前端）**：中断源与接续它的新 Job 共用父回答、必然并排成两条**状态对立**的行，
      不点明「这条已经有人接手」就是「同一个任务怎么又中断又完成」。修法：源行 note 追加
      「已由新任务接续」（标记来自 resume 链路写进 `result.resumed_into`，前端只做翻译），
      并把 `resumed_into` / `resume_skipped` / `superseded_reason` 加进 `TASK_DETAIL_KEY_LABELS`
      （否则详情里铺的是一串英文键）。
    - **旧数据要兜住**：写入口修好了，**仓库里已落库的脏标题不会自己变好**——用户在旧记录上看到的
      依旧是 `完成 8/10`，会以为没修。所以 `taskDisplayTitle()` 再兜一层：进度形文案
      （`TASK_PROGRESS_MESSAGE`：已知步骤词，或含 `数字/数字`）不作标题，回退类型名。
    - **复用的手法**：验证用 `verify/tasks_panel_render_check.mjs` **真执行**渲染函数断言产出 HTML
      （新增 `extract` 项时注意——**新加的顶层 `const` 必须一起 extract**，否则 `ReferenceError`
      让整个脚本红，而它看着像"面板坏了"）；后端侧用真行为用例
      （`tests/test_job_resume_guard.py::ResumeLabelTests`，3 例：原名不被进度顶替 / 无名时回退
      `Job(kind)` / 连续恢复不叠后缀）+ 前端源码级口径（`tests/test_tasks_aggregate.py::TaskPanelResumeDisplayTests`）。
      教训：**「显示不对」往往一半是数据脏、一半是口径缺**；只修一边，用户会觉得"根本没修"。

115. **自检脚本的扫描范围宽于判据，噪音就会把信号淹掉（2026-09-20，`verify/_release_check.py` 的路径审计）**：
    `_release_check.py` 末尾那节「本地绝对路径审计」原先用 `rglob(verify/*)` 把目录下**所有文件**当文本读，
    于是构建残留全进来了：`_rel265/naiba-chat.exe`(197) 与 `_release_exe_check.exe`(200) 两个二进制、
    三份 `_build*.log`(246)、隔离目录里的 `chat.db` / `config.json`、截图 png……**742 处命中里 667 处来自
    `.exe` / `.log` / `.png` / `.db` / `.pyc`**，其余 75 处又多来自隔离目录的 `config.json` 与测试桩假路径
    （`media_markup_check.mjs` 的 `C:\a.png` 本来就是正常夹具）。真正需要人看的几处被埋掉——**这个自检等于失效**。
    - **根因不是「扫多了」，是同一个判断写了两遍**：判据本来只有一处——§8.8 守门
      `tests/test_no_absolute_paths.py`（`git ls-files` ∩ `CODE_EXTS`、排除 `docs/`、正则
      `USER_PATH` + 占位白名单 + `REPO_PATH`）。这里又自己写了一套「盘符绝对路径」宽正则，
      两套口径必然漂移（与 §九.113 同一类事故：同一个判断有两个实现）。
    - **修法**：审计范围收敛到与守门**完全相同**的文件集，并**直接加载守门模块复用它的常量**
      （`importlib.util.spec_from_file_location`，与 `tests/test_release_watch.py` 加载
      `verify/release_watch.py` 是同一个手法）——判据单一实现，扫描范围跟着判据走，不再手抄一份。
      改完结果：扫描入库代码/配置 **330 个、命中 0 处**（原先 742 处；`tests/test_release_watch.py` 那条
      `setUpClass` 也要能继续加载本脚本，实跑 17 例 OK）。
    - **留下两条判据**：① **自检的扫描范围必须等于判据的适用范围**——宽出来的部分不代表"更严格"，
      只代表**这条自检已经没人看得懂**；② 报告「命中 N 处」必须同时报「扫描 M 个文件」，
      只有 N 没有 M 无法区分「干净」与「根本没扫到」。
    - **配套（未做，属会话收尾）**：`verify/` 下的构建残留（`_rel265/`、`_release_exe_check.exe`、
      `_build*.log`、`chat_font_smoke_*` / `tasks_panel_tmp_*` 隔离目录）应由
      `verify/cleanup_verify.py --apply` 清掉（§8.7：收尾时先问再清）。审计口径修好之后，
      这些残留**不再影响**发版自检的输出。

116. **子代理的两种上下文模式 = 两个互斥工具（`subagent` fork / `subagent_spawn` spawn）：模式选择权完全归用户（2026-09-20）**：
    dsh（`deepseek-ai/deepseek-harness`）把子代理注册成两个 provider，全部差别只有一个可选的
    `seed` 字段（父日志前缀）：`dsh-subagent-fork-in-process` 的 `inheritsParentContext=true`、
    种子取父日志截至最后 `turn/end` 的配平前缀；`dsh-subagent-spawn-in-process` 缺省即 fresh child。
    naiba-chat 原本只有 fork：`naiba/subagent.py` 无条件 `build_model_history(conversation.messages)`，
    子代理**每步推理**都重放父会话全量历史。
    - **两个工具、参数同形**：`subagent`（继承父会话完整历史）/ `subagent_spawn`（不带任何历史），
      参数都是 `instruction`/`allowed_tools`/`label`。`fork` **不是模型参数**，而是注册时固化的常量
      （`subagent_handler_factory(app, fork=...)` 由 `naiba/tools/providers/jobs.py` 分别传 True/False）：
      模型就算传了 `fork` 也不作数（守门钉死）。`JobSpec.params["fork"]` 落库的就是这个确定值、
      `resume` 不重算；两种工具**同一种 Job**（kind 仍是 `subagent`）⇒ 面板/取消/清理/迁移零改动。
    - **为什么不做成参数（前两版草稿的死结，别再回头）**：模型选模式要判两件事——① 任务是否依赖
      前文（它自己发的任务，判得准）；② **当前会话在哪个供应商上（它必然不知道**：卡片配置在后端、
      不进模型上下文）。让模型填参数 = 让它猜环境；让后端定默认 = 替它判断任务。
      **最终解法是让模式根本不进运行时决策**：变成工具集层面的用户二选一，模型零判断、后端零猜测。
      代价是放弃"单次调用级切换"（可接受：fork/spawn 是供应商+使用习惯层面的偏好而不是逐任务摇摆；
      真要混用就建两个 Agent 切换顶部 Agent）。
    - **成本特性是这两种模式各自适用面的全部理由**：fork 在**前缀缓存命中**的供应商
      （DeepSeek / Kimi 官方 API）上按缓存价计费、重放代价低；在无缓存、或**按 `max_tokens`
      预扣费的中继**（如 teynex ≈¥9/百万，见 §九.104）上是全价重放，长会话里每个子代理的
      「起步价」= 父会话体量。spawn 的起步上下文只有 system + instruction，任何供应商下都便宜，
      代价是 instruction 必须自包含、且失去与主会话的前缀缓存共享。
    - **默认与存量**：`_DEFAULT_SELECTED_TOOLS`（= 标准模式）本就不含子代理工具 ⇒ 新建 Agent 两个
      都不开，由用户在工具页自己选；`subagent` 侧行为逐字节不变；旧 Job 记录没有 `fork` 字段，
      `params.get("fork", True)` 缺省 true ⇒ resume 行为不变、**无需迁移**（`JobRegistry.resume`
      原样透传 `params`，见 `naiba/jobs.py:~423`）。归一化唯一出口仍是 `naiba/subagent.py:_coerce_fork()`
      （handler 与 runner 共用）：显式 `false`/`"false"`/`"0"`/`"no"`/`"off"` 才 spawn，
      其余（含 `None`、空串、无法判读）一律回落 fork。
    - **互斥归一唯一权威 = `naiba/run/session.normalize_tool_mutex`**（`MUTUALLY_EXCLUSIVE_TOOL_GROUPS`
      = `(("subagent", "subagent_spawn"),)`）：同组多个同时出现 → **只保留组内排前者**，其余丢弃
      （保持原顺序、组内按名字去重不误删；只含单个时是恒等操作）。组内排前者 = fork = 安全方向：
      **选错只是费钱、结果仍对**，反过来让子代理丢上下文是既费钱又出质量事故的方向。
      **三个入口全覆盖**，缺一个就有"双开"的洞：① `resolve_allowed_tools` 返回前（每轮 Run 的最终闸门，
      **只此一处即可保证运行时绝不相撞**）；② `bake_session_tool_ids` 固化前（**落库的就必须是归一后的
      集合**，前端显示与运行时同源；已固化的会话只归一返回值、不顺手改写库）；③ `enable_conversation_tools`
      追加后（"只加不删"的注入路径不留洞：注入另一个互斥工具不会造成双开，`added` 仍返回被请求的工具名）。
      `JOB_TOOLS` 加 `subagent_spawn`，`JOB_CREATOR_TOOL_DEPS` 加 `subagent_spawn → ("job_output",)`。
    - **前端镜像 `AGENT_TOOL_MUTEX_GROUPS`（`public/js/09-settings.js`）**：与后端同名组**逐字一致**
      （与 `AGENT_TOOL_DEP_RULES`/`JOB_CREATOR_TOOL_DEPS` 同款前后端镜像，**改一边必须改另一边**）。
      两条语义必须分清：**单点勾选听用户的**（勾哪个留哪个，`applyAgentToolDependency` 里按刚点的那个删同组）
      ；**批量勾选（分类级 / 二级分组全选）关掉逐项互斥**（`{ applyMutex: false }`）后统一
      `normalizeToolMutex`，即按组内排前者保留 fork，并 toast 提示一次——否则用户看不出为什么少勾了一个。
      ⚠️ 副作用：整组从此**永远进不了"全选"态**（spawn 被有意排除），再点那个全选框等于重新勾上。
      归一挂进 `normalizeToolScope`（表单打开 / 预设卡 / 模板卡三条载入路径）+ `saveAgentToolSet`
      + `usableTemplateTools`；后端 `config._clean_tool_set_tools` 再兜一层（手攒的工具集、直写
      `/api/tool_sets` 的请求、手工编辑的 config.json 都从这里收敛；config 是层级 2「只依赖 core 与 paths」，
      故用**函数内局部导入**取互斥定义，不破坏 DAG 红线）。
    - **预设不留洞**：full 走 `group:*` 会把互斥的两个同时展开 ⇒ 显式 `"exclude": ["subagent_spawn"]`
      （**要 spawn 的用户在工具页手动换**）；其余四档预设本就不含子代理工具，不动。
    - **规则必须放系统提示常驻区，且只讲启用的那个**：描述有 ≤100 字 / ≤3 句预算（§九.23）——
      本次第一版把 cost 分段写进 `description` 直接被守门拦下（**314 字 / 4 句**）。现行分工：
      `naiba/tools/registry.py` 两个工具各一句分工描述（约 95 字 / 3 句）；`naiba/skills/agent.py` 的
      `SUBAGENT_FORK_RULE` / `SUBAGENT_SPAWN_RULE` + `SUBAGENT_COMMON_RULES`（硬约束：一次调用能做完的
      别开、单父 ≤4、子代理不能再派生），由 `subagent_context_rule(allowed)` **按实际启用的工具**选一条注入——
      模型看不到另一种模式的存在，也不需要"模式"这个概念。**凡动 `registry.py` 的 `description=` 直接跑
      `tests.test_tool_description_slim`（或全量），不要用工具名筛**——守门测试名里没有工具名。
    - **划界（改这里时不要顺手扩大）**：权限收窄（`SUBAGENT_BLOCKED_TOOLS` 同时含 `subagent` 与
      `subagent_spawn`/白名单）、深度 ≤2、单父 ≤4、父取消递归取消、结果回传（`job_wait` 的 2000 字符截断
      ⇒ **父模型只看到摘要**）两种模式完全共用；`build_model_history` 本身与 `SkillAgent.run` 签名不动。
    - **旧的"后端兜底计费形态"已整块删除（同日复盘）**：草稿②曾让 `provider_presets.profile_billing_shape()`
      判 `official_cache`/`relay`/`local`，并在常驻区给模型一句"本会话哪种模式便宜"（`SUBAGENT_ENV_NOTES`
      / `subagent_environment_note`）。模式归用户后这句提示**失去作用对象**（模型不能切模式），
      留着就是过时文案，故连名单 `CACHE_BILLING_PRESET_IDS` 一起删净。教训：**提示的"可行动性"就是它存在的
      理由**——行动权被谁拿走，提示就该跟着走。
    - 守门分工：`tests/test_subagent_fork.py`（27 例）管"模式怎么落地"——runner 分叉 / handler 固化
      （模型传参不作数）/ 两个 schema 参数同形且都没有 `fork` / 常驻区按启用工具选规则，**真路径**用
      `SkillAgent.run` + 假 `complete` 抓真实 system 消息断言"开哪个工具就只说哪条规则、没开时一句不出现"；
      `tests/test_tool_mutex.py`（24 例）管"互斥怎么归一"——归一函数边界 / 三个入口 / 预设不留洞 /
      后端工具集兜底 / 前端镜像与接线（含批量勾选的两处 `{ applyMutex: false }` 与单点勾选路径）。

## 十、权威说明

**此后本文档是唯一权威的"项目现状说明书"**；任何新增的历史性文档都不应再混入本文件。