# 更新日志

本文件收录 Cat Chat（原 Naiba Chat）的版本说明；历史条目保留发布时的名称。
最新版本的要点见 [README.md](README.md)；应用内「设置 → 软件更新」的详细更新内容来自发布清单 `naiba-chat-update.json`，与本文件无关。

---

## 2.7.5 Beta 详细说明

2.7.5 Beta 带来一个新能力与一组重点修复：**新用户装完后「选供应商 → 粘 Key → 开聊」**——添加 API 与首启引导改为「预设卡片列表 + 表单」，内置 14 条供应商预设（在线大厂 DeepSeek / Kimi / 智谱 GLM / 通义千问 / OpenAI / Claude / Gemini，中转 TE 中转 / 摆烂中转，本地 Ollama / LM Studio / llama.cpp / Unsloth，以及全手填自定义），每条预填 API URL、请求格式与推荐模型，表单只剩 API Key 一个空；中转站的引导文案按「这是什么站 → 怎么注册充值 → Key 在哪创建」三段式写清，并配「打开注册页」按钮。**ComfyUI 批量生成三连修**：负 seed 随机值不再超出节点声明范围（此前 rgthree 的 seed=-1 被替换成 2^63 级巨数、ComfyUI 整单 400 拒收）、shots 参数对 workflow_paths 真正生效（此前被静默无视、shots=10 只出 1 张）、提交失败透传 ComfyUI 的具体拒收原因（此前只剩一句「第 N 段提交失败」让模型盲试 6 轮）。同一版还修掉「上下文重置工具显示成功、实际没重置」——per-call 上下文副本是浅拷贝，工具写进运行上下文的标记落副本上就丢，现在写入一律同步回落共享对象。以下是 2.7.3 Beta 的内容：2.7.3 Beta 修的是「手机端一看就错」这一类问题：**切后台回来，上一次的提问不再自己回到输入框**——发消息的失败分支此前把「请求已经被后端接住、只是流被手机系统掐断」也当成「没发出去」，于是把整段提问塞回输入框；而手机切后台时浏览器挂起网络、杀掉进行中的请求是系统常态，于是每次回来都要面对一条「请求失败 + 旧提问已回填」，再发一次还会撞上「该会话正在运行」。现在按**「流是否已被受理」**把两条失败路径分开：受理之后的断线一律算掉线——不回填草稿（后端那轮还在跑），改按退避重连把进度接回来。同一批修的还有三处手机端会撞上的毛病：**已经允许过的操作不再重复弹确认卡**（重载或重连后的事件回放会把处理过的确认请求原样重放，点下去只会得到「未授权」——现在回放出来的确认只作历史回执展示、没有按钮；操作失败的原因也直接写回卡片本身，不再只弹一条转瞬即逝的提示；等待确认的时限从 5 分钟提到 30 分钟，切后台回来不至于发现确认早已作废）；**插话队列的预览整段折行**（排队等着审阅、编辑、删除的内容，不该被挤成一行省略号）；**输入框打了很多字却只显示一半**（移动 Chrome 的字体提升会放大普通元素的文字、却不动输入框这类表单控件，而输入框是「透明 textarea + 镜像层显字」结构，两层被不同策略缩放后字号与换行点就错位了——现在全站关掉浏览器的自动缩放，字号一律以页面样式为准）。以下是 2.7.2 Beta 的内容：2.7.2 Beta 修的是「配置被自己人清掉」这一类问题：**MCP 服务列表不再被一次覆盖更新清空**——加载配置时有一道「退役旧版捆绑 ComfyUI 桥接」的清理，判据写成了「id 等于 comfyui 就丢弃」，而当年那个旧桥接注册的条目恰好就叫 comfyui，你自己按官方指南装好 comfy-mcp 后把它取名 comfyui 再自然不过，于是被当成自家旧资产一起清掉；更要命的是清理紧跟着一次落盘（每次启动都会把配置写回磁盘），所以服务不是「被忽略」而是**被永久删除**、每次启动都会重演——你平时不重启、只在覆盖更新时重启，看到的就是「一更新就没」，而 API 卡片、Agent 等其他设置都不在这份过滤名单里、所以全部幸存，这正是「只有 MCP 列表空了」的原因。现在过滤判据从**「名字」收窄为「签名」**：只有参数里出现旧桥接脚本文件名 `comfyui_mcp_server.py` 的条目才会被剥离，你自己注册的服务不论叫什么名字都不再被误删（特意没有按路径片段 `comfyui-mcp` 匹配——真实案例里用户的虚拟环境目录就叫 `comfyui-mcp-env`，按目录名匹配照样误伤）。**临时出路**：在 2.7.1 及更早版本上，把该 MCP 服务的 id 改成 `comfyui` 之外的名字（例如 `comfy-mcp`）就不会被过滤命中，登记后重启也还在；升级到 2.7.2 后原来的名字同样保留。以下是 2.7.1 Beta 的内容：2.7.1 Beta 补的是「K3 没吃上兜底」这个缺口：上一版给「思考开启、且你没显式设置输出上限」的会话注入 min(32768, 端点上限) 当总量闸门，但 **Kimi K3 的上限当时标着「待实测」、按 0 处理（等于不注入）**——也就是说 K3 会话其实一直没被这道闸门保护。这次拿真实端点把 K3 测了一遍：官方 `max_completion_tokens` 上限就是 **1,048,576**，实测 4096~1048576 全阶梯通过（1M 真实成交）；10M 以上撞的是中继**按 max_tokens 预扣费**的额度墙（不是协议拒绝），2^31-1 才被端点判 `max_tokens is invalid`。于是 `kimi_k3` 预设的 ceiling 由 0 回填为 **1048576**，K3 会话从此按 `min(32768, 1048576)` 注入兜底。顺带记下一条排查经验：**验证思考强度字段是否真的生效必须走流式**——中继的非流式响应不返回推理内容、`reasoning_tokens` 记 0，很容易被误读成「字段被吞了」；流式下推理内容真实出现、且 token 数随档位变化。以下是 2.7.0 Beta 的内容：2.7.0 Beta 治的是「思考失控」这一类问题：**思考型模型「想个没完」现在有三道闸门拦着**——推理刷屏几万甚至四十万字符、正文一个字没有，输出额度被烧光后上游直接截断，推理 token 还按输出计费，一段死循环等于白烧钱；更隐蔽的是**上一轮的失控思考会被原样全量回灌进下一轮**，模型被自己的循环锚定，于是每一轮都复读同一结论（这次用真实数据库量化确认：单条落库思考 **431,726 字符**、整个会话合计 **153 万字符**，而对应正文只有二十几个字——以前被误判成「模型尽力了，做不到」）。现在**流式期有预算熔断**（推理累计超 4.8 万字符且正文仍为空就主动中断、接上已有的降档重试；正文一出现立即解除，正常长思考不受影响）、**回放期有双闸门**（单条最多 4000 字符、同一轮合计最多 16000 字符，额度由新到旧分配并附省略标记；**只截回放、不动落库**，库里永远保留原文，展示 / 导出 / 排查都不受影响，截断结果确定所以前缀缓存不破）、**计费期有默认上限**（不再「不填输出上限就等于上游默认近乎无限」——确认模型会思考且你没显式设置时按 min(32768, 端点上限) 兜底，你自己设的值永远优先）。另外**修掉一个会静默骗人的隐患**：思考强度的识别以前靠模型名子串嗅探 + 六条分支硬编码，换个中继名就失效、网关用自有词汇也无从配置——现在是一张**声明式预设表**（10 个预设，覆盖 OpenAI / DeepSeek / Kimi / Claude / Gemini / Ollama / LM Studio），未声明的档位在发请求前就报错；顺带修掉「Kimi K2 调一次思考强度就白打一次 400」的老 bug。另有单次请求 15 分钟墙钟兜底（180 秒只是「每次读的空闲超时」，delta 一直来就永远不触发）、LM Studio「有推理零正文」也走降档重试、重复答复给提示（不拦截）、以及「运行设置」里新增的思考回放单条 / 整轮两个可调参数。以下是 2.6.11 Beta 的内容：2.6.11 Beta 带来一个念叨很久的能力和三个修复：**AI 回答到一半，你可以「插话」了**——以前模型一旦开始回答，输入框就锁死，想起一个补充条件只能干等它说完（或者狠心点停止）；现在输入框照常用，打字回车把话插进队列，想让它生效就点「引导」，这句话会作为新指令追加到对话末尾、模型下一步就照着做；写错了能改、后悔了能删，没点「引导」的话模型根本看不见它。**思考型模型「想个没完」不再整轮判死**：DeepSeek 这类模型在复杂长任务里偶发推理刷屏（实测一次想了约十万字、正文一个字都没有），以前三次重试原样重发、次次重演，最后弹一句看不懂的「执行失败」；现在重试会自动把思考强度降一档（高→中→低）打破循环，大多能自己救回来，救不回也会明白告诉你「思考把输出额度烧光了，建议调低思考强度再试」。**Kimi K3 的思考强度四档都能用了**：按官方方言映射（低→low、中→high、高→max，「关」落到最低档），此前「中」档会被官方端点直接拒绝、「关」不发字段反而变成默认最高档。另外修好了状态栏的**「重连中」误报**——模型长思考时，右下角不再错误地显示掉线横幅。以下是 2.6.10 Beta 的内容：2.6.10 Beta 治的是「啰嗦」：用 Agent 跑多步任务时，模型每轮调用工具前都爱顺口报一句进度——「我已定位根因」「继续验证」「现在补测试」——这些话以前**每轮都留着**，一轮跑完最后那条回复里整整齐齐堆着十几句进度播报，真正的结论被埋在最后一句。现在**一段正文只有最后一段留在结果里**，前面的过程预告一律丢弃，历史消息也会跟着一起干净；同时在系统提示里明确要求不要播报进度。另外修好 **Kimi K3 的工具调用兼容**：它会用保留 token 把工具调用混在普通文本里返回，以前这些 token 会原样吐进对话里，现在在**流式边界**就被拦住。以下是 2.6.9 Beta 的内容：2.6.9 Beta 让「换字体」这件事不用再打字：以前想换会话区字体，得自己填一串 CSS `font-family`，还得先搞清楚这台机器上装了什么字体、名字该怎么写——普通用户根本无从下手。现在「设置 → 外观 → 会话字体」的第四张卡片从「自定义」变成**「指定字体」**，点开是一个下拉列表，**11 种常见中文字体**（微软雅黑 / 苹方 / 宋体 / 楷体 / 黑体 / 仿宋 / 思源黑体 / 思源宋体 / 霞鹜文楷 / 得意黑 / 鸿蒙黑体）点一下就能选，手机端点开会直接弹**系统选择器**。列表里每一项都会**现场实测这台设备上有没有这个字体**，没装的在后面缀「（未安装）」——只是提醒、不禁选，因为设置是存在服务端、多设备共享的：电脑上没装不代表手机上没装。想手填 CSS 字体串的老办法一个字没少，降到列表最后一项「手动填写…」里；旧配置零迁移，之前存过的自定义字体串原样还在。字号滑杆与字体列表都只做本地预览，统一由「保存外观」落库，拖一下不会发一次请求。以下是 2.6.8 Beta 的内容：2.6.8 Beta 修的是「重置了上下文，界面却还锁着」这个死结：**点「新会话」之后，圆环真的归零、输入框立刻能用**——上一版起，本地模型上下文不够时会引导你点本条回复上的「新会话」，但那条线划下去之后，界面上的占用圆环仍停在 100%、输入框仍是禁用态：历史明明已经划到线以下、可以继续聊了，界面却坚持说「上下文已满」，用户唯一能做的只有刷新页面。根因是圆环取「有效用量」时只看最后一条带用量的消息，完全没认这把「分割线」——线以上的旧用量继续被当成当前占用，所以圆环不归零、输入框不解锁。现在圆环从消息列表末尾向前扫描、**扫到最新一条分割线就停**：线以上（含被标记的那条消息本身）的用量一律不算，线以下也没有用量就按「暂无数据」处理——圆环归零、输入框解锁、发送键可用；撤销分割线后，同一套扫描自动把锁加回来，不需要任何额外分支。**刚重置完还想接着写的那一句，现在真的发得出去**：模型写完交接文档、分割线出现的那一刻，本轮回复其实还在收尾（运行标志尚未落下），此前「运行中不得用历史值覆盖实时值」的保护会把种子内容重新填进一个仍然禁用的输入框——你看到的就是「字已经填好了、就是按不下去」；现在只对「上下文已重置」这一种场景放行，其余历史重渲染照旧不许擦掉实时值，保护本身没有变松。提示也说得更明白：划完线不再只说「下一条消息起……」，而是明确告诉你「**上下文占用已清零，可直接继续**」——知道可以继续，用户才敢继续。另外兼容旧数据：分割线同时认新形态（`metadata.session_start`）与更早版本留下的 `role=session` 标记行。以下是 2.6.7 Beta 的内容：2.6.7 Beta 修的是「说不清、看不准」这一类问题：**Agent 是什么，现在点一下就有答案**——顶栏 Agent 选择器左侧多了一个圆形「?」按钮，点开是说明弹层：Agent 就是「岗位角色」（人设 + 工具箱 + 技能包，工具箱在会话开始时锁定、中途不能换），三种预设 Agent（通用 / 编程 / 短剧）各适合干什么，以及想用 ComfyUI 出图的两条路线——**HTTP 直连**（启动本机 ComfyUI，到 设置 → Agent 的工具页一键套用「ComfyUI 联动」预设，不用配 MCP）与 **MCP 连接**（设置 → MCP 连上服务后在工具页手动勾选），两条任选其一即可。**工作区重名终于分得清**：工作区允许重名（分组按名字归并），光看下拉名字分不清谁是谁——现在侧栏分组标题与下拉的每一项都带目录路径的悬停提示，下拉自身也会显示当前选中项的目录。**未注册的工作区分组不再被显示成「未分组」（本次重点）**：注册表存在 config.json 里、会话的分组名存在库里，两份配置各存一份注册表就会分叉；此时下拉 `select.value = 分组名` 找不到对应选项会**静默落成空值**，界面于是把「属于 naiba-chat 的会话」显示成「未分组」——你以为会话被搬走了，而且切走之后再也切不回来（那个分组在下拉里根本不存在）。现在会把该分组本身补回下拉并标注「（未注册）」，悬停提示写清「当前值是什么、怎么恢复」（在侧栏「新建工作区」登记同名工作区即可）；补回来的分组**只用于显示、绝不回写 config.json**——注册表归谁由你自己决定，程序不擅自把孤儿分组扶正。**手机端长按粘贴修好了**：手机经局域网 `http://192.168.x.x` 访问属非安全上下文，网页里既没有 `navigator.clipboard`、`execCommand('paste')` 也被浏览器一律禁用，所以系统长按菜单是手机上唯一可行的粘贴路径——而此前 `contextmenu` 对可编辑元素是无条件 `preventDefault()`，等于把这条唯一通路一起关掉，你实测到的就是「粘贴失败：浏览器未授权」；现在触摸 / 手写笔长按直接放行给系统菜单，没有剪贴板权限时的提示也换成带动作的一句话（「请按 Ctrl+V，手机可长按输入框用系统菜单」）。**顶栏「收起」以前等于没收**：只隐藏了操作区又把两个下拉收拢到第一行，你看到的仍是「一行 API + 一行 Agent + 一条箭头」，高度基本没省；现在收起态整条顶栏只剩一个按钮，并把「展开顶栏」文字显示出来（此前只有一个 18px 小箭头，认不出这是唯一入口），能力只是临时收起、点一下全部恢复。以下是 2.6.6 Beta 的内容：2.6.6 Beta 修的是「模型说不了、用户看不懂」这一类问题：**本地模型上下文不够时不再静默与甩原始 JSON**——本地模型的上下文窗口是总窗口（prefill 的 prompt 与生成共用 n_ctx），历史一旦超出窗口，后端就会拒绝请求：llama.cpp / Unsloth 回 400 `exceed_context_size_error`，而这条错误此前是原文透传给用户看的（`请求失败：HTTP 400: {原始JSON}`）；Ollama 的流式响应里本来也带 `error` 字段，但它从未被读取——后端说的话被整块丢弃，只剩一句「流式响应中没有文本内容」。现在三种流形态与 HTTP 4xx 走同一个识别层，统一报因：**后端名 + 后端自报的真实窗口 + 两条动作**（点本条回复上的「新会话」，或到 设置 → 模型 填真实「上下文窗口」）——窗口取服务端真值而不是本机配置，因为「设置里填着 32768、后端其实只开了 4096」正是最常见的坑；判定还被提到兼容重试链之前，免得 4xx 溢出的语义被「剥离字段后重试」冲淡、白打一次注定失败的请求。**工具越堆越多时提前收手**：Agent 循环里工具结果一轮轮累积、此前只在整轮开始前查一次窗口，现在每步调用前增量复查（纯读、不改发给模型的字节序列，前缀缓存不受影响），越预算就保住已生成的正文并附上新会话指引，而不是等后端一个 400 把整轮成果打掉。**截断原因也说得清了**：`finish_reason=length` 此前一律按「撞输出上限」自动续写一次，可本地模型生成到 n_ctx 时后端报的同样是 length——「撞输出上限」（续写有用）与「窗口耗尽」（续写只会让请求更大）在协议层长得一模一样；现在结合用量区分，贴到窗口 95% 以上才提示「上下文窗口已耗尽，建议新会话后继续」。以下是 2.6.5 Beta 的内容：2.6.5 Beta 补上三件「本该有」的事：**消息全文搜索**、**分支导航**与**删除单条消息（含撤销）**。**搜索**——侧栏搜索框升级为「标题 / 全文」双模式，全文模式下输入即全库检索消息正文，结果显示为「会话标题 + 命中片段（命中词高亮）+ 第 N 条」，点一下直接跳到那条消息并高亮定位两秒，跨会话、跨历史窗口都能一步到位。**分支导航**——此前「分支」只是新开一个会话、事后完全查不到它从哪来，现在建立分支时登记来源与分支点，侧栏出现 ⑂ 徽标与「⑂N」下级分支计数，点开分支链面板能看到源会话与所有兄弟分支并一键切换（侧栏保持平铺、不画树）。**删除单条消息**——用户消息多了「删除」、AI 回复也多了「删除」，删之前按角色给出粒度（仅删这一条 / 删除这一轮）并写清后果，删完底部有 10 秒撤销条，撤销会把内容逐字节原样插回（含时间、附件、活动轨迹），连前缀缓存命中都能恢复。以下是 2.6.4 Beta 的内容：2.6.4 Beta 修的是「正在写的东西被拆掉」这一类问题：**点任务面板不再把正在写的回复弄丢**——后台任务跑着时点面板里任一行，界面会跳回那条会话并整体重渲染消息区，而那一刻正在流式输出的助手气泡（正文在回复结束前只存在于界面里、落库发生在结束之后）会被整块拆掉且没有人挂回来；流仍在写，只是写进了一个已经不在页面上的节点，于是表现为「AI 回复消失了，只剩下前一条提问」，此后一直不回来、重启刷新都没用；现在重渲染会把这条气泡原样搬回消息区末尾，流继续往上写。**重启不再吃掉半截回复**——服务重启会把仍在跑的对话轮次判为「已中断」，而模型此前已经吐出的正文只留在运行事件流里、界面上无从看到；现在启动时会把这段内容重建为一条带「未完成」标记的消息（正文、思考、工具活动、中断原因都在，能看到跑到哪一步），一个字都没产出的轮次不会留下空壳消息。**更新后自动重启，窗口会自己出现**——更新脚本此前以隐藏窗口启动新进程，而本程序本来就是窗口化打包（没有控制台要藏），结果主窗口一起被藏进托盘、只能到托盘双击唤回；现在以普通窗口启动，另加一道「窗口就绪即显示并置前」的兜底。另外**更新检查改为只在你点「检查更新」时才发起**（此前启动 4 秒会自动查一次元数据，只查不装，但你会「没点就看到新版本」）。以下是 2.6.3 Beta 的内容：2.6.3 Beta 修的是「编辑被打断」这一类问题：**编辑到一半不再被后台的一次刷新悄悄吃掉**——编辑消息时底部整块输入区被搬进那条气泡，而消息列表的重渲染（会话轮询、后台任务写消息、流式落库都会触发）此前会无条件退出编辑态，于是改到一半的字掉回底部输入框、原附件丢失、发送键从「重新发送」变回「发送」（此时按回车会变成发新消息），全程零提示；现在同一会话刷新且那条消息还在时会**原样复挂**（改过的字、附件、光标位置与「重新发送」按钮全部保留），只有消息真被删掉或切换了会话才退出编辑，而且**退出时会把进入编辑前的底部草稿逐字还原回输入框并明确提示**，不再静默丢弃。以下是 2.6.2 Beta 的内容：2.6.2 Beta 让流式过程更稳：**消息列表不再抖**——流式期间每个增量都会贴底，而容器此前的平滑滚动（`scroll-behavior: smooth`）会让页面长期处于「追赶最新内容」的动画状态、肉眼可见地晃，上滑阅读历史时也会被内容高度变化拽出抖动；现在程序化贴底一律瞬时定位（容器默认瞬时 + 代码内 `withInstantScroll` 双保险），只有你主动跳转（刻度轨点击）仍是平滑的。**长回复不再掉帧**——流式渲染此前固定每 40ms 把「越写越长的整段文本」重跑一次 markdown 并整体替换（成本随长度呈 O(n²)），长回复后半段明显掉帧；现在按累积长度自适应节流（40ms 起、每 4000 字 +50ms、240ms 封顶），短回复保持跟手。以下是 2.6.1 Beta 的内容：2.6.1 Beta 修的是「设置被自动清空」这一类问题：**聊天背景图不再「自己消失」**——启动时对背景图的探针只要失败（图片已被缓存清理删掉 / 换了数据目录后路径失效 / 启动瞬间的网络抖动），此前会把背景设置清空并写回服务端，一次探测失败等于永久丢失；现在只标记「文件暂不可用」、设置原样保留，并在设置卡给出明确提示与出路（点「更换图片」重新选择即可），唯一会清空背景的入口只剩你主动点「清除背景」。保存也改成**只提交本次改动**：改强度、调取景不再受失效图片牵连。服务端同时兜底——换了数据目录或换了机器后，旧路径会按文件名在当前受管目录（含按日分目录）里自动找回并接上；确实找不到也保留原路径，等你重新选择。以下是 2.6.0 Beta 的内容：2.6.0 Beta 把「AI 正在干活」从一句静态文字变成了看得见的过程，并给模型输出补上一道**截断守护**。**工具执行实时可见**：`pwsh` 与 `run_skill_script` 逐行把子进程输出推给界面，工具卡片里就是一块等宽滚动输出区（最多保留最近 200 行、贴底才自动跟随），摘要行显示「Running · 工具名 · 已运行 Ns」、跑完换成「Completed · 工具名 · Ns」。**停止与超时即时生效**：这两类命令改用「读者线程 + 队列」执行，点「停止」不再要等命令自己跑完，超时与取消都返回明确的 `exit_code=-1` 与原因说明。**截断不再静默**：全链路开始记录模型响应的终止原因（`finish_reason`），答复撞上输出上限、或流在没有终止原因的情况下停在句中断点时自动**从断点续写一次**（不重开头、不加前言），并在消息末尾标注「本轮回复可能被截断」（是已续写、还是续写后仍被切）。**后台日志真的能看**：任务面板展开详情即按游标增量拉取该任务的逐行日志（`job_log` 事件一直在写进运行事件流、前端此前从未消费，「转到后台以后还能看日志」其实是空头承诺），同时任务行改用落库的真实任务名、子 Agent 类型标签统一为「AI 子任务」。以下是 2.5.0 Beta 的内容：2.5.0 Beta 给「聊天背景」补上了**内置背景图**与**白板式取景编辑器**：内置 6 张跟皮肤色系的渐变图（紫雾晨曦 / 海雾 / 玫瑰石英 / 林间薄雾 / 石墨夜色 / 素纸），设置里点一下即用，不必自己找图（图片由后端幂等生成到 `data/backgrounds/`，随数据目录迁移、不占仓库体积、也不受缓存清理影响）。编辑器里给出「整张图 + 自由取景框」：拖框选区域、拖八个手柄自由改形状（角改两向、边改单向——竖向、横向都能切），滚轮 / 滑杆等比缩放（下限 = 整张图、上限 400%），另有「填满 / 完整显示 / 复位」一键适配；框比例与对话区不一致时，框里那块会被**完整等比**铺进对话区、多余部分用同图模糊底补上（不拉伸、也不二次裁切）；右侧「效果预览」与真实对话区共用同一套渲染规则，所见即所得。**竖图不再尴尬**——以前要么被裁成一条、要么缩成中间一条纸带，现在能看见整张图、自己决定裁哪一段，「完整显示」时没铺满的两侧改用同一张图的模糊放大版补满。另新增**「旋转 90°」**（服务端把图真的顺时针转成新文件，原图不动；编辑器里旋转后按「取消」会把派生文件回收）。取景（裁剪区域）随外观一起保存，多端一致。以下是 2.4.1 Beta 的内容：2.4.1 Beta 修的是「界面在撒谎」这一类问题：**任务面板只列真正的后台作业**——此前面板把 `background_tasks` 里两类记录平铺，`chat` / `plan_execute` 那些行只是「每发一句话就插一次」的回答记录，于是看起来像「我发过的每一句话都是一个任务」；现在过滤放在 SQL 层只回后台作业（ComfyUI 生成 / 命令执行 / HTTP 轮询 / 子 Agent 等），按「由哪次回答派生」分组、组标题取「类型 + 时间」，顶栏徽标在没有活动任务时回落为历史总数并弱化（不再「全部跑完就显示 0」），面板顶部常驻统计条、轮询失败时如实显示最后成功更新时间与原因。**「停止」真的会停**：取消现在同时写入「取消已下发」的持久标记（此前只把状态写成 `stopping`，而冻结判断读的是另一个从没人写的标记，下一次状态更新又把它打回 `running`），并新增 90 秒看门狗把卡在网络等待 / 退避里的作业强制置为已取消（只对登记过取消事件的后台作业生效）；级联取消不再把已完成的父行改写成「已取消」。**输入区「模型」改为可搜索下拉**：中转站未分组时 `/v1/models` 能返回 400+ 个模型，原生下拉只能靠滚动定位；现在原生 `select` 降级为隐藏的值容器（仍是唯一值来源），可视化层是触发按钮 + 搜索浮层——输入即过滤、`↑`/`↓` + `Enter` 选择、`Esc` 关闭并把焦点还给按钮、点外部收起、重开时定位到当前已选模型，浮层挂到 `body` 不被输入区裁剪、朝上展开时锚底边（不再被裁剪也不再「到处飘」）。另修暗色下行内代码「浅底浅字」（实测对比度约 1.2，等同不可读）。2.4.0 Beta 新增**视频抽帧工具**（`probe_video` / `extract_frames`）与**本地模型防卡死**：模型现在能直接「看懂视频」——`probe_video` 读取时长 / 帧率 / 分辨率 / 总帧数 / 编码 / 是否含音轨，`extract_frames` 支持按**时间点 / 固定间隔 / 画面切换（keyframes）**三种模式抽帧、可额外生成一张拼图**联系表**（先看概览再抽单帧细看），抽出的帧进入消息媒体区（带缩略图）、交给 `vision_analyze` 即读；上传视频时自动提示模型按「先 `probe_video` → 再抽帧 → 再看图」的顺序处理。**本地模型不再「永久卡住」**：「设置 → 运行设置」新增**本地首字节超时**（默认 120 秒，0 = 关闭——本地后端 prefill 期间一个字节都不吐，「正在 prefill」与「永远做不完」在界面上无法区分）与**单轮 Agent 最大步数**（默认 200，0 = 不限制——模型反复调工具拿不到结论时不再无限烧下去、也不再一直占着本地锁）；本地上下文窗口改为**自动探测并写回**（探测到就按真实窗口算，探测不到按 32k 兜底，不再对本地模型套用在线 256k）；统一「谁算在运行」的状态词表（含 `stopping`）后，启动清理不再漏掉「已取消但线程还没退出去」的僵尸任务（它曾让整条会话永久显示「回复进行中」、三个救援入口全部失效），「停止」改为**关掉连接打断卡住的读**、前端超时也照常复位界面（不再以服务端确认为前提）。另修**出站网络重试**（连接器「选择」与「重建后重试」两张判定表对齐，此前「代理已开启但未填地址 + 关系统回退」一档重建侧没覆盖、重试只是空转；并杜绝「缓存一空就静默退回直连」）与设置页「**目标版本**」下拉的层级 / 裁剪（改挂 popover 宿主进入 top layer，不再被设置面板裁掉或被对话框盖住）。2.3.9 Beta 修复**更新检查与出站网络**：更新清单只列可自动更新的版本（过滤 2.0.0 及更早的大版本迁移版本）、按版本号从新到旧排序并按版本号去重——同一版本重复发布不再出现重复项；是否「当前版本」的判定改为「版本相同**且提交相同**」，同一版本号重发（资产/提交变了）不再被误判为已安装，可正常选择安装；清单指向 2.0.0 时更新面板与安装入口都明确提示「请前往 GitHub Release 手动下载」；「目标版本」下拉由原生 `select` 改为**自绘下拉框**（原生弹层在桌面内嵌窗口里不可控），支持 `↑`/`↓`、`Enter`/空格、`Esc` 与点击外部关闭；手动代理只接受 HTTP/HTTPS（填错明确提示并回退直连），任何代理设置变更都会让手动/系统/直连三种连接器全部失效重建，幂等请求（`GET`/`HEAD`/`OPTIONS`）遇连接异常时按当前策略重建连接后重试一次（`HTTPError` 不重试、也不会悄悄切换代理模式）。2.3.8 修复**待发附件被自动清理误删**：自动清理此前拿「`uploads` 与 `generated` 的总大小（含被引用、删不动的文件）」去比阈值，而引用组永不删除、「降到上限」永不可达，清理循环因此退化成「删光所有未引用文件」——刚上传、还在输入框待发（尚未落库）的附件会被当场删掉，表现为**附件区破图、点击图片无法放大、模型报「未找到图片文件」**。现在加**两道保护**（15 分钟保护窗口 + 上传入口显式保护本次刚落盘文件），并在**发送前校验附件是否还在**（缺失就保留草稿、剔除该项并提示重新上传；服务端同样拦下）。2.3.7 在 2.3.6 的基础上新增**外观主题与皮肤**：设置页置顶的「外观」区块可选**主题模式**（跟随系统 / 日间 / 夜间——跟随系统会随 Windows 的浅色 / 深色设置实时切换）与**四套界面皮肤**（紫藤 / 海洋 / 玫瑰 / 森林，只换品牌色相：按钮、选中态、消息气泡等，版式与信息层级不变）；偏好保存到服务端（`config.json` 的 `appearance`），同一实例的电脑与手机端保持一致，浏览器本地另存一份用于首屏无闪烁，面板内可「保存」「恢复默认」，非法或历史配置值自动归一化。设置导航同时改为分组（常规 / 模型与 Agent / 工具与联网 / 数据与隐私 / 关于）并新增关键词搜索。2.3.6 起已具备：**「编辑」= 完整输入区就地编辑**——点「编辑」后底部整块输入区（输入框、`/` Skill 引用、`@` 工作区引用、图片与文件上传、待发送附件、思考强度与 meta 行）搬进那条用户气泡，编辑时照常粘贴 / 拖拽上传、增删附件、用 `/` 与 `@` 引用，Enter 重新发送、Shift+Enter 换行、Esc 取消，退出后恢复进入编辑前的底部草稿与附件；`/`、`@` 弹层稳定盖在「正在编辑此消息 / 取消」之上。2.3.5 起已具备：**AI 回复「重新生成」**、**用户消息「编辑」回归**——两者的重发都从**那条提问**处截断（而不是从回复处），之前的问答逐字节保持原样、前缀缓存继续命中，反复点也不会累积重复提问；引用与附件一并保真（`/ref` 技能引用与 `@` 工作区引用逐字保留，图片 / 文件原样随行，纯附件轮次也能重发）。HTTP API 与事件协议版式不变。以下是 2.3.4 起已具备的能力：**权限判定与实际执行统一以当前会话工作区为准**——此前切换过工作区的会话会把工作区内的文件判成越界、反复弹确认（现在 `read_file` / `list_directory` / `search_files` / `write_file` / `edit_file` / `read_pdf` 判定与执行共用同一个根），**点「允许执行」后也不再丢失运行上下文**（工作区、取消信号、任务归属随确认一起暂存），子 Agent 同步改用会话工作区；越界确认卡片直接写明「当前工作区」，另新增可选权限诊断开关（`NAIBA_DEBUG_PERMISSION=1`）。手机端顶栏新增**收起开关**，把被顶栏吃掉的高度还给会话区；审批档位提示文案写明实际边界（自动＝工作区内放行、越界仍确认；完全＝不再询问、可访问工作区外）。HTTP API 与事件协议版式不变。以下是 2.2.3 起已具备的能力：**Skill 删除**重构——删除改为确认对话框，可选「移至回收目录（可恢复）」或「彻底删除」，设置页新增回收目录列表（可一键恢复或清空），并修掉「同一 Skill 有多份副本时点了删除立刻复活」与「删掉的内置 Skill 下次启动又回来」两个顽疾；**会话模型选择**收紧为「只从当前 API 检测到的模型里选」，未检查模型或选中项不在目录里会拦下发送（不再把未经验证的模型名发给服务端），并修掉供应商更换默认模型后旧会话仍继续发送旧模型名的问题；冻结版打包补齐 multipart 解析器（修复安装版上传附件失败）。手机端「功能对等」——手机经局域网打开后，电脑能做的事手机都能做，只换形态——右侧文件面板从「整体隐藏」改为全屏抽屉、消息末尾「本轮修改的文件」条目恢复可点、桌面右侧的对话刻度轨换成顶栏「轮次」下拉、触摸目标统一不小于 44px；同时修复 **Skill 安装目录归属**（散装上传先建独立子目录、删除带归属校验，杜绝「删一个 Skill，全部消失」，升级自动迁移历史裸定义文件并保留引用），增强**在线模型稳健性**（Codex Responses 聚合事件回填、空流按瞬时故障重试、HTTP 5xx 退避重试并落盘失败请求体取证），并做界面打磨（审批模式改为上拉框、输入区模型只从当前 API 的模型目录里选、API 供应商卡片更紧凑）。详见下方「2.4.1 Beta 主要能力」（2.4.0 的能力一并保留在下方）。

## 2.7.3 Beta 主要能力

- **切后台回来，旧提问不再自己回到输入框（本次重点修复）**：`sendChatMessage` 的 catch 此前对所有非「用户主动中断」的异常一视同仁——连 `consumeRunStream` **中途断线**也走 `restoreRejectedSubmit()` 把整段提问塞回输入框。手机切后台时浏览器挂起网络、杀掉进行中的 fetch 流是系统常态，于是每次回来都看到「请求失败 + 旧提问回填」，诱导用户再发一次、撞上 `ACTIVE_RUN`。现在用 `requestAccepted` 标志把两条失败路径分开：**拿到 2xx（流已开始）之后的断线一律按掉线处理**——不回填草稿（后端 Run 还在跑），有 runId 走 `scheduleRunReconnect` 退避重连（`finally` 里配 `reconnectScheduled` 跳过 `finishRunSubscription`，与 `resumeRun` 的既有收尾约定一致）；`run_started` 尚未到达时先 `detachRunSubscription()` 再 `openConversation` + `resumeConversationRun` 兜底（后者要求 `abortController` 为空，漏了 detach 会被它自己的守卫静默早退）。**判据：恢复草稿的正当性只存在于「服务端可能没收到这条消息」的窗口内；流一旦被受理，前端的唯一职责是把进度接回来，而不是把消息还给用户。**
- **已允许过的确认卡不再复活、点了不再说「未授权」（本次修复）**：三个叠加根因一起改。① 页面重载/重连后 `/api/runs/<id>/events?after=0` 会把**已处理过**的 `tool_confirm` 事件原样回放，前端照单渲染出带「允许执行」按钮的活卡，点下去后端 `owns_confirmation=False` → 409「确认请求不属于该运行或已失效」（用户口中的「未授权」）。修复在**流出口**（`_stream_run`）：回放与实时的 `tool_confirm` 统一查 `owns_confirmation`，不再待决就标注 `confirm_resolved: True`——实时事件此刻必在 pending（executor 先登记后发事件），不受影响；前端据此渲染「确认已结束」的历史回执（无按钮），并按 `confirm_id` 去重，防同一事件二次插卡。② `approveTool`/`rejectTool` 的 catch 只 toast、不改卡片，卡片永远停在「正在执行...」，用户会以为已经批准；现在失败原因写回 `.tool-confirm-actions`（`confirmEl` 必须提出 try 外才能被 catch 用到）。③ 确认等待超时 **300s → 1800s**：手机切后台超过 5 分钟是常态，以前回来时确认早已被自动拒绝；超时文案也改成按 timeout 动态拼分钟数，不再写死「5 分钟」。**教训：凡是从存储回放出来的「可操作 UI」，都要回答「这个操作现在还成立吗」——事件是历史，按钮是现在，两者不能共用一份渲染。**
- **插话队列预览整段折行，不再挤成一行省略号（本次修复）**：`.run-guidance-preview` 由 `nowrap + ellipsis` 改为 `pre-wrap + overflow-wrap:anywhere`，卡片改 `align-items:flex-start` 并给图标/徽标 3px 顶部补偿对齐首行。**队列是用户要逐条审阅、编辑、删除管理的对象，「一行一句话省地方」在这里是错误取舍**，窄屏手机上尤其看不清自己排了什么。
- **输入框「文字只显示一半」：关掉移动端字体提升（本次修复）**：移动 Chrome 的 FontBoosting 会放大**普通 div** 的文字、却不动**表单控件**——输入框是「透明 textarea + div 镜像层」结构（`.composer-input textarea` 用 `color:transparent`、`.input-mirror` 负责显字），镜像层被放大后两层字号与换行点错位，可见文字就只剩一部分；boosting 是否触发取决于布局启发式，所以表现为「有时候才会」。现在 `body` 加 `-webkit-text-size-adjust:100%; text-size-adjust:100%`，全站字号一律以我们的 CSS 为准。**该修复属结构推断、尚待实机复验**（用户提供的截图未送达）：若手机端仍只显示一半，请回报机型与浏览器版本。
- **验证**：全量单测 **1431 例通过**（`python -m unittest discover -s tests -t tests`，41s）。本次四项改动均为前端/接口层的定向修复、未新增测试用例，相应结论与判据已登记进维护说明 §九.107~110。

## 2.7.2 Beta 主要能力

- **覆盖更新不再清空你自建的 MCP 服务（本次重点修复）**：`ConfigStore` 启动时那道「退役旧版捆绑 ComfyUI 桥接」的清理，判据是 `id == "comfyui"` **一刀切**——任何 id 叫 `comfyui` 的条目都会被丢弃；而旧桥接当年注册的条目恰好就叫 `comfyui`，用户按官方指南装好 comfy-mcp 后把它取名 `comfyui` 是完全自然的选择。叠加 `__init__` 末尾的 `self.save()`（**每次启动都把剥离后的配置写回磁盘**），后果不是「读不到」而是**永久删除、且每次启动重演**；用户平时不重启、只在覆盖更新时重启，于是表现为「一更新就没」——而 providers / Agent 等其他配置不在过滤名单里、全部幸存，这正是「只有 MCP 列表空了」的原因。
- **过滤判据从「名字」收窄为「签名」（本次修复）**：新增 `_is_retired_comfyui_bridge()`，只按旧桥接的**脚本文件名签名**（`command` / `args` 含 `comfyui_mcp_server.py`）识别，用户自建服务无论叫什么名字都不再被误删。**特意没有按路径片段 `comfyui-mcp` 匹配**——真实案例里用户的虚拟环境目录就叫 `comfyui-mcp-env`，按目录名匹配照样误伤。
- **临时出路（针对 2.7.1 及更早版本）**：把该 MCP 服务的 id 改成 `comfyui` 之外的名字（例如 `comfy-mcp`）就不会被过滤命中，登记后重启仍在；升级到 2.7.2 后原来的名字同样保留。
- **验证**：全量单测 **1431 例通过**。`tests/test_config_migration.py` 里「旧桥接退役」用例改为带脚本签名的形态，并新增「**用户自建 comfyui 服务存活**」用例——守门形态直接取自真实事故（`…\comfyui-mcp-env\Scripts\comfy-mcp.exe`），防止将来有人「顺手」把判据写回按目录名匹配。

## 2.7.1 Beta 主要能力

- **Kimi K3 的输出上限接上兜底（本次重点）**：`kimi_k3` 预设的 `max_output_ceiling` 由「待实测」的 0 回填为 **1048576**——0 意味着 A0 对 K3 **不注入**默认输出上限，K3 会话等于没有总量闸门。真实端点实测（teynex 中继 → `kimi-k3`）：K3 官方 `max_completion_tokens` 上限即 1,048,576，4096~1048576 全阶梯 HTTP 200（1M 真实成交）；10,485,760 返回 403，但那是 New API 系中继**按 max_tokens 预扣费**（约 ¥9 / 百万 token）撞上余额的额度墙、不是协议拒绝；2,147,483,647 才被端点判 `max_tokens is invalid`（500）。现在 K3 会话在思考开启且未显式设置上限时按 `min(32768, 1048576)` 注入。
- **思考强度方言的实测结论**：官方 `reasoning_effort` 只认 low / high / max（与预设表词表一致，思考关不掉）；中继对越界取值（none / minimal / xhigh，甚至 K2 的 `thinking` 字段）一律宽容 200、不校验。**验证思考字段是否真的生效必须走流式**——非流式响应不返回 `reasoning_content` 且 usage 的 `reasoning_tokens` 记 0，容易被误判成「字段失效」；流式下 `reasoning_content` 真实出现、token 数也随档位变化（本次单次实测 low=15 / high=108 / max=51，样本有限仅供参考）。
- **守门补齐**：新增一条钉死 K3 上限的回归用例（防止这个值被静默改回 0），全量单测 1430 例通过。

## 2.7.0 Beta 主要能力

- **思考三段治理：流式期熔断 / 回放期双闸门 / 计费期默认上限（本次重点）**：这三段各管一段、互不替代。**流式期**——`REASONING_STREAM_BREAK_CHARS`（4.8 万字符）在三条流解析路径（SSE / Ollama / LM Studio）统一累计推理增量，**只在正文仍为空**时主动中断并抛出与「空流降档重试」同一个异常，于是熔断自动获得既有的「高→中→低」降档链；正文一旦出现立即解除，绝不误伤正常的「先想后答」。**回放期**——`build_model_history` 的两条出口（消息路径与 trace 重放路径）共用同一个 `_ReasoningReplayBudget`：单条 `MODEL_REASONING_REPLAY_MAX_CHARS`(4000) 硬闸门 + 整轮 `MODEL_REASONING_REPLAY_TURN_CHARS`(16000) 软闸门，**由新到旧**分配额度（新思考离本轮最近，最可能承载「上轮结论是怎么推出来的」），被压缩的条目也始终保留「前缀 + 省略标记」（满足 DeepSeek / Kimi「reasoning 必须回传」的存在性契约）。**只截回放、不动落库**——库里永远保留原文；截断是纯函数（同一条消息 + 同一组参数 ⇒ 逐字节一致），所以跨轮前缀缓存不破。**计费期**——`max_tokens` 是「思考 + 正文」的总量闸门，不填 = 上游默认值（多数近乎无限）；现在确认模型会思考且用户未显式设置时注入 `min(32768, 端点上限)`，用户显式设的值永远优先；注入值在上游被拒时**只撤自己注入的那一个值**重试一次，绝不动用户的设置。
- **A0 默认输出上限的判据是「模型级」而不是「协议族级」（本次关键设计）**：`auto` 档必须拿到**模型级证据**（模型名命中已知思考模型清单，或卡片显式声明）才填上限——只命中协议族预设（例如 gpt-4o 走 `openai` 默认预设）**一律不发**。否则不支持思考的模型会被填上 32768 → 400 → 剥字段重试，每个请求都完整重演一次，白打一次 400 往返且永久如此。真实请求体复测：gpt-4o / auto 档顶层无 `max_tokens`，且全程**只发 1 次请求**（零 400 往返）。Claude 的 `max_tokens` 是协议必填（既有 4096 兜底），本版把它抬到 `min(32768, 端点上限)`。
- **思考强度改数据驱动：`naiba/llm/thinking.py` 声明式预设表（本次重构）**：原先散在 `_reasoning_params` 里的**六条 if 分支 + 模型名子串嗅探**整体退役，换成一张表——10 个预设（`openai` / `openai_codex` / `deepseek` / `deepseek_codex` / `kimi_k3` / `kimi_k2` / `claude` / `gemini` / `ollama` / `lm_studio`），每个预设声明三件事：**方言**（思考字段在协议里怎么拼）、**档位词表**（界面档 → 真正发给 API 的字符串）、**输出上限 ceiling**。命中顺序「卡片手填 > 模型名 > `request_format` 默认」，带**资格约束**（`kimi-k3` 走 codex_responses 中继时不得命中 K3 预设，须落到 `openai_codex`，保住「codex 不做 K3 特判、由中继翻译」的既有契约）。三条纪律：声明 ≠ 选择（预设只声明可选项，不替你选默认档）；**未声明的档位在发请求前就失败**（`ThinkingConfigError`，不是发出去等 400）；不跨适配器家族搬字段。
- **降档链按词表声明顺序下探（本次修复）**：降档不再是「应用四档减一」（`high→medium→low`），而是**按该预设词表的声明顺序**退一档——K3 那种只有 low / high / max 的词表原本会退到不存在的中档，现在退到它真正支持的档位。off / auto / 单档 ⇒ 无可降（沿用既有语义）。
- **Kimi K2 调思考强度不再白打一次 400（顺带修掉的老 bug）**：K2.x 收到 `reasoning_effort` 会被官方端点拒绝，而旧代码对 openai_chat 只给 K3 开了特判、K2 落到最后一行「原样发字段」——于是把思考强度从默认调到任意一档，首请求必然 400 一次（靠既有的剥字段自愈兜过去，**真实危害是「调档静默失效 + 白打一次往返」，不是硬失败**）。现在 K2 命中自己的预设（不发字段），从数据层根治。
- **单次请求总时长兜底：15 分钟墙钟（本次修复）**：`ONLINE_STREAM_TOTAL_TIMEOUT_SECONDS`(900) 经 `_iter_stream_lines` 从进入生成器起算墙钟，超时由看门狗关连接并抛 `StreamTotalTimeout`；`complete` 捕获后按瞬时故障退避重试，**若已收到推理却零正文则直接转成空流降档**。此前代码注释写「在线已有 180 秒总超时兜底」——那是误解：`ONLINE_MODEL_TIMEOUT_SECONDS=180` 是 urllib **每次读操作的空闲超时**，推理 delta 持续到达就不断重置它，一段 43 万字符的思考循环可以无限流下去，直到上游掐断。可用 `options["stream_total_timeout_seconds"]` 覆盖（0 = 关闭），只挂在线请求（本地已有首字节超时 + 1800s 上限）。
- **LM Studio「有推理、零正文」补齐降档重试（本次修复）**：此前本地路径在这种形态下直接抛 `RuntimeError`，不参与降档重试；现在对齐在线语义抛 `EmptyModelStreamError`，降档白名单从 `{codex_responses, openai_chat}` 扩入 `lm_studio`。**顺带修掉一个「界面在撒谎」**：本地模型报错时文案写死「在线模型流式响应中没有文本内容」——本地 LM Studio 报错说自己是云端；现在按画像给出「LM Studio / Ollama / 本地模型 / 在线模型」，并加源码级负向断言防止写死回流。
- **重复答复检测：只提示、不拦截（本次新增）**：Agent 终态答复生成后，与历史尾部最近一条 assistant 正文做规范化比较（`strip` + 折叠空白后逐字相同），且长度 ≥ 20 字符（避免误伤「好的」这类短应答）时，发一条 `status` 事件提示「模型可能陷入重复；建议换个说法或开新会话」。**不拦截**（用户可能确实要求复述）、**不落 metadata**（消息契约不变，`build_model_history` 的字节稳定性不受影响）。
- **新增两个可调参数 + 一次界面/文案收口**：「设置 → 运行设置」新增**思考回放单条上限**（`reasoning_replay_max_chars`，默认 4000）与**整轮上限**（`reasoning_replay_turn_chars`，默认 16000），均为非负整数、**0 = 关闭**；参数经 `config.reasoning_replay_options()` 注入，**覆盖 `build_model_history` 的全部 3 个活调用点**（主聊天 `run/chat.py`、子代理 `subagent.py`、封存的 `plans.py`）——缺一个就会出现「同一会话两种回放字节」，前缀缓存断且行为不一致。参数入口旁的提示写明代价：调小省上下文、调大保留上轮思路，**改值后下一次请求会重建一次缓存**。模型卡的 `max_output_tokens` 帮助文案也补上了「思考开启且留空时按 min(32768, 端点上限) 兜底」。
- **验证**：全量单测 **1429 例通过**。新增四个守门测试——`tests/test_reasoning_replay.py`（21 例：双闸门本体、由新到旧分配、只截回放不动落库、截断实现唯一、三个调用点透传、设置面板接线）、`tests/test_thinking_presets.py`（30 例：命中度量、未声明档位发前失败、K3 方言映射、K3 + codex_responses 资格约束、降档链按词表顺序）、`tests/test_stream_guards.py`（18 例：熔断只在正文为空时降档、LM Studio 有推理零正文、墙钟超时进重试、0 值确实关闭、空流报错来源标签）、`tests/test_repeat_answer.py`（9 例：只提示不拦截、不落 metadata、<20 字符不误伤）。另做**三条真实路径复测**（探针不入库）：真实库会话（`239e93ba…`，50 条消息、落库思考合计 1,533,638 字符、单条最大 431,726 字符）开启闸门后**回放 4024 字符**、关闭闸门原样回放 431,734（对照组成立）、两次构建**逐字节一致**、请求体 448,745 → 14,325 字节；真实请求体验证 A0 在四种协议各自的专有键上落点正确（`max_tokens` / `max_output_tokens` / `generationConfig.maxOutputTokens`）且 gpt-4o 只发 1 次请求；真实流式验证 12,000 字正常思考不触发熔断、52,000 字零正文在 50,000 字符处主动中断并给出提示、正文一出现即解除。

## 2.6.11 Beta 主要能力

- **运行中可以「插话」（本次新能力）**：AI 还在回答时，输入框照常可用——打字回车把话插进队列（界面上是一条待引导的消息行），面板上三个动作随你选：**引导**（这句话作为新的用户指令**追加**到对话末尾，模型下一步就能看到并照做）、**编辑**（改完再引导）、**删除**（收回）。三条硬规矩保证它不乱来：① 未点「引导」的插话**不会进模型上下文**——它只是你的草稿；② 引导后只**追加到对话末尾**、不改写不重排历史，DeepSeek 前缀缓存照常命中；③ 运行结束（跑完 / 停止 / 失败）后残留队列自动冻结，不会发给下一轮的模型，但**仍可删除或取回草稿**。与运行结束后的「选择」面板互不干扰，两个输入通道各管各的时间窗。
- **思考失控自动降档重试（本次重点修复）**：思考型模型（DeepSeek 等）在复杂长任务里偶发「想个没完」——推理刷屏约十万字符、正文和工具调用一个都没有，输出额度被思考烧光后上游直接截断（实测上游零计费、不计 token）。以前 Naiba 的重试是**原样重发同一请求**，模型三次掉进同一个推理循环，整轮 Agent 任务判死「执行失败」。现在检测到「有思考、零正文」的空流时，重试会**自动沿「高→中→低」降低思考强度**再发（只改本次重试的请求，不动你的会话设置），用更小的思考预算打破循环；三档都烧完仍失败时，报错会带诊断（思考未产出正文、最长一次思考约 N 万字）和可操作建议（调低思考强度或精简上下文），不再只有一句看不懂的「在线模型流式响应中没有文本内容」。
- **Kimi K3 思考强度四档全部可用（本次修复）**：K3 官方的 `reasoning_effort` 只认 low / high / max 三档（默认 max），且思考永远开启、无法关闭。现在按官方方言映射：低→low、中→high、高→max、「关」→low（最贴近「关」的语义）。此前「中」档原样发 `medium` 会被官方端点拒绝，「关」不发字段反而落到默认 max（最高档）——两个档位都是反的。判定只命中 K3，K2.x 系列不受影响（它用的是另一套 `thinking` 参数）。
- **状态栏不再误报「重连中」（本次修复）**：模型长思考期间，右下角曾一直挂着「重连中… · 已等待 X 秒」并走秒，看着像掉线了。根因是横幅的显示状态寄存在计时器字段上，而计时器的寿命比横幅短——重连成功后该清横幅时，判据早已被抹掉。现在横幅由连接状态机单点托管（离开「重连中」状态即清点），与计时器寿命彻底分离；「正在思考 · 已等待 X 秒」的计时是特性、不受影响。
- **验证**：全量单测 **1333 例通过**。插话：19 例单测（消费点追加写、终态冻结、终态后可删等）+ 34 项真实后端浏览器冒烟（`verify/interjection_smoke.py` + `.cjs`，隔离实例 + 真库播种 + 系统 Edge）；降档重试 6 例与 Kimi K3 方言 3 例守门（`tests/test_llm_runtime_retry.py` / `test_llm_protocols.py`）；冻结版入口自检 `verify/frozen_interjection_check.py` 38 项。

## 2.6.10 Beta 主要能力

- **工具调用前的「过程预告」不再堆进答案里（本次重点修复）**：Agent 跑多步任务时，模型**每轮调用工具前**都会顺口说一句「我已定位根因」「继续验证」「现在补测试」，这些话以前**逐段都留在结果里**——一轮跑完，最后那条回复里堆着十几句进度播报，真正的结论被埋在最后一句；用户看到的就是「说得挺热闹，就是没有结论」。根因在三层：流式层把**工具协议之前**的自然语言当正常正文发出（`_forward_guarded_text`，**这本身是设计如此、不该改**——正文确实出现在协议之前），运行层把这十几段正文**全部**收进「活动时间线」（`metadata.activity` 是前端唯一的展示来源），重建半截回复时又把它们**全部**拼回正文。现在三层统一口径——**一段正文只有「最后一段」才有资格留在结果里**：`_build_activity_timeline` 只保留最后一段正文（其余是预告别名，按 `type == "prose"` 判别，工具与思考条目位置不动）；`_rebuild_partial_run` 以**最后一次工具事件**为界，之前的累积正文一律丢弃——**停在工具步上就是空正文**，不吐半句废话。历史消息同样受益：`activityMarkup` 只渲染最后一段正文，**不必重算旧数据**，老会话翻出来也是干净的。
- **系统提示补了一句「不许播报进度」（本次调整）**：明确要求**调用工具前不要输出过程预告或进度播报**（如「我已定位根因」「继续验证」「现在补测试」），也不要重复或改写同一句进度；只在**遇到真实阻塞、需要用户决策、或已启动长时间后台任务**时才用一句话说明。前端能过滤、提示能收敛，才算把「啰嗦」两头都堵上。
- **Kimi K3 的工具调用不再漏进正文（本次修复）**：K3 及其它 **Harmony 风格兼容端点**会把工具调用序列化成**保留 token 混在普通文本里**返回（`<|open|>tools<|sep|>…`），而不是返回原生的 `function_call` 项。此前这类输出**既没被识别成工具调用、也没被拦住**，结果是把整段协议原样吐在对话里。现在流式边界的协议守卫认这套 token——包括被 SSE 切成两半的 `<|open|>to` + `ols`（靠「可能是协议前缀」的长度回退留住半个 token），**跨包也能拦住**；解析层同时支持**并行多工具调用**与**参数类型还原**（`integer` / `boolean`），**截断的调用明确报「解析失败」而不是猜**。
- **验证**：全量单测 **1305 例通过**。新增隔离真实实例 + 假 K3 模型冒烟 `verify/tool_prefix_prose_smoke.py`（**4 轮工具调用**，跑的是真后端真链路），断言是**双向**的——既要证明**流里确实出现过预告**（前提成立，不是「本来就没话说」），又要证明最终结果里**只剩最后一段正文**、4 条工具活动齐全、预告词不留；并做过**回退反证**（把修复退回旧写法后冒烟立刻变红）。前端渲染另加 **6 项真实执行断言**（`verify/media_markup_check.mjs`：只画最后一段正文、最终答复在位、预告无残留、3 条工具保留、单段正文不受影响、只有工具时无正文）。冻结版编译成功，并由**冻结版入口自检** `verify/frozen_tool_prose_check.py` 确认 exe 内的**时间线重建、半截回复重建、打包前端资源**都是新代码（9 项）。

## 2.6.9 Beta 主要能力

- **换字体不用再手敲字体名（本次重点）**：会话区字体的第四张卡片由「自定义」改为「**指定字体**」，点开是一个下拉列表，内置 **11 种常见中文字体**：微软雅黑、苹方、宋体、楷体、黑体、仿宋、思源黑体、思源宋体、霞鹜文楷、得意黑、鸿蒙黑体。手机端点开直接弹**系统选择器**，不用在小屏上敲英文字体名。字体清单是 JS 里的一份常量（`CHAT_FONT_PICKS`：`key` + 中文标签 + 探测名），**不进 HTML**、也不进后端——后端只认键，白名单 = 3 档结构字体（`system` / `serif` / `rounded`）+ 11 个预置键 + `custom`，共 15 个；非法键在加载路径静默归一到 `system`、在更新路径显式报错并列全所有可选值。
- **没装的字体会自己标出来，但不禁选（新增）**：列表里每一项都用 **canvas 度量现场探测**该字体能否真的参与排版——把候选字体与 `monospace` / `sans-serif` / `serif` **三条基线**分别拼成 `font-family`，量同一段中英混排文字，任一基线下宽度不同即视为已安装。**必须多条基线**：单一基线可能恰好就是探测字体本身，那样「没装」会被误判成「装了」。没装的在标签后加「（未安装）」，但**只标注、不禁选**——设置存在服务端、多设备共享，这台电脑没装不代表手机没装，禁掉会造成「电脑上选不了、手机上明明能用」。探测结果按字体名缓存（一次面板构建 12 项 × 3 条基线，不缓存就每次开设置都重算），异常一律按「已安装」处理：探测是提示增强，不是门禁。**不用 `window.queryLocalFonts()`**——它要权限弹窗，且只在安全上下文可用，而手机经局域网 `http://192.168.x.x` 访问是非安全上下文，直接不可用。
- **手敲入口降级为高级兜底（本次调整）**：列表最后一项是「手动填写…」，选中后才展开原来的输入框，想直接填 CSS `font-family` 串的老办法原样保留。旧配置**零迁移**：此前存过 `family=custom` + 自定义串的用户，打开面板会正确回显到「手动填写…」；之后切到某个预置字体再切回来，手敲过的串也**不会丢**（选预置键时不带该字段，后端原样保留）。两级显隐是分开的：单选按钮管「列表行」显隐，列表的当前取值管「手动输入行」显隐。
- **预览与落库仍是同一套规矩**：字号滑杆和字体列表都只做**本地预览**，统一由「保存外观」落库——拖动、下拉都不会各自发请求（与主题、皮肤保持同一种交互，面板里不出现「有的即时存、有的要点击」）。另外，字体族这个「跨一层间接」的效果**验到了最终渲染**：`fontFamily` 声明在字体缺失时照样原样返回，只断言声明等于空断言，所以冒烟改用 canvas 度量两两对照。
- **验证**：全量单测 **1296 例通过**（新增 `test_chat_font` 17 例源码级护栏 + `test_appearance` 15 例后端契约，其中钉住两条最容易出事的边界：**占位值 `__pick__` 绝不许落库**——它只是单选按钮的前端占位值，一旦漏进请求体、后端白名单会拒，用户只看到「保存失败」；以及**字体清单的键必须与后端白名单逐字对应**，否则「点得动、存不上」）。新增隔离真实实例 + 无头 Edge 冒烟 `verify/chat_font_smoke.py` **39 项断言**（桌面 + 手机竖屏）：拖动过程中零 `POST /api/settings`、清掉 `localStorage` 后刷新仍生效（证明服务端才是真相来源）、点选字体后比对 canvas **渲染度量**确认正文真的换了字体，并且「（未安装）」标注是与**现场重新度量逐项对照**得出的、不是只读界面文案。冻结版编译成功，并由冻结版入口探针确认打包资源与 **11 个预置键的往返**均为新代码（17 项）。

## 2.6.8 Beta 主要能力

- **点「新会话」后圆环归零、输入框立刻可用（本次重点修复）**：2.6.6 Beta 起，本地模型上下文不够时会引导你点本条回复上的「新会话」；但那条线划下去之后，界面上的占用圆环仍停在 100%、输入框仍是禁用态——历史已经划到线以下、可以继续聊了，界面却坚持说「上下文已满」，用户唯一能做的只有刷新页面。根因是圆环取「有效用量」时**只看最后一条带用量的消息**，完全没认这把「分割线」，于是线以上的旧用量继续被当成当前占用。现在圆环**从消息列表末尾向前扫描、扫到最新一条分割线就停**：线以上（含被标记的那条消息本身）的用量一律不算；线以下没有用量就按「暂无数据」处理。判据链是 `updateContextComposerLock`（输入框 `disabled` / placeholder 的唯一写入点）+ `updateSendButtonState`（发送键的唯一写入点）+ 圆环百分比 + **打字之后发送键可用**——「按钮存在」不等于「用户能用」。
- **刚重置完还想接着写的那一句，现在真的发得出去（本次重点修复）**：模型写完交接文档、分割线出现的那一刻，本轮回复其实还在收尾（运行标志尚未落下），而此前「运行中不得用历史值覆盖实时值」的保护会把种子内容重新填进一个**仍然禁用**的输入框——用户看到的就是「字已经填好了、就是按不下去」。现在只对「上下文已重置」这一种场景放行（`contextReset` 越过该守卫），其余历史重渲染照旧不许擦掉实时值，保护本身没有变松。
- **提示说得更明白（本次修复）**：手动划出分割线后，不再只说「下一条消息起……」，而是明确告诉你「上下文占用已清零，可直接继续」——知道可以继续，用户才敢继续。
- **旧数据同样适用**：分割线同时认新形态（`metadata.session_start`）与更早版本留下的 `role=session` 标记行；撤销分割线后同一套扫描自动回锁，无需任何额外分支。
- **验证**：新增隔离真实实例 + 无头 Edge 冒烟 `verify/context_reset_lock.py`（14 项断言 + 3 张留证截图），覆盖真库播种的「满量打开即锁 → 点真实『新会话』→ 圆环归零 + 输入框解锁（真能打字、发送键亮）→ 点『撤销』→ 回锁」全链路，以及「线以下还有更小用量时按线以下算」（10,000 / 100,000 = 10%）。并做过**回退反证**——把 `updateContextUsage` 退回旧写法后冒烟立刻变红（分割线已显示、圆环仍 100%、输入框仍禁用、`page.fill` 超时），确认不是「测试全绿但等于没测」。守门单测补 6 例，全量 **1268 例通过**；冻结版编译成功，并用冻结版入口探针确认 exe 内的前端资源确实是新代码。


## 2.6.7 Beta 主要能力

- **未注册的工作区分组不再被显示成「未分组」（本次重点修复）**：会话的**分组名存在库里**，而**工作区注册表存在 `config.json` 里**——两台实例各存一份注册表时必然分叉。此时下拉框执行 `select.value = 当前分组名` 找不到对应选项，会按 HTML 规范**静默落成空字符串**，界面于是把「属于 naiba-chat 的会话」显示成「未分组」：状态显示错误，用户还会以为会话被搬走了；更麻烦的是**切走之后再也切不回来**——那个分组在下拉里根本不存在。现在会把该分组本身补回下拉、选项标注「（未注册）」，并把「当前值是什么、怎么恢复」写进下拉的悬停提示（在侧栏「新建工作区」登记同名工作区即可）。补回来的分组**只用于显示、绝不回写 `config.json`**——注册表归谁由用户决定，程序不擅自把孤儿分组扶正；配置修好后重开实例，提示自然消失。
- **Agent 是什么，现在点一下就有答案（新增）**：顶栏 Agent 选择器左侧新增圆形「?」按钮，点开是说明弹层——**Agent = 一个「岗位角色」**（人设 + 工具箱 + 技能包；没勾选的工具一律用不了，工具箱在会话开始时锁定、中途不能换），**三种预设 Agent 怎么选**（通用 / 编程 / 短剧各自适合什么、动手前会不会先说明），以及**想用 ComfyUI 出图的两条路线**：路线一 **HTTP 直连**（启动本机 ComfyUI → 设置 → Agent 编辑 Agent → 工具页一键套用「ComfyUI 联动」预设，含 `comfyui_prepare_workflow` / `comfyui_batch` 与 `job_output` / `job_status` / `job_wait`，**不需要配置 MCP**）；路线二 **MCP 连接**（设置 → MCP 配置并连接 ComfyUI MCP 服务，连上后 MCP 引入的 `comfy_*` 工具会出现在 Agent 工具页、手动勾选启用）。两条路任选其一即可，一般不需要同时开。弹层与「上下文用量」同一套开合规则（`fixed` + 打开时挂到 `body`，避免顶栏的 `overflow` 裁剪；超出视口会翻到按钮上方；`resize` / `scroll` 重新定位，点外部关闭），按钮带 `aria-expanded` 状态。
- **工作区重名分不清 → 悬停看目录（新增）**：工作区允许重名（分组按名字归并），光看下拉里的名字分不清谁是谁。现在侧栏分组标题、下拉里的每个选项都带**目录路径**的悬停提示，下拉自身也会显示当前选中项的目录。
- **手机端长按粘贴（本次修复）**：手机经局域网 `http://192.168.x.x:8765` 访问属**非安全上下文**——网页里既没有 `navigator.clipboard` 对象，`document.execCommand('paste')` 也被所有现代浏览器一律禁用，因此**系统长按菜单（复制 / 粘贴 / 全选）是手机上唯一可行的粘贴路径**。而此前 `contextmenu` 对可编辑元素是**无条件 `preventDefault()`**，等于把这条唯一通路一起关掉，用户实测到的就是「粘贴失败：浏览器未授权」。现在按**指针类型分流**：触摸 / 手写笔（由捕获阶段的 `pointerdown` 记录 `pointerType`，因为 `contextmenu` 事件本身不带这个信息）长按**直接放行给系统菜单**，鼠标右键仍是原来的自定义菜单；剪贴板两条程序化通道都不可用时的提示也换成**带动作**的一句话——「浏览器不允许网页读剪贴板：请按 Ctrl+V，手机可长按输入框用系统菜单」，旧文案照着读没有任何下一步可走。
- **顶栏「收起」以前等于没收（本次修复）**：手机端点收起后，用户看到的仍是「一行 API + 一行 Agent + 一条箭头」——因为旧实现只隐藏了操作区、又把两个下拉收拢到第一行，被顶栏吃掉的高度基本没还回来。现在收起态的判据是**「还剩什么」**：`.topbar.compact > *:not(.topbar-collapse-toggle){display:none}`，整条顶栏只剩折叠条一个按钮；并且这个**唯一入口**加了底色、边框与「展开顶栏」文字（此前只有一个 18px 小箭头，用户认不出这是唯一的展开入口）。能力只是被临时收起、**没有被删除**，点一下全部恢复，选择状态照旧记忆在 `localStorage`。
- **验证**：新增隔离真实实例 + 无头 Edge 冒烟 `verify/agent_help_popover.py`（23 项断言：**配置驱动的快捷卡片必须渲染齐**、弹层开合与点外部关闭、长按走系统菜单而非 `preventDefault`、收起态只剩折叠条、零页面错误/坏响应）；全量单测 **1262 例通过**。顺带修好一处**验证资产名册漂移**：清理脚本的保留集合此前只认手写名单，实测会误删 7 个受版本控制的脚本（含被其它冒烟依赖的公共 harness `_serve_tmp.py`），现在改为「手写名单 ∪ 版本控制清单」，并新增守门单测 `tests/test_verify_assets.py` 把两份名册锁在一致状态。

## 2.6.6 Beta 主要能力

- **本地模型上下文不够时，报错说人话（本次修复）**：本地模型的上下文窗口是**总窗口**——prefill 的 prompt 与生成共用 `n_ctx`，所以历史一超出窗口，后端就直接拒绝。llama.cpp / Unsloth 用 400 `exceed_context_size_error` 拒掉请求，而这条错误此前是**原文透传**给用户看的（`请求失败：HTTP 400: {...}`）；Ollama 的流式响应里本来也带 `error` 字段，但它**从未被读取**——后端说的话被整块丢弃，用户只看到一句「Ollama 流式响应中没有文本内容」。现在三种流形态（Ollama NDJSON、SSE 内嵌 error、LM Studio error 事件）与 HTTP 4xx 走同一个识别层，统一给出一句话：**后端名 + 当前真实窗口 + 两条动作**——点本条回复上的「新会话」继续，或到 设置 → 模型 里把真实「上下文窗口」填对。在线模型（DeepSeek / OpenAI 等）走同一条链路，同样受益。
- **窗口取后端自报的真值，不取本机配置**：llama.cpp / LM Studio 在错误体里回 `n_ctx`，DeepSeek / OpenAI 回 `maximum context length is N`，这比本机配置或探测值可信得多——最常见的坑就是「设置里填着 32768、后端其实只开了 4096」，用户对着真值才知道该往设置里填什么。取不到真值才回退到配置值。**判定只作用于服务端返回的错误体，从不扫用户正文**，不存在把正常对话误判成溢出的可能。
- **溢出判定放在重试兼容链之前**：在线链路有一串「剥离 `stream_options` / `tools` / `reasoning` 字段后重试」的兼容逻辑，4xx 溢出的语义很容易被它吃掉——白打一次注定失败的请求，还把错误信息冲淡一遍；现在溢出判定是 HTTPError 分支的第一件事，命中即直接报因。
- **工具越堆越多时提前收手（新增）**：Agent 循环里每次工具调用的结果都会累积进上下文，而窗口检查此前只在**整轮开始前**做一次，循环中途照样能把它撑爆。现在每一步模型调用前都做增量复查（**纯读**，不改变发给模型的字节序列，前缀缓存契约不受影响），越预算就**优雅收尾**：已经生成的正文照常落盘返回、末尾附上「新会话」指引，而不是等后端甩回一个 400 把整轮成果一起打掉。
- **截断原因说清是哪种（本次修复）**：`finish_reason=length` 此前一律按「撞输出上限」处理并自动续写一次。但本地模型生成到 `n_ctx` 时，后端报的**同样是** `length`——「撞输出上限」（续写或调大 `max_tokens` 有用）与「窗口耗尽」（续写只会让下一次请求更大）在协议层长得一模一样，界面上给的却是同一条建议。现在结合用量区分：**本地模型 + prompt 与生成加起来已贴到窗口 95% 以上 → 「上下文窗口已耗尽，建议新会话后继续」**，否则仍是「已达模型输出上限」。老数据没有成因标记的照旧显示，不会误判。
- **验证**：新增守门单测 27 例（特征串识别、三处流内错误捕获、循环内越预算停发请求、length 成因分类、老数据兼容），**全量 1256 例通过**；新增隔离实例 + 无头 Edge 冒烟 20 项断言——其中挂了一个**真的会返回 400 + `n_ctx:4096` 的假本地后端**，断言界面上出现的是「4096 tokens」而不是设置里的 32768、且全文不含原始 JSON；冻结版实跑自检 14 项，证明新识别层与新文案真的进了 exe。顺带修好一处静默失效的验证脚本（桩对象缺依赖导致整条检查空转）。

## 2.6.5 Beta 主要能力

- **消息全文搜索（新增）**：侧栏搜索框升级为「标题 / 全文」双模式——标题模式仍是原来的即时本地过滤，全文模式下输入即全库检索**消息正文**（300ms 防抖），结果显示为「会话标题 + 命中片段（命中词高亮）+ 第 N 条」。点一下直接跳到那条消息并高亮定位两秒：跨会话自动切过去、历史窗口按需加载，长会话里也能一步到位。检索**不复用给模型用的那套「按会话分桶」口径**（那是给模型自述上下文用的，语义不同，硬合并会把模型侧口径泄漏到界面）；结果超过单次上限时明确标注「已截断」，绝不静默丢结果；空查询不会白扫全库。
- **分支导航（新增，数据库 v17→v18）**：此前「分支」只是新开一个会话，事后完全查不到它从哪来、有没有兄弟。现在建立分支时会登记**来源会话与分支点消息**，侧栏条目左侧出现 ⑂ 徽标（悬停显示源会话标题）、右侧显示「⑂N」表示该会话有多少个下级分支，点开**分支链面板**即可看到源会话与所有兄弟分支（按创建时间排序），当前所在位置有标记、一键切换。侧栏**仍保持平铺**——树形与「按最近使用排序」和虚拟列表根本冲突，多级分支还会打乱行索引。旧数据**不回溯推断**（宁可显示无关联，也不按标题猜错），源会话被删掉时徽标降级提示「源会话已删除」而不是报错。
- **删除单条消息 + 撤销（新增）**：用户消息操作区变为「编辑 · 分支 · **删除**」，AI 回复为「复制 · 重新生成 · **删除** · 新会话」。删除按角色给出粒度——**「仅删这一条」**对提问和回复都可用；提问另有**「删除这一轮」**，连同它后面的回复一起删、遇到下一条提问即停。确认框会写清后果（后面还有几条消息、是否带图片、是否会带走上下文起点）。删完底部出现 **10 秒撤销条**，点「撤销」把内容**逐字节原样插回**（含时间、附件、活动轨迹），连前缀缓存命中都能恢复——判据是「删了又撤销，发给模型的历史与删除前逐字节一致」。遗留的「会话起点」标记行不允许被单独删除（那会让上下文起点悄悄前移）。
- **验证**：新增守门单测 77 例（搜索 17 / 分支链 23 / 删除撤销 37）；隔离实例 + 无头 Edge 冒烟 39 项断言（含 6 张截图），覆盖「搜索命中→跳转高亮」「分支面板切换」「删除确认→撤销复原」全链路；全量单测 1229 例通过；冻结版编译成功并通过运行期自检 24 项——证明新接口与新界面钩子真的进了 exe。冒烟还抓到一个单测照不出的真 bug：删除确认框此前把两个按钮写成同一个条件，导致 **AI 回复一条都删不掉**（弹窗只剩「取消」），已修复并把「按钮**可见性**」钉进守门测试。

## 2.6.4 Beta 主要能力

- **点任务面板不再把正在写的回复弄丢（本次紧急修复）**：后台任务跑着时点任务面板里任一行，界面会跳回那条会话并整体重渲染消息区，而那一刻正在流式输出的助手气泡——正文在回复结束前只存在于界面里，落库发生在结束之后——会被整块拆掉且没有人挂回来；流仍在写，只是写进了一个已经不在页面上的节点。于是表现为「AI 回复消失了，只剩下前一条提问」，此后一直不回来，重启、刷新都没用。现在重渲染会把这条气泡原样搬回消息区末尾（判据：确有活动流 + 气泡还在消息区 + 属于当前会话 + 库里还没有这一轮的助手消息），流继续往上写。
- **重启不再吃掉半截回复（本次修复）**：服务重启会把仍在跑的对话轮次判为「已中断」，而模型此前已经吐出的正文只留在运行事件流里，界面上无从看到。现在启动时会把这段内容重建为一条带「未完成」标记的消息：正文、思考、工具活动时间线、已发出的 Skill 列表全部保留，并写明中断原因（「本次回答未完成：服务重启，运行已中断」），一眼看出跑到哪一步；一个字都没产出的轮次不会留下空壳消息。
- **更新后自动重启，窗口会自己出现（本次修复）**：更新脚本此前以隐藏窗口的方式启动新进程，可本程序本来就是窗口化打包（没有控制台窗口要藏），于是主窗口一起被藏进托盘——进程活着、托盘图标在，界面却不出来，只能到托盘双击「打开窗口」唤回。现在以普通窗口启动，另加一道「窗口就绪即显示并置前」的兜底（所以你下一次更新也能享受到）。
- **更新检查改为手动触发（本次调整）**：此前启动 4 秒后会自动查一次更新元数据（只查、绝不自动安装），于是你没点「检查更新」就能看到「发现新版本」，容易被当成 bug；现在只在设置页点「检查更新」时才发起，版本说明与一键更新入口照旧。
- **验证**：新增守门单测 13 例；新增一条真链路冒烟（真 `POST /api/chat` + 真运行 + 本地假 SSE 模型 10 段 × 400ms + 拦 `/api/tasks` 注入面板行，11 项断言：点任务行确实触发了重渲染、同一条气泡仍在消息区并继续接收后续增量、跑完后端只有一条助手回复且正文首尾俱全、零页面错误），并做过反证——把复挂那行停掉后 3 项立刻变红；全量单测 1149 例无新增失败；冻结版编译成功并通过运行期自检。

## 2.6.3 Beta 主要能力

- **编辑不再被后台刷新静默取消（本次修复）**：编辑消息时恰好撞上一次会话同步——轮询（后台任务写消息、流式落库都会引爆）、重新打开当前会话、清空会话都算——此前消息列表重渲染会无条件退出编辑态：编辑框消失、改到一半的内容掉回底部输入框、原附件丢失、发送键从「重新发送」变回「发送」（再按回车就变成发新消息），全程零提示。现在同一会话刷新且被编辑的消息还在 → 输入区先归位、编辑现场（文字、附件、光标位置）原样暂存，重渲染后自动挂回新生成的气泡，用户无感知。
- **强制退出也不丢草稿（本次修复）**：只有在被编辑的消息真的没了（被截断 / 删除）或切换会话时才退出编辑，此时把「进入编辑前的底部草稿」（文字 + 附件）逐字还原回输入框，并明确提示「编辑已取消，草稿已还原到底部输入框」；当时没有草稿则只提示「编辑已取消」。
- **两条路径同一份逻辑**：首次进入编辑与重渲染后复挂共用同一个挂载函数，避免复挂漏一步出现「输入框在、附件没了」的半残状态；归位必须发生在重渲染之前（重渲染会连坐销毁搬进气泡的输入区）。
- **验证**：守门单测 26 例（含「重渲染不得无条件退编辑态」「两种进入方式必须共用一个挂载函数」两条源码守门）；隔离实例 + 无头 Edge 冒烟扩到 51 项断言，新增两条用例用拦截会话详情接口的方式造出**真实的一次同会话同步**（追加消息 → 断言编辑框仍在原气泡、改过的字保留、按钮仍是「重新发送」；消息消失 → 断言草稿逐字还原 + 提示出现），并做过反证——把重渲染退回旧行为，用例立刻复现「输入区回到最底部、按钮变回『发送』」；全量单测 1139 例无新增失败。

## 2.6.2 Beta 主要能力

- **流式滚动不再抖动（本次修复）**：消息列表默认瞬时定位——流式期间每次增量贴底不再动画化，页面不再长期处于「追赶最新内容」的状态；上滑阅读历史时的高度变化也不会被动画放大成抖动。程序化贴底内部走 `withInstantScroll`（临时把 `scroll-behavior` 置 `auto` 再写、写完还原），即使以后 CSS 误挂平滑也不会动画化、不会连发 scroll 事件干扰懒加载判定。
- **用户主动跳转仍是平滑的**：唯一保留平滑滚动的入口是刻度轨点击（调用点显式传 `behavior:'smooth'`），跳转体验不降级。
- **长回复不再掉帧（本次修复）**：流式 markdown 渲染改为自适应节流——短回复保持 40ms 跟手，累积每 4000 字加 50ms、240ms 封顶，不再对越写越长的整段文本高频重跑 markdown + 整体替换。
- **既有契约不变**：懒加载窗口以「轮」为边界、预渲染补高度不跳视口、刻度轨跳转先渲染再定位、分割线同进同出等行为全部保持（守门单测 + 浏览器冒烟全过）。

## 2.6.1 Beta 主要能力

- **聊天背景图不再「自己消失」（本次修复）**：启动探针失败（图片文件被缓存清理删掉 / 数据目录迁移后路径失效 / 启动瞬间抖动）不再清空 `chat_background`——只标记「文件暂不可用」，背景设置、取景与强度全部原样保留，设置卡给出「点「更换图片」重新选择」的明确出路；**唯一**会把背景清空的入口只剩你主动点「清除背景」。
- **背景图文件暂缺不再牵连其它设置（本次修复）**：保存改为只提交本次改动（改强度、调取景不会再把失效的图片路径一起送审），文件缺失时滑杆与编辑器照常可用；换图成功后自动重探一次可用性，文件补回后背景自动重新出现（不必重启）。
- **迁移与找回兜底（本次修复）**：换了数据目录或换了机器后，旧路径按文件名在当前受管目录（含 `uploads/YYYY-MM-DD/` 分日目录）里自动找回并接上；确实找不到也保留原路径等你重选——丢文件可以，丢设置不行。缓存自动清理把当前背景图算作「在用」，主图与其缩略图成组保留。
- **守门与验证**：背景图相关单测 30 例（含「探针失败不得清空设置」的前端源码守门）；隔离数据目录 + 真实浏览器的两阶段冒烟覆盖「文件缺失 → 设置保留并提示 → 补回文件自动恢复」。
- **工具执行实时可见（本次重点）**：`pwsh` 与 `run_skill_script` 不再「跑完才一次性给结果」——现在逐行推送子进程的 stdout / stderr，工具卡片里就是一块等宽滚动输出区（最多保留最近 200 行；**只有你贴着底部时才自动跟随**，往上翻看历史不会被新行拽回去），摘要行显示「Running · 工具名 · 已运行 Ns」并每秒刷新，命令结束时换成「Completed / Failed · 工具名 · Ns」；命令参数与最终结果照旧折叠在同一张卡片里，展开即见。
- **停止与超时即时生效**：这两类命令的执行改为「读者线程 + 队列」轮询（200ms 粒度），不再阻塞在管道读上——点「停止」立刻终止子进程（此前要等命令自己跑完才有反应），超时按上限强制终止。两者都返回明确的 `exit_code=-1` + 原因说明，不再抛异常、也不再因为「没有退出码前缀」被当成成功（非零退出码仍是失败，语义不变）。Python 脚本补 `-u` 与 `PYTHONUNBUFFERED=1`，管道下游不再块缓冲，输出不会攒满 4KB 才出现。
- **模型输出截断守护（本次重点）**：全链路开始记录模型响应的**终止原因**（`finish_reason`，各供应商字段名归一；供应商没给也如实留档）。当最终答复撞上输出上限、或流在没有终止原因的情况下停在句中断点时——实测事故里那条正文正好停在「：」、前文还有一整段重复，而全链路没有任何终止原因留痕、只能靠反向推断——现在会自动**从断点续写一次**（不重开头、不加前言），并在消息末尾明确标注「本轮回复可能被截断：已达模型输出上限 · 已自动续写一次」；供应商明确回报正常收尾时不触发续写。
- **后台任务的日志真的能看**：任务面板展开「详情」时，按游标增量拉取该任务的**逐行日志**——`job_log` 事件此前一直在写进运行事件流，但前端从未消费，「转到后台以后还能实时看日志」在本次之前是空头承诺。日志区等宽、限高、随列表轮询自动续拉且不重复，面板重渲染后展开态与内容一并还原。
- **任务面板行标题与文案**：任务行优先显示**落库的真实任务名**（如「子任务：整理素材并提交出图」，AI 显式给的名字优先；兜底才回退到类型名），子 Agent 的类型标签统一为「AI 子任务」，状态与错误文案去掉重复前缀。
- **稳定性与守门**：新增工具实时输出（含走真实装配链的集成用例）与终止原因 / 截断判定两组守门测试；工具进度事件按时间片节流并限量（单次调用最多 300 条、单行最长 400 字符），长命令不会把运行事件库撑爆。

## 2.5.0 Beta 主要能力

- **内置背景图（新增）**：内置 6 张程序化生成的背景（紫雾晨曦 / 海雾 / 玫瑰石英 / 林间薄雾 / 石墨夜色 / 素纸），设置 → 外观 → 聊天背景里点一下即用；后端幂等生成到 `data/backgrounds/`（随数据目录迁移、不占仓库体积、不参与缓存清理，也不会把用户换掉的那张覆盖回来）。
- **白板式取景编辑器（新增，本次重点）**：设置 → 外观 → 聊天背景 → 「调整背景图」打开独立弹层——概览里给出**整张图 + 自由取景框**（框外压暗）：拖框选区域、拖**八个手柄**自由改形状与大小（角改两向、边改单向——竖向、横向都能切，不再有"某个方向切不动"），滚轮 / 滑杆等比缩放（下限 = 整张图、上限 400%），方向键 1px 微调（Shift 10px）；「填满 / 完整显示 / 复位」一键适配。取景框永远是图片内的一个自由矩形，**框里那块会被完整等比铺进对话区**（框比例与对话区不一致时两侧留白，用同一张图的模糊放大版补满；不拉伸、也不二次裁切）。右侧「效果预览」与真实对话区共用同一套取景渲染规则（同一组 CSS 变量、同一段百分比尺寸算法），所见即所得；取景（裁剪区域）随设置保存、多端一致；未选图片时入口置灰。
- **竖图不再尴尬（修复本次报障）**：`cover` 的固有语义是「铺满 + 裁掉溢出」，竖图此前要么被裁成一条、要么缩成中间一条纸带。现在概览里能看见**整张图**并按需裁剪；「完整显示」时没铺满的两侧改用**同一张图的模糊放大版**补满（电视 / 短视频的做法），不再是两条空白。
- **旋转 90°（新增）**：在服务端把图真的顺时针转 90° 另存为新文件（无损 PNG，原图不动），转完**取景跟着一起转**（构图不丢）；在编辑器里旋转后按「取消」会回滚设置并回收派生文件，等于没发生。
- **背景图链路加固**：背景图路径只允许来自 `data/uploads` 或 `data/backgrounds`（防目录穿越），并按图片**内容**过格式白名单（PNG / JPEG / WebP / GIF / BMP / AVIF；TIFF / HEIC 改名也拦得住）；背景图被计入「在用」，缓存自动清理不会把它删掉。

## 2.4.1 Beta 主要能力

- **任务面板只列真正的后台作业（本次重点）**：`background_tasks` 同时装着「每次回答的记录」与「后台作业」——`chat` / `plan_execute` 那些行是用户每发一句话就插入一次的运行记录，`comfyui` / `shell` / `http_poll` / `subagent` 才是真正的任务，`parent_job_id` 只是「我由哪次回答派生」的归属锚点。旧面板平铺两者且不显示归属，用户看到的是「我发过的每一句话都是一个任务」。现在面板只请求后台作业（`/api/tasks?jobs_only=1`，**过滤放在 SQL 层**——默认只返回 50 条且回答记录占绝大多数，先捞再筛会把较早的作业挤出窗口；上限一并提到 200），**按 `parent_job_id` 分组**、组标题取「类型 + 时间」（刻意不引用用户消息原文——那可能含不该复述的内容），组头给出「共 N · 失败 K」。
- **任务计数与新鲜度不再撒谎**：顶栏徽标此前只统计活动状态，全部终态时显示 `0`，于是「库里有 13 条记录」与「0 任务」同时成立；现在没有活动任务时回落为总数并弱化显示。面板顶部常驻统计条（共 N · 运行中 M · 失败 K · 已完成 J），**轮询失败时整条转告警色并显示最后成功更新时间与原因**，不再把上一次的旧状态当成最新状态呈现；任务完成提示改用任务类型（如「ComfyUI 生成已完成」）而不是内部 Agent 名。
- **「停止」真的会停（本次重点）**：① 取消现在同时写入「取消已下发」的**持久标记**——此前 `cancel` 只把状态写成 `stopping`，而状态冻结条件读的是另一个从没人写的标记，那段防护一直在空转，worker 下一次更新就把 `stopping` 打回 `running`（观感是「点了停止它还在跑」）。② `stopping` 属于活动状态，worker 若卡在网络等待或退避里不退出就永久占着活动名额，新增 **90 秒看门狗**强制置为已取消并摘掉线程句柄；该看门狗**只对登记过取消事件的后台作业生效**，被误传到 Job 接口的顶层对话 Run 不会被强行标成已取消。③ 级联取消不再篡改历史——后台任务的常态是「回答早已发出、只剩子任务在跑」，此时父行只是归属锚点，点「停止」不再把已完成的父行改写成「已取消」。
- **输入区「模型」改为可搜索下拉（本次重点）**：中转站未分组时 `/v1/models` 能返回 400+ 个模型，原生 `select` 只能靠滚动定位。现在原生 `select` 降级为**隐藏的值容器**（仍是唯一值来源，既有落库 / 重拉链路完全复用），可视化层是「触发按钮 + 可搜索浮层」：输入即过滤（对模型 id 与显示名做大小写不敏感子串匹配，头部显示「过滤后 / 总数」）、`↑`/`↓` 环绕移动 + `Enter` 确认、`Esc` 关闭并把焦点还给触发按钮、点击浮层外部收起、**重开时定位到当前已选模型并滚入视野**、底部「↻ 重新检测模型」仍是动作不落库。浮层 `position: fixed` 且打开时挂到 `document.body`（不被 `.composer-wrap` 裁剪，也躲开「编辑消息时整个输入区被搬进消息气泡」），**朝上展开时锚底边**——只算 `top` 的话，过滤把候选从 400+ 缩到几条后面板会原地收缩、在按钮上方空出一大截（即用户报的「弹层到处飘」）；高度顶住可用空间后由候选列自己滚动，窄屏不产生横向滚动。
- **暗色可读性**：消息里的行内代码在暗色下沿用浅色配方（浅底 + 浅字），实测对比度约 1.2、等同不可读；改为深底 + 显式文字色。任务状态补「停止中」中文标签。

## 2.4.0 Beta 主要能力

- **视频抽帧工具（本次重点）**：Agent 工具集新增视频抽帧两件套（在「读取与检索」组里手动勾选；亦已并入「只读 / 标准 / 长会话 / ComfyUI 联动」四档预设与新建 Agent 的默认勾选，只读模式只收纯读取的 `probe_video`）。`probe_video` 读元信息（时长 / 帧率 / 分辨率 / 总帧数 / 视频编码 / 是否含音轨）；`extract_frames` 三种模式——`times` 按指定时间点、`interval` 按固定间隔、`keyframes` 按画面切换择优（`threshold` 灵敏度 0-1 可调），单次最多 8 帧（上限 12），可指定起止时间与单帧长边，并额外生成一张拼图**联系表**（先看概览、再抽单帧细看）；抽出的帧与联系表自动进入消息媒体区（带缩略图，会话内可直接点开放大），配合 `vision_analyze` 即可读懂画面内容。视频抽帧结果按「视频路径 + 文件大小 + 修改时间」缓存并限量（同一参数目录 ≤200 张、同一视频跨参数目录合计 ≤600 张），重复调用不重算。
- **本地模型不再「永久卡住」（本次重点）**：「设置 → 运行设置」新增两项——**本地首字节超时（秒）**（默认 120，填 0 = 关闭；本地后端在 prefill 期间可能长时间不吐任何字节，与「永远做不完」在界面上无法区分，连续该时长没有输出即断开并报错，不再干等 30 分钟总超时）与 **Agent 最大步数**（默认 200，填 0 = 不限制；单轮回复允许的模型调用次数上限，达到即停止并说明原因）；**本地上下文窗口自动探测并写回配置**——探测值单独存 `context_window_probed`、绝不覆盖用户手填的 `context_window`，取值优先级「手填 > 探测值 > 兜底」，本地兜底 32k（在线仍 256k，不再对本地模型不设上限）；**统一「谁算在运行」的状态词表**（活动 / 终态两份常量，`stopping` 一并收口）——启动清理、活动列表与取消判定共用同一份定义，重启不再遗留「已取消但线程还没退出去」的僵尸任务（它会让整条会话永久显示「回复进行中」、救援入口全部失效）；**「停止」改为关闭连接来打断卡住的读**（卡在读上的请求点一下就能真停）；**前端点「停止」不再以服务端确认为前提**——轮询超时也照常清空运行态、复位输入框与救援入口，后端其实仍在运行时再次发送会明确提示而不是永久冻结。
- **出站网络重试与设置页下拉修复**：连接器「选择当前实例」与「丢弃旧实例、重建后重试」原本是两张各自独立的 `if/elif` 判定表，「代理已开启但未填地址 + 关系统回退 → 降级直连」这一档在重建侧没有对应分支——落到该档时不丢弃任何连接器、把刚失败的实例原样返回，所谓「连接异常时按当前策略重建后重试一次」实际空转；现在两张表对齐、统一从一个出口重建，并杜绝「缓存一空就静默退回空 ProxyHandler 直连」（以为在走代理比直接报错更危险）。设置页「目标版本」下拉改挂到设置对话框内的 `popover="manual"` 宿主（进入浏览器 top layer，宿主自身 `display: contents`、不产生可拦截点击的画布），同时解决祖先 `overflow` 裁剪与模态 `<dialog>` 层级覆盖——下拉不再被面板切掉一半，也不再被整体盖住。
- **更新检查与出站网络修复（2.3.9 重点）**：① 版本列表**归一化**——「目标版本」只列可自动更新的版本（过滤掉 2.0.0 及更早的大版本迁移版本），按版本号从新到旧排序、并按版本号去重，同一版本重复发布不再出现多条重复项；② 修复「同一版本重新发布后装不上、仍显示已是最新」——是否「当前版本」的判定改为「版本相同**且提交相同**」，同一版本号重发（资产/提交变了）不再被当成已安装，可正常选择并安装；③ **2.0.0 大版本迁移明确不支持自动更新**——清单指向它时更新面板直接提示「2.0.0 属于大版本更新，请前往 GitHub Release 手动下载」并给出链接，安装入口对不可自动更新的版本也会明确拒绝并说明原因；④ 「目标版本」下拉由原生 `select` 改为**自绘下拉框**（原生弹层在桌面内嵌窗口里不可控）——`↑`/`↓` 切换、`Enter`/空格 开合、`Esc` 与点击外部关闭，选中项自动滚入视野，选项带「（当前）」与发布日期；⑤ 手动代理只接受 HTTP/HTTPS（填错时明确提示并回退直连），任何代理设置变更都会让**手动 / 系统 / 直连三种连接器全部失效重建**（含系统代理捕获的旧环境状态），不再出现「改了代理却仍走旧通道」；⑥ 幂等请求（`GET`/`HEAD`/`OPTIONS`）遇连接异常时**按当前策略重建连接后重试一次**——更新检查与下载、联网搜索、模型列表在代理/VPN 抖动时更稳；已拿到 HTTP 状态码的错误不重试，也不会因一次失败就悄悄切换代理模式。
- **修复待发附件被自动清理误删（2.3.8 重点）**：根因是自动清理把「`uploads` + `generated` 的总大小（含被引用、删不动的文件）」当基准，而引用组永不删除 ⇒「降到上限」永不可达 ⇒ 循环退化成「删光所有未引用文件」，把刚上传、还在输入框待发（尚未落库、引用保护对它无效）的附件当场删掉——表现为**附件区缩略图破图、点击图片无法放大、模型 `vision_analyze` 报「未找到图片文件」**（本机实测总缓存约 560MB、阈值 256MB，每次上传都会触发一次全量清扫）。修复三件事：① 自动清理新增 **15 分钟保护窗口**（窗口内落盘或改动过的缓存组一律不删，同时覆盖「工具刚产出、`metadata` 还没写回」的临时态）+ 上传入口**显式保护本次刚落盘的主图与缩略图**，清理结果如实报告跳过数（`skipped_recent` / `skipped_protected`）；② 待发附件缩略图加载失败改为**两级兜底**（缩略图不在但主图还在 → 换主图，与消息气泡同策略；主图也没了 → 降级为文件占位图标并摘掉失效的大图链接），点开大图失败时明确提示「图片文件已不存在」而不是静默关闭；③ **发送前附件校验**：清空草稿前先核对附件是否还在（只判定宿主缓存树 `uploads` / `generated` 内的文件，URL 与工作区等外部路径不参与判定），命中丢失项就剔除该项、保留文字草稿并提示重新上传；服务端 `submit_chat` 用同一判定兜底（缺失直接报「附件文件已丢失（可能已被缓存清理）：xxx。请重新上传后再发送。」），不再把幽灵路径喂给模型。设置页「清理旧缓存文件」（手动清理）语义不变：仍是显式操作、不感知引用。
- **外观主题与皮肤 + 设置导航搜索（2.3.7 重点）**：设置页置顶新增「外观」区块——**主题模式**可选「跟随系统 / 日间 / 夜间」（跟随系统随 Windows 深浅色设置实时切换，无需重启），**界面皮肤**可选「紫藤（默认）/ 海洋 / 玫瑰 / 森林」（只换品牌色相——按钮、选中态、消息气泡、链接等，版式与信息层级不变）；偏好保存到服务端（`config.json` 的 `appearance`），同一实例的电脑与手机端保持一致，浏览器本地另存一份用于首屏无闪烁；面板内可「保存」「恢复默认」，未保存时提示「有未保存的外观更改」，非法或历史配置值自动归一化（`DARK` → `dark`、未知皮肤 → 紫藤）。设置左侧导航同时改为按「常规 / 模型与 Agent / 工具与联网 / 数据与隐私 / 关于」分组，并新增关键词搜索（输入「深色」「代理」「备份」等词只留相关分类，无匹配时提示「没有匹配的设置」）。
- **「编辑」= 完整输入区就地编辑（2.3.6 重点）**：点击「编辑」后，完整的 `.composer-wrap`（输入框、`@`/`/` 引用、图片与文件上传、附件列表、思考设置和 meta 行）从底部移动到该用户气泡内，底部仅留隐藏占位；气泡展开为全宽编辑态，编辑头提供取消按钮。编辑时照常粘贴 / 拖拽上传、增删附件、用 `/` 与 `@` 引用，**Enter 重新发送、Shift+Enter 换行、Esc 取消**；原消息附件随列表带出，可逐项删除，也可继续添加；取消时恢复原草稿与附件，确认时按当前列表重发。**并修掉编辑态里 `/`、`@` 弹层与「正在编辑此消息 / 取消」互压的层级问题**——弹层稳定盖在编辑头部之上，弹层关掉后「取消」恢复可点。底部输入区不再与会话内编辑框同时显示。
- **AI 回复「重新生成」（2.3.5 重点）**：每条完整 AI 回复末尾、「复制」右侧新增「**重新生成**」（操作区顺序：复制 · 重新生成 · 新会话）——点一下就用这条回复对应的**那条提问**原样重新作答，不必再手动复制粘贴重问。**截断点是提问本身、而不是回复**：重发时先删掉那一问及其之后的全部消息，之前的问答**逐字节保持原样**，因此**前缀缓存继续命中**；也正因为截断点落在提问上，反复点「重新生成」不会像「从回复处截断」那样越点越多重复提问。只有**非最后一轮**才弹确认框（并写明「之后还有 N 条会一并删除」），**最后一轮一键直发**、不打断；截断不可逆这点已在确认框里讲明。会话运行中该按钮自动隐藏（与「分支」「新会话」同口径）。
- **用户消息「编辑」回归（2.3.5 重点）**：每条用户消息下方恢复「**编辑**」（操作区顺序：编辑 · 分支）——在该条消息的位置**就地展开编辑框**（不跳到底部输入框），改字后按同一套重发链路重开。原文回填取用户原样文本（含 `/ref` 技能引用与 `@` 工作区引用），附件也在编辑框里带出。
- **编辑 / 重发的引用与附件保真**：`/ref` 技能引用与 `@` 工作区引用**逐字保留**——`@` 引用仍由后端在发送时按该会话工作区替换为绝对路径，重发结果与首次发送一致；编辑框带出该轮原有的图片 / 文件（与「分支」同口径，含缩略图字段），重发原样随行；**纯附件轮次**（只发文件、不写字）也能重发，不再被「内容不能为空」误拒。


- **工作区判定与执行同源**：权限判定与实际读写统一以**当前会话的工作区**（Run 快照 `workspace_dir`）为准——此前判定用当前工作区、实际读写却可能按启动时的工作区解析，切换过工作区的会话会出现「工作区内的文件被判越界、反复弹确认」；现在 `read_file` / `list_directory` / `search_files` / `write_file` / `edit_file` / `read_pdf` 的判定与执行**共用同一个根**。越界确认卡片直接写明「当前工作区：…」，一眼看出是拿哪个目录在比；另新增可选诊断开关（`NAIBA_DEBUG_PERMISSION=1`）输出执行器身份、请求路径（原始与解析后）、当前工作区、允许根与命中结果，用于定位「为什么被判越界」。**语义与安全边界不变**：自动＝工作区内免确认、工作区外（读也算）仍必须确认；工作区内指向外部的链接（junction / 符号链接）一律按真实目标判定、仍算越界（新增路径矩阵护栏测试）。
- **修复：点「允许执行」后运行上下文丢失**：待确认的工具调用现在连同完整运行上下文（工作区、取消信号、任务归属等）一起暂存，批准后按原上下文执行——此前批准后上下文为空，依赖它的能力（产物目录、后台任务归属、上下文重置等）会行为异常；同步确认与「已确认、后台执行」两条路径都已修正。
- **修复：子 Agent 改用会话工作区**：子 Agent 此前按启动期默认工作区判定与执行，会话工作区内的路径会被判越界，解压/安装 Skill 还会直接报「无法确定工作区目录」；现在子 Agent 继承该会话的工作区与审批档位（权限不超出父级），工作目录与产物位置与父会话一致。
- **手机端顶栏可折叠 + 审批文案明确**：手机（≤760px）顶栏新增收起开关——收起后隐藏整条操作区（轮次 / 文件 / 任务 / Skill / MCP / 刷新临时让位，点细条即回），模型与 Agent 两个下拉并回第一行，把被顶栏吃掉的高度还给会话区；选择记在本地、刷新后保留，桌面端不渲染该按钮。审批档位提示文案写明实际边界：自动＝「工作区内自动放行，越界仍确认」、完全＝「不再询问，可访问工作区外」。


- **会话模型下拉可随时重新检测**：会话底部「模型」下拉末尾固定一项「**↻ 重新检测模型**」——选中它即**强制重新拉取**当前 API 的模型目录（绕过「同一 API 本页只拉一次」的缓存限制），拉取完成后**自动还原你刷新前的选择**。供应商新上架的模型因此立刻可选，不必再进设置页 → 编辑供应商 → 点「检查模型」。该入口失败会明确出声：拿不到目录提示具体原因、尚未配置 API 提示去设置页、目录为空提示「该 API 当前没有可用模型」；它只是一次动作、不是模型，会从选择出口被抹掉，**不会被写进会话、也不会发送给服务端**。
- **修复：空模型目录不再被当作「已查过」**：此前判断能否复用目录缓存的条件是「本页有没有查过这个 API」，而不是「缓存里有没有模型」——某些 API 在还没有可用模型时（如本地模型尚未加载）返回空列表，这一页就再也不会重拉，下拉框永久停在「请先在设置中检查模型」，即使模型随后已经可用。现在改为「缓存里确实有模型才复用」，空结果会自动重新拉取。
- **行为变更：未选择模型时不再默认选中目录第一项**：会话没有明确选择过模型时，下拉框不再自动选中检测目录的第一项（该顺序由供应商接口的返回顺序决定、没有任何含义——此前 teynex 这类供应商的新会话会静默用上比你配置更弱的模型），改为显示禁用占位项「请选择模型…」；已保存的模型仍优先展示，不在检测目录里时依旧置顶「已保存：X（请重新检查模型）」。**影响范围**：所有尚未选择过模型的会话（含历史会话）都需先在下拉框里显式选一次才能发送。
- **Skill 删除重构**：删除 Skill 改为确认对话框——明确「移至回收目录（可恢复）」与「彻底删除（不可恢复）」两种方式，并列出该 Skill 被哪些 Agent 引用（删除会一并移除引用）；设置页「已安装 Skill」下方新增**回收目录**列表，显示已回收的 Skill（名称 + 原路径），可一键**恢复**（移回托管目录并重新启用）或**清空回收目录**（永久删除）。**修掉「点了删除却没删掉」**：同一 Skill 在多处目录（或打包内置 + 托管副本）各有一份时，只搬走其中一份会让它立刻从另一份复活——现在这类 Skill 统一按 ID 隐藏（可在「已隐藏 Skill」里恢复），一次点击即从列表消失且重启不回来，内置 Skill 只处理托管副本、程序包原件不动；**启动时的内置同步与旧数据目录合并改为跳过「已删除（隐藏）」的 Skill 目录**，删掉的内置 Skill 不再被重新写回。
- **会话模型选择收紧（行为变更）**：会话底部「模型」下拉**只列当前 API 上检测到的模型**——移除供应商默认模型兜底与手动输入；**未在设置中「检查模型」或选中项不在检测目录里时，发送会被拦下**并提示「请先在设置中检查模型，并在会话底部选择检测到的模型」，不再把未经验证的模型名发给服务端；历史会话保存的模型若不在检测目录里，置顶显示「已保存：X（请重新检查模型）」，点「检查模型」拿到目录后自动刷新会话下拉；切换 API 后清空该会话的模型选择，等待新 API 的检测目录填充。
- **供应商默认模型换代不再影响已有会话**：此前在输入区没主动选过模型的会话，会把供应商当时的默认模型名一并落库成会话级覆盖；供应商之后改了默认模型，这些会话仍会继续发送旧模型名。现在保存 API 配置时若默认模型被改写，只清空「覆盖值恰好等于旧默认模型名」的会话（它们本就在隐式跟随），显式固定成其它模型的会话保持不变；修正为静默处理（不推进会话更新时间、不重排侧栏），并把图像能力重置为待重探。
- **冻结版打包补齐 multipart 解析器**：PyInstaller 显式收集 `python_multipart`——此前它是 `naiba.http` 的懒加载导入、未被自动打进包，可能导致安装版上传附件失败。
- **单文件 Skill 导入宽松化**：单个 `.md` 与 zip 里顶层散放的 `SKILL.md` 不再强制 YAML frontmatter——自动取首个标题作为 `name`、首个非标题段落作为 `description` 补出最小 frontmatter，正文原样保留，裸 Markdown 也能直接安装并在列表里正确显示。
- **异步任务（Job）韧性修复**：ComfyUI 批量生成每轮同时核对 `/history` 与 `/queue`——**既不在历史也不在队列**的 prompt 判定为已被 ComfyUI 丢弃（服务重启或队列被清空），按预算（`max_resubmit`，默认 1，持久化在 checkpoint）**自动重新提交同一工作流**继续生成，预算用尽才记为「ComfyUI 已丢失该任务」并正常收尾，不再空转到 `wait_timeout`（默认 2 小时）——修复「任务面板长期挂着『运行中 · 完成 5/8』且永远不动」；已失败的镜头也计入完成集合，恢复后不会重复轮询、把同一处错误反复累计；队列查询失败时保守处理、不误判。**启动时的中断恢复改为幂等**（`resumed_into` / `resume_skipped` 持久标记），且 checkpoint 完全相同的中断记录视为同一批的重复副本、只恢复最新的一条，杜绝「重启一次任务翻一倍」（实测此前一批 8 段生成被放大成每对话 13 份）。**后台子 Job 不再占用对话的活跃 Run 互斥位**——只有顶层 Run 占用对话，长跑或卡住的异步任务不会再让该会话发不出新消息（此前会 409 拒绝且提示语误导）。任务面板恢复**「停止」按钮**（新增 `POST /api/jobs/{id}/cancel`，走 Job Registry 会真正中止 worker；`chat` / `plan_execute` 仍走原取消接口）。新增 8 例守门测试覆盖上述四条不变量。
- **手机端「功能对等」（不删能力、只换形态）**：手机（局域网访问）不再是「缩小版电脑」——电脑能做的手机都要能做，放不下的控件换等价形态。右侧文件面板从「整体隐藏」改为**全屏抽屉**（与桌面共用同一个面板与开合链路，只换呈现形态），消息末尾「本轮修改的文件」条目在手机上恢复可点（此前被 `pointer-events` 关掉），点开即打开文件抽屉；桌面右侧的「对话刻度轨」换成顶栏**「轮次」下拉**（列出「第 N 轮 · 用户消息摘要」，选中即跳到该轮，滚动时自动显示当前所在轮次，不足 2 轮隐藏；复用刻度轨同一份轮次模型与跳转函数，跳转到未渲染轮次会先扩懒加载窗口再定位）；触摸目标统一 ≥44px（顶栏按钮、侧栏抽屉开关、输入区图标与发送、文件面板关闭）；跨越 760px 边界不再关掉文件面板，「手机端」判定只有唯一来源（CSS 760px 与前端常量同界），窄屏也不隐藏任何文字标签。
- **Skill 安装目录归属修复（防「删一个 Skill，全部消失」的数据事故）**：单文件 `.md` 导入与顶层散放 `SKILL.md` 的 zip 以前会把定义直接写在托管目录根下，导致该 Skill 的 `root` 等于整个托管目录，删除时把**所有 Skill 一起搬走**。现在凡有文件直接落在导入根的上传都先建独立子目录（目录名取 frontmatter `name`），带顶层目录的文件夹上传保持原样、不多包一层；删除前校验归属（移动目标必须严格位于托管目录之下，`root` 等于托管目录时退化为只回收该定义文件），越界一律拒绝；启动时自动把历史裸 `SKILL.md` 迁入同名子目录，并同步 Agent 固定 Skill 引用与隐藏 ID。
- **在线模型稳健性（聚合回填 + 空流重试 + 5xx 取证）**：Codex Responses 流补齐**聚合事件回填**——仅收到 `response.output_item.done` / `response.completed`（没有逐字增量）时也能正确提取正文、思考与 reasoning item id，并把单独出现的 `function_call` 组装为工具调用（同一 message 只回填一次，`response.incomplete` 的被截断内容不作为成功正文）；该格式下「流式消费完但既无正文也无有效工具调用」按瞬时故障退避重试，其它在线格式行为不变；HTTP 5xx（含 500）与 429/502/503/504 同等退避重试（1.5s / 3s，最多 3 次尝试），思考回传类 400/422 与全部 5xx 都把失败请求体落盘取证（超长字段截断、不含 API Key）。
- **界面打磨（审批上拉框 / 模型只从目录选 / 供应商卡片更紧凑）**：审批模式由平铺四段改为**上拉框**——收起态只显示当前档与风险配色，点开才在按钮上方弹出列表（浮层置顶、不被输入区裁剪，贴顶自动下翻、夹在视口内），切换会话、打开其它浮层、Esc、点外部都会收起；输入区「模型」选择器移除手动输入，模型一律从**当前 API 的模型目录**里选，历史会话保存的模型若不在目录里会置顶「已保存：X」并默认选中（避免被静默换掉），并修掉「目录返回时会话已切换」导致旧会话模型被顶到新会话的问题；API 供应商卡片压成两行（名称 + 格式标签与当前角标），模型名不再上卡片，卡片更矮（116px → 88px）。

- **会话级模型选择（API 与模型分离）**：顶栏「模型」下拉改为 **API 下拉**——只负责选连接配置，选项直接显示 API 名称（不再拼「名称 · 模型」），切换后提示「API 已切换」；输入区新增 **「模型」选择器**：打开会话时自动拉取当前 API 的模型目录（`/api/providers/models`），**只列出检测到的模型**（不再回退 API 配置里的默认模型，也没有手动输入入口），下拉末尾固定一项「↻ 重新检测模型」可随时强制刷新目录，未明确选择过模型时显示占位项「请选择模型…」；下拉选择即保存到该会话；模型选择**按会话记忆**（`conversations.model_name`，分支对话继承同一 API 与模型名，互不干扰），顶栏切换 API 时该会话的模型名一并清空重选；**切换 API 或模型名后自动重新探测图像能力**（`chat_supports_images` 重置为待探测，发送时按新模型真实能力判断——换到文本模型不再误发图片、换到多模态模型不再被当作纯文本）。数据库 v17 幂等迁移，升级自动完成。

- **长会话模式（翻历史 + 重置上下文）**：Agent 工具集新增「长会话」一组——`find_conversations`（列出最近会话的 id/标题/时间/条数与末条预览，可按标题匹配）、`recall_history`（按关键词检索：不给会话 id 就全库搜、每个会话最多 3 条片段；给了 id 就只搜那个会话、按时间正序、片段更长）、`read_conversation`（按序号区间读历史会话原文，用于复核片段），加上 `reset_context`。配套新增 **「长会话模式」预设** = 标准模式 + 这四件套。检索结果会明示"检索了全部 N 个会话 / M 条消息"、截断显式、会话 id 不存在直接报错；**当前会话的命中会标注「在上下文中」或「已划出上下文」**——被「新会话」分割线划出去的老消息不会被当成你还记得的内容。
- **长会话懒加载**：打开会话只渲染最近 10 轮，向上滚动时自动往前预渲染（每次 10 轮，视口不跳动）；渲染窗口**以「轮」为边界**，所以「新会话」分割线永远和它下面那条回复一起出现、不会被拆到两段里。右侧刻度轨仍然覆盖全部轮次，点没渲染出来的轮次会先渲染再跳过去。
- **交接文档快捷消息**：快捷消息面板新增内置预设「写交接报告并 reset_context」（在「快捷消息」列表里，带「默认」标签）——点一下即插入到输入框，要求模型把交接报告写进工作区（目标/进度/关键文件路径/已定约束/后台任务/下一步）并用 `read_file` 复核后调用 `reset_context`；**这条预设和用户自建条目同权**：可编辑、可删除，改动与删除都记在你的配置里，软件更新不会覆盖也不会复活。
- **同一窗口内「新会话」分割线**：每条完整 AI 回复末尾、「复制」右侧新增「新会话」按钮——在**任意位置**划一条分割线，**此线以上的消息不再进入模型上下文，线以下的消息仍保留在上下文中**（所以可以只裁掉前面跑题的部分、保留最近的进展），聊天记录一条不删、分割线可逐条撤销；允许多条分割线并存，靠时间与位置区分，最新那条决定当前上下文起点。**模型也能自己重置**：Agent 工具集里勾上 `reset_context` 后，模型可先把交接文档写进工作区、再调用它（**文档不存在或为空会被拒绝**，每轮最多成功一次），本轮立即收尾并在该条回复下画出「模型重置」分割线；随后**种子消息自动填入输入框**（默认含交接文档路径 + 「请先读取再继续」 + 仍在运行的后台任务清单，模板可在「设置 → 运行设置 → 新会话种子消息」里改，占位符 `{handoff_path}` / `{task_count}` / `{task_list}`），由你确认后点发送开始新一段。
- **上下文用量提醒**：输入框旁的上下文圆环随**每次模型请求**刷新（不再等整轮结束），**输入框下方「思考 自动」右侧还常驻一行实时百分比**（如「上下文 8.2%」，达 70% 转黄、90% 转红，悬停看完整 `已用 / 上限`），达到「设置 → 运行设置 → 上下文提醒阈值」（默认 80%，0 = 关闭提醒）时弹窗提醒——**正在运行的会话**涨过阈值即弹，**已结束的会话**改为点发送时才弹（可「继续发送」，被拦下时草稿不清空）；距上次提醒再涨 5 个百分点才再次提醒，换会话后重新计算；弹窗建议让模型撰写交接文档并开始新会话。
- **在线模型 5xx 自动重试**：供应商返回 5xx（如 HTTP 500）时不再直接终止整轮，改为按瞬时故障退避重试（1.5s / 3s，最多 3 次尝试）并在状态栏提示重试进度；失败请求体落盘到进程工作目录的 `naiba-model-error-payload.json`（超长字段截断、不含 API Key），便于复现定位。
- **设置页卡片化**：API 供应商与 Agent 管理改为卡片网格（一行最多三张、末尾固定「添加 API」/「新增 Agent」卡片、卡片右上角 × 删除），点卡片才弹出该条目的设置弹层并加载已有配置；字段与文案保持原样，只换展示方式。**默认 Agent 不再在卡片上标「默认」角标或加高亮**——默认项由顶栏 Agent 选择器体现，卡片上任何强调色都会被误读成「当前选中」；**每张 Agent 卡片在「固定 Skill」下方显示它当前用的工具集名称**（命中内置预设就显示预设名，如「工具集：标准模式」；空配置显示「未限制（全部工具）」，其余显示「自定义 · N 个工具」）。
- **Agent 自动 ID 与自定义头像**：新建 Agent 不再手填 ID（保存时后台分配持久化唯一 ID，已有 ID 显示在弹层副标题）；Agent 编辑弹层新增「自定义头像」，上传图片自动中心裁切为 256px WebP，使用该 Agent 的会话会把助手气泡的「AI」圆标换成这张头像，Agent 卡片上也显示缩略图。
- **内置 Agent 预设下线**：四个内置预设（dsh-standard / dsh-code / dsh-minimal / dsh-cordis）不再自动出现；此前编辑过、已存进配置的副本会摘掉「内置」标记、变成普通可删除的 Agent（内容保留），引用它们的会话自动回退默认 Agent。
- **工具与 Skill 选择器改版**：固定 Skill 改为卡片网格并与工具集等高；工具集分组标题/说明/计数一行呈现，「视觉」分组补说明并前移到「命令执行」之后；展开后的工具卡片更紧凑，只有展开的那一组标题条变浅灰以区分层次；修复列表在弹层里被压成一条线、点不动的问题。**工具集分区改为「卡片态 ↔ 编辑态」两态同框切换**：弹层高度恒定不跳，默认只显示 5 张只读预设卡（只读 / 标准 / 长会话 / ComfyUI 联动 / 全能）与「我的工具集」卡片、末尾一张虚线「添加自定义工具集」卡；点任意一张卡都会先按下、卡片整块向上滑出、工具列表从下方滑入，命名栏自动填上被点卡片的名字，勾选就是该卡的工具（可继续增删；「添加自定义工具集」固定以标准模式为起点）；选完可存成「我的工具集」复用，内置预设只读不可改、自定义工具集可删除。**「我的工具集」存后端 config.json（`tool_sets`），不再存 localStorage**——冻结版 pywebview 默认 `private_mode=True`，WebView2 的 localStorage 每次退出都会被清空，此前保存的工具集重启后会消失；旧版本留在 localStorage 里的数据启动时自动搬到后端一次。
- **设置页右键菜单修复**：设置页文本框右键菜单恢复可用（撤销/重做/剪切/复制/粘贴/删除/全选）——此前被模态弹层整体盖住；同时不再误弹会话页的选区菜单；数字类输入框右键「复制」改为复制整值。
- **桌面壳持久化**：内嵌窗口的 WebView2 改用持久 profile（此前 pywebview 默认私有模式，profile 放在临时目录、退出即删），侧栏宽度、文件面板宽度、顶栏 Skill 勾选、交互模式这些本机偏好不再每次启动重置。
- **会话页对话刻度轨**：会话右侧新增一列横条，每个用户轮次一条，滚动时视口中心所在轮次加粗凸起；长会话只显示附近 30 条，悬停显示该轮概要（第 N 轮 + 用户消息一行、AI 回复两行），点击跳到该轮；消息列整体左移约 13px 让位，窄屏自动隐藏。
- **后端全面模块化重构（阶段 0–4）**：原先约 2.4 万行堆叠在根目录的后端代码迁入 `naiba/` 包（组装根/传输层/配置/存储/运行/工具/技能/模型/视觉/网络等领域分层），依赖图单向无环、机械可验证（DAG 守门测试）；`server.py` 收口为 136 行门面，HTTP 层仅剩薄包装与传输职责，业务逻辑全部下沉至 `NaibaChatApp` 与领域模块。
- **路径与配置**：`PathContext` 统一管理源码/冻结双模式目录（冻结版固定 `%LOCALAPPDATA%\NaibaChat`，旧数据迁移与自定义数据目录切换语义保留）；`ConfigStore` 迁入 `naiba/config.py`；静态资源版本哈希改为 lazy-once。
- **契约化**：`RunContext`（18 键 + 校验工厂）/`EventType`（44 种，与前端反查对齐）/`MetadataKeys` 显式化；`AppContext`/`ConfigView` Protocol 注入收窄（14 处 `app: Any` → `AppContext`）。
- **修复与清理**：模型测试连接 `NameError`（`@staticmethod`+`self` 错配）与上传 500（`self.app` 残留引用）等搬移期缺陷；诊断开关兜底统一；死代码清理（MCP 诊断管道、废弃插话等）。
- **守门测试**：新增权限矩阵（full/confirm/auto/deny 全模式）、装饰器-绑定一致性、`self.app` 残留引用扫描等网格化守门，共 83 个单测；全量功能回归（11 域 48 项）通过。
- **工具系统增强**：视觉类工具单入口（vision_analyze 按会话模型能力自动分流 + vision_image_ops 本地图像计算）；移除 call_mcp 网关；多模态/文本模型同一工具集，退役工具自动迁移/提示；DeepSeek 思考模式工具轮兼容修复（reasoning item 按官方 Schema 回传：`reasoning_text` 内容块列表 + 服务端唯一 id，旧历史无 id 自动合成兜底）；Harness 兼容别名（read/write/edit/grep）从模型可见工具清单隐藏（执行兼容保留）；工具返回对模型可见性统一（机器字段剥离、截断带标记、前端展示与模型上下文同源；vision_image_ops 产物路径对模型保留，供其引用裁剪/热力图产物）；glob_files 并入 list_directory（单入口枚举）。
- **行为兼容**：对外 HTTP API 路径、事件流协议与前端完全不改；13 个根模块保留兼容 shim（`from naiba.X import *`）；`launcher.py`/PyInstaller spec 零改动。
- **历史数据管理**：推理流保持逐词实时显示、回复完成后自动整理为整段思考（事件流不再逐 token 累积）；终态任务快照自动收缩；升级时自动压缩存量历史数据（迁移前先整库备份到 `data/backups`）；设置页「历史数据管理」可查看数据库占用并一键压缩回收空间；实测 400MB 存量库压缩至 ~90MB，继续会话发送给模型的历史消息逐字节不变（前缀缓存命中不受影响）。
- **时间线与用量可观测**：活动时间线规则定稿（思考块一律折叠、思考/工具严格物理序、最终答复固定时间线末尾、每次模型请求开始处以横线分割，与用量明细的请求序号对应）；流式期间消息尾端常驻用量框，每完成一次模型请求即刷新缓存命中率/tokens（最后一次请求口径）与**生成速率**（输出 tokens ÷ 该次请求耗时），完成后显示本轮总耗时、**平均速率**与完成日期，「请求明细」展开逐次请求的 token/命中率/速率/耗时；会话顶部新增「首次请求上下文」折叠卡，可查看第一轮发送给模型的内容（完整系统提示词原文、工具 JSON 定义、生成参数），第一轮完成后即时显示（终态事件后立即拉取，切换会话时同样刷新）。
- **文件上传流式化**：上传改为 multipart 流式直传（内存更省、真实传输进度、单文件可取消、多文件并行上限 3、80MB 前端前置校验）；上传内容自动去重（重复文件复用既有缓存）、按日分目录存储、移除待发送附件时自动清理未引用文件；上传前/上传中的图片缩略图不再请求空路径。**待发送附件在输入框上方以竖直列表显示**：一行一个文件、固定高度（约 4 行）后内部滚动，文件名过长自动截断（悬停看全名），图片附件显示预览缩略图（点击看大图）、其它文件显示文件图标，每行右侧可单独移除；附件行用浅底卡片 + 描边与投影，和输入框层次分明。
- **缓存管理**：图片/PDF/视频/音频等统一「缓存文件」统计（设置页打开即刷新实时值）；超限自动清理（阈值默认 256MB、设置页可调/填 0 关闭）**只删未被消息引用的最旧文件**——历史消息引用的附件永不自动删除；手动清理与自动清理共用同一阈值口径，界面明确提示「按时间从旧到新、不区分是否被引用」。
- **PDF 解析与识别**：内置 PDF 工具三件套——`read_pdf`（文本层提取：页标记、单次 30000 字符/50 页、截断续读提示、扫描版自动引导转页图）、`pdf_render_pages`（页面渲染为 PNG 页图，可复核的 AI 看图素材）、`pdf_zoom_region`（局部高清放大：小字/表格/图表看不清时矢量源重渲精读）；上传或提及 PDF 后模型按需提取文本/渲染页图并用 `vision_analyze` 识图；页图缓存在受管数据目录（自动清理覆盖），不污染工作区；`read_file` 对 PDF/图片/二进制文件不再输出乱码，改为明确提示并引导对应工具。
- **多图分批读取**：`vision_analyze` 单批默认 4 张（装载/分析两路径统一），超出自动分批注入并带批次标注（共 N 张/已展示前 M 张）——多页 PDF、多图分析时模型清晰知道"还有剩余",不再因静默截断而误解；用户上传附件与宿主托管产物（uploads/generated）在读取策略中视为可信根，免越界确认（真正的工作区外路径仍确认）。
- **纯附件发送**：输入框不写文字也能直接发送文件/图片（拖入、粘贴、附件按钮均可）；发送按钮按可用性变化——无可发送内容时灰暗禁用，可发送时恢复实心主色，上传未完成或上下文已满时禁用并给出原因提示；仅发附件时模型侧会明确收到「用户未输入文字，只发送了以下附件」+ 附件路径，不会凭空脑补用户诉求；纯附件首轮会话标题自动取首个附件名；发送按钮状态同样跟随程序化填字刷新（快捷消息插入、技能与 `@` 引用回填、选择按钮写入后即时更新）。
- **@ 引用工作区文件/目录**：输入空格 + `@` 弹出工作区目录列表（懒加载，只列当前目录，隐藏 `.` 开头的条目），方向键选择、Tab 进入子目录 / Shift+Tab 返回上级、Enter 或点击即引用；目录条目右侧另有箭头按钮可直接进入（手机端无 Tab 键的替代入口），列表首项为「返回上一级」；选中目录时名称左侧显示 Tab 键提示。引用后输入框与消息气泡保留 `@相对路径` 高亮，发送给模型时自动替换为该文件的绝对路径（目录保留尾部分隔符，便于模型处理整目录）；只有工作区内真实存在的路径才会被替换，邮箱等普通 `@` 文本不受影响。
- **快捷消息面板**：输入区底部新增「快捷消息」按钮，点击展开常用提示词列表（**独立列表**，与「开始新对话」页的自定义指令互不影响，各自增删改）；**每条只有正文、不用起标题**（列表以正文首行作标题、其余行折叠预览，新增/编辑弹窗只填正文）；列表默认按「使用次数 × 2 + 新鲜度加分」排序——常用条目稳定靠前，新添加的条目在 7/30/90 天内有曝光窗口不会永远沉底，并列时新增的在前；点击条目把正文插入输入框（可继续编辑再发送）并累计使用次数，行内 `✎` 编辑、`✕` 删除；面板在窄屏上自动贴边对齐，触屏下操作按钮常显。
- **图片查看**：点击会话里的图片打开大图，灯箱内可用左右按钮、`←`/`→` 键或**点击屏幕左右半屏**连续浏览当前会话的全部图片（按在对话中出现的顺序，首尾循环），底部显示 `当前 / 总数`；**滚轮缩放、缩放后拖动平移、双击复位**（触屏支持单指拖动、双指捏合、双击切换），`Esc` 或点击空白处关闭。输入区待发送的附件与右侧文件面板里的图片仍按单张查看。
- **工具产物就地显示（多媒体内嵌）**：工具产出的图片/视频/音频不再等整轮结束才集中堆到消息末尾，而是**紧跟产生它的那次工具调用就地显示**——流式期间工具一返回就出现，工具块保持折叠、媒体始终可见，同一张图不会重复显示（消息末尾网格自动跳过已就地渲染的附件）；提取策略由工具在注册表显式声明（`metadata["media"]`：`inline`/`intent_gated`/`never` × `none`/`scan`/`structured`），不再按工具名枚举猜测，且只对成功的工具结果提取（失败信息里的路径不会变成破图）；单次调用按类型分桶上限（图 20 / 视频 8 / 音频 8）**先截断再入缓存**（列一次目录不会把几百张图拷进缓存卡住），超出时明确提示「共 N 个媒体，仅显示前 M 个」；`/api/file` 支持 Range 请求（视频/音频进度条可拖动、大文件不整包进内存），用户上传与工具产出的视频/音频都能在气泡里直接播放；GIF 保留动画并生成首帧缩略图（此前缩略图必然 404 破图）；后台任务（ComfyUI 等）产物在任务完成后**自动挂回发起它的那条消息**、挂到对应工具调用下方，无需再追问模型；缓存改为按内容哈希（修复 ComfyUI 同名覆盖时命中旧图）并修复带查询串的图片 URL 被截断（表现为「任务完成却看不到图」）。
- **开始页预设可编辑**：开始页内置预设（通过 MCP/HTTP 调用 ComfyUI、设置本地 Comfy MCP、列出可用工具/所有文件、等待用户指令）与自建指令同权——悬停即可编辑与删除；删除后底部出现「恢复默认预设」一键找回；升级后新增的内置预设自动出现，已删除的按预设 id 记录、不会复活；编辑内置预设保留其说明与图标。
- **思考强度交互**：思考按钮点击只做一件事——展开/收起强度列表（此前收起时会顺带切档）；列表高亮当前档位，输入框下方与「就绪」同行常显当前强度（`思考 自动/关闭/低/中/高`）。
- **Skill 脚本编码规范**：安装/编辑 Skill 的说明补充脚本规范——脚本统一放 Skill 目录的 `scripts/` 子目录，读写文件显式指定 UTF-8（冻结版默认编码跟随系统 locale，裸 `open()` 写中文会乱码）；同时修复 Skill 脚本输出中文乱码（隐藏入口强制子进程 UTF-8 输出）。
- **顶栏精简**：「任务」「Skill」计数改为普通数字文本（不再用胶囊徽标，按钮也不再重复「Skill」前缀）；MCP 状态**只看圆点颜色**（绿=已连接、红=未连接或出错、黄=连接中、灰=未配置服务，鼠标悬停看具体状态）；「刷新」回到顶栏常驻并移除「⋯」溢出菜单，顶栏「卸载模型」按钮删除（本地模型卸载仍在「设置 → API 供应商」）；文件面板按钮改用与其它顶栏按钮一致的浅色样式，按钮不再被压成竖排文字，窄屏也不隐藏文字标签。
- **输入区精简**：移除「轻量」与「富文本」两个开关——工具与 Skill 的能力范围完全由 Agent 设置（Agent 的工具集 / 技能集）决定，不再有会话级轻量模式；富文本始终开启（安全白名单 HTML 正常渲染）。同时移除会话界面中「本次对话规则：未设置」提示条（会话规则已统一由 Agent 的系统提示词决定）。
- **会话收藏与侧栏交互**：会话条目的齿轮改为**五角星**，点击即收藏/取消收藏（乐观更新，失败回滚）；侧栏最下方新增跨工作区的「已收藏」分组，收藏的会话**同时保留在原工作区**（两处都显示，不会"消失"），且**收藏不会重排工作区内的会话顺序**（收藏只改标记、不推进更新时间，避免收藏项被顶到最前、启动时"最新 5 条"被挤占）；重命名与删除并入条目的「⋯」菜单（fixed 挂到 body 不被侧栏裁剪，Esc 或点击外部关闭），**新建工作区也改为对话框**（选目录 → 填名称，不再依赖内嵌浏览器的原生 prompt）；顶栏重复的「新会话」按钮已删除（每个工作区分组自带「＋ 新会话」，列表为空时保留兜底入口），**收起按钮并入「工作区」操作行**（不再单独占一行）；**点击会话不再把列表强制滚到最上**——已可见就保持当前位置、不可见才用最小位移带进视口；**会话条目整条可点**（含上下边缘与左侧留白，鼠标落上去即变手型）；侧栏底部只保留**一个状态点**（绿=服务已建立、红=连不上服务端，随每次请求自动刷新）；**收起侧栏只重新居中对话列**（列宽恒定 880px，不再放大到整屏——此前收起后助手区块左移、用户气泡右移、整体错开）；条目更紧凑（高度 40→34px、标题 13→12.5px、时间 11→10px、行内间距 6→4px）；**滚轮步进按 1.5 倍接管**（≈150px/格，触控板小步进仍走原生），并修掉三处手感问题——虚拟列表的滚动上限此前漏算容器 padding（每次滚动被回写吃掉 16px，"越滚越慢"、底部到不了）、滚动时每帧重写 DOM（改为只在可视行区间变化时重绘，实测 160px 滚动的重绘次数从 20 降到 ≤8）、悬浮分组表头时删除按钮撑高行（改为绝对定位 + visibility，划过不再抖动）。
- **会话级系统提示词退役**：系统提示词**只有一个来源——Agent**（`agent.system_prompt`，为空时回退全局默认）；顶栏「对话设置」按钮、会话条目齿轮入口与整个对话框（含流式输出开关、清空对话）一并移除，`conversations.system_prompt` 作为遗留列保留但**不再注入模型**（历史值仍可在「首轮上下文」卡片里看到当时原文）；角色卡导入与快捷提示词套用统一迁到**设置 → Agent → 编辑**的「系统提示词（预设与规则）」块（标题行右侧即工具栏：套用快捷提示词 / 存为快捷提示词 / 导入角色卡 PNG / 自定义头像；角色卡为**追加**语义，不覆盖手写内容；超 12000 字符截断并提示）；快捷提示词改为**在 Agent 表单里管理**——「存为快捷提示词」把当前文本框另存（弹窗可改标题与正文）、面板里点条目套用、✎ 编辑、× 删除（**设置页的「快捷提示词」页已整体下线**）。

### 历史能力（1.7.8 及更早）

- **对话快捷系统提示词**：新增「设置→对话快捷系统提示词」，可新增/编辑/删除/搜索可复用的系统提示词预设；对话设置中可直接选择预设一键填充系统提示词，编辑预设不会改动已保存的对话。
- **角色卡自动收录**：导入 SillyTavern 角色卡 PNG 解析成功后，自动把该角色的人设文本收录为一条快捷系统提示词，无需手动复制。
- **更新检查自动选中新版本**：检查更新完成后自动在下拉框中选中新版本，无需展开下拉框即可看到有更新；手动选择版本后仍按用户选择保留。

### 历史能力（1.7.7 及更早）

- **文件预览隔离**：右侧文件面板的 Markdown 预览改用独立的 `markdownFilePreview` 渲染入口，固定按纯文本/Markdown 渲染，不受对话渲染开关影响，逻辑更清晰。
- **渲染性能优化**：内容不含任何 HTML 标签的消息在富文本还原阶段直接原样返回，跳过正则解析，降低大段纯文本回复的渲染开销。

### 历史能力（1.7.6 及更早）

- **对话富文本渲染**：AI 回复中的安全白名单 HTML 富文本（`b`/`i`/`u`/`s`/`span` 与 `font color` 彩色文字）渲染为实际效果；渲染前先整体 HTML 转义、仅对白名单标签按安全规则还原，颜色只允许标准颜色名或十六进制色值，注释一律剔除，其余标签保持纯文本显示，杜绝注入。右侧文件面板中的 Markdown 预览固定按纯文本渲染。
- **文件面板可拖宽**：右侧文件面板新增左缘拖拽分隔条，宽度可在 280px 至窗口一半之间自由调整并记忆（重启保留），不再固定；图片预览取消 58vh 高度上限、改为面板内纵向滚动，可完整查看高图。

- **Job 查询与状态提示分级**：跨会话查询 Job 时区分「记录已被清理」与「Job ID 从未存在」；job_status 按状态给出下一步指引（completed 用 job_output 读结果、运行中用 job_wait 等待等）；用户消息含明确 Job ID 时直接验证、不先检索历史。
- **SillyTavern 角色卡解析增强**：parse_sillytavern_card 对 chara 负载先尝试 gzip 解码并兼容 data 字段包裹，支持更多角色卡导出格式。

- **SillyTavern 角色卡导入**：对话设置中新增「导入角色卡 PNG」，由服务端解析 PNG 内嵌的 ccv3/chara 元数据（兼容 gzip 压缩），把姓名、性格、背景/场景、说话示例、开场白、角色说明、后置指令等字段归一化为系统提示词人设文本，一键填入即可扮演该角色；当前 Agent 自带系统提示词时会提示建议切换到提示词留空的 Agent。
- **细节优化**：会话列表的对话设置按钮在未悬停时改为半透明可见，不再完全隐藏，更易发现。

- **工作区名称绑定**：会话所属工作区（分组）与磁盘目录改由服务端统一绑定——指定已注册工作区时目录一律取其注册路径，不再信任客户端随分组提交的目录；分组名不存在会直接拒绝并给出明确提示。
- **切换分组即切换目录**：把会话切换到某个工作区分组时，该会话的磁盘工作区同步指向该分组注册目录；「移至未分组」只改侧栏分类、保留原有目录，不会移动文件。
- **启动自动修复**：启动时检查全部会话，凡分组指向已注册工作区但目录与注册值不一致的自动改回注册目录；未分组或指向已删除分组的会话保持原样，避免误搬文件。
- **状态同步**：切换工作区后前端立即以服务端返回的目录刷新界面，侧栏分组、底部提示与磁盘目录保持一致。
- **代码块语法高亮**：聊天中的代码块按语言实时着色（支持 JS/TS、Python、JSON、Bash/Shell、YAML、CSS、HTML，语言别名自动归一化，如 `js`/`javascript`/`jsx`、`py`/`python`、`sh`/`bash`/`ps1`），关键字/字符串/数字/函数名/注释分色渲染；流式输出随内容增长实时高亮，不再等整段结束才着色。
- **UI 图标 SVG 化**：设置、发送/停止、刷新、卸载模型、删除/编辑/关闭、对话设置、快捷指令等按钮图标从 emoji/字符替换为内联 SVG，手机与桌面渲染一致清晰，不再出现 emoji 字体缺失导致的方块或乱码。
- **顶栏「更多」菜单**：卸载模型、刷新页面等低频操作收进顶栏 ⋯ 溢出菜单，主操作区更简洁；菜单带 aria 支持，点击菜单外部自动关闭。
- **快捷指令卡片化**：空状态快捷指令改为「图标+标题+说明」卡片布局（通过 MCP/HTTP 调用 ComfyUI、设置本地 Comfy MCP、列出工具/文件等），按钮标题精简、说明行补充用途，并适配窄屏。
- **聊天布局调整**：AI 消息外层新增消息卡片结构，样式与代码高亮配色统一。
- **工具精简**：移除 `fetch_page`（抓取并提炼网页正文）工具，Agent 编辑页工具目录、内置 Agent 全量工具范围与权限确认清单同步移除；需要读取网页内容时改用 `http_request` 或已配置的 MCP 抓取能力。

- **本轮修改文件**：AI 每轮用 `write_file`/`edit_file` 成功写入的文件会在消息末尾按「新建/编辑」列出摘要（图片/视频/音频等多媒体产物仍走原附件卡片预览），点文件名在右侧文件面板打开，随时回看 AI 改过什么；中止/失败轮次已写入的文件同样留痕。
- **右侧文件面板**：多标签页管理打开的文件；Markdown 直接渲染预览，文本文件可手动编辑并保存回磁盘（Ctrl/⌘+S 或 Ctrl/⌘+Enter 保存、Esc 取消）；图片直接预览并可点开大图，二进制文件给出明确提示。
- **文件访问边界**：面板只允许查看本会话改动过或位于会话工作区内的文件，「保存写回」要求同时满足两者，路径越界一律拒绝，不能当作任意文件读写入口。
- **侧栏折叠**：左侧会话栏支持折叠与展开（桌面端记忆状态、腾出聊天区）；右侧文件面板收起后可从顶栏「文件 N」按钮一键重新打开已保留的标签。

- **检查更新更稳**：启动时复用 6 小时内的版本列表缓存，减少对 GitHub API 配额的无谓消耗；列表接口失败（403/限流/网络异常）时自动回退读取本地缓存，不再因一次接口失败就报错。
- **latest 静态直连兜底**：列表接口不可用或所选版本清单拉取失败时，自动改用 GitHub `releases/latest/download` 静态清单判断是否有新版本（不占 API 配额），并合成可安装条目，兜底场景下仍可一键安装。
- **错误提示分级**：GitHub 接口限流（403/429）时提示「访问频率受限，请稍后重试」；其它 403 提示检查网络或代理；网络无法连接给出明确提示。

- **删除会话不中断当前输出**：删除非当前会话时只刷新侧栏列表，不再自动重载当前会话视图，正在流式输出的消息行保持原样继续渲染。
- **流式输出稳定**：修复之前删除任意会话后自动重载当前会话，导致进行中的流式消息行被挤出 DOM、输出看似消失、要等 run 结束重新渲染才重现的问题；现在只有删除当前会话才切换到下一个会话。

- **选项识别收紧**：只认明确的「请选择/选一个/choose from」等完整意图，正文或代码里的裸 `select`/`choose`/`pick` 不再误判成选项按钮；英文只保留完整词组（`pick one`/`choose one`/`select one`/`which one`/`which of`/`choose from`/`select from`）。
- **意图必须紧跟选项**：意图行与选项组之间最多允许少量空行/单行间隔，长距离的「选择」字样不再触发按钮；选项文本过长（整段话）不算交互选项，意图行过长（长句里恰好含「选择」）也不算意图。
- **紧凑选项正确拆分**：「请选择语言：1. 中文 2. 英文」这类同一行紧凑选项可正确拆分为多个选项按钮，CJK 标点后的编号也能识别。
- **编号残留剥离**：选项中的「- 1. 安装依赖」会剥离编号残留，只保留「安装依赖」；孤立的 bullet 符号（`- `/`• `）不再打断正在收集的选项组。
- **点击选项只发选项文本**：点选按钮发送时只发送该选项本身，不再拼接「请选择…」前缀，避免把问题重复带进下一条消息。

- **命令执行统一**：`run_command` 已并入 `pwsh`，命令执行只保留 `pwsh` 一个工具，避免重复声明与选择困惑；升级后旧配置、Agent 工具范围与历史会话中的 `run_command` 会自动迁移为 `pwsh`，内置/自定义 Agent 均不会丢失命令执行能力。
- **手机端渲染优化**：修复移动端横向滚动条问题（视口宽度改用 100% 并隐藏横向溢出）；顶部模型/Agent 选择器改为网格布局自动换行，操作按钮在窄屏下换行到第二行，超小屏（≤380px）降级为两列；输入框在窄屏下完整占宽，图标与发送按钮不再被压缩；底部工作区、权限开关等元信息在超小屏改为单列排列。

- **暂停/取消竞态修复**：一次暂停立即中断当前事件流、看门狗与待执行重连，并停止轮询恢复；「正在停止」期间禁止发送和恢复旧 Run，服务端确认终态后才恢复；取消父 Run 会级联取消子 Job 与 follow-up Run；完成/暂停并发窗口加锁，取消后不再创建自动 follow-up。
- **断线重连稳定**：重连复用同一回答气泡并从最后事件序号继续，不再重复回放；`/api/chat/cancel` 支持通过 `conversation_id` 定位尚未返回 `run_id` 的 Run；未处理插话保留在记录中并标记为停止，不会自动发送；Job 即使在轮询返回的竞态中完成，也会服从已有取消标记。
- **RunningHub 多媒体生成**：内置 runninghub 技能，覆盖文生图、图生视频、文生视频、TTS、音乐、3D 建模、图片放大、AI 应用（webappId）等 420+ 端点；支持 AI 站（runninghub.ai）与 CN 站（runninghub.cn）双站点，技能随 EXE 打包，升级后启动时自动同步到本地数据目录。
- **Skill 脚本执行稳定**：安装版（冻结 exe）执行 .py Skill 脚本不再二次启动主程序触发实例锁；冻结版走 `--run-skill-script` 隐藏入口只执行脚本，`__main__` 语义、工作目录与参数透传保持不变。
- **失败内容保留**：在线请求异常（如 HTTP 500）时已输出的思考/部分回复/工具活动不会被错误信息覆盖丢失，会重建为「未完成」assistant 消息持久化并保留独立错误提示，刷新/重渲染/后续上下文可见。
- **启动错误清晰**：数据目录/锁文件不可写等权限/环境问题不再误报为「已在运行」，会给出具体目录与原因；启动失败以简洁中文提示退出，不再输出堆栈。

- **多轮图片缓存稳定**：图片对话历史按每条 user 消息完整携带并稳定重发，不再因「只保留最近图片/占位翻转」导致 DeepSeek 缓存命中率波动；图片以 `image_url`/`input_image` 内容块发送，命中率随对话稳定上涨。
- **切换 Agent 附加 Skill 引用**：切换顶部 Agent 后自动在输入框末尾追加该 Agent 预设 Skill 的 `/ref` 引用（已在框内的跳过，避免重复）。
- **Skill 系统重构**：输入 `/` 弹出已安装 Skill 索引，可在任意时刻向当前会话注入任意 Skill（`/ref` 引用）；引用在首轮注入 system、后续轮追加到会话末尾，保持 DeepSeek 前缀缓存稳定。
- **安装/编辑 Skill**：新增「安装 Skill」「编辑 Skill」按钮，AI 可调用 `install_skill`/`unpack_skill_archive` 安装技能，或用 `inspect_installed_skill` 定位并编辑已有技能。
- **左侧边栏改进**：会话列表虚拟化/懒加载、默认折叠、可拖拽调宽，交互更轻量清晰。
- **意图直接触发工具**：工具不再依赖先启动某个 Skill。Skill 只提供领域说明和流程建议，不是能力开关。
- **通用 Agent 自动化**：支持多步工具调用、后台 Job、状态查询、计划执行、子任务和失败后的有界恢复。
- **会话工具集固化**：会话可用工具在首条消息时固化（`enabled_tool_ids` / Agent 的 `tool_scope`），之后不可中途更改；工具选择页支持按分类「全选/全不选」，联动工具须同时选中。
- **更少的中断确认**：读取、检查和常规工作区操作可按权限模式自动执行；只有高风险操作、缺少凭据或不可推断的关键选择才需要用户确认。
- **流式渲染稳定**：代码块等特殊显示框随内容增长实时渲染在框内，不再在流式阶段被当作纯文本、等整段回复输出完才突然变成代码框；含大括号/方括号的代码块（如 Java/C/JS 的 `{ }`、`[Shot 1]` 标签）不再被误判为工具调用而中断流式输出，多个代码块可随内容同步渲染；Markdown 表格（`| 列 | 列 |` + `| --- | --- |`）可正确渲染——流式时表头+分隔行一到即渲染成表格、数据行逐行增长，单元格支持加粗/内联代码/链接，含 `|` 的内联代码与 `\|` 转义竖线都不会被切碎；长答复不再被截断到 2000 字符；`**a * b**` 这类含字面星号的加粗可正确渲染；思考块与工具块按时间顺序交错显示，正文不再被清空吞掉。
- **ComfyUI 自动任务**：可检查 API 工作流、修正运行参数、批量提交、轮询队列并收集图片、视频和音频产物。工作流推荐「改本地文件、再引用」：用 `read_file`/`comfyui_prepare_workflow` 读取、`edit_file` 局部修改，再用 `comfyui_batch` 的 `workflow_paths` 提交文件路径，避免把大工作流 JSON 内联进对话。
- **宿主管理产物**：生成结果由 Naiba Chat 宿主下载、校验、保存到受管理数据目录并附加到消息；即使中止/取消的轮次也会展示已生成的图片缩略图。用户明确要求「把某张图保存/复制到指定目录」时，模型会实际执行以满足该要求。
- **图片与视频预览**：聊天内可直接预览常见图片和视频格式，也支持音频播放与文件下载；生成产物会自动缓存并生成缩略图（含内容级去重，避免同图反复显示）。
- **多模态路由**：原生视觉模型可直接接收图片；文本模型由模型按需调用 `vision_analyze` 工具（独立视觉后端）看图，结果与其它工具一样以工具块呈现。
- **视频理解**：`probe_video` / `extract_frames` 让模型能分析视频——按时间点 / 固定间隔 / 画面切换抽帧（含关键帧定位切点与拼图联系表），抽出的帧直接交给 `vision_analyze`；上传视频时自动注入「先探测、再抽帧、再看图」的处理策略。
- **在线与本地模型**：支持 OpenAI-compatible、Anthropic、Gemini、Ollama、LM Studio、llama.cpp、Unsloth 等常见接口；针对 DeepSeek 思考模式会正确回传 `reasoning_content`。
- **网络健壮性**：针对代理/VPN TUN 中途切换或代理进程重启导致的连接拒绝，会自动改用直连（绕过系统代理）重试；修复取消请求时的连接泄漏；错误提示附带完整接口路径便于定位。
- **Skill 与 MCP 扩展**：Skill 可提供领域知识、模板和脚本。MCP 依赖升级到 2.1.1，并在应用启动时自动连接已启用服务（对启动时未连上的做周期重试）；`register_mcp` 只是登记服务，需重开会话后其工具才进入可用集，`call_mcp` 仅能调用当前会话可用集内的 MCP 工具。
- **可校验更新**：更新检查基于 GitHub Releases 版本列表，支持在设置中选择版本安装与回退；每个候选版本都会校验 manifest（仓库、commit、文件名、SHA-256）与可执行文件有效性，任何一项不一致都会拒绝安装并给出明确原因。
